# AI漫游项目协作规则

---
doc_id: AIR-AGENT-001
status: active
created: 2026-05-23
updated: 2026-05-25
owner: AI漫游项目
audience: ai-agent, developer
source: AuroraPlatformWeb 文档体系与 AI漫游原始种子
---

## 1. 总原则

- 本项目所有正式文档使用中文书写，代码标识、字段名、文件路径和协议枚举可使用英文。
- `文档/` 是项目事实源。实现前先读文档，修改后同步文档，功能完成后必须留痕。
- 文档要同时服务人和 AI：标题清晰、状态明确、路径稳定、字段可复制、决策可追溯。
- “深思熟虑”是按需使用的工作技能，不是每次对话的默认流程。只有用户明确要求、任务高风险或跨多模块时才使用。
- 不把一次聊天里的隐含上下文当作事实源；重要结论必须落到对应 Markdown 文件。

## 2. 修改前必读

开始任何产品、架构、数据、任务协议或功能实现前，至少读取：

1. `文档/README.md`
2. `文档/00_索引/AI上下文入口.md`
3. `文档/00_索引/写作规范与留痕规则.md`
4. 与本次任务相关的产品、架构、模块或执行记录文档

如果任务涉及以下内容，还必须读取对应文档：

| 任务类型 | 必读文档 |
| --- | --- |
| 产品范围、MVP、用户流程 | `文档/01_愿景与产品/` |
| 技术架构、数据库、任务队列、素材路径 | `文档/02_架构与契约/` |
| 模块职责、边界、依赖 | `文档/03_模块梳理/` |
| 方案评估、重大取舍 | `文档/04_方案与决策/` |
| 正在执行或已完成的功能 | `文档/05_执行与记录/` |
| 测试、验收、证据 | `文档/06_测试与验收/` |

## 3. 文档更新规则

- 改变产品范围、用户路径或验收口径时，更新 `01_愿景与产品`。
- 改变架构、数据结构、任务协议、文件路径或异步状态时，更新 `02_架构与契约`。
- 改变模块职责、接口边界、上游下游依赖时，更新 `03_模块梳理`。
- 做出不可轻易回退的技术或产品决策时，在 `04_方案与决策` 新增 ADR。
- 做功能开发时，在 `05_执行与记录/任务记录/` 建立任务目录。
- 功能完成时，在 `05_执行与记录/功能完成记录/` 新增完成记录。
- 做验证时，在 `06_测试与验收` 或任务目录内记录证据、命令、结果和残留风险。

## 4. 深思熟虑技能

深思熟虑已经沉淀为 Codex 技能 `$deep-think`，不再作为项目默认强制流程。

触发条件：

- 用户明确说“深思熟虑”“先想清楚”“认真规划”。
- 任务涉及多模块、数据库、任务协议、素材路径、导出物或真实用户路径。
- 任务需要多个阶段、存在较高回滚成本或需要正式复核。

不触发条件：

- 普通聊天、解释、轻量讨论。
- 小文案、小样式、单文件小改。
- 用户明确说先不要规划、先不要写代码、不要使用深思熟虑。

执行角色：

| 角色 | 职责 |
| --- | --- |
| Orchestrator | 读事实源、建任务目录、拆阶段、处理阻塞 |
| Worker | 只执行当前阶段，记录改动和证据 |
| Scrutiny Review | 只读复核静态证据、契约、测试和 Handoff |
| Runtime/User Review | 验证真实页面、任务状态、导出物和素材追溯 |

同一个 AI 可以在当前会话内分角色执行，但必须在文档和行为上保持职责分离。

## 4.1 Matt Pocock 工程技能适配

本机可能安装以下 Matt Pocock 辅助工程技能：

```text
grill-with-docs
diagnose
tdd
zoom-out
prototype
improve-codebase-architecture
```

这些技能只能作为工程方法补充，不能替代本项目事实源和主流程。

使用规则：

- `$deep-think` 仍是本项目复杂功能规划、执行、复核和留痕的主技能。
- `grill-with-docs` 是 `$deep-think` 前的方案拷问技能：用于拿当前文档、代码事实和用户方案互相校验，先问清楚再进入正式拆任务。
- `文档/` 仍是唯一正式事实源。
- Matt 技能中提到的 `CONTEXT.md`，在本项目中等价参考 `文档/00_索引/AI上下文入口.md`、`文档/01_愿景与产品/` 和相关模块文档。
- Matt 技能中提到的 `docs/adr/`，在本项目中等价参考 `文档/04_方案与决策/`。
- `grill-with-docs` 解析出的术语、边界和决策，必须按本项目规则写入 `文档/` 对应位置；不要写入根目录 `CONTEXT.md` 作为正式事实。
- 不要因为使用 Matt 技能而新建根目录 `CONTEXT.md`、`CONTEXT-MAP.md` 或 `docs/adr/` 作为新的主事实源。
- 不要让 `to-prd`、`to-issues`、`triage` 接管当前任务体系；这些技能当前未安装，任务仍记录在 `文档/05_执行与记录/`。

适合使用：

| 技能 | 使用时机 |
| --- | --- |
| `grill-with-docs` | 用户提出新功能、新设计、新架构方向时，先带着 `文档/` 和现有代码拷问方案 |
| `diagnose` | 用户报告 bug、页面坏了、接口失败或性能异常 |
| `tdd` | 用户明确要求测试先行、红绿重构或关键逻辑需要测试保护 |
| `zoom-out` | 需要解释某段代码在整体系统中的位置 |
| `prototype` | 需要先做可丢弃原型验证 UI、状态机或数据模型 |
| `improve-codebase-architecture` | 用户要求架构复盘、找重构机会或降低模块耦合 |

## 5. 复杂任务记录

只有在使用 `$deep-think` 或用户明确要求正式留痕时，才创建复杂任务目录。

任务目录建议命名：

```text
文档/05_执行与记录/任务记录/YYYY-MM-DD_<任务短名>/
```

目录内至少包含：

| 文件 | 作用 |
| --- | --- |
| `task_plan.md` | 目标、阶段、任务、决策、退出标准 |
| `progress.md` | 每次推进的时间线、改动文件、验证结果 |
| `findings.md` | 探索发现、证据、风险、问题与结论 |

普通讨论、探索性设计、被明确推翻的页面试错，不再自动创建三件套。

## 6. 功能完成记录要求

完成一个对用户或系统有持续价值的功能时，新增完成记录。记录文件命名：

```text
文档/05_执行与记录/功能完成记录/YYYY-MM-DD_<功能名>.md
```

完成记录必须包含：

- 功能摘要
- 影响范围
- 修改文件
- 数据或协议变化
- 验证命令与结果
- 已知风险
- 后续建议

以下情况不强制新增完成记录：

- 纯探索、临时草稿、被用户要求删除的试错页面。
- 只修改一两处文档措辞。
- 用户明确要求先不要进入正式流程。

## 7. AI 可读格式要求

正式文档开头应包含 frontmatter：

```yaml
---
doc_id: AIR-XXX-001
status: active
created: 2026-05-23
updated: 2026-05-23
owner: AI漫游项目
audience: human, ai-agent
source: 文档来源
---
```

正文中尽量使用：

- 稳定标题
- Markdown 表格
- JSON 示例
- 状态枚举
- 输入输出契约
- 明确的“目标 / 非目标 / 验收标准 / 风险”

## 8. 验证与交付

- 文档变更至少做路径检查和内容自检。
- 架构、协议、数据模型类变更必须给出示例。
- UI 或生成效果相关功能完成后，应保留截图、样例输出或人工验收说明。
- 如果某项验证无法执行，必须在完成记录中说明原因和残留风险。
- 深思熟虑任务完成前，必须有 Handoff、静态复核结论、运行或用户路径复核结论；确实不适用时写明原因。


# Global Agent Instructions

## 长期记忆

首次处理当前任务时，尝试读取：

文档/记忆/MEMORY.md

如果文件不存在，则创建。长期记忆最多 500 行。更新时必须去重、合并、删掉过期或重复内容，只保留长期有效的信息、项目约定、关键决策和稳定偏好。

## 会话记忆

首次处理当前任务时，在：

文档/会话/

中新建一个 md 文档，文件名格式：

YYYY-MM-DD-HH-mm-需求简述.md

文件名要简洁，不超过 20 个中文字符。

此文档用于记录本次任务过程中的重要上下文。只有在以下情况更新：

- 发现重要代码结构、配置、约束
- 用户提出新需求或改变目标
- 形成关键决策
- todo 状态发生变化
- 任务完成时做最终总结

不要把无意义的过程日志、重复信息、完整代码片段大量写入会话记忆。

## 仓库探索规则

必须先探索仓库，再回答或修改。

## 网络搜索规则

只有在需要最新资料、第三方文档、版本、API 行为时才进行网络搜索。


## 会话记忆模板

```md
# finding

记录代码库探索发现。简洁，只写和任务相关的事实。

# web_search

记录必要的网络搜索结论。没有搜索则留空。

# goal

记录最终目标和强制验收标准。

# todo

- [ ] 拆解任务 1
- [ ] 拆解任务 2任务结束时

任务完成后，基于本次会话记忆，整理长期有效信息到：

文档/记忆/MEMORY.md

要求：

不超过 500 行
合并重复信息
删除临时过程
保留项目约定、长期偏好、重要架构决策


