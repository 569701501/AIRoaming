# 自动化测试体系

---
doc_id: AIR-QA-AUTOMATION-001
status: active
created: 2026-06-24
updated: 2026-07-11
owner: AI漫游项目
audience: human, ai-agent, developer
source: 任务 2026-06-24_测试基础设施与回归保护
---

## 1. 测试框架

项目使用 **Vitest** 作为单元/集成测试框架。

选型依据:
- 后端 NestJS + ESM(`"type": "module"`) + tsx,前端 Vite 5,shared 纯 TypeScript + ESM。
- Vitest 原生支持 ESM + TypeScript(零编译配置),与现有 Vite 栈同源。
- Jest 在 ESM 下需复杂 transform 配置,与 Vite 生态割裂。

## 2. 运行命令

| 命令 | 作用 | 范围 |
| --- | --- | --- |
| `corepack pnpm test` | 跑全部自动化测试(聚合) | shared + server |
| `corepack pnpm --filter @airoaming/server test` | 只跑后端测试 | server |
| `corepack pnpm --filter @airoaming/shared test` | 只跑 shared 测试 | shared |
| `corepack pnpm --filter @airoaming/server test:watch` | watch 模式 | server |
| `corepack pnpm -w typecheck` | 类型检查(三包) | shared + web + server |

## 3. 测试文件约定

- 测试文件放在被测代码同目录,命名 `*.spec.ts`。
- 后端:`apps/server/src/**/*.spec.ts`
- shared:`packages/shared/src/**/*.spec.ts`
- 配置文件:`vitest.config.ts`(各包根目录)。

## 4. 当前测试覆盖

| 测试文件 | 数量 | 锁住的风险 |
| --- | --- | --- |
| `server/src/projects/projects.service.source-guard.spec.ts` | 8 | sourceText 非空校验(2026-06-24 空覆盖 bug 回归保护) |
| `server/src/projects/project-repository.spec.ts` | 6 | Repository 写入→重载往返一致性 + 加载优先级 |
| `server/src/projects/workflow.util.spec.ts` | 9 | 工作流状态机章节状态→步骤映射 |
| `server/src/projects/script-import.util.spec.ts` | 24 | 剧本导入分析纯函数(拆章/分类/边界/格式化/摘要) |
| `server/src/projects/image-provider.service.spec.ts` | 6 | 多图片 provider 请求、响应和配置边界 |
| `server/src/projects/candidate-generation-spec.spec.ts` | 3 | 服务端候选生成规格与来源输入 |
| `server/src/projects/image-candidate.util.spec.ts` | 2 | 候选锁定纯逻辑 |
| `server/src/projects/chapter-script.service.spec.ts` | 2 | 章节剧本服务关键行为 |
| `server/src/projects/image-dimensions.util.spec.ts` | 1 | 图片尺寸读取 |
| `server/src/projects/candidate-reference-resolver.spec.ts` | 1 | 候选参考素材解析 |
| `server/src/projects/image-candidate.contract.integration.spec.ts` | 1 | 候选生成、Asset、任务 artifact 与规格一致性 |
| `server/src/tasks/task-artifact.service.spec.ts` | 1 | 任务输入、输出和错误 artifact |
| `shared/src/script-format.spec.ts` | 15 | 章节剧本格式纯函数(strip/extract/format) |
| **合计** | **79** | shared 15 + server 64 |

## 5. 测试设计原则

1. **事故驱动,非覆盖度驱动**:测试范围由"发生过的事故 + 高风险路径"决定,不为覆盖率数字而测。
2. **测行为契约,不测实现细节**:断言"输入→输出/异常",不断言内部状态、mock 调用次数。实现重构时测试不需要改。
3. **真实临时目录测 fs**:Repository 测试用 `os.tmpdir()` 隔离目录(通过 `AIROAMING_WORKSPACE_ROOT` env 注入),测真实文件行为,不 mock fs。
4. **秒级确定性**:全部测试 < 1 秒,无网络/全局状态/时序依赖,无 flaky。
5. **反向验证**:回归测试在首次编写时验证过"去掉被测逻辑→测试变红",确保非假绿。

## 6. 回归测试与 bug 修复的关系

2026-06-24 修复了"剧本正文被空内容覆盖"的 bug。该 bug 的回归测试由 `projects.service.source-guard.spec.ts` 锁住:

- 修复涉及的 3 个写入入口(saveChapterDraft / updateProjectDraft / writeChapterDraftFromAI)都有"空 sourceText → 抛异常"的测试。
- 修复逻辑被移除时,对应测试会立即失败。

## 7. 尚未覆盖的范围(已知边界)

| 范围 | 状态 | 说明 |
| --- | --- | --- |
| 前端组件测试 | 未建 | 未搭 @vue/test-utils;前端 canSave 判空靠后端校验兜底 |
| e2e 端到端 | 未建 | 全链路(前端→后端→DB/fs)无自动化测试；目标见 `七阶段完整链路验收基线.md` |
| DialogueService / OpenCode 链路 | 未测 | AI 工具分流逻辑未测;写入校验已通过 Service 层覆盖 |
| Service 编排逻辑 | 部分 | 候选生成契约已有集成测试；角色/场景图、七阶段正常/返修/重启路径仍未覆盖 |
| Controller 层 | 未测 | 薄层,Nest 抽象,按需补 |

## 8. 新增测试的指引

新增功能或修复 bug 时:
1. 若涉及数据写入或状态变更,优先补回归测试(输入→异常/输出)。
2. 纯函数放 shared 测;后端业务放 server 测;Repository 用真实临时目录。
3. 回归测试命名清晰,注释说明锁住的是哪个事故或风险。
4. 提交前跑 `corepack pnpm test` + `corepack pnpm -w typecheck`。

## 9. G0 E2E 规划状态

2026-07-11 已形成开发级方案，尚未安装或实现：

- Vitest 继续负责 shared/server 纯函数与 Nest Service characterization。
- Web 完整应用 E2E 选择独立 Playwright Test；首版覆盖 API smoke、项目库、工作区外壳和七阶段流程栏。
- 不在 G0 同时搭 Vue 组件测试；D4/D5 再按编辑器风险评估 Vitest Browser 或少量组件测试。
- 测试 Server 使用临时 `AIROAMING_WORKSPACE_ROOT`、独立端口、loopback fake OpenCode/图片供应商与假 key，禁止真实外部请求。
- 当前排版复制源图和目录式素材包属于已知错误，只测前置门禁，不写绿色成功契约。
- 未来目标行为在对应 G1–G6 切片开始时按 red → green 实施，不批量写长期 skip/todo。

2026-07-11 当前开发波次只实施 G0–G5。矩阵中的 G6 ZIP 与 G7 总验收用例继续保留为长期索引，但不进入当前默认 CI，也不作为 G0–G5 完成门槛。

详细方案：`文档/04_方案与决策/2026-07-11_G0七阶段行为刻画与E2E测试骨架方案.md`。

用例索引：`文档/06_测试与验收/G0七阶段行为用例矩阵.md`。

G3 漫画版式切片开始时，使用既有 G0 Playwright 骨架把必选/非法/不可变用例转为 red → green；数据库层另以临时 SQLite 验证无 default、canonical CHECK 和 immutable trigger，迁移层验证旧值决议与幂等。完整矩阵见 `文档/06_测试与验收/G3漫画版式入口与锁定验收清单.md`。当前清单全部为 `not_run`。

## 10. G5 计划测试面

G5 accepted（尚未实现）需要在 G0 的 Vitest + Playwright 之上新增 renderer golden 和 migration 命令；当前尚未安装或执行：

```text
pnpm test:e2e
pnpm test:render
pnpm test:migration:g5
```

- Shared：LayoutDocument codec/normalize/digest、geometry/crop、RichText、command/inverse/batch、preset/source projection。
- Server：Working Copy 乐观并发、线性 LayoutRevision、stale replacement、preflight、publication task/Outbox/current applicability。
- Web/browser：selection/Undo/autosave/IME/vertical text/手机无写请求/AI pending expiry。
- Renderer：固定 binary/font 连续三次 sha、PNG/PDF/切片结构、语义 bounds 与故障恢复。
- 旧复制源图导出必须先 red 后删除，不能修改断言使其继续 green。

完整 200 项唯一验收标识和真实用户路径见 `文档/06_测试与验收/G5成稿编辑器与确定性导出验收清单.md`。
