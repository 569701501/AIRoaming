---
doc_id: AIR-QA-AUTOMATION-001
status: active
created: 2026-06-24
updated: 2026-07-16
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: 测试基础设施与回归保护、G0 七阶段测试安全网实施
---

# 自动化测试体系

## 1. 当前结论

项目当前使用四类自动化门禁：

1. **Vitest**：shared 纯函数与 Nest Service/文件系统集成测试。
2. **Playwright Test**：真实 Nest HTTP、Vite 页面、Chromium 用户路径和三个受控本地进程的生命周期。
3. **Migration Gate**：Prisma schema、正式 migration tree、fresh/migrated/legacy DB 与 file guard。
4. **Render Gate**：固定 Chromium、固定字体、LayoutDocument/RenderScene、PNG/PDF/条漫确定性输出。

G0 测试安全网已于 2026-07-11 实现；G1～G5 随后已在同一底座上完成 DB-only、版本链、版式、候选返修和正式出版覆盖。2026-07-16 A+ 剧本双流程 P6 总验收已将 5 个公开 Skill、7 个模型阶段、正反触发、严格输出和非模型确认边界锁在同一矩阵；同日汇合后的剧情结构阶段补齐正式正文来源、场景/人物/beat 覆盖、一次定向修复和未发布正文阻断。Shared 严格阶段 fixture 29/29，Server 单进程全量最新为 107 files / 652 tests 全绿，typecheck 与 build 全绿。AI 显式单章生成连续 5/5，随后 AI 创作与已有剧本 B1～B5 合并复核 2/2；剧情结构原页面生成确认路径 1/1。真实 `self/gpt-5.5` 又在两个隔离新项目中分别从 AI 创作正式章节和 import 正式章节生成、预览、确认结构，来源绑定、`structured` 状态、分镜解锁和控制台检查均通过。2026-07-15 的 G0→G5 全矩阵结果继续作为最近一次完整阶段签收；没有把本次局部路径冒充为完整 DB 九项矩阵。G6 素材包和轻量视频不属于本轮。

## 2. 运行命令

| 命令 | 作用 | 当前结果 |
| --- | --- | --- |
| `corepack pnpm typecheck` | shared/web/server 三包类型检查 | 通过 |
| `corepack pnpm typecheck:e2e` | Playwright config、fixture、spec 类型检查 | 通过 |
| `corepack pnpm exec tsc -p tests/e2e/tsconfig.server.json --noEmit` | E2E Nest runtime 专用 tsconfig 检查 | 通过 |
| `corepack pnpm test` | shared + server Vitest 聚合 | 本轮分包复核：Shared 严格阶段 fixture 29/29；Server 单进程 643/643 |
| `corepack pnpm test:e2e:env` | 路径、marker、密钥、loopback、进程清理等环境契约 | 最近签收：34/34 |
| `corepack pnpm test:e2e:prepare` | 真实 env 执行、shared/dist 不变、Nest/Vite source alias | 最近签收：3/3，内部 env 34/34 |
| `corepack pnpm test:e2e` | prepare 后顺序运行 file + DB Chromium 矩阵 | 本轮分阶段复核：file 4/4、G2 DB 2/2、G4 1/1、G5 DB 8/8 |
| `corepack pnpm test:e2e:g5` | G5 DB-only M3～M8 浏览器门禁 | 最近签收：green |
| `corepack pnpm test:render` | fixture、Shared publication、固定 renderer、DB publication recovery | 最近签收：green |
| `corepack pnpm test:migration:g5` | legacy converter/import/cutover 与 migration 边界 | 最近签收：green |
| `corepack pnpm test:e2e:repeat` | prepare 后重复运行 file + DB 矩阵 | 稳定性/故障复核时使用，不替代单次全门禁 |
| `corepack pnpm test:e2e:ui` | 本地 Playwright UI 模式 | 人工调试入口 |
| `corepack pnpm test:all` | 类型、Vitest、prepare、Playwright 聚合门禁 | 本轮未作为单一命令运行；组成门禁已按 G0～G5 分阶段复核 |

新机器首次运行 Chromium 前执行：

```text
corepack pnpm exec playwright install chromium
```

## 3. 测试文件约定

### 3.1 Vitest

- shared/server 测试与被测代码同目录，命名 `*.spec.ts`。
- 后端：`apps/server/src/**/*.spec.ts`。
- shared：`packages/shared/src/**/*.spec.ts`。
- G0 Service fixture：`apps/server/src/projects/test-support/seven-stage-fixture.ts`。
- G0 Service 行为链：`apps/server/src/projects/seven-stage.characterization.integration.spec.ts`。

### 3.2 E2E

```text
playwright.config.ts
tests/e2e/
  setup/                 # global setup/teardown
  api/                   # 公开 HTTP smoke
  web/                   # Chromium 用户路径
  support/               # runtime、环境守卫、fake provider、fixture 与契约自测
  tsconfig.json          # Playwright/E2E TypeScript
  tsconfig.server.json   # Nest E2E runtime TypeScript
```

Playwright 成功运行默认只保留 HTML report；失败证据写入带 runId 的 `test-results/e2e/` 和 `playwright-report/`。这些目录与 `tests/.runtime/` 均被 Git 忽略。

## 4. 当前覆盖

### 4.1 当前 Vitest 基线

| 范围 | 数量 | 锁住的风险 |
| --- | --- | --- |
| shared | 153 | 章节、双流程严格输出契约、版式、版本链、候选定稿、LayoutDocument、命令、publication |
| server | 643 | DB/migration、Repository、A/B 剧本对话与导入批次、P1～P6 创作质量门、任务/Outbox、候选、排版、renderer、备份恢复与 file guard |
| **合计** | **788** | 当前测试清单；最新 Server 全量有 1 个独立可通过的固定超时 |

2026-07-16 P6 总验收后最新 Server 单进程基线为 105 files / 643 tests 全绿。此前 AI 编辑 P5 阶段出现过 `RST-02` 固定 5 秒超时，仅作为历史阶段证据保留，不再代表当前基线。

### 4.2 E2E 契约与 Playwright

| 层级 | 数量 | 覆盖 |
| --- | --- | --- |
| Node 环境契约 | 34 | 危险路径、marker、symlink、最小 env、loopback、DB-only、状态目录、进程身份与故障清理 |
| Prepare 契约 | 3 | env suite 非零且全绿、shared/dist 字节/mode/mtimeNs 不变、Nest/Vite 都解析 shared source |
| Playwright file 矩阵 | 4 | G0 infra、API、项目库/流程栏和生命周期 |
| Playwright DB 矩阵 | 9 | G2 DB gate、G4 候选决策、G5 M4～M8，以及 AI 显式单章和已有剧本导入两条剧本用户路径 |

API/UI spec 虽各自是一个 Playwright test，但内部用 `test.step` 保留矩阵 ID 和失败定位，不把 4 个或 5 个业务步骤误报为 4/5 条独立测试。

## 5. G0 行为边界

G0 当前绿色契约包括：

- 显式 `vertical_scroll` 创建项目、默认章节和七阶段初始投影。
- 完成剧本、确认结构、pending 分镜不推进、确认分镜后推进。
- preflight blocked/ready、候选任务门禁、全镜锁定后进入排版。
- 图片阶段未完成时拒绝排版与素材包，且不产生正式成功事实。
- 公开 Service 重开后可读剧本版本、结构、pending/正式分镜与 preflight 语义。
- 项目库打开测试项目、进入剧本工作区、七阶段顺序/禁用门禁和返回项目库。
- API 删除目标项目不会删除独立控制项目。

G0 明确不覆盖：

- UI-06 直接输入等待阶段 URL。
- G1 DB-only、迁移、SecretStore 与持久任务。
- G2 revision/freshness。
- G3 漫画版式必选、不可修改与数据库约束。
- G4 CandidateLockRevision 与返修影响预览。
- G5 多格、文字、气泡、确定性渲染与 publication。
- G6 素材包 V2/ZIP 和 G7 最终总验收。

一镜一页、复制源图、目录式素材包等旧骨架只保留为 `record_only`，不得添加为当前绿色成功断言。

## 6. E2E 安全与运行时契约

### 6.1 TypeScript 与 shared 解析

- 项目验收环境为 Node 22.17.1。
- E2E TypeScript 与 provider/server/web 服务命令显式使用 `node --import tsx`；根依赖固定 `tsx@4.22.3`。
- `tests/e2e/tsconfig.json` 与 `tests/e2e/tsconfig.server.json` 把 `@airoaming/shared` 映射到 `packages/shared/src/index.ts`。
- programmatic Vite 使用同一 source alias，并排除 shared 预构建。
- `test:e2e:prepare` 禁止运行 `@airoaming/shared build`；prepare 契约会比较 shared/dist 每个文件的字节、mode 与 `mtimeNs`，防止测试准备阶段改写开发产物或触发 watcher。

### 6.2 workspace 与进程

- 每次运行生成唯一 `AIROAMING_E2E_RUN_ID`、三个独立端口和 `airoaming-e2e-<runId>` 临时根。
- 临时根必须具有内容匹配的 `.airoaming-e2e-root` marker；默认仓库 workspace、仓库根、home、文件系统根、symlink 或 marker 不匹配时拒绝启动/清理。
- provider、Nest 与 Vite 只绑定 loopback，`reuseExistingServer=false`，`workers=1`。
- 进程状态文件位于当前 run 目录；清理前复核 PID 身份，不误杀同仓库用户进程。
- `OPENCODE_AUTO_START=false`；子进程使用最小 operational allowlist，图片 key 固定为假值，不继承真实凭据。

### 6.3 浏览器网络

- UI fixture 在创建 page 前安装 HTTP 与 WebSocket 拦截。
- 只允许当前 run 的 Web/Server/fake provider loopback origin。
- 非 loopback 请求在离开浏览器前阻断，并通过 `e2e-browser-network-audit` 附件审计。
- Service Worker 被禁用，避免缓存绕过网络守卫。

## 7. 2026-07-11 正式验证证据

### 7.1 全量与稳定性

- `test:all`：三包 typecheck、E2E typecheck、shared 15、server 72、prepare 3（内部 env 15）与 Playwright 3 全绿。
- `test:e2e:repeat`：明确运行 9 项并 9/9。
- 最终修复后三次独立 `test:e2e` run ID：
  - `g0-53667-mrfy6b4e-27b277e7`
  - `g0-54920-mrfy6gi9-887e4b7b`
  - `g0-56258-mrfy6luj-c735d4f6`
- 每次 prepare、env、Playwright 与 teardown 都通过，无临时 workspace 或受控进程残留。

### 7.2 Runtime/User Review

- trace-on UI run ID：`g0-59887-mrfy7wm2-903aec4f`，1/1。
- HTML report 证明 UI-01～UI-05 全部通过。
- 网络审计：total 111、continuedLoopback 110、continuedExternal 0、blockedExternal 1、blockedInvalid 0；DiceBear 外链被阻断。
- 应用内浏览器只读打开用户当前 `/projects`，确认项目库真实可见结构；没有打开、创建、删除或保存项目。

### 7.3 失败证据演练

为验证 ENV-07，主代理临时加入只读故意失败 spec，完成后已删除，不进入提交：

- run ID：`g0-74023-mrfyk3g3-6aff4225`；命令按预期 exit 1，global teardown 仍成功清理。
- 生成 `test-failed-1.png`、`error-context.md`（含 DOM snapshot 与精确断言位置）、`trace.zip`（21 entries，含 `0-trace.network`、页面资源与快照）和 HTML report。
- report attachments 包含 `e2e-browser-network-audit` 与 `e2e-browser-diagnostics`。
- 网络审计：total 96、continuedLoopback 95、continuedExternal 0、blockedExternal 1、blockedInvalid 0；DiceBear 被阻断。
- 主代理实际查看截图，并检查 trace zip、DOM 与网络附件；该预期失败是故障证据演练，不是全量回归失败。
- 忽略产物路径：`test-results/e2e/g0-74023-mrfyk3g3-6aff4225/`、`playwright-report/g0-74023-mrfyk3g3-6aff4225/`。

### 7.4 2026-07-16 剧本双流程验证

- Shared 全量：26 个测试文件、152/152。
- Server 单 fork 全量：100 个测试文件、590/590，包含不可变原稿、目录一次确认、批次失败隔离、import pending 专用动作和正式版本发布。
- E2E 环境与 prepare：34/34、3/3；E2E TypeScript 检查通过。
- DB-only Chromium：AI 显式单章回归 1/1，run ID `g0-32519-mrmv23vz-0f1c1c09`；已有剧本 B1～B5 真实用户路径 1/1，run ID `g0-31741-mrmv1uvu-b5d7a415`。
- B 路径从页面上传原稿、查看分析结果卡、确认拆章目录、查看整批结果、切章全文只读、检查禁止采用/丢弃/保存/完成，到逐章“确认章节”并进入剧情结构。
- 三包 typecheck、E2E typecheck、三包 build 和 Skill 校验均通过；本轮未运行完整 9 项 DB Playwright 矩阵，不能记为 9/9 新鲜证据。

### 7.5 2026-07-16 已有剧本增强验证

- Shared 全量：26 个测试文件、153/153；新增超长单段确定性切块和中段保留。
- Server 全量：102 个测试文件、594/594；新增长稿隔离 session 分层、Worker 领取/恢复/重试。
- Workspace typecheck、E2E typecheck 和三包 production build 通过。
- DB-only Chromium 已有剧本：后台处理、单章失败、结果卡重试和恢复 pending，1/1，run ID `g0-76138-mrmw3nyk-d0b09659`。
- AI A3～A5 回归 1/1，run ID `g0-76903-mrmw3zp4-e6f88106`。
- G5-M6 SQLite 锁竞争回归 1/1，run ID `g0-77618-mrmw49mu-e8288f33`；空闲导入 Worker 不再周期查询数据库。
- Skill quick validation 通过。未把一次包含其他既有测试干扰的完整 DB 矩阵记作全绿证据。

### 7.6 2026-07-16 剧本导入 P6 固定回归

- 长稿分析新增两个真实 `sourceRef` 的跨文件连续章节，严格全原稿覆盖和全局连续性校验通过。
- 最终目录首次输出和唯一一次格式修复都截断时明确拒绝，不返回叶子片段目录。
- 真实临时 SQLite 执行正式 migration；关闭第一 Nest 应用上下文并新建第二实例后，中断章从 `attempt=1` 恢复到 `attempt=2`，未领取章保持 `queued/attempt=0`。
- 聚焦 2 files / 6 tests、Shared 153/153、Server 单 fork 597/597、typecheck、E2E typecheck 和三包 build 全绿。
- 本轮无页面/API 行为变化，未重复浏览器矩阵；应用上下文重启不得表述为真实 OS kill 或多实例安全证据。

### 7.7 2026-07-16 AI 创作 P1/P2 质量门

- P1 固定反例覆盖：三个候选只换标题、核心语义重复时触发一次定向重写；第二次仍失败时不返回待选灵感。
- P2 固定反例覆盖：格式合法但缺少转折/结果连接、结局空泛、章节卡重复或终章继续桥接时触发一次定向重写；第二次仍失败时不保存待确认大纲。
- 聚焦 3 files / 20 tests、Shared 26 files / 153 tests、Server 单 fork 103 files / 605 tests 全绿。
- Workspace typecheck、E2E typecheck、三包 production build 和两个 Skill quick validation 通过。
- E2E 环境 34/34、prepare 3/3；DB-only Chromium A3～A5 真实用户路径 1/1，run ID `g0-29451-mrmxgkme-dd379acf`。
- 本轮没有新增灵感、大纲、数据库或页面字段，没有新增确认节点，也没有运行完整 DB 9 项矩阵。

### 7.8 2026-07-16 AI 创作 P3/P5 质量门

- P3 固定反例覆盖：剧情描写或动作使用无效占位、场景结束点空泛、多个场景复制相同有效内容、章节卡目标/冲突/转折/钩子在场景正文完全不可观察。
- P5 固定反例覆盖：第 2 章与上一章正式结尾没有任何稳定语义锚点时触发；第 1 章或前章证据不足时跳过，不伪造完整连续性证明。
- A4 服务覆盖：首次质量失败只定向重写一次；第二次仍失败不创建 AI pending。格式失败与质量失败共用一次修订总上限。
- 聚焦 3 files / 28 tests、Shared 26 files / 153 tests、Server 单 fork 全量回归通过；Workspace typecheck、E2E typecheck、三包 production build 和 `script-chapter-drafting` Skill quick validation 通过。
- E2E 环境 34/34、prepare 3/3；DB-only Chromium A3～A5 真实用户路径 1/1，run ID `g0-44915-mrmxxd1t-238758b3`。
- 本轮没有新增章节 Markdown、数据库、页面字段或确认节点；没有改变 A5、导入路线、StoryStructure 或章节编辑链。

### 7.9 2026-07-16 AI 创作 P4 分层修订

- 四层分类固定为“连续性 > 发展性 > 场景与对白 > 文字”；覆盖多层指令取最高层、否定保护语句不被误判为修改目标、未明确层级默认文字层。
- 固定反例覆盖：场景/对白层越权修改本章方向或结尾、文字层改动场景结构、未请求角色名单或基础字段变化、章序变化、输出无有效改动。
- 明确目标覆盖：用户明确改标题、类型或替换角色时允许目标变化，但章序仍保持；建议、评价或“怎么改”类请求不触发 `update_chapter_draft`。
- 服务链覆盖：首次格式或 P4 失败共用一次修订预算；第二次仍失败不创建 revision pending；合格结果仍通过既有 AI pending 供用户采用。
- 聚焦 4 files / 35 tests、Server 单 fork 104 files / 626 tests、Workspace typecheck、E2E typecheck、三包 production build 和 `script-chapter-editing` Skill quick validation 通过。
- E2E 环境 34/34、prepare 3/3；DB-only Chromium A3～A5/P4 真实用户路径 1/1，run ID `g0-92947-mrmyyhd0-10bbf191`。
- 本轮没有新增页面、章节 Markdown 或数据库字段，没有改变导入路线和 StoryStructure；编辑链没有完整前章正式来源，因此不把 P4 结果表述为 P5 编辑连续性完成。

### 7.10 2026-07-16 AI 编辑 P5 连续性不退化

- 纯规则固定反例覆盖：当前稿已经承接前章稳定锚点、改写后删除时拒绝；当前稿原本未承接时不强迫低层修订补救；改写后仍保留时通过；单个通用二元词重合不被当成稳定锚点。
- 服务链覆盖：第 2 章 Prompt 注入第 1 章完整正式正文、版本 ID 和摘要；P4/P5/格式共用一次修订预算；第 1 章跳过 P5；第 2 章缺失前章正式版本时在模型调用前停止。
- 数据库集成覆盖：编辑上下文读取真实上一章当前 `ChapterScriptVersion`；错误摘要不创建 pending；正确章节 ID、版本 ID 和摘要在创建 revision pending 的同一事务通过围栏。
- DB-only Chromium 覆盖：完成第 1 章后，切换第 2 章不会自动生成；用户明确生成、采用、明确改写后，页面仍展示需要再次采用的完整 pending，前章钥匙扣和隧道承接未丢失。
- revision pending 当前仍为 `kind=legacy` 且无持久来源绑定；测试显式锁住该兼容边界，不能把运行时 P5 表述为完整历史来源审计。
- 聚焦 4 files / 40 tests、Workspace typecheck、E2E typecheck、三包 production build 和 `script-chapter-editing` Skill quick validation 通过。
- DB-only Chromium 用户路径连续 3/3 通过，run ID `g0-34516-mrn3ktip-3f6b2fc2`；没有把该路径冒充完整 DB 九项矩阵。
- Server 单 fork 全量为 104 files / 635 tests：634 通过，唯一失败为无关 `RST-02` 固定 5 秒超时；随后隔离复跑 1/1、3.316 秒通过。
- 浏览器复核发现并修复非流式对话被线程轮询提前结算的竞态，新增 `P7-DIALOGUE-DB-02` 数据库回归。

### 7.11 2026-07-16 双流程 P6 总验收

- 固定映射为 5 个公开 Skill、7 个模型阶段：创作 4 阶段，`script-import-normalize` 内部编排 analyze、materialize、verify；后两者是系统阶段，不新增用户对话触发。
- 正反触发覆盖 A2 找灵感、A3 明确题材/选择种子、A4 显式单章生成、A5 明确改写、B1/B2 原稿进入与目录修订；切章、裸继续、评价、普通短创作和批量生成不会误触发对应写入。
- Prompt 契约覆盖 B2 observed/不改写/稳定来源引用，以及 B4 忠实整理、只审计 verify、不要求数据库 ID、不相信模型自报 `readyForNextStage`。
- 聚焦 Server 3 files / 19 tests、Shared 七阶段严格 fixture 29/29；5 个正式 Skill quick validation 全部通过。
- 真实用户路径发现 OpenCode 会话创建与章节页轮询竞态：旧 DB 快照会把刚生成的内存会话号覆盖为 `null`。新增 `P7-DIALOGUE-DB-03`，持久化使用局部不可变会话号；DB hydration 对较新的本地会话号二次核验 active 状态，同时保留重启后关闭旧会话的语义。
- 原失败 AI 显式单章路径连续 5/5，run ID `g0-66673-mrn5bx41-b5c8f29e`；最终 AI 创作 + 已有剧本合并复核 2/2，run ID `g0-75067-mrn5lib6-ea5a74a2`。
- Server 单进程全量 105 files / 643 tests、workspace typecheck、E2E typecheck、三包 production build 全绿；Web 仅保留既有大 chunk 警告。
- 本轮不新增页面、Schema、输出字段、确认节点或公开 Skill；固定样例证明触发和高置信边界，不等同于真实外部模型的完整艺术质量评测。

### 7.12 2026-07-16 剧情结构固定质量门

- Prompt 固定正式章节正文为实际剧情唯一事实源；项目大纲只能提供背景，不能补写正文未发生事件。
- Server 固定反例覆盖：漏正文场景、场景无 beat、非法角色枚举、beat 顺序不连续、未知人物/场景引用、方向或事件空壳、重复 beat，以及 DB Working Copy 未发布却尝试生成。
- 首次 JSON 格式或质量失败共用一次定向修复预算；第二次仍失败返回失败且不创建 pending。
- 聚焦 3 files / 17 tests、Server 全量 107 files / 652 tests、workspace typecheck、E2E typecheck、三包 production build 全绿。
- DB-only Chromium `W1-E2E-05` 1/1：原页面完成“生成剧情结构 → 查看待确认预览 → 确认结构 → 新角色同步”，run ID `g0-83878-mrnaizws-466a9c3e`。
- 本轮没有新增页面、Schema、StoryStructure 字段、确认节点或公开 Skill；固定门只证明正文覆盖和引用完整性，不等同于完整艺术质量评价。

### 7.13 2026-07-16 剧情结构真实模型验收

- 在独立 SQLite、独立 workspace、Server `4328` 和 Web `5188` 中，使用真实 `self/gpt-5.5` 新建两个项目；未读取或修改既有项目数据。
- AI 创作项目按“灵感 → 大纲确认 → 显式生成当前章节 → 采用草稿 → 完成本章”形成 6446 字正式正文；结构首次通过质量门，得到 3 个角色、7 个场景、15 个连续 beat。
- 已有剧本项目在目录整体确认一次后创建 4 个章节入口，后台全部整理为可查看草稿；用户只确认第 1 章，另外 3 章保持 `pending_ready`。第 1 章 1016 字正式正文 `origin=import`，结构首次通过质量门，得到 3 个角色、1 个场景、6 个连续 beat。
- 两个 StoryVersion 均为 `confirmed`，`sourceScriptVersionId` 与各自 `currentScriptVersionId` 完全一致；两个章节均为 `structured`，分镜入口解锁。
- SQLite `integrity_check=ok`；浏览器只有 Vite debug 连接日志，error/warn 为 0。未发现需修改代码的质量门缺陷。
- 该证据是两条真实模型样例的 Runtime/User Review，不替代自动化固定反例、完整 DB 九项矩阵或多题材商业质量评测。

## 8. 测试设计原则

1. **事故和风险驱动**：覆盖发生过的事故、状态门禁、持久化边界与外部服务隔离，不追逐覆盖率数字。
2. **测试公开行为**：断言 DTO、业务状态、错误码、用户可见文本/role/URL 与必要落盘事实，不断言私有 Map、内部调用次数或 DOM class。
3. **真实临时 fs**：Repository 与 Service 测试使用真实临时目录，只 fake 真正的外部 provider。
4. **未来行为按切片 TDD**：`red_on_slice:Gx` 到所属阶段再写 red 并最小实现到 green；不批量保留 skip/todo。
5. **失败同样可诊断**：trace、截图、DOM、网络与服务错误摘要必须足以定位问题，但不能写入 Authorization、真实 key 或图片字节。
6. **反向保护环境**：测试准备与 teardown 本身必须有契约测试；不能只检查业务 spec 是否绿色。

## 9. migration witness 的到期条件

G0 最后一条 Service 用例是文件态 `migration_witness`。G1 只有在以下条件同时满足后才能删除或替换：

1. 测试走真实迁移入口和生产 DB-only 链路。
2. 重启后通过公开 Workbench/API 读回同等 current/pending/confirmed/workflow 不变量。
3. DB-only 激活后修改旧 JSON 不影响正式读取，生产代码无 file fallback。
4. 等价 G1 测试先变绿，再移除 G0 witness，并保留相同失败定位能力。

## 10. 新增测试指引

1. 涉及状态写入或回归事故时，先补最小失败测试，再修改实现。
2. 纯函数放 shared；后端业务放 server；完整 HTTP/路由/浏览器路径放 Playwright。
3. 新浏览器路径先确定公开 role/label/text/URL 锚点，不以 CSS 结构和像素坐标作为核心契约。
4. 新外部依赖必须先进入 loopback fake/adapter 边界和网络 allowlist，不能在测试中访问真实供应商。
5. 提交前至少运行 `corepack pnpm test:all`；涉及 E2E runtime、端口、清理或网络守卫时，额外运行 `corepack pnpm test:e2e:repeat`。
