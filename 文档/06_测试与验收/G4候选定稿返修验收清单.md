---
doc_id: AIR-QA-20260711-G4-CANDIDATE-LOCK
status: active
created: 2026-07-11
updated: 2026-07-12
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: G4候选定稿修订与返修开发方案、G4契约字典、ADR-0010、G0/G1/G2 验收基线
---

# G4 候选定稿返修验收清单

## 1. 使用规则

本清单在 G4 实施时按 red -> green 执行。当前所有项目均为 `not_run`；文档已写好不等于代码已实现或测试已通过。

前置：

1. G0 临时 workspace、fake image provider、API/Chromium E2E 骨架已通过。
2. G1 DB-only、目标 Schema、persistent task、GenerationTaskSource、Asset ready 协议和幂等 importer 已完成。
3. G2 immutable version、current/pending、NewWorkGate、TaskApplicabilityGuard、ChapterProductionState 已完成。
4. G3 项目漫画版式已锁定；G4 不修改该契约。
5. 所有测试使用临时 DB/workspace 和假 key，不触碰真实项目、OpenCode auth 或图片密钥。

状态：

| 状态 | 含义 |
| --- | --- |
| `not_run` | 尚未实现或未执行 |
| `passed` | 有命令、退出码和证据 |
| `failed` | 已执行但不符合契约 |
| `blocked` | 外部前置不满足，必须记录，不得当 passed |

## 2. 测试 fixture

### 2.1 标准章节

```text
project_g4_<runId>
  chapter_001
    current confirmed storyboard: shot_001, shot_002, shot_003
    current confirmed preflight: ready/current

shot_001:
  candidate_A / asset_A ready / source current
  candidate_B / asset_B ready / source current
  candidate_C / asset_C ready / source current

shot_002:
  candidate_D / asset_D ready / source current

shot_003:
  candidate_E / asset_E ready / source current
```

### 2.2 非法候选

```text
candidate_rejected       status=rejected
candidate_superseded     status=superseded
candidate_asset_staged   Asset.status=staged
candidate_asset_missing  FK/迁移异常 fixture
candidate_other_shot     属 shot_002
candidate_other_chapter  属其他 chapter
candidate_historical     source TaskApplicability=historical
candidate_legacy_unknown source TaskApplicability=legacy_unresolved
```

### 2.3 下游 fixture

```text
layoutwc_001
  image_001 -> shot_001 / lockrev_A
  image_002 -> shot_002 / lockrev_D

layoutrev_001 (Chapter.currentLayoutRevisionId)
  binding image_001 -> shot_001 / lockrev_A
  binding image_002 -> shot_002 / lockrev_D

layoutrev_old
  binding image_old -> shot_001 / older lock revision (already stale)

exportrev_001 (Chapter.currentExportRevisionId)
  -> layoutrev_001

task_layout_active
  status=running, source layoutrev_001 + current lock set digest

task_image_active
  status=running, type=image_generate, target=shot_001, source preflight
```

### 2.4 legacy snapshot

至少包含：合法 lockedCandidateId、缺失 Candidate、跨 Shot Candidate、多个 locked status 冲突、selected、layout 匹配/不匹配、export 可识别/无法识别。

## 3. Shared 类型与 parser

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| SHR-01 | CandidateStatus | 精确为 generated/rejected/superseded | `not_run` |
| SHR-02 | 全仓类型 | runtime 无 selected/locked Candidate 枚举值 | `not_run` |
| SHR-03 | CandidateLockAction | 精确 lock/replace/clear | `not_run` |
| SHR-04 | CurrentCandidateDecision | unset/finalized/cleared 分支穷尽 | `not_run` |
| SHR-05 | WorkbenchShotV2 | 无 lockedCandidateId，始终有 currentCandidateDecision | `not_run` |
| SHR-06 | WorkbenchCandidateV2 | favoriteAt/isCurrentFinal/sourceApplicability 完整 | `not_run` |
| PAR-01 | lock/replace 合法 body | 返回 discriminated intent | `not_run` |
| PAR-02 | clear 合法 body | target 规范化 null | `not_run` |
| PAR-03 | clear 含 candidateId/null | 400 CANDIDATE_ID_FORBIDDEN | `not_run` |
| PAR-04 | lock/replace 缺/空 ID | 400 CANDIDATE_ID_REQUIRED | `not_run` |
| PAR-05 | 非法 action/body 类型 | 稳定 400，不招致 500 | `not_run` |
| PAR-06 | commit 省略 expected | 400 EXPECTED_CURRENT_REVISION_REQUIRED | `not_run` |
| PAR-07 | 首次 commit expected=null | 解析成显式 null | `not_run` |
| PAR-08 | digest 非 sha256:64hex | 400 IMPACT_DIGEST_INVALID | `not_run` |
| PAR-09 | reason 空/空白 | 存 null | `not_run` |
| PAR-10 | reason 500/501 字符 | 500 可接受，501 返 400 | `not_run` |
| PAR-11 | 额外字段 | 400 CANDIDATE_LOCK_BODY_UNKNOWN_FIELD | `not_run` |

## 4. SQLite 约束

| ID | 动作 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| DB-01 | fresh migration | CandidateLockRevision/Shot current 表字段与 FK 存在 | `not_run` |
| DB-02 | 同 Shot 重复 revision | unique 拒绝 | `not_run` |
| DB-03 | 同 previous 分叉两条 next | unique 拒绝 | `not_run` |
| DB-04 | lock/replace candidateId null | CHECK 拒绝 | `not_run` |
| DB-05 | clear candidateId 非空 | CHECK 拒绝 | `not_run` |
| DB-06 | first revision 非 v1/lock | trigger 拒绝 | `not_run` |
| DB-07 | previous.clear 后 replace | trigger 拒绝 | `not_run` |
| DB-08 | previous.finalized 后 lock | trigger 拒绝 | `not_run` |
| DB-09 | replace 同 Candidate | trigger 拒绝重复修订 | `not_run` |
| DB-10 | revision != previous+1 | trigger 拒绝 | `not_run` |
| DB-11 | cross Shot previous/candidate/current pointer | FK/trigger 拒绝 | `not_run` |
| DB-12 | 从同 Shot 非 current 历史 revision 插入 detached next | trigger 拒绝，线性链不分叉/不断层 | `not_run` |
| DB-13 | runtime decidedAt null | CHECK/trigger 拒绝 | `not_run` |
| DB-14 | legacy decidedAt null | 允许，recordedAt 非空 | `not_run` |
| DB-15 | UPDATE revision row | immutable trigger 拒绝 | `not_run` |
| DB-16 | DELETE revision row | immutable trigger/FK 拒绝 | `not_run` |
| DB-17 | 废弃当前定稿 Candidate | trigger/service 拒绝 | `not_run` |
| DB-18 | 硬删历史引用 Candidate | FK restrict | `not_run` |
| DB-19 | revision insert 后 pointer update 故障 | 整个事务回滚 | `not_run` |
| DB-20 | `integrity_check/foreign_key_check` | `ok` / 0 行 | `not_run` |
| DB-21 | schema checksum | trigger/index 与发布清单一致 | `not_run` |

## 5. 修订状态机

| ID | 初始 | 操作 | 必须断言 | 状态 |
| --- | --- | --- | --- | --- |
| FSM-01 | unset | lock A | v1 lock(A), previous=null | `not_run` |
| FSM-02 | unset | replace B | ACTION_INVALID，无写入 | `not_run` |
| FSM-03 | unset | clear | ACTION_INVALID，无写入 | `not_run` |
| FSM-04 | finalized A | replace B | v2 replace(B), previous=v1 | `not_run` |
| FSM-05 | finalized A | replace A | no_op，行数/updatedAt 不变 | `not_run` |
| FSM-06 | finalized A | clear | next clear, candidate=null | `not_run` |
| FSM-07 | cleared | clear | no_op，不再新增 clear | `not_run` |
| FSM-08 | cleared | lock A | next lock(A) | `not_run` |
| FSM-09 | cleared | replace B | ACTION_INVALID | `not_run` |
| FSM-10 | v1 A -> v2 B -> v3 clear -> v4 A | 查历史 | 四条线性链，v1 未复用 | `not_run` |
| FSM-11 | 历史详情 | 尝试“设回 current” | 无该 API；必须新 lock/replace | `not_run` |

## 6. Lock set codec

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| LKS-01 | 所有 active Shot finalized | state=complete，digest 非空 | `not_run` |
| LKS-02 | 一 Shot unset | incomplete + missingShotIds，digest/sourceApplicability=null | `not_run` |
| LKS-03 | 一 Shot clear | incomplete + clearedShotIds，digest/sourceApplicability=null | `not_run` |
| LKS-04 | current pointer 断链 | unresolved + unresolvedShotIds，digest/sourceApplicability=null | `not_run` |
| LKS-05 | Candidate/Asset 断链 | unresolved | `not_run` |
| LKS-06 | retired Shot 无定稿 | 不阻塞 complete | `not_run` |
| LKS-07 | 只改 Storyboard order | digest 不变 | `not_run` |
| LKS-08 | 数据库返回顺序变化 | digest 不变 | `not_run` |
| LKS-09 | 任一 Shot revision ID 变化 | digest 必变 | `not_run` |
| LKS-10 | 只改 Candidate label/favorite | digest 不变 | `not_run` |
| LKS-11 | 字节黄金 fixture | Node 与独立 JCS/SHA256 实现结果一致 | `not_run` |
| LKS-12 | 定稿结构完整，上游后续更新 | state=complete + sourceApplicability=historical，新排版返回 CANDIDATE_LOCK_SET_SOURCE_NOT_CURRENT | `not_run` |
| LKS-13 | legacy 定稿有直接指针但无来源 | state=complete + sourceApplicability=legacy_unresolved | `not_run` |
| LKS-14 | strict codec 收到 incomplete/unresolved + 非空 sourceApplicability | 拒绝；不得伪填适用性 | `not_run` |
| LKS-15 | strict codec 收到 complete + null 或任意 unknown field | 拒绝；Shared/runtime/response DTO nullability 一致 | `not_run` |

## 7. 影响分析与 digest

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| IMP-01 | 首次 lock，无下游 | affected 全空 | `not_run` |
| IMP-02 | Working Copy 引 current revision | 对应 element 进 affected | `not_run` |
| IMP-03 | Working Copy 引更旧 revision | 计 alreadyStale，不进 newly affected | `not_run` |
| IMP-04 | Working Copy 无 source revision | unresolved count，不猜 current | `not_run` |
| IMP-05 | legacy placement 无 ID | 同 document digest 稳定生成同 elementId | `not_run` |
| IMP-06 | formal binding 引 current revision | binding/layout revision 去重进集 | `not_run` |
| IMP-07 | 同 element 多 role | affected element 只计一次 | `not_run` |
| IMP-08 | Export 指受影响 layout | Export ID 进集，文件不变 | `not_run` |
| IMP-09 | 活动 layout/package task | Task ID 进集 | `not_run` |
| IMP-10 | 同 Shot image_generate task | 不进集 | `not_run` |
| IMP-11 | finished/cancelled task | 不进 activeTaskIds | `not_run` |
| IMP-12 | 不同 DB 行顺序 | impactDigest 一致 | `not_run` |
| IMP-13 | 只改展示文案/数量字段 | digest 不变 | `not_run` |
| IMP-14 | 受影响 element/layout/export/task 增减 | digest 必变 | `not_run` |
| IMP-15 | action/target/expected 任一变化 | digest 必变 | `not_run` |
| IMP-16 | no-op preview | affected 数组为空，digest 仍合法 | `not_run` |

## 8. Preview API

| ID | 动作 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| PRE-01 | unset + lock A | expected=null，noOp=false，无 DB 写 | `not_run` |
| PRE-02 | finalized A + replace B | 返回当前 v1 和完整影响 | `not_run` |
| PRE-03 | finalized A + replace A | noOp=true | `not_run` |
| PRE-04 | finalized A + clear | targetCandidateId=null | `not_run` |
| PRE-05 | cleared + lock B | 合法，影响不冒充旧 stale | `not_run` |
| PRE-06 | Candidate 跨 Shot | 400 mismatch，不泄露其他项目细节 | `not_run` |
| PRE-07 | rejected/superseded/Asset 未 ready | 稳定错误 | `not_run` |
| PRE-08 | candidate source historical/unresolved | 409 SOURCE_NOT_CURRENT | `not_run` |
| PRE-09 | G2 上游 dirty/pending/stale | preview 可展示影响，响应明确 commitBlockedReason | `not_run` |
| PRE-10 | 任意 preview | CandidateLockRevision/Shot/Layout/Export/Task 计数不变 | `not_run` |

## 9. Commit API

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| COM-01 | 合法首次 lock | created v1 + Shot pointer，同事务 | `not_run` |
| COM-02 | 合法 replace | created next + 返回最新 lock set/workflow | `not_run` |
| COM-03 | 合法 clear | created clear + lock set incomplete | `not_run` |
| COM-04 | expected 过期 | 409 REVISION_CONFLICT，无写 | `not_run` |
| COM-05 | impact 过期 | 409 IMPACT_CHANGED，无写 | `not_run` |
| COM-06 | preview 后 Candidate 被 rejected | 提交拒绝，无写 | `not_run` |
| COM-07 | preview 后 Asset 不 ready | 提交拒绝，无写 | `not_run` |
| COM-08 | preview 后上游变 stale | 409 UPSTREAM_WORK_NOT_CONFIRMED | `not_run` |
| COM-09 | 当前 Candidate 相同 | no_op，revision/updatedAt 不变 | `not_run` |
| COM-10 | 提交成功 | 旧 Layout/Export/Asset 内容 digest/mtime 不变 | `not_run` |
| COM-11 | 提交成功 | Chapter currentLayout/currentExport/milestone 不变 | `not_run` |
| COM-12 | 受影响任务 | commit 后发 cancelRequested，取消失败不回滚 lock | `not_run` |
| COM-13 | 响应 | Server 权威 shot/candidates/lockSet/productionState 一致 | `not_run` |

## 10. 并发、重放与故障注入

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| CON-01 | 成功 v1 后丢响应，重放同 lock | result=replayed，仍仅 v1 | `not_run` |
| CON-02 | 成功 v2 后丢响应，重放同 replace | replayed，仍仅 v2 | `not_run` |
| CON-03 | 成功 clear 后丢响应 | replayed，不生第二 clear | `not_run` |
| CON-04 | 同 expected 并发 B/B | 一个 created，一个 replayed，仅一条 next | `not_run` |
| CON-05 | 同 expected 并发 B/C | 一个 created，一个 409，不分叉 | `not_run` |
| CON-06 | A->B->C 后重放旧 A->B | 409，不把 B 历史匹配当 replay | `not_run` |
| CON-07 | A->B->A 后重放最早 lock A | 409，v3 不被认为 v1 replay | `not_run` |
| CON-08 | 两 SQLite writer 触发 BUSY/unique | 重读并稳定分类 replay/conflict，无 500 | `not_run` |
| CON-09 | insert revision 后注入崩溃 | 重启后 revision/pointer 全无 | `not_run` |
| CON-10 | pointer update 后 commit 前崩溃 | 整事务回滚 | `not_run` |
| CON-11 | commit 后、取消任务前崩溃 | lock 保留，旧任务完成仍 historical | `not_run` |

## 11. 收藏、废弃和恢复

| ID | 操作 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| PRF-01 | favorite generated | Server 设 favoriteAt | `not_run` |
| PRF-02 | 重复 favorite | 幂等，无额外下游变化 | `not_run` |
| PRF-03 | unfavorite | favoriteAt=null | `not_run` |
| PRF-04 | reject 非 current | status=rejected + favoriteAt=null | `not_run` |
| PRF-05 | restore rejected | status=generated，favoriteAt 仍 null | `not_run` |
| PRF-06 | reject 当前定稿 | 409 CANDIDATE_IS_CURRENT_FINAL | `not_run` |
| PRF-07 | reject/restore superseded | 409 transition invalid | `not_run` |
| PRF-08 | reject 历史引用 Candidate | 允许，历史 Asset/Layout/Export 不变 | `not_run` |
| PRF-09 | 任意 favorite/reject | CandidateLockRevision 行数、lock set digest、layout/export freshness 不变 | `not_run` |

## 12. Layout/Export freshness 和门禁

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| FRS-01 | binding revision == Shot current finalized | resolution=current | `not_run` |
| FRS-02 | binding revision != Shot current | stale + SOURCE_LOCK_CHANGED | `not_run` |
| FRS-03 | Shot current clear/unset | 可解析旧 binding 为 stale | `not_run` |
| FRS-04 | binding 缺 source revision | unresolved | `not_run` |
| FRS-05 | binding 跨作用域/断链 | unresolved | `not_run` |
| FRS-06 | 任一 unresolved + 其他 stale | 聚合 unresolved 优先 | `not_run` |
| FRS-07 | Layout sourceLockSetDigest != bindings 重建 | unresolved/integrity_blocked，导出返回 LAYOUT_SOURCE_DIGEST_MISMATCH | `not_run` |
| FRS-08 | current pointer layout + source current | position=current, ArtifactFreshness=current | `not_run` |
| FRS-09 | current pointer layout + source stale | position=current, ArtifactFreshness=stale | `not_run` |
| FRS-10 | historical layout + source current/stale | ArtifactFreshness=historical，详情保留 sourceResolution | `not_run` |
| FRS-11 | stale layout 尝试新导出 | 409 LAYOUT_SOURCE_STALE，无 task/revision | `not_run` |
| FRS-12 | unresolved layout 尝试导出 | 409 LAYOUT_SOURCE_UNRESOLVED | `not_run` |
| FRS-13 | Export completionApplicability=current 后 replace | 存储值不变，查询 source stale | `not_run` |
| FRS-14 | replace/clear | currentLayout/currentExport pointer 不清空 | `not_run` |
| FRS-15 | 新 current layout/export 完成 | 切新 pointer，旧行转 historical 由 position 派生 | `not_run` |

## 13. 任务适用性

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| TSK-01 | current layout export task 启动 | sources 含 LayoutRevision + lock set digest | `not_run` |
| TSK-02 | 运行中 replace | task 进 preview activeTaskIds，发 cancelRequested | `not_run` |
| TSK-03 | 任务忽略取消并成功 | succeeded + historical，不切 currentExport | `not_run` |
| TSK-04 | 任务完成与 replace 竞争 | 任一串行顺序都不会用旧源覆盖新 current | `not_run` |
| TSK-05 | 新 Candidate image task | 只绑 current Preflight，不绑 lock set | `not_run` |
| TSK-06 | images/layout/exported 里程碑后生成 Candidate | 上游 current 时可创建，不让 layout stale | `not_run` |
| TSK-07 | 上游 dirty/pending/stale | 新 image task 仍被 G2 NewWorkGate 拒绝 | `not_run` |

## 14. 工作流和完成动作

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| WF-01 | 未全定稿 | 候选 active，不可 complete | `not_run` |
| WF-02 | 首次全定稿 | 可 complete，里程碑到 images_done | `not_run` |
| WF-03 | complete 重放 | 幂等，里程碑/updatedAt 不无意义抖动 | `not_run` |
| WF-04 | 已达里程碑 replace | 候选 done，排版 needs_update | `not_run` |
| WF-05 | 已达里程碑 clear | 候选 needs_update，新排版/导出阻塞 | `not_run` |
| WF-06 | clear 后 re-lock | 候选自动恢复 done，不再要求 complete | `not_run` |
| WF-07 | 任意返修 | Chapter.milestoneStatus 不回退 | `not_run` |
| WF-08 | 历史 layout/export | historyAvailable=true，不算 failed | `not_run` |

## 15. API 与旧路径清理

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| API-01 | GET workbench | 新 Shot/Candidate/lockSet/productionState 契约 | `not_run` |
| API-02 | GET revision history | 按 revision DESC 稳定分页，不可写 | `not_run` |
| API-03 | 其他 Project/Chapter/Shot ID 组合 | 404/400 稳定且无跨域读写 | `not_run` |
| API-04 | 旧 POST candidate/lock | 路由不存在或明确升级错误，不绕 preview | `not_run` |
| API-05 | 旧 lockedCandidateId payload | Server 不接受写入 | `not_run` |
| API-06 | 旧 Candidate status=locked/selected payload | runtime parser 拒绝 | `not_run` |

## 16. Web / Playwright

| ID | 操作 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| UI-01 | 打开候选图工作台 | 仍是原七阶段入口，无新独立工作台 | `not_run` |
| UI-02 | 无定稿候选卡 | 显示收藏/废弃/定稿此图 | `not_run` |
| UI-03 | 首次定稿且零影响 | 内部先 preview，可不弹大型影响框 | `not_run` |
| UI-04 | 当前定稿卡 | 显示“当前定稿”，不显示 locked 内部词 | `not_run` |
| UI-05 | 其他可用候选 | 主按钮“更换为定稿图” | `not_run` |
| UI-06 | replace 零影响 | 仍弹确认，说明建新定稿 | `not_run` |
| UI-07 | replace 有影响 | 准确显示格子/布局/导出/任务数量 | `not_run` |
| UI-08 | clear | 总是确认，说明会阻塞新排版 | `not_run` |
| UI-09 | 影响弹窗文案 | 无 stale/digest/revision ID，有“旧导出保留” | `not_run` |
| UI-10 | 409 revision conflict | 刷新当前决策，不自动覆盖 | `not_run` |
| UI-11 | 409 impact changed | 重新展示新 preview，不自动提交 | `not_run` |
| UI-12 | 废弃当前定稿 | 按钮不可用，Server 仍会拒绝绕过 | `not_run` |
| UI-13 | 候选预览大图 | 与缩略卡使用同一 action policy | `not_run` |
| UI-14 | 已导出章节 | 可继续生成新候选 | `not_run` |
| UI-15 | 只生成/收藏新候选 | 排版/导出仍 current | `not_run` |
| UI-16 | 正式 replace 后进排版 | 显示 N 个来源已更新，新导出不可用 | `not_run` |
| UI-17 | 查历史导出 | 旧文件仍可查看/下载 | `not_run` |
| UI-18 | 键盘/读屏 | 按钮 label、dialog title/description、focus return 可感知 | `not_run` |

## 17. 迁移演练

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| MIG-01 | 合法 lockedCandidateId | 建 legacy v1 + Shot current | `not_run` |
| MIG-02 | 候选缺失/跨 Shot/Asset 异常 | blocker，不猜 revision | `not_run` |
| MIG-03 | 仅 Candidate.status=locked | 转 generated，不建 current | `not_run` |
| MIG-04 | 多 locked status 冲突 | 依 Shot 直接证据或 blocker，不选最新 | `not_run` |
| MIG-05 | status=selected | generated + favoriteAt，不建 lock | `not_run` |
| MIG-06 | selected 无可靠时间 | 使用 recordedAt 并记 warning | `not_run` |
| MIG-07 | layout candidate == v1 candidate | 回填 source revision | `not_run` |
| MIG-08 | layout candidate != v1/无 v1 | unresolved，不反推假 revision | `not_run` |
| MIG-09 | legacy placement 无 ID | deterministic elementId | `not_run` |
| MIG-10 | export 可识别 | 绑 layout/source，历史文件保留 | `not_run` |
| MIG-11 | export 无来源 | legacy_unresolved，不设 current | `not_run` |
| MIG-12 | 同 snapshot 重跑 | ImportedEntitySource no-op，无重复 revision | `not_run` |
| MIG-13 | source digest 改变 | SOURCE_MUTATED_AFTER_IMPORT，不改已导入行 | `not_run` |
| MIG-14 | 真实 workspace 对照 | pre/post manifest 一致，无反写 | `not_run` |

## 18. 重启、备份与恢复

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| RST-01 | v1/v2/clear 后重启 | current pointer/历史链完整 | `not_run` |
| RST-02 | stale layout 后重启 | 仍派生 stale，不回退里程碑 | `not_run` |
| RST-03 | 备份/恢复 | revision 数、指针、digest、历史 Asset 一致 | `not_run` |
| RST-04 | 改坏旧 candidates/storyboard JSON | DB-mode 当前定稿不受影响 | `not_run` |
| RST-05 | 旧任务在重启后完成 | applicability guard 仍防止覆盖 | `not_run` |

## 19. 性能、安全与可观测性

| ID | 场景 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| NFR-01 | 100 Shot/100 LayoutRevision/100 Export 预览 | 使用索引，无 N+1 Asset 文件读取 | `not_run` |
| NFR-02 | `EXPLAIN QUERY PLAN` | impact 主查询命中 lock/binding/export/task 索引 | `not_run` |
| NFR-03 | 超长/恶意 ID/reason/body | 稳定 4xx，无 SQL/路径泄露 | `not_run` |
| NFR-04 | 错误/日志扫描 | 无 key、prompt 全文、input JSON、本地绝对路径 | `not_run` |
| NFR-05 | digest 日志 | 可记 ID/digest/code，不记素材二进制 | `not_run` |
| NFR-06 | commit trace | 可关联 preview policy、expected/new revision、result created/replayed/no_op | `not_run` |

## 20. G4/G5 边界验收

| ID | 检查 | 必须断言 | 状态 |
| --- | --- | --- | --- |
| BND-01 | G4 新代码 | 没有提前实现高自由 LayoutDocument 编辑器 | `not_run` |
| BND-02 | Working Copy 影响 | 由 codec 临时投影，没有新可写 binding 真值表 | `not_run` |
| BND-03 | G4 排版页 | 只显 stale 摘要/门禁，不静默替图 | `not_run` |
| BND-04 | G5 接入面 | 可直接读 element source revision、current lock set、freshness reason | `not_run` |
| BND-05 | G5 尚未完成 | G4 不被宣称为“完整画布返修可发布” | `not_run` |

## 21. 建议命令和证据

实施后至少执行：

```bash
corepack pnpm test
corepack pnpm -w typecheck
corepack pnpm prisma:validate
corepack pnpm typecheck:e2e
corepack pnpm test:e2e
corepack pnpm test:e2e -- --repeat-each=3
```

还必须保留：

- migration checksum、`sqlite_master` 约束/trigger 证据。
- `PRAGMA integrity_check` 和 `PRAGMA foreign_key_check`。
- 并发/故障注入的 revision 行、Shot pointer 和任务 applicability 查询结果。
- Playwright screenshot/trace：影响弹窗、409 重新确认、排版来源已更新、历史导出可查看。
- 测试 runId、临时 workspace marker、Node/pnpm/Chromium 版本和退出码。

## 22. Runtime/User Review

人工用户路径至少执行：

1. 每镜生成多张候选，收藏两张，确认下游不变。
2. 定稿 A，完成本章候选图，创建布局/导出 fixture。
3. 返回候选图，查看更换 B 的准确影响，确认旧产物保留文案。
4. 提交 B，确认候选仍 done、排版 needs_update、旧导出可打开。
5. 取消定稿，确认该 Shot 阻塞新排版；再 lock A，确认产生第四条修订。
6. 已导出状态下生成新 Candidate 但不定稿，确认当前布局/导出不 stale。
7. 运行 layout export task 时 replace，确认迟到结果只能 historical。
8. 模拟两个窗口并发选 B/C，确认一个成功、一个明确冲突。

## 23. 签收闸门

| 复核 | 必须结论 | 状态 |
| --- | --- | --- |
| Static/Scrutiny Review | 契约、Schema、索引、事务、测试和迁移无 blocker | `not_run` |
| Runtime/User Review | A->B->clear->A、导出后返修和迟到任务真实路径通过 | `not_run` |
| Handoff | 改动文件、命令、证据、风险、G5 接入面完整 | `not_run` |

G4 未完成 G5 画布修复交互是已知边界，不是 G4 漏测；但如果 stale 不能被查询、不能阻止新导出或旧产物被改写，G4 必须判定失败。
