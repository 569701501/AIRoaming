---
doc_id: AIR-G3M-CONSTRUCTION-DEPENDENCY-001
status: active
created: 2026-07-12
updated: 2026-07-13
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: G1 M2～M4、G3 漫画版式迁移字典、G3-core 完成记录与当前代码
---

# G3-M 施工包：依赖边界与切片门禁

## 1. 文档定位

本文是 G3 剩余功能的唯一施工入口。G3-M 不是一个独立的漫画版式 mapper，而是：

```text
G1 maintenance/snapshot/import/backup/cutover runtime
  + G3 comic-format decision plugin
  + 真实 workspace rollout evidence
```

Luna 不得把本文交付压成一个任务，也不得在缺少 G1 前置时直接实现或运行 production activate。

## 2. 当前真实基线

| 能力 | 当前状态 | 证据/影响 |
| --- | --- | --- |
| G3-core | completed | 两值、0010、API/PATCH、file alias、Candidate/Prompt V2 已实现并通过自动测试 |
| 迁移表与数据库约束 | available | PersistenceState、MigrationRun、ImportedEntitySource、MigrationIssue 已存在 |
| maintenance coordinator | implemented | open→draining→closed、mutation lease、participant/runtime bundle 与 maintenance CLI 已有 MNT-01～06 证据 |
| snapshot/runtime bundle | implemented | immutable snapshot、source/snapshot manifest、redactor、runtime bundle 与 path guard 已有 SNP/runtime 证据 |
| importer/decision runner | implemented（shadow） | M2 decision codec、M3-A0～A15 切片、16-slice full shadow、db:import CLI 已实现；final 继续 fail-closed |
| verifier/shadow replay | completed | 来源证据注册表、integrity/FK/ledger/API/Asset/DB-only/pending Dialogue、双 fresh、replay 与正式签字均已完成 |
| backup/restore | hardening_required | M5-A0～A3 已实现 happy path；A4 需收口一致性、ledger/release identity、secret/path/补偿故障矩阵 |
| DB-only activate | prerequisite_blocked | trigger 存在，但 capability gate、SecretStore、backup 和 activate CLI 尚未满足 |
| DB runtime coverage | partial | 已交付业务 read-model 与 DB-only 写隔离；Settings/SecretStore、backup/activate 等仍保留为后续门禁 |
| SecretStore/明文清除 | not_implemented | 正式 C3/C4/C7 前置未满足；M5-A0 必须登记，D2/M6 前另行闭合 |

结论：M0～M4 foundation 已完成；M5-A0～A3 已提交但正式验收退回 `hardening_required`，当前只可从 M5-A4-1 继续。不能执行 final import、pre-cutover backup 或真实 C7 激活。

## 3. 交付名称与允许声明

| 名称 | 含义 | 当前 |
| --- | --- | --- |
| G3-M foundation | M0～M4 在临时 workspace/SQLite 完成 | completed |
| G3-M rollout tooling | M5 备份恢复 happy path 已实现，A4 专项收口未完成 | hardening_required |
| G3 production-ready | 全量 DB capability、SecretStore、final import、真实切换与复核均通过 | release_blocked |

只完成 mapper 不得写 G3-M completed；只完成临时 final run 不得写 production-ready。

## 4. 强制前置门

### 4.1 开发门 D0

- 当前工作树有明确 commit，任务书写出 commit SHA。
- G3-core typecheck/server tests/G1 三项 check 全绿。
- 所有测试使用临时 workspace、临时 dataRoot、临时 DB 和 fake key。
- 不访问默认真实 workspace。

### 4.2 Shadow 门 D1

开始 M3 前必须：M0 maintenance、M1 snapshot、M2 decision codec 全绿；snapshot sourceManifestDigest 与 decisions artifact 绑定。

### 4.3 Final-ready 门 D2

允许在临时根执行 final run 前必须：

- 全量 importer 覆盖 G1 导入顺序，不只是 Project/comicFormat。
- DB capability registry 中 required 项全部 implemented。
- SecretStore、CredentialRedactor、runtime bundle 已实现。
- 两轮 fresh shadow import、幂等重跑与公开 DTO 对照通过。

### 4.4 Activate 门 D3

运行 activate CLI 前还必须：

- 用户对本次真实停写再次明确授权。
- 协调 backup/restore 演练通过。
- final MigrationRun=succeeded、open blocker=0。
- PersistenceState=ready_for_activation 且身份与 final run/effective manifest 完全一致。
- DB maintenance smoke 通过，firstBusinessWriteAt 仍为 null。
- 旧 metadata 已按 G1 C6 规则封存，Secret 扫描为 0。

当前 M4 临时实现、双 fresh 证据与正式签字已完成；M5-A0～A3 已实现但 A4 尚未完成。D2 的 Settings/SecretStore、final importer 和 required capability 仍未满足；D3 也未满足。不能借 M5 happy path 放行 pre-cutover 或 activate。

## 5. 施工切片

| 切片 | 目标 | 主要输出 | 可否现在领取 |
| --- | --- | --- | --- |
| G3-M0 | maintenance gate 与 participant/runtime bundle 骨架 | MaintenanceModule、同进程控制、活动计数、503 | 是 |
| G3-M1 | immutable snapshot 与 manifest | snapshot CLI、redactor、pre/post/source/snapshot digest | 是，依赖 M0 closed fixture |
| G3-M2 | 版式 mapper、decision artifact 与 MigrationIssue codec | 决议文件、JCS digest、issue/report codec | 是，依赖 M1 |
| G3-M3 | importer run/账本与 comic-format plugin | audit/shadow/final runner、确定性 sourceKey/ID | 是，但全量 importer 需分子切片 |
| G3-M4 | verifier 与 shadow/replay | integrity/FK/ledger/API/asset/source 对照 | completed，2026-07-13 已签字 |
| G3-M5 | offline coordinated backup/restore | db-capabilities、app-backup/app-restore、manifest、空根恢复 | hardening_required；当前只领取 M5-A4-1 |
| G3-M6 | final cutover/activate/rollback | cutover coordinator、activate CLI、Runtime Review | 阻塞，D2/D3 通过后领取 |

## 6. 依赖图

```text
M0 maintenance
  -> M1 snapshot/runtime bundle
    -> M2 decision artifact
      -> M3 importer
        -> M4 shadow verifier（completed）
          -> D2 full DB/SecretStore gate
            -> M5 backup/restore rehearsal
              -> D3 user-authorized M6 cutover/activate
```

M0～M4 可在当前波次推进；M6 不得通过补默认值、跳过 SecretStore 或只导入 Project 来绕过。

## 7. 计划代码边界

```text
apps/server/src/maintenance/
  maintenance.module.ts
  maintenance-coordinator.service.ts
  maintenance-participant.ts
  maintenance-admin.controller.ts
  maintenance-error.filter.ts
apps/server/src/migration/
  snapshot-manifest.ts
  snapshot.service.ts
  credential-redactor.ts
  runtime-bundle.service.ts
  migration-decision.ts
  comic-format-migration.plugin.ts
  migration-run.repository.ts
  migration-import.service.ts
  migration-verify.service.ts
  db-capability-registry.ts
apps/server/src/backup/
  app-backup.service.ts
  app-restore.service.ts
apps/server/src/cli/
  maintenance.ts
  db-snapshot.ts
  db-audit.ts
  db-import.ts
  db-verify.ts
  app-backup.ts
  app-restore.ts
  db-activate.ts
```

实际拆文件可微调，但模块职责、协议名和 CLI 语义不得自行合并成 normalize/migrate-all 等含糊入口。

## 8. Stop condition

遇到任一条件立即停止当前切片并记录 blocker：

- 需要读取或写入默认真实 workspace/正式 DB。
- 需要给 four_panel、缺失或非法值自动选择 canonical。
- snapshot pre/post manifest 不一致或发现 symlink/越界。
- decision sourceManifestDigest、issueKey 或 sourceDigest 不匹配。
- DB capability registry 仍有 required=unsupported 却准备 activate。
- final run 有 open blocker，或 verification identity 与当前 release 不同。
- 备份未完成恢复演练、Secret 扫描非零、firstBusinessWriteAt 已非空却尝试 file-only。

## 9. Luna 单切片任务书

```text
目标切片：G3-Mx
当前 commit：<sha>
必读：本施工包五份资料 + 对应 G1/G3 原方案章节
允许修改：<精确文件>
禁止修改：相邻切片、真实 workspace、G5
输入 fixture：<临时绝对路径>
最小测试：<精确命令>
退出证据：<测试、report、残留 blocker>
Stop condition：<从本文复制>
```

禁止给 Luna 的任务描述：按照 G3 剩余文档全部做完、缺什么自己补、直接切到 DB-only。

M5-A0～A3 已提交；当前从 `文档/05_执行与记录/任务记录/2026-07-13_G3-M5A4验收收口/handoff.md` 领取 A4-1，不得跳到 D2/M6。

## 10. 完成判定

G3-M foundation：M0～M4 的实现、临时证据与正式签字齐全，当前状态为 `completed`；仍不得写成 production-ready。

G3-M completed：M5-A4、D2、M6 工具、回滚和全部 deferred 用例通过。

G3 production-ready：另加真实 workspace audit=0、用户授权切换、Runtime/User Review 与观察期证据。三者不得混写。
