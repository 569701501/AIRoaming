# <功能或阶段>验收清单

---
doc_id: AIR-QA-YYYYMMDD-XXX
status: draft
created: YYYY-MM-DD
updated: YYYY-MM-DD
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: 需求 / 方案 / ADR / 实现任务
---

## 1. 验收目标与范围

- 本次要证明：
- 范围内：
- 范围外：
- 前置阶段或依赖：

## 2. 事实源与版本

| 类型 | 路径 / 版本 | 用途 |
| --- | --- | --- |
| 需求或方案 |  |  |
| ADR |  |  |
| 数据/任务/文件契约 |  |  |
| 实现任务或提交 |  |  |
| 测试环境 |  |  |

## 3. 结果状态

每个用例只允许使用下列状态，不能用模糊的“基本通过”：

| 状态 | 含义 |
| --- | --- |
| `not_run` | 尚未执行 |
| `passed` | 已执行且证据满足预期 |
| `failed` | 已执行但结果不满足预期 |
| `blocked` | 因明确外部条件无法执行 |
| `not_applicable` | 经说明后确认不适用 |

## 4. 需求—用例追踪

用例 ID 在本文档内必须唯一且稳定；后续修订不得复用旧 ID 表达另一件事。

| 用例 ID | 需求 / 不变量 | 类型 | 预期证据 | 状态 |
| --- | --- | --- | --- | --- |
| `XXX-001` |  | static / service / API / E2E / runtime |  | `not_run` |

## 5. 静态与合同检查

| 用例 ID | 检查项 | 命令或方式 | 预期 | 状态 | 证据路径 |
| --- | --- | --- | --- | --- | --- |
| `STA-001` | 类型与枚举一致 |  |  | `not_run` |  |

## 6. 主用户路径

| 用例 ID | 前置条件 | 操作 | 预期结果 | 状态 | 证据路径 |
| --- | --- | --- | --- | --- | --- |
| `USR-001` |  |  |  | `not_run` |  |

## 7. 数据、任务与文件追溯

| 用例 ID | 检查项 | 预期结果 | 状态 | 证据路径 |
| --- | --- | --- | --- | --- |
| `DAT-001` | current 指针 / 来源摘要 |  | `not_run` |  |
| `TSK-001` | 任务输入、attempt、迟到结果 |  | `not_run` |  |
| `FIL-001` | Asset、storageKey、摘要与真实字节 |  | `not_run` |  |

## 8. 失败、恢复与安全

| 用例 ID | 场景 | 预期结果 | 状态 | 证据路径 |
| --- | --- | --- | --- | --- |
| `ERR-001` | provider / 文件 / 数据库失败 | 失败可解释且不伪成功 | `not_run` |  |
| `RST-001` | 服务重启或任务恢复 | 状态和 current 不漂移 | `not_run` |  |
| `CON-001` | 并发、重复提交或丢响应 | 幂等或明确冲突 | `not_run` |  |
| `SEC-001` | 密钥、日志和导出检查 | 不泄露秘密 | `not_run` |  |

## 9. 回归与兼容

| 用例 ID | 旧数据 / 旧客户端 / 邻接阶段 | 预期结果 | 状态 | 证据路径 |
| --- | --- | --- | --- | --- |
| `REG-001` |  |  | `not_run` |  |

## 10. Runtime/User Review

- 环境：
- 执行人：
- 执行时间：
- 真实页面路径：
- 真实任务或产物 ID：
- 截图 / trace / 导出物：
- 结论：`通过 / 不通过 / 不适用`

## 11. 残留风险与例外

| 风险或例外 | 级别 | 影响 | Owner | 后续动作 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 12. 最终判定

- [ ] 所有 P0/P1 用例均为 `passed` 或有获批的 `not_applicable`。
- [ ] 无未解释的 `failed/blocked/not_run`。
- [ ] 自动化证据与 Runtime/User Review 没有互相矛盾。
- [ ] 文档、代码、数据库、任务和产物状态一致。

最终结论：`通过 / 不通过`

