# finding

- 漫画成稿页面主体在 `apps/web/src/components/workbench/LayoutExportWorkspace.vue`（约 3163 行），路由 `/projects/:projectId/layout`，内部 stepKey `layout_export`。
- 页面结构：顶栏（章节选择/保存状态/撤销重做/立即保存/重新排一版/手机预览/版本与出版）+ 三栏编辑壳（左 tool-rail 工具、左中 canvas-navigation 页面列表+镜头素材、中间画布+canvas-toolbar、右侧 inspector 属性/图层）+ AI 抽屉 + m6 版本与出版折叠面板 + 来源/冲突/错误提示条。
- 智能入口三处：顶栏「重新排一版」(整章)、画布工具栏「智能调整」(局部)、AI 抽屉（选中/页段/场景 + 意图）。
- tool-rail 的「平移」按钮无 @click，是死按钮。
- 文档主操作口径是「预览并导出」，但页面顶栏没有该按钮；导出入口在「版本与出版」折叠面板内，自动化文档时顶栏显示禁用的「导出（开发中）」。

# web_search

# goal

用户觉得漫画成稿页面操作需要调整，先讨论方案，暂不改代码。待用户确认具体痛点后再定调整范围。

# todo

- [x] 探索漫画成稿页面结构与操作现状
- [ ] 与用户确认具体不顺畅的操作点
- [ ] 形成页面操作调整方案
