# finding

- 漫画成稿页面主体在 `apps/web/src/components/workbench/LayoutExportWorkspace.vue`（重构后约 3990 行），路由 `/projects/:projectId/layout`，内部 stepKey `layout_export`。
- 会话期间该文件被外部更新过一次（3163→3972 行，新增 Konva 交互层/发布四步/气泡预设等），编辑以最新磁盘内容为准。
- 契约测试 `LayoutExportWorkspace.contract.test.mjs` 是源码正则契约，对 testid/文案/响应式断点有硬约束；e2e 对按钮名 `立即保存/手机预览/重新排一版/智能调整/导出本章` 有依赖。
- db 类 e2e 必须用 `node tests/e2e/support/run-e2e-matrix.mjs --mode=db`（忽略文件参数，跑整套）；e2e 会重写 evidence png，stash 前先 checkout。
- `layout-editor-m4.spec.ts` 在改动前基线即失败（Konva 层拦截画格点击），既有问题。

# web_search

# goal

用户觉得漫画成稿页面太挤、信息多、样式不统一，要求重新设计。已定方向：阅读优先、点哪调哪、一键导出；米白纸画布+暗色界面保留。本次落地 P1（布局+样式），P2/P3 后续。

# todo

- [x] 探索页面结构与操作现状
- [x] 与用户确认痛点与方向（米白纸色画布、版式跟随项目）
- [x] 建任务文档（deep-think）
- [x] P1a：tokens + 顶栏瘦身 + 画布纸色 + 强调色/字号统一
- [x] P1b：tool-rail 合并为画布悬浮工具条（智能入口下拉合并、左栏收起留 P2）
- [x] 验证：契约 16/16、web 47/47、typecheck、e2e db 19/20（m4 既有失败）
- [x] P2：智能入口合一（顶栏单一智能调整+抽屉范围选择+显式生成）、左栏可收起、换图镜头条、裁切可见入口
- [x] P2 验证：契约/类型全过；e2e db 三轮布局 spec 全绿（m4 既有失败、script-import 间歇抖动无关）
- [x] P3-1：属性面板按对象收敛（未选中=阅读顺序+页面设置折叠；选中=常用操作+精确调整折叠）
- [x] 左栏收起布局修正（显式 grid-column 1/2/3）
- [x] 拖拽失效诊断：根因是 addText/addBalloon 把 activeTool 置 text（Konva pass-through 陷阱），修复为保持 select；顺带修复 m4 既有失败，e2e db 20/20 全绿
- [x] 留痕：progress/findings/完成记录/当前UI信息架构/MEMORY
- [ ] 用户运行复核后决定：右侧「更多设置」折叠、左栏段落操作下沉+默认收起、导出抽屉成品预览
- [ ] 用户确认后提交 git（全部改动在工作区）
