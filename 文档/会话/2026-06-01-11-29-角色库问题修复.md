# finding

- 本次审视发现项目角色库首版存在状态机闭环问题：完成本章后的主动作不区分角色库是否已 ready；角色层级提升后旧参考图类型可能阻塞四视图生成；已定稿角色重生成图时旧定稿与待确认状态混杂。
- 当前项目角色提取实现为启发式文本解析；若无明确“主要角色/角色设定/人物设定”段落，不应扫描整篇章节剧本，否则会把基础方向、场景字段或对白误识别为项目角色。

# web_search


# goal

修复项目角色库首版的状态一致性、前端动作入口、角色提取误判和文档过期描述；确保类型检查通过。

# approach

- 前端完成本章后的主动作根据项目角色库 ready 状态跳到项目角色库或本章剧情结构。
- 后端角色更新时校验重名、锁定已使用角色，并在层级变化时重新校验参考图类型；不兼容的旧 primary reference 不再继续作为定稿图。
- 角色参考图重新生成不覆盖已有定稿；新图作为候选资产，用户确认后才替换 primary reference。
- 角色提取只读取明确角色设定段，避免误写。
- 同步产品/契约文档中过期的 6 步、blocked 和角色库状态描述。

# question


# todo

- [x] 修复前端入口和角色库表单安全性
- [x] 修复后端角色状态机和参考图确认校验
- [x] 收紧项目角色提取规则
- [x] 同步文档
- [x] 跑类型检查和 diff 检查

# result

- 完成本章后的主动作现在会根据项目角色库是否 ready，分别进入项目角色库或本章剧情结构。
- 后端角色库补齐重名校验、`in_use` 锁定、层级变化时的 primary reference 兼容性校验；重新生成参考图只新增候选资产，不覆盖已有定稿。
- 项目角色提取仅解析明确角色设定段，避免误把章节剧本字段写入角色库。
- 后端 workflow blocked 文案改为等待原因；图片生成请求增加超时并隐藏 provider 原始错误正文。
- 已同步 `核心用户流程`、`当前UI信息架构`、`核心数据模型` 中相关过期描述。
- 验证通过：shared/server/web 类型检查均通过，`git diff --check` 通过。
