# finding

- 当前真实执行状态经冻结 release 的只读 `db:cutover status` 核验为 `completedThrough=C4`，evidence=`sha256:69d08d7b8a28343907fa939d4f6040d7807247eb46f9a2c39512c806f6328642`。
- 既有 Luna 总入口仍有 `WAIT_R0B_AUTH`、R0B/R1 未运行等旧状态，和 v5 实际证据冲突。
- v5 plan 中的 `maintenanceWindow` 已完成 C1 安全校验，是历史证据绑定字段，不是 C5 以后任务的排期或等待条件。

# web_search


# goal

把 Luna 计划改成清晰、唯一、无排期的依赖驱动执行入口：不写工期、开始/结束日期或等待日期；授权满足后立即连续执行，只在人工授权门、真实 blocker 或 E0 无候选通过时停止。

# approach

- 新增 Luna 当前唯一执行入口，旧 v5 窗口文档降为历史证据。
- 同步总 Handoff、总计划、进度、事实、测试矩阵和 R0-R2 授权清单。
- 删除本轮盘点中的天数估算，只保留剩余阶段、比例和复杂度。
- 完成静态一致性复核、路径检查和只读状态复核。

# question


# todo

- [x] 核验 v5 C4 当前证据和冻结 release。
- [x] 建立无排期原则与当前唯一执行入口。
- [x] 同步总计划、状态矩阵和 R0-R2 授权事实。
- [x] 删除 Luna 天数估算并完成复核留痕。

# result

- 当前唯一入口为 `2026-07-14_G0至G5剩余连续施工/luna_current_handoff.md`。
- 当前状态统一为 v5 C4 passed / WAIT_AUTH_C5；只读 production status 与 evidence 匹配。
- 计划不含工期、预计天数、开始/结束日期或等待日期；日期只作留痕。
- AUTH-C5 后 C5→C6、AUTH-C7 后 C7、R2 后 OBS-01～10→G4→G5 连续执行。
- Scrutiny=`passed`，Runtime/User Review=`passed_read_only`；未生成 AUTH、未执行真实 step。
