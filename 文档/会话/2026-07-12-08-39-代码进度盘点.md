# finding

- 当前分支为 `codex/g0-test-safety-net`，相对 `main` 多 4 个提交；工作区存在 G1 数据库化相关未提交改动，不能只按已提交代码判断进度。
- 项目文档明确：G0 测试安全网已完成；当前开发波次目标为 G1–G5，G6/G7 后置。
- 现有产品已形成文件态七阶段可演示链路：剧本、剧情结构、分镜、出图准备、候选图、排版导出、素材包均有前后端入口；但排版仍是一镜一页并复制候选源图，素材包仍是本地目录与 manifest，不是 G5/G6 正式出版物。
- 当前生产运行态没有接入 Prisma Client：项目与章节写 workspace JSON/Markdown，任务是进程内 Map，设置含密钥仍写 `workspace/settings/app-settings.json`。
- G1 可执行切片为 1/12 完成、1/12 进行中、9/12 pending、1/12 等正式切换授权；G2～G5 均 pending。
- G1 M0-A 已有 44 模型/556 标量字段 Schema、确定性 migration 纯计划与隔离 SQLite/Prisma 演练，但正式 migration 目录不存在，Repository/runtime/importer/SecretStore/持久任务尚未接线。
- r7 实际派生门禁为 2 份审查、1 accepted、1 rejected；开放 P1 是 Schema/migration writer 未把审查时 manifest 快照与实际渲染输入强绑定，`migrationGenerationAllowed=false`。总控计划与 QA 仍写 r7 0/2，状态记录已落后于真实 review bundle。
- Git 工作区有 33 个已跟踪文件修改、71 个未跟踪文件；G1 核心实现与审查证据大多尚未提交。
- 验证：三包 build 通过；类型检查通过；server 独立 253/253、shared 15/15、E2E 环境 31/31、prepare 3/3、Chromium/API/UI 4/4 通过。根级 `test:all` 连续两次在同两条嵌套 pnpm package-script JSON 输出解析测试失败；目标文件单跑 19/19、server 全套单跑 253/253，属于当前聚合并发/输出解析稳定性缺口，因此总门禁不能宣称全绿。前端构建另有主 JS 约 924 kB 的非阻塞拆包告警。

# web_search


# goal

盘点当前代码真实完成度，给出已完成、进行中、未开始/缺口、测试可运行程度及建议下一步；以代码、Git 状态、任务记录和实际验证共同为准。

# approach

核对事实源与当前实施记录，审阅分支和未提交差异，映射 G0–G5 到真实代码，并运行不破坏用户工作区的验证命令。

# question


# todo

- [x] 读取项目事实源与长期记忆
- [x] 核对当前分支、未提交差异和 G1–G5 实施记录
- [x] 审阅真实代码与测试覆盖
- [x] 运行安全验证并形成进度结论

# result

- 当前代码定位为“可演示 Alpha + 正在建设生产化底座”，不是可交付生产版。
- 用户可见主链路已能走到候选锁定，排版/素材包是旧骨架；正式 DB-only、版本 freshness、漫画版式锁定、候选返修修订和确定性出版均未完成。
- 下一步应先修复 r7 writer/manifest TOCTOU 并重审，再生成正式 migration、跑 SCH-00～15，随后进入 Project/Chapter/Script DB-only 垂直切片；同时修复根级 `test:all` 的两条稳定性失败并同步过时状态文档。
