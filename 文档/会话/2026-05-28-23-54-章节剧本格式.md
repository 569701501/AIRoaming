# finding

- 项目事实源已确认：剧本阶段是项目内第 1 步，右侧章节 `script.md` 是当前剧本事实源；左侧对话支持用户提供剧本整理和 AI 灵感种子两种来源。
- 现有实现中灵感生成已落地，但数量仍写死为 5 个：`DialogueService.buildInspirationSeedsPrompt`、`parseInspirationSeeds`、`script-inspiration-seeding`、agent/template 和工具 README 都需同步改为 3 个。
- 现有 `script-chapter-drafting` 只要求返回章节 Markdown，没有固定「章节剧本」结构；导入剧本的 `ProjectsService.importScriptToChapters` 也只是把原文按章节包装为 Markdown，需要增加固定格式兜底。
- 剧本阶段已有统一 AI 边界契约，可在此基础上补充“剧本阶段禁出主体列表、正式场景列表、剧情节拍、分镜剧本、镜头编号、图片 Prompt、JSON”的约束。
- 已新增共享章节剧本格式工具：灵感数量常量为 3，固定格式包含基础方向、本章方向、剧本亮点、视觉基调、剧本正文、本章结尾。
- `DialogueService` 已改为：灵感生成要求 3 个种子；从种子生成和章节编辑 prompt 强制固定「章节剧本」；AI 返回不合规时后端用固定格式兜底包装。
- `ProjectsService.importScriptToChapters` 已改为：用户提供剧本导入时，非标准章节文本写入前包装为固定「章节剧本」。

# web_search


# goal

调整剧本阶段能力：支持“用户上传剧本”和“AI 灵感种子”两条入口；AI 灵感生成每次给出 3 个种子，用户可选择其一或重新生成；最终进入剧本整理/生成时，剧本阶段只输出固定格式的「章节剧本」，不输出主体列表、正式场景列表、剧情节拍、分镜剧本、镜头编号、图片 Prompt 或 JSON。

# approach

使用 `$deep-think`：先读项目事实源、现有剧本对话设计与 OpenCode AI 技能，再更新技能/契约/必要前端提示，最后做静态验证和文档留痕。

最终方案：不新增 API 字段；把灵感数量、章节剧本模板和禁输边界沉到共享工具与后端 prompt；产品/架构/模块文档同步为当前事实。

# question


# todo

- [x] 读取事实源与长期记忆
- [x] 梳理现有剧本链路和技能约束
- [x] 制定并落地章节剧本固定格式
- [x] 验证改动并记录完成结果

# result

已完成。验证命令：

```text
corepack pnpm --filter @airoaming/shared typecheck
corepack pnpm --filter @airoaming/shared build
corepack pnpm --filter @airoaming/server typecheck
corepack pnpm --filter @airoaming/web typecheck
git diff --check
```

结果均通过。完成记录已写入 `文档/05_执行与记录/功能完成记录/2026-05-28_章节剧本固定格式.md`，长期记忆已同步当前规则。

未启动真实 OpenCode 模型做端到端生成；残留风险是模型输出仍需在真实 runtime 下验收固定格式遵守程度。
