# finding

- 已用运行时 Prompt 组装器复现：项目名为 `1111` 时，A2 Prompt 明确出现“必须结合……项目名称……”和“项目名称：1111”。问题不是模型随机联想，而是生产 Prompt 的确定性输入。
- 根因位于 `script-inspiration-seeding/references/inspiration-prompt.md` 与 `buildInspirationSeedsPrompt`：模板把项目名称定义为创作事实，组装器注入 `snapshot.project.name`。
- A2 直接调用专用 Prompt，不经过通用对话 Prompt；OpenCode 会话标题虽含项目名，但不是本次正文污染的直接根因。
- 创建项目时还会用 `name` 兜底 `storyTitle` 和 `description`。A2 当前未读取这两个字段，因此不是本次直接原因，但会给后续创作阶段留下次生污染风险。
- 现有 A2 测试只验证严格 JSON、P1 规则和不带当前章节正文，缺少“项目管理名称不得进入模型创作内容”的回归断言。
- `PROJECT_NAME` 还被 A3、A4、A5、剧情结构、分镜等 Prompt 注入；应先确立全链路规则，再决定只修 A2 还是统一清理。

# web_search


# goal

完成全链路修复：新建项目名称（例如 `1111`）只作管理标签，不再被 A2、后续创作、结构或分镜 Prompt 当作创作事实；不改变现有页面字段与流程。

# approach

构造项目名为 `1111` 的 Prompt 组装反馈环，核对 `script-inspiration-seeding` 生产模板、TypeScript 注入和运行调用链；先定位根因，再决定修复范围。

# question



# todo

- [x] 复现项目名进入灵感 Prompt
- [x] 排查 Prompt 模板、运行注入和会话标题三条可能路径
- [x] 补全链路失败回归测试
- [x] 修复项目创建默认值和所有生产 Prompt 注入
- [x] 完成专项、全量、静态与运行复核

# result

全链路修复完成。项目名不再进入 A2 至分镜、通用对话、持久任务、参考图或 AI 状态工具；新项目不再把名称复制为故事标题/描述，旧同值副本在 Prompt 边界按未确认处理。专项、DB 创建/重启、Skill 校验、类型和构建通过；全量唯一失败为既有迁移测试并发超时，独立重跑通过。未调用付费模型。
