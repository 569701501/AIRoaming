# finding

- `apps/server/src/projects/projects.service.ts` 已在工作台快照中返回 `chapters` 和 `currentChapter`，并让 `story` 镜像当前章节正文。
- 剧本页主要读取点包括 `ScriptDocumentEditor.vue`、`ScriptOutlinePanel.vue`、`ProjectDialoguePanel.vue`、`ProjectWorkbenchView.vue` 和 `workbench-store.ts`。
- 后端剧本步骤对话 prompt 在 `apps/server/src/dialogue/dialogue.service.ts` 组装，原先只读取 `snapshot.story.sourceText`。

# web_search


# goal

让 `WorkbenchSnapshot` 的 `chapters/currentChapter` 成为剧本编辑器、当前章节信息面板和对话框读取当前章节正文的主要来源。

# approach

在已接入后端默认章节的基础上，收紧共享契约中的 `WorkbenchSnapshot.chapters/currentChapter`，并把前端编辑器/信息面板、后端对话 prompt 从旧 `snapshot.story.sourceText` 迁到当前章节；`snapshot.story` 保持兼容镜像，避免旧 UI 和现有保存链路断裂。

# question


# todo

- [x] 读取章节文档与当前读取点
- [x] 更新共享契约与后端快照保证
- [x] 切换编辑器、信息面板和对话 prompt 到当前章节
- [x] 验证类型检查与留痕

# result

已将 `WorkbenchSnapshot.chapters/currentChapter` 收紧为剧本页当前章节读取主契约，并让编辑器、当前章节信息面板、左侧对话框空态、后端对话 prompt 和 mock 任务 target 优先使用当前章节。`corepack pnpm typecheck` 和 `git diff --check` 均通过；本次未做浏览器运行态截图验证。
