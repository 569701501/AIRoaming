# finding

- 用户提供的候选图 PNG 经 FFmpeg SSIM 对比，以 `0.999160` 匹配到项目 `shot_015/candidate_0346ad28-a3f9-4690-8ed0-132b1479cc0d.webp`；该候选不是单镜头干净画面，而是带章节标题、三段分格、对白气泡和旁白的扁平整页漫画。
- 已按 2026-07-09 生成时代码和正式 storyboard 复原完整 Prompt，SHA1 前 12 位为 `92e6d9f126e5`，与 Candidate/Asset 的 `promptDigest` 完全一致。Prompt 当时直接包含章节标题、三句 dialogue、caption、`comic panel` 和 `format: vertical_scroll`，负面提示未禁止文字、气泡、分格、边框和页面布局。
- 候选引用链存在第二个系统问题：前端把本章所有角色定稿图 ID 传入每个 shot；后端 `resolveReferenceImage` 只取第一张，因此所有镜头实际都以酷拉皮卡四分格角色设定图走 `image_edit`。Grok edit 请求又没有传 `size/aspect_ratio`，本批竖滑条漫候选实际全部输出 1280×720 横图。
- 当前工作区已有未提交改动移除了候选 Prompt 的 dialogue/caption，这是必要但不充分的缓解；仍保留 chapter 标题、`comic panel`、`format: vertical_scroll`，仍无明确的 single-shot/no-text/no-panel 契约，也仍存在错误参考图和 Grok edit 尺寸问题。
- 已定位待复核页面：`文档/04_方案与决策/全功能竞品对照-终版.html`，页面标注调研日期为 2026-07-09，覆盖漫画垂直平台、生图平台、故事板/视频平台和开源工作流四类样本。
- 当前工作区已有用户未提交改动；本任务只新增复核记录，不覆盖无关改动。
- 原 HTML 的主要方法问题：约 96 个竞品矩阵判断只有 6 个底部来源链接，没有单元格证据、评分标准和“未公开/未实测”状态；`独家/最强/最优/80%` 等推断被写成事实。
- 当前代码已实现原 HTML 列为缺口的 prompt 预览、候选 taskId 批次、全章未锁镜头批量生成、分镜候选缩略图、拖拽重排和重新编号。5 态枚举与视觉已存在，但 `selected/rejected/superseded` 缺少主动操作、转换语义和持久化 API。
- AI Comic Factory 当前官方源码支持单格 Redraw、单格 Prompt 编辑、可编辑 Caption/气泡、多种布局和浏览器打印 PDF；原 HTML 对它的关键判断错误，且 300 DPI 未获源码证实。
- 当前排版导出仍是一镜一页 PNG 序列；多镜拼版、气泡、长图/ZIP/PDF 尚未实现。因此“完整生产链路”只能表述为 MVP 状态链与追溯骨架打通。
- 新版 HTML 拆分“直接竞品”和“局部能力参考”：直接竞品矩阵只对照 AI漫游、Dashtoon、LlamaGen、Anifusion、AI Comic Factory；Midjourney、Katalist、LTX、Runway、即梦、可灵、ComfyUI 只记录可借鉴环节和不可推断边界。

# web_search

- 候选图生成契约修正所需 provider 官方能力：xAI 官方确认多图 edit 最多 3 张并可显式传 `aspect_ratio`，单图 edit 输出比例继承输入图；OpenAI GPT Image edit 支持最多 16 张输入并支持 1024×1024、1024×1536、1536×1024；火山引擎 Seedream 官方说明支持单/多参考图与 size，不同接入面的参数形态需要按当前模型适配。来源：xAI Image Generation / Multi-Image Editing、OpenAI Images API、火山引擎 Seedream 提示词指南与图片生成处理器。
- LlamaGen 官方发布记录确认：单 panel redraw、PDF、版本/生成历史、批量重画，以及 storyboard 的 PDF/Word/Excel/MP4 导出。来源：`llamagen.ai/releases/2024-03-10`、`2024-03-15`、`2025-09-22`、`2026-01-22`。
- Anifusion 官方页面确认：可编辑文字/气泡层、layouts、无限画布、inpaint、character sheet、PNG/PDF 和图生视频；不能据此证明“manga最强”。来源：`anifusion.ai/all-features/`、`anifusion.ai/features/ai-comic-creator/`。
- Dashtoon 官方发布说明确认 Story Mode、逐格 screenplay、无限画布、inpaint 和气泡编辑；官网确认发布/变现与 JPEG/PNG 发布格式。未找到 PDF 与候选历史强弱的公开证据。来源：`insiders.dashtoon.com/dashtoon-studio-august-2024-release/`、`dashtoon.com/create`、`dashtoon.com/publish`。
- Midjourney 官方文档确认默认四图、reroll、参考图权重和 Editor mask/inpainting；受限单图编辑器说明 inpaint 不必以前置完整无限画布。来源：Midjourney Getting Started、Discord Quick Start、Editor、Image Prompts。
- Katalist/LTX/Runway/ComfyUI 官方资料分别确认 script-to-storyboard、Storyboard/Timeline、Gen-4 References 一致性和 App Mode；它们适合作局部能力参考，不宜与漫画平台共用总分。

# goal

重新调研页面中的关键竞品能力与判断，找出事实错误、证据不足、过度推断和优先级偏差；在复核结论基础上新增一份完整、可直接讨论功能方案的 HTML。用户进一步希望提炼最值得吸收的竞品设计，并明确是否需要编辑画布及其边界。

# approach

先读取项目事实源与原调研证据，再逐项核对竞品官网、官方文档、产品页面或官方仓库；对无法公开验证的能力明确降级为“待实测/推断”。新版 HTML 使用自包含 CSS/JS，拆分直接竞品与局部能力参考，统一呈现当前状态、差距、优先级和待讨论决策题。

# question

- 是否确认按 `候选图干净底图生成契约修正计划` 进入实现；确认后从回归 fixture 和后端权威 GenerationSpec 开始，而不是继续在前端补 Prompt。
- 候选图正式产物是否确认收口为“单镜头、单幅、无文字、无气泡、无分格/边框的干净底图”；若未来需要一键整页生成，应作为排版成稿阶段的可选扁平草稿模式，而不是复用 `Candidate.shotId` 语义。
- 是否确认采用结构化轻画布，并把无限画布、绘画和画布内 inpaint 排除在 V1 外。
- 首个漫画交付格式待用户确认：默认建议 `vertical_scroll`；若首发渠道要求页漫/审阅/印刷，则改为 `page_horizontal`。
- 是否确认把漫画成稿编辑放在现有第 6 步，建议用户可见名称为“排版成稿”，不新增第八个顶层步骤。

# todo

- [x] 提取 HTML 的全部竞品、能力矩阵与来源
- [x] 对关键能力和产品定位做最新官方资料复核
- [x] 对照 AI漫游当前代码/文档，复核“我方能力”列
- [x] 输出错误分级、修订建议与证据
- [x] 更新会话记忆与长期记忆
- [x] 生成完整复核版 HTML
- [x] 做桌面与移动视觉复核
- [x] 完成最终验证与交付
- [x] 提炼最值得吸收的竞品设计模式
- [x] 评估无画布 / 轻画布 / 完整画布路线
- [x] 给出画布 V1 推荐边界与讨论问题
- [x] 诊断候选图提前生成整页漫画、分格和文字的原因
- [x] 深思熟虑制定候选图干净底图契约修正计划
- [x] 红绿实现后端权威 CandidateGenerationSpec 与无费用任务闭环
- [x] 红绿实现 shot 级引用、显式优先级与 provider 安全适配
- [x] 接入前端预览、生成请求、引用计划与版本追溯
- [x] 完成静态复核并整理真实运行验收清单
- [ ] 经用户确认后执行一次 Grok 单镜真实视觉验收

# result

- 已新增 `文档/04_方案与决策/2026-07-10_全功能竞品对照复核.md`，原 HTML 未修改。
- 结论：核心分组和“章节级生产工作台”方向可保留；现状矩阵、AI Comic Factory 描述、绝对化排名及 P0/P1/P2 排期需重写。
- 已更新长期记忆中的竞品报告结论，合并已过期的 2026-07-09 口径。
- 已新增完整自包含页面 `文档/04_方案与决策/2026-07-10_全功能竞品对照复核版.html`，包含证据口径、总判断、当前真实状态、直接竞品卡片与矩阵、局部能力参考、修正后优先级、6 个后续决策题和官方来源。
- 浏览器复核：桌面 1440×1000 无全局横向溢出；移动 390×844 卡片单列、矩阵容器内横向滚动；矩阵筛选 11 行可按三类切换；共 10 个主 section、20 个外部来源链接；控制台 0 error。移动端标题换行问题已修正。
- 已新增待确认提案 `文档/04_方案与决策/2026-07-10_竞品设计吸收与轻画布建议.md`。建议优先吸收三套机制：可回退生成迭代、漫画成稿编辑、跨阶段追溯；采用结构化轻画布，不采用无画布或完整自由/无限画布。
- 轻画布 V1 建议覆盖模板、多格排版、格内裁切、文字/气泡层、撤销重做、自动保存、导出检查和确定性 PNG 导出；局部重绘放在候选图单图遮罩弹窗，并生成新候选/素材，不在排版画布内原地修图。
- 静态复核确认：现有 `LayoutPage.placements[]` 可演进，但当前服务仍是一镜一页且导出只复制第一张源图；正式实现需补稳定 placement id、裁切变换、文字/气泡层、布局修订和导出快照。
- 本轮进一步确认流程位置：Anifusion / AI Comic Factory 式成稿编辑应升级现有第 6 步 `layout_export`，内部覆盖初排、画布编辑、导出检查和正式导出，不新增第八步。分镜保留叙事对白来源，候选图保留图片生成/锁定，画布只保存布局、裁切和文字层覆盖。
- 页面空间建议：排版成稿步骤让画布成为主区域，AI 对话默认折叠为窄栏/抽屉；当前折叠按钮只有外观没有实际行为。候选重新锁定后必须显式更新或标记已有布局草稿待确认。
- 候选整页漫画问题已完成只读诊断：直接原因是生成时 Prompt 把章节名、对白、旁白和竖滑格式传给模型；放大原因是每个镜头都误用本章第一张四分格角色定稿图走 Grok image edit，且 Grok edit 未接入目标纵向尺寸。该问题证明候选阶段必须先建立“单镜头干净底图”生成契约，否则后续轻画布没有可编辑素材。
- 已新增 draft 计划 `文档/04_方案与决策/2026-07-10_候选图干净底图生成契约修正计划.md`。方案以服务端权威 CandidateGenerationSpec 为事实源，按阶段完成 clean-plate Prompt、shot 级 reference resolver、多 provider adapter、旧候选兼容、任务追溯和 10 张/provider 的真实视觉验收；安全降级为纯文生图，禁止继续取全章第一张参考图。
