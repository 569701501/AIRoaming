# 漫画成稿评估与 P0 修复

# finding

- 编辑器骨架严谨(命令化、CAS 自动保存、导出预检),短板在画布直接操纵(盲拖、无画布文字编辑、无 undo/Delete)与第三套蓝色私有主题。
- 死代码:`activeTool === "text"` 全链不可达;`LayoutDocumentVisualPreview.vue` 只被契约测试读源码断言;`LayoutDocumentMiniPreview.vue` 无引用。
- 缩放不一致:工具条滑杆 max 0.6(LayoutExportWorkspace.vue:331),滚轮可到 0.8(LayoutKonvaInteractionLayer.vue:543 / LayoutExportWorkspace.vue:2756)。
- 文字/气泡删除无确认;带图画格删除有 confirm。

# web_search

(无)

# goal

- 把漫画成稿功能/样式/易用性评估留痕到 `文档/05_执行与记录/`。
- 完成 P0:Delete 快捷键+删除确认、统一缩放范围、清理 text 死代码、删除孤儿组件。
- 验收:typecheck 通过、相关测试通过、不留不可达分支。

# todo

- [x] 评估文档留痕
- [x] Delete 快捷键 + 删除确认
- [x] 统一缩放范围
- [x] 清理 text 工具死代码
- [x] 删除孤儿组件及引用
- [x] 验证(typecheck + shared 257/server 777/web 54 全绿)+ 更新 MEMORY
- 发现:e2e m7/m8 仍测已退役四步出版 UI,db e2e 套件预计红灯,需用户决定何时重写

# 总结

P0 全部完成;随后应用户要求重写全部收缩遗留 e2e(m7/m8 + 剩余 8 个),db 18/18 + file 4/4 全绿。「按镜头排版」确认废弃。重写中发现并修复真实 V2 bug:范围样式命令 payload 携带 Vue Proxy 导致 structuredClone 失败、样式静默无效(已改 JSON 克隆)。任务记录:`文档/05_执行与记录/任务记录/2026-07-26_漫画成稿体验评估与P0修复/task_plan.md`。改动未提交。
