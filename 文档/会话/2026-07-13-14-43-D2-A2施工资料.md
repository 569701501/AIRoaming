# finding

- D2-A1-2 已完成并提交；当前 required capability 的 `blockedIds` 为 6。
- D2-A2 原路线把非破坏性公开写与清空、重置、批量导入等破坏性语义放在同一阶段，现有数据库不可变历史、里程碑单调和删除 trigger 不允许直接照搬旧文件实现。
- G2 已明确 DB 模式下旧 draft/complete/source-pending 写路由不得静默启用，必须切换到带 CAS 的 Working Copy / Publish / Pending Suggestion 公开契约。
- M5-A0～A4 已完成；当前不是 M5 实施中，而是 D2-A2 开始前。
- 当前 registry 有 8 个聚合 capability、36 个 operation，6 个 blocker；final importer 与 db:activate 尚未实现。
- 用户将执行偏好改为“一份总目标交给 Luna 连续开发”，不再逐阶段人工确认；只在真实数据、真实凭据、真实停写、archive 或 activate 前停一次。

# web_search


# goal

把 D2-A2 至 D2-A8、M6 工具和 C0～C7 隔离演练的全部剩余工作整理为一份 Luna 可连续执行的总目标；内部阶段自测、复核、提交后自动续跑，最终只在真实切换授权前停止。

# approach

- 保留 D2/A8/M6 的严格依赖顺序，但把“用户逐步授权”改成“Luna 内部阶段门禁后自动续跑”。
- 把危险 legacy operation 区分为 implemented 与 retired；retired 必须有 replacement、拒绝测试和 replacement 成功证据，不能用普通 409 冒充完成。
- 连续开发终点为 `ready_for_real_cutover_authorization`；真实根、真实 Keychain/provider、真实 pre-cutover/archive/activate 不在本轮授权。
- 禁止再建设 review-attestation、双签或 CAS 审查基础设施，只保留简洁 Markdown 复核。

# question


# todo

- [x] 核对 D2 路线、G2 契约、现有服务与数据库约束
- [x] 写 D2-A2-1 五份施工资料与深思熟虑任务记录
- [x] 静态自检文档路径、范围、验收和停止线
- [x] 更新长期记忆并独立提交文档
- [x] 核实 M5/D2/6 blocker/final/M6 当前事实
- [x] 盘点 D2-A2～A8、M6 C0～C7 全部剩余工作
- [x] 编写连续执行总 Handoff、契约、测试、文件地图与续跑协议
- [x] 同步 active 路线和旧 Luna 入口
- [x] 静态复核、更新长期记忆并提交总资料

# result

A2-1 五份资料仍作为第一内部阶段的详细契约；本次新增 D2 至 M6 连续交付总目标，覆盖后续全部业务 capability、final importer、综合见证、M6 tooling 和隔离演练。静态复核已通过，现行 active 文档冲突已消除；尚未实现 A2 或更下游代码，待提交后可直接交给 Luna 连续执行。
