# finding

- D2-A1-2 已完成并提交；当前 required capability 的 `blockedIds` 为 6。
- D2-A2 原路线把非破坏性公开写与清空、重置、批量导入等破坏性语义放在同一阶段，现有数据库不可变历史、里程碑单调和删除 trigger 不允许直接照搬旧文件实现。
- G2 已明确 DB 模式下旧 draft/complete/source-pending 写路由不得静默启用，必须切换到带 CAS 的 Working Copy / Publish / Pending Suggestion 公开契约。

# web_search


# goal

补齐一套可直接交给 5.6 Luna 执行的 D2-A2 首个施工包，明确唯一范围、接口与事务语义、文件函数地图、测试矩阵、能力登记规则、复核与停止线；本轮只写文档，不实现 D2-A2 代码。

# approach

- 将 D2-A2 拆成 A2-1 非破坏性公开写闭环与 A2-2 破坏性 clear/import/reset 语义两段。
- 先交付 A2-1，避免 Luna 在没有退役模型和维护语义的情况下猜测物理删除、历史覆盖或 milestone 回退方案。
- A2-1 完成后仍保持聚合 capability 为 partial、`blockedIds=6`；先独立复核，再编写 A2-2 handoff。

# question


# todo

- [x] 核对 D2 路线、G2 契约、现有服务与数据库约束
- [x] 写 D2-A2-1 五份施工资料与深思熟虑任务记录
- [x] 静态自检文档路径、范围、验收和停止线
- [x] 更新长期记忆并独立提交文档

# result

五份主施工资料、任务三件套和 Handoff 静态复核已完成；A2 拆为 A2-1 非破坏性闭环与 A2-2 clear/import/reset，当前只允许 Luna 领取 A2-1。静态路径/frontmatter/范围/operation 对照和 `git diff --check` 均通过；尚未实现任何 A2 代码。
