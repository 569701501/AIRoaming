# finding

- 候选图工作台三段草稿直接继承正式 Shot 的 `comic.panelDescription/coreAction/comic.composition`；当前问题是上游细度不足，不是页面缺字段。
- 现有 `shot_prompt_generate` 必须在正式 Storyboard 和 Preflight 确认后运行，不能直接作为分镜生成前置任务。
- 推荐把分镜生成改为一次用户动作内的两段处理：先拆镜，再自动做全章视觉描述整理，最后整体保存 pending。
- 已完成共享章节级详细说明整理器，并接入 Dialogue 与持久 `shot_generate`。第二段只覆盖 `coreAction/comic.panelDescription/comic.composition/promptDraft`；镜头结构、引用、对白和 motion 冻结。
- 候选图页默认读取新分镜的详细说明，旧按钮改为可选“重新优化本镜头”。历史正式分镜不自动重写。
- 真实点击“杀令入棺”重新生成后，骨架与一次返修共耗时约 229 秒，第二段未开始；5 镜构图中的阅读动线箭头 `→` 被固定门误判为地点切换。旧正式 12 镜保留且没有新 pending。
- 后续真实运行依次暴露并修正：中文“两人”识别、非人主体误计入人类总数、行首表演提示污染逐字对白、剧情字迹被照抄为生图文字要求。
- 最终真实运行的骨架约 61.5 秒、章节级详细说明约 26.8 秒，均一次通过并完整写入 12 镜 pending。
- 保存后页面首次回读因候选工作台混扫 current/pending Shot 报 `SHOT_NOT_FOUND`；现已只读取 current Storyboard Shot，DB 集成回归和真实 API 200 均通过。
- 浏览器已显示“待确认预览 · 12 镜”，五类代表性多人/非人镜头详细说明可见；没有点击“确认分镜”，图片 Provider 调用为 0。
- 用户再次生成时稳定复现第 13 镜 `VISUAL_BRIEF_VISUAL_GROUP_COUNT_MISSING`：详细说明点名了“商队众人”，但未写“一群/一队/数名”等可绘制范围；自动返修原样返回。
- 根因初步锁定为两处：返修 Prompt 只透传错误码，未翻译成具体修改动作；群体范围识别规则也不支持“一群商队众人”这类自然中文。
- 已将多人总数和 group 范围编译为逐镜约束，返修 Prompt 会把常见错误码翻译成具体改写动作；群体范围提取按实际绑定名识别中性表达，同时保持裸群体名 blocking。
- 真实页面重新生成最终得到 12 镜 pending；详细说明首次 JSON 截断后一次返修通过，第 4/7/9/11/12 镜的群体范围全部可见，未自动确认，浏览器 error/warn 为 0。

# web_search


# goal

实现“分镜生成时自动产出详细候选图描述”的流程，使正常生图不依赖用户点击 AI 优化；多人关系清楚，失败不保存部分结果。

# approach

不新增顶层步骤或平行数据字段；复用现有 Shot 画面/动作/构图字段，在同一 `shot_generate` 内增加章节级自动整理，并保留单镜 AI 优化作为返修。

# question



# todo

- [x] 核对现有链路与字段职责
- [x] 比较候选方案
- [x] 用户确认推荐设计
- [x] 实施两条生产路径、页面和定向测试
- [x] 真实候选图页面验收
- [x] 全量测试、隔离 E2E、静态复核和正式留痕
- [x] 真实点击重新生成并定位失败原因
- [x] 修正构图阅读箭头误判并重新做真实运行验收
- [x] 修正真实模型暴露的人数、对白、画内文字与 current/pending 回读问题
- [x] 为第 13 镜群体范围失败补回归测试并修正返修指令/自然中文识别
- [x] 复跑分镜详细说明相关回归并完成留痕

# result

群体范围返修缺口已修正。系统不放宽质量门，而是把已知绑定条件编译成逐镜小清单，并给返修模型明确动作。相关 44/44、Server 全量 815/815、类型检查、diff 检查和真实浏览器路径通过；最新 12 镜仅写入 pending，等待用户确认。
