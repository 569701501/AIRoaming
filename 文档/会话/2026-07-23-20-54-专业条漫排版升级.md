# finding

- 当前目标章节过去 5 次 `layout_compose` 均未取得运行时视觉凭据，11/11 镜头使用 `rule_fallback`；现有规则把气泡优先放入画格上部，并把“气泡主体大部分位于画格内”误当作关联正确，导致文字遮挡人物、动作和关键道具仍可通过质量检查。
- 参考成熟竖向条漫后确认：规则降级时应把气泡主体放在画格外留白，并按对白量扩展段落；视觉模式只有在气泡完整位于验证过的文字安全区内时才允许画内排版。
- 现有领域已具备 `speech/thought/shout/caption` 四类气泡、受控 FontAsset、Shared 字形测量、确定性 renderer 和 V2 人工保护，可在同一编辑器和文档上扩展，不应另建排版器。
- 用户确认采用上述专业条漫方向，并要求增加多种气泡样式与字体。
- 已完成 Shared/Web/Server 的安全排版、语义气泡和真实字体升级；目标章节自动安全排版应用为 v71，并经两处手机断行宽度精修保存为最终 v73。
- 最终 v73 为 9 个画布、11 个画格、19 个气泡/旁白、2 个 geometry 人工保护；桌面与 390px 手机均为 0 文字溢出、0 气泡碰撞、0 气泡与画格相交。

# web_search

- 参考页面：`https://www.dongmanmanhua.cn/SUSPENSE/ruchuyizhedenver/%E7%AC%AC10%E8%AF%9D/viewer?title_no=1954&episode_no=10`
- 关键结论：气泡主体通常位于画格间留白或真实负空间，尾巴或小范围边缘可跨画格；普通对白、思考、喊叫、旁白和设备声应采用不同语义样式，不随机换皮。

# goal

- 让规则降级输出也不会遮挡角色、关键动作和道具。
- 让视觉模式只在真实安全区内使用画内气泡。
- 增加由对白语义驱动的专业气泡样式和可移植中文字体搭配，并保持编辑、保存、渲染、导出一致。
- 对目标章节重新排版，以真实页面截图和渲染产物验收。

# approach

- 规则模式使用“画格 + 独立对白留白区”的外置气泡布局，并按文字实际测量扩展 section。
- 视觉模式保留成熟漫画常见的画内负空间，但气泡必须完整位于 verified `textSafeRegion`，并硬避让人脸、身体、手、关键动作、道具、其他画格和气泡。
- 样式由 `speech/thought/shout/caption` 等语义确定；字体使用本地、许可明确、可嵌入导出的受控字体，不依赖系统字体。
- 先补失败测试，再修改共享规划、质量门、编辑器和 renderer；完成后重排当前章节并截图复核。

# question


# todo

- [x] 完成代码与字体资产盘点
- [x] 修复安全排版候选与质量门
- [x] 实现语义气泡样式与字体体系
- [x] 重排目标章节并完成视觉、渲染、导出验收
- [x] 完成静态/运行复核与正式留痕

# result

`complete_with_known_runtime_limit`。

- 最终 Working Copy：v73。
- 文档摘要：`sha256:2351a6606da0a29f7f9860a45b76c9e39cd1518a4b54dc21674e24fe0d1fca9a`。
- Shared 全量 234/234、Web 合同 8/8、Server 合同 2/2、renderer/字体 7/7；三端 build/typecheck 通过。
- Scrutiny Review：PASS；Runtime/User Review：PASS。
- 已知限制：视觉 Provider 的运行时凭据仍未接通；当前系统安全且诚实降级，但尚不能利用画内视觉负空间。
