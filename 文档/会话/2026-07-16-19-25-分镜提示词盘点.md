# finding

- 用户希望在完成剧本与剧情结构提示词后，继续处理分镜及后续阶段提示词；项目内已有多批分镜、出图准备和提示词资产文档，需要先区分当前事实源、已完成实现与历史方案。
- 当前分镜流程、字段和确认门已经成立，页面字段无需调整；两条上游路线在已确认 StoryVersion 后共用同一套分镜能力。
- 当前分镜 Prompt 存在质量门缺失、枚举不一致、AI 引用与数据库 ID 边界不清、无一次定向修复四个问题。
- 页面允许直接编辑待确认镜头；对话框虽然提示“调整节奏”，后台尚无独立的 AI 待确认草稿修订动作。
- 用户进一步澄清：本轮所说的“后面的提示词更改”不是只做分镜保存/引用契约，而是继续 2026-07-09 外部借鉴方案中的整套下游提示词治理，范围包括 P06 分镜、P23 角色参考图、P24 场景参考图、P25/P26 候选图实际生成与预览。S1 只是分镜运行基础，不能替代这套外部方法吸收和真实图片评测工作。
- 相关事实源为 `2026-07-09_提示词外部借鉴优先级.html`、`2026-07-09_项目提示词资产盘点.html` 和 `2026-07-09_开源AI漫画平台出图准备调研.md`。原方案结论是：生图类 Prompt 可直接借鉴成熟分层结构；分镜只借鉴漫画镜头语言与质量标准；项目私有事实源和写入协议不照搬。

# web_search


# goal

盘点当前分镜及后续阶段的 Prompt / Skill 文档、生产实现、输出字段和页面流程，确定下一阶段从哪里开始，不重复设计已有能力。

盘点完成后用户授权继续，当前目标已进入 S1：在不改页面字段和数据库 Schema 的前提下，完善首次生成、待确认草稿调整和 AI 本地引用到正式 Storyboard Working Copy 的映射。

用户已纠正后续目标：S1 完成后回到外部借鉴方案，系统推进分镜、角色参考图、场景参考图和候选图提示词，而不是继续只讨论分镜数据契约。

# approach

- 使用 `$deep-think` 建立正式盘点记录。
- 先核对长期产品/架构事实源，再核对分镜 Prompt、实现和测试。
- 本轮先定边界与推进顺序，不直接改 Prompt 或页面。

# question


# todo

- [x] 找齐分镜与后续 Prompt 文档
- [x] 核对当前生产实现和页面字段
- [x] 区分有效、部分有效、历史文档
- [x] 给出下一阶段推进顺序
- [x] 完成 S1 首次生成与待确认草稿调整 Prompt
- [x] 完成 AI 本地引用到正式 DB ID 的映射
- [x] 接入 DB Storyboard Working Copy 保存与确认
- [x] 完成定向测试、类型检查和构建

# result

盘点完成。正式结论写入 `文档/04_方案与决策/2026-07-16_分镜及后续提示词改造顺序.md`。下一项为 S1：分镜生成契约、待确认草稿调整动作与引用映射；本轮未改生产代码和页面字段。

S1 已启动。新增发现：当前 DB 分镜对话仍走已禁用 legacy save/confirm，AI 临时 Shot ID 与角色名也无法通过 DB V2 范围校验，必须在本阶段一起收口。

S1 已完成：生产 Prompt 已区分首次生成和调整当前 pending，修正正式枚举和结构引用边界；DB 模式只读正式 ScriptVersion，并通过 StoryboardVersion Working Copy 完成 Project Character 映射、stable Shot ID 分配、保存与确认。页面字段、数据库 Schema 和确认门未变化。下一步进入 S2 固定质量门与一次定向修复。

后续范围经用户纠正：下一步需要先重新串起 7 月 9 日外部借鉴方案的 P06/P23-P26，再决定具体施工顺序；不能把 S2 当成整个提示词优化计划。

P06/P23-P26 已在后续正式任务 `2026-07-16_下游提示词优化` 中完成：角色、场景、候选图和分镜 Prompt 已收口，普通/DB/页面/worker 使用同一候选图领域规格；fake-provider 新项目 Chromium 1/1、Server 670/670、类型检查与构建通过。真实付费出图未执行。
