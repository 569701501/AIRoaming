# finding

- 0017 已提供不可变原稿、严格导入分析候选、整本章节目录、导入批次、逐章批次项、忠实度报告、密封 import pending 和逐章直接发布事务。
- 当前生产对话仍走旧的本地正则分析与 `importScriptToChapters` 直接写正文；DB-only 下该写入能力已退役，因此 B1～B5 尚未真正接线。
- Shared 已有 `import-analysis/1.0`、固定章节剧本 import 模式与 `import-fidelity/1.0` 严格解析器，但生产 Prompt 和 Skill 仍是旧协议。
- 当前剧本编辑器只按通用 AI pending 展示“采用/丢弃”；尚未根据 `kind=import` 显示专用“确认章节”。
- 当前对话工具结果只能展示旧 `ScriptImportAnalysis`，无法展示观察性大纲、来源边界、整批状态和失败章节。

# web_search


# goal

接通已有完整剧本 B1～B5 生产路线：保存不可变原稿，生成并展示严格观察性分析和拆章目录，用户整体确认一次后创建全部章节并完成逐章 materialize/verify 尝试，用户可自由切章完整只读查看 import pending，并通过“确认章节”直接发布正式 `ChapterScriptVersion`。

强制验收：不改变现有页面内容字段；不提供手动整理、AI 重新整理、采用、丢弃或批量确认；单章失败不回滚其他章；正式版本后进入现有 StoryStructure。

# approach

- 复用 0017 数据模型和 `ScriptWorkflowSourceRepository`，补只读上下文、批次编排和公开确认接口，不再保留第二套 DB 导入写入逻辑。
- 分析、章节整理和忠实度验证都使用严格契约；格式失败最多做一次只修格式的重试。
- 首版在目录确认的对话请求内运行持久批次编排，逐章隔离失败并返回整批结果；长稿分层和后台断点续跑若无法在本包诚实完成，作为明确后续限制保留。
- UI 继续使用现有对话区、章节下拉框和正文区，只根据来源 kind 切换动作与只读说明。

# question


# todo

- [x] 冻结 B1～B5 生产 Prompt、DTO、状态与失败契约
- [x] 接入原稿保存、严格分析候选和目录确认结果卡
- [x] 接入整批 materialize/verify 与失败隔离
- [x] 接入 import pending 全文只读和逐章确认
- [x] 完成单元、集成、DB-only 浏览器验证与正式留痕

# result

- B1～B5 已接通真实 DB-only 生产链，页面内容字段保持不变。
- 目录整体确认一次后创建全部章节并完成逐章生成/验证尝试；用户自由切章，全文只读查看并逐章直接确认正式版本。
- 自动验证通过：Shared 152/152、Server 590/590、E2E env 34/34、prepare 3/3、A/B 两条 DB-only Chromium 路径各 1/1，typecheck/build/Skill 校验全绿。
- 后续增强：超长稿分层、后台断点续跑、失败项重试入口；不改变当前产品流程。
