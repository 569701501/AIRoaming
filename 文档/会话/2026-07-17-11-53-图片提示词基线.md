# finding

- 现有正式图片 Prompt 已分为角色参考图、场景参考图、候选图三类；2026-07-16 已完成 P23～P26 的一次收口。
- 候选图普通任务、DB 持久任务、页面预览与 worker 已共用 `candidate-generation-spec.ts` 的领域内容构造逻辑。
- 当前缺口不是立即增加更多 Prompt 规则，而是缺少独立、可重复运行的图片 Prompt 固定样例集和离线质量基线。
- 现有局部单元样例已保护生产规则；新增基线的价值是把后续真实视觉 A/B 的 5 类镜头和 3 个 provider profile 固定成机器可读清单，而不是复制测试。

# web_search


# goal

在不调用付费图片服务、不改页面和数据库的前提下，固化角色图、场景图、候选图的正式 Prompt 输入样例、实际输出快照和可执行质量检查，为后续真实图片 A/B 提供可信基线。

# approach

- 先审计现有三类 Prompt 的事实源、调用链和既有测试。
- 冻结覆盖单人、多人、无角色、角色预览/定稿、场景参考和 provider profile 的固定样例。
- 优先复用生产构造器生成快照，检查禁词、媒介边界、人物/场景约束和页面/worker 同源；不另造一套测试 Prompt。
- 证据充分前不改生产 Prompt，不调用真实图片 provider。

# question


# todo

- [x] 完成相关文档、代码和测试盘点
- [x] 冻结离线固定样例与评测口径
- [x] 实现并运行固定基线测试
- [x] 完成静态复核、运行复核和文档同步

# result

- 新增 3 个参考图案例、5 类候选镜头和 15 个 provider profile 的固定离线基线；未来获准真实运行时固定生成 30 张。
- 离线基线、4 files / 13 tests、Server 118 files / 711 tests、typecheck、build 和 diff check 全部通过。
- 生产 Prompt 未修改；真实图片 provider 未调用，真实视觉质量继续 `not_run`。
