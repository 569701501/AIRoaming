# finding

- G2-A0 已完成 shared canonical/document/source/production 基础；G2-A1 已完成 SQLite Overlay、事务重试与 scoped query 基础。
- B1 的真实持久化模型已存在于 `Chapter`、`ChapterScriptVersion`、`ChapterScriptPending`，不新增迁移。
- 现有 ProjectsService/ChapterScriptService 保持 G1 兼容；新增 ScriptVersion facade 和路由承接 G2 DB-only 能力。
- G1 0008 Working Copy CHECK 已同步允许 current Script + 空 dirty Working Copy，否则无法满足施工资料中的 clear 契约；结构计数保持 195 CHECK/194 trigger。

# web_search


# goal

- 完成 Script Working Copy、ScriptVersion、AI pending 与历史的数据库事务闭环。
- 验收：CAS、幂等、publish 不重复、fresh SQLite 重启读回、全量测试和类型检查通过。

# approach

- 用 shared Script 文本 codec 计算规范文本和 digest。
- Repository 内使用 scoped Chapter 读取与 `updateMany(...rowVersion)` CAS；事务由 A1 runner 统一托管。
- API 仅新增 G2 Script 路径，保留 G1 旧路径和文件模式。

# question


# todo

- [x] 新增 Shared Script API contract 和错误码
- [x] 实现 ScriptVersionRepository
- [x] 接入 API facade 和路由
- [x] 补充 fresh SQLite/CAS/replay/restart 测试
- [x] 完成复核、handoff、文档和长期记忆

# result

- G2-B1 已完成。Script DB repository、API facade、CAS/幂等、publish/clear/revert、pending adopt/discard、history copy 和 restart readback 已通过验证；G2-C1/D1/E/F 仍待后续阶段。
