# finding

- 章节方案文档要求先增加 `Chapter` 和章节 DTO，再实现后端章节服务与前端章节列表。
- 当前 `packages/shared/src/domain.ts` 已有项目、任务、素材等枚举，但缺少 `ChapterStatus` 和生成任务 target 类型枚举。
- 当前 `packages/shared/src/dto.ts` 的 `WorkbenchSnapshot` 仍围绕项目级 `story.sourceText`，没有章节列表、当前章节、章节草稿保存或完成本章 DTO。
- 当前后端和前端仍依赖旧的项目级草稿接口；本轮共享契约为避免破坏现有实现，`WorkbenchSnapshot.chapters` 和 `WorkbenchSnapshot.currentChapter` 先设为可选字段。

# web_search


# goal

先补章节工作流的共享契约：在 `packages/shared` 增加 Chapter 相关类型、章节列表和当前章节 DTO、章节保存草稿和完成本章请求，并让生成任务 target 支持章节归属。

# approach

只改共享契约和必要留痕，不实现后端章节服务、不改前端章节列表。优先复用现有 `domain.ts` 和 `dto.ts` 的类型组织方式，保持后续后端与前端迁移路径清晰。

# question


# todo

- [x] 读取章节相关文档与共享契约现状
- [x] 修改 `packages/shared` 章节类型和 DTO
- [x] 运行类型检查或构建验证
- [x] 更新会话记忆、长期记忆和必要完成记录

# result

已完成章节共享契约第一步：新增 `ChapterStatus`、生成任务 target 类型、章节列表/详情/剧本版本 DTO、保存章节草稿请求响应、完成本章请求响应，并为 `WorkbenchSnapshot` 与下游工作台对象预留章节归属字段。验证通过：`corepack pnpm --filter @airoaming/shared typecheck`、`corepack pnpm --filter @airoaming/shared build`、`corepack pnpm typecheck`。后端章节服务和前端章节列表尚未实现。
