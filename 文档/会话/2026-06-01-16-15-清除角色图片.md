# finding

- 当前 workspace 只有项目 `b224e22c-9cdf-4195-851c-6bae207789c8` 存在角色图文件，路径为 `workspace/projects/b224e22c-9cdf-4195-851c-6bae207789c8/assets/characters/`。
- 角色库事实源为 `shared/characters.json`，素材引用事实源为 `shared/assets.json`。
- 现有角色图文件 12 个；`assets.json` 中有 9 条角色图片资产记录。

# web_search


# goal

清除当前已生成的项目角色图片和对应引用，让用户可以重新通过产品生成角色预览图/定稿图。

# approach

删除 `assets/characters/` 下的角色图片文件；清空 `shared/assets.json` 中角色图片资产；重置 `shared/characters.json` 中角色的图片引用、确认时间、定稿引用和视觉版本，但保留角色名称、层级、描述和来源。

# question


# todo

- [x] 定位角色图文件和项目事实源
- [x] 清理角色图片文件
- [x] 清理资产引用和角色引用
- [x] 验证文件与引用已清空

# result

- 已清理项目 `b224e22c-9cdf-4195-851c-6bae207789c8` 下 12 个角色图片文件。
- 已从 `shared/assets.json` 删除 9 条角色图片资产记录。
- 已重置 `shared/characters.json` 中 5 个角色的 `referenceAssetIds`、`previewReferenceAssetId`、`previewConfirmedAt`、`primaryReferenceAssetId`、`finalizedAt` 和 `visualVersion`；保留角色名称、层级、描述和来源。
- 已重启后端开发子进程并通过 `/api/health` 与 workbench API 验证：角色引用均为空，项目资产数为 0。
