# finding

- 当前分支为 `codex/g0-test-safety-net`，初始工作树干净，HEAD=`eb26743`。
- M5 A0～A3 独立提交为 A0 `91a450c`、A1 `b767e30`、A2 `0f20cff`、A3 `68bb694`。
- 定向测试 11/11 和 server typecheck 通过；真实专项覆盖只有 4 个 capability 测试和 7 个 backup/restore 测试。
- backup 的 DB 派生读取早于 `BEGIN IMMEDIATE`；restore 未核对 16-run ledger/current release；secret/路径/补偿故障矩阵缺失。M5 结论由 completed 改为 hardening_required。

# web_search


# goal

正式复核 Luna 完成的 M5；对已确认的 P1 缺口编写 M5-A4 分切片施工文档，并固定 D2→M6 后续路线。本次只写文档，不直接实现业务代码。

# approach

按 `$deep-think` 分离 M5 静态/运行证据复核、M5-A4 修复和 D2/M6 编排；当前只交 Luna A4-1。

# question


# todo

- [x] 复核 M5 提交、实现边界与验收证据
- [x] 明确 D2/M6 的真实前置阻塞和实施范围
- [x] 建立 M5-A4 任务计划、实施契约、验收清单与 handoff
- [x] 做文档交叉复核、同步长期记忆并提交前验证

# result

- M5 独立复核结论：实现 happy path 可运行，但正式验收不通过，状态改为 `hardening_required`。
- 已建立 `2026-07-13_G3-M5A4验收收口/` 八份施工/复核资料，当前只交 Luna A4-1。
- 已新增 `2026-07-13_G3-D2与M6推进路线.md`，固定 M5-A4→D2→D3→M6 顺序。
- 验证通过：M5 定向 11/11、server 49 files/314 tests、workspace typecheck、G1 manifest/schema/migration、diff check。
