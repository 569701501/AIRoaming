# finding

- 用户正常打开的开发页面使用默认 Web `5173`、API `4310` 和默认项目数据库。
- 2026-07-16 分镜真实模型验收使用独立 Web `5192`、API `4332`、隔离 SQLite 与隔离 workspace。
- 两个测试项目只存在于隔离数据库；验收结束后 `4332/5192` 已停止，因此默认项目库看不到它们。

# web_search


# goal

解释测试项目不可见的原因，并明确以后如何让用户查看测试结果。

# approach

- 依据真实模型验收的 Runtime Review、progress 和当前 Web 默认端口核对环境。
- 不修改产品代码和数据。

# question

- 后续是继续使用隔离环境并保留可访问地址，还是明确在用户默认环境创建带“测试”标记的项目，待用户决定。

# todo

- [x] 核对测试与默认环境。
- [x] 确认项目不可见原因。
- [x] 向用户说明环境边界和可选查看方式。

# result

测试与用户当前页面不是同一数据库和服务实例；不可见是隔离策略的预期结果，不是项目创建失败。
