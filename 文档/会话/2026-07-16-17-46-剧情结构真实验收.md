# finding

- 当前下一步是使用真实模型验证双流程汇合后的剧情结构质量门，而不是继续增加页面字段或流程。
- 本机 OpenCode 服务可达，模型列表包含 `self/gpt-5.5`；现有应用占用 4310/5173，本轮需使用独立端口、SQLite 和 workspace。
- 已启动隔离 Server 4328 和 Web 5188；默认模型接口确认 `self/gpt-5.5`，独立 SQLite 已完成 17 段迁移。
- AI 创作项目已通过：正式正文 6446 字，结构为 3 角色/7 场景/15 beats，确认后 StoryVersion 绑定精确 ScriptVersion，章节 structured、分镜解锁、console 0 error/warn。
- 已有剧本项目已通过：目录一次确认后 4 章后台整理完成；第 1 章逐章确认形成 1016 字 import 正式版本，结构为 3 角色/1 场景/6 beats，版本精确绑定、章节 structured、分镜解锁。
- 第 2～4 章保持 pending_ready、未被连带确认；切换后可只读查看完整草稿。

# web_search


# goal

新建两个隔离项目，分别从 AI 创作正式章节和已有剧本导入正式章节生成并确认剧情结构，检查内容质量、来源版本绑定和页面状态。

# approach

- 使用 `$deep-think` 留痕并区分 Orchestrator、Runtime/User Review 与 Scrutiny Review。
- 使用独立临时数据根启动 Server/Web，真实模型请求走已确认可用的本机 OpenCode 服务。
- 发现真实缺陷时先记录失败样例，再做最小修复；没有缺陷则只更新验收证据。

# question


# todo

- [x] 读取事实源并确认真实模型可用
- [x] 启动隔离应用
- [x] 新建 AI 创作测试项目
- [x] 验证 AI 创作正式章节的剧情结构
- [x] 新建已有剧本测试项目
- [x] 验证导入正式章节的剧情结构
- [x] 完成复核、清理、文档和提交

# result

`passed_real_model`。两条来源均使用真实 `self/gpt-5.5` 在隔离新项目中形成正式章节、生成并确认剧情结构；数据库来源绑定、页面状态、下游解锁和控制台检查全部通过，本轮无需修改产品代码。
