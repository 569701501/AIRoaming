# finding

- 用户已明确授权：仅执行 R0-B 只读发现与 release-specific shadow；不暂停写入、不生成 AUTH、不执行 C1～C7。
- R0-A 已由独立 Scrutiny/Runtime Review 收口，当前代码基线为 `3fda7d0`；工作树含用户既有文档修改，不能使用全量暂存。
- R0-B 的事实源为 `文档/05_执行与记录/任务记录/2026-07-13_R0-R2真实切换施工包/`，完成后必须停在 SH-10 人工 Migration reviewer 复核。
- recovery archive `2026-07-10-150041-chapter-001-before-recovery.tar.gz` 含与当前 identity 匹配的缺失 structure 候选；archive/member SHA-256 分别为 `336c9f...6f23`、`4eac7b...0dd3`。
- 临时 overlay 恢复候选后，shadow 推进到 Story importer 并因 43 个角色名 token 失败；43/43 可唯一解析。当前 storyboard 65/65 token 也可唯一解析，但 importer 无条件拒绝非空引用且未写 child relations。
- full shadow 依赖顺序需从 `story -> storyboard -> characters` 改为 `story -> characters -> storyboard`；release root 用最终 remediation commit 的仓库外 worktree 解决 overlap。

# web_search


# goal

- 为 Luna 写出可连续执行的 R0-B blocker remediation 施工包：代码兼容、overlay、条件式单文件恢复、外置 release worktree、SH-01～09，并停在人工 SH-10。
- 本轮只写资料和临时验证，强制保持真实数据写入、停写、默认 Keychain、真实凭据、AUTH、C0～C7 的操作次数为 0。

# approach

- 先运行既定 release freeze 只读门禁，再从 CLI/文档确定真实源与 shadow 入口。
- 使用仓库外私有临时根创建不含真实秘密的 plan/snapshot/shadow 目标；仅记录摘要、计数和 gate 结论。
- 证据写回 R0-R2 任务目录，不写真实绝对路径、不自签 SH-10。

# question


# todo

- [x] R0-B 发布冻结与只读发现
- [x] 两个 release-specific shadow
- [x] 复现恢复候选后的 importer 次级阻塞
- [x] Luna 五份 blocker remediation 施工资料
- [ ] Luna 执行修复、SH-01～09 并停在 SH-10

# result

R0-B 代码已由本次任务实际执行并提交 `74a6d71`。resolver、Story/Storyboard legacy character 兼容、Storyboard child relations、full order、milestone 单调保护、preview confirmedAt 约束均已通过定向与全量门禁。clean overlay A/B 使用独立 source/target 根，一致通过前 8 slice（Storyboard child=65），第 9 个 preflight 同报 `PREFLIGHT_SOURCE_UNRESOLVED`，原因是 legacy preflight 缺少 sourceSnapshot。按契约停止，真实 structure 未恢复，未生成 AUTH、未停写、未执行 C0～C7、未访问默认 Keychain/真实凭据。下一步需单独决定是否为 preflight source 建任务，不能把本次 R0-B 标成 SH-10 就绪。
