# finding

- 用户已把文本对话 provider 切换为 `xai/grok-4.5`，设置状态显示已配置。
- 当前 Base URL 不是 xAI 官方地址，而是用户配置的中转地址 `https://sub.lydcloud.uk/v1`。
- `/api/ai-runtime/models` 已能返回并默认选中 `xai/grok-4.5`，说明设置保存、模型选择和 OpenCode 模型兜底链路正常。
- 最小 OpenCode 请求稳定返回 HTTP 500；OpenCode 日志真实错误为 `ProviderModelNotFoundError`，说明 UI 的兜底模型项不等于 OpenCode 已注册模型。
- 临时把 `grok-4.5` 注册到当前中转使用的 OpenAI-compatible provider 后，请求到达 `https://sub.lydcloud.uk/v1/chat/completions`，上游明确返回 `400 Unknown Model`。
- 最初使用本机另一把同站凭据读取 `/v1/models`，其目录没有 Grok；用户指出图片 Grok 已使用同一中转后，进一步比对发现该凭据与项目当前 Grok 凭据指纹不同，原“中转不提供 Grok”结论作废。
- 文本与 Grok 图片配置实际使用同一 Base URL、同一密钥指纹。通过 macOS Keychain 安全读取项目真实 Grok 凭据后，`/v1/models` 返回 `grok-4.5`、`grok-4.5-latest`、`grok-4.20-*` 和 Grok Imagine 等模型。
- 使用项目真实 Grok 凭据直接请求中转 `/v1/chat/completions`，`model=grok-4.5` 返回 HTTP 200、回复 `OK`，响应实际模型为 `grok-4.5-build`；证明中转、密钥和 Grok 文本服务均正常。
- 用户确认已开放 `grok-4.5` 后再次复测：中转 `/v1/models` 返回 `grok-4.5`，直接 Chat Completions 仍返回 `OK`；但 AI漫游 `/api/ai-runtime/models` 只是通过 fallback 显示 `xai/grok-4.5`，运行中 OpenCode `/config` 的 `provider.xai` 仍为空，Grok provider/model 列表也为空。
- 同轮通过 OpenCode 4396 创建临时会话发送 `xai/grok-4.5`，仍返回 HTTP 500；日志再次确认 `ProviderModelNotFoundError`。临时会话已删除，请求未到达中转。

# web_search

- OpenCode 官方配置文档确认：自定义 provider/model 必须进入 `opencode.json` 的 `provider.models`；`OPENCODE_CONFIG` 或 `OPENCODE_CONFIG_CONTENT` 可用于运行时配置：`https://opencode.ai/docs/config`。
- OpenCode 官方 provider 文档确认：OpenAI-compatible 中转应使用 `@ai-sdk/openai-compatible`；原生 xAI 和自定义中转是不同接入形态：`https://opencode.ai/docs/providers`。

# goal

复现并修复 Grok 请求失败，确保设置中的 OpenAI-compatible Grok 中转配置真实进入 AI漫游受控 OpenCode，并完成新项目与冷启动文本回归；不得调用图片生成。

# approach

按 diagnose 技能构建最小反馈环并定位根因；按 deep-think 分 Worker、静态复核和运行复核推进。采用“业务逻辑 ID 与运行时 Provider ID 分离、配置与秘密分离、未注册模型不展示”的定向方案。

# question


# todo

- [x] 用最小 OpenCode 文本请求复现失败
- [x] 排列并验证 3～5 个可证伪假设
- [x] 定位 OpenCode 注册与上游模型目录两个独立阻塞
- [x] 添加失败回归并实现受控 Provider 注册
- [x] 完成新项目、冷启动和秘密边界验证
- [x] 清理临时诊断服务并更新事实源

# result

- 根因一：`OpenCodeRuntimeService.listModels()` 会把设置中的默认模型作为 fallback 返回给页面，但 OpenCode 真实 provider registry 中没有 `xai/grok-4.5`，所以页面可选、发送时报 `ProviderModelNotFoundError`。
- 用户纠正后使用项目真实 Grok 凭据重新验证，确认中转支持 `grok-4.5`；此前 `Unknown Model` 来自另一把同站凭据，不能代表项目当前账号能力。
- 最终根因收敛为 AI漫游/OpenCode 配置缺口：页面 fallback 暴露了 `xai/grok-4.5`，但 OpenCode provider registry 没有该模型，因此在访问中转前就失败。正确修复应把中转按 OpenAI-compatible provider 注册到 AI漫游受控 OpenCode 配置，并确保设置保存的模型、Base URL 和密钥进入同一 provider。
- 用户开放模型后的复测确认当时只剩 AI漫游/OpenCode provider registry 未同步；该缺口随后已通过专用运行时 Provider 注册修复。
- 临时 4397 诊断服务已停止；直接中转最小文本验证成功。
- 已把逻辑 `xai/grok-4.5` 映射到受控 OpenCode 的 `airoaming_xai/grok-4.5`，使用 `@ai-sdk/openai-compatible` 与设置中的 Base URL；API Key 仍单独进入 auth。
- 已移除不可运行模型 fallback，并补齐同地址同指纹的冷启动凭据恢复。
- 目标单测 14 项、类型检查、Server/Web 构建通过；全仓唯一失败是无关固定 5 秒超时，隔离复跑通过。
- 新项目 `Grok文本回归-0718` 首次与删除 auth 后冷启动两次项目对话均完成，无错误；未调用图片生成。
