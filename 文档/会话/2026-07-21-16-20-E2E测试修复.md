# finding

- OpenCode 固定结构生产请求包含 `format.type=json_schema`，服务端读取响应 `info.structured`；E2E 假 provider 当前忽略 `format`，仍只返回 `parts[].text` 的 Markdown JSON。
- Playwright 固定 `workers: 1`、`fullyParallel: false`，但同一 DB 矩阵共用一个 fake provider 进程和累计 `requestAudit`；候选图用例按全历史断言，受前序图片请求污染。

# web_search


# goal

只修复 E2E 假 provider 的固定结构响应合同，以及候选图用例的请求历史隔离；不改生产 OpenCode 逻辑、数据库或项目删除流程。验收为两项最小回归、原失败单项和完整 DB E2E 矩阵通过。

# approach

- 先增加假 provider 的 JSON Schema 响应回归测试并观察失败，再实现 `info.structured` 返回。
- 候选图用例在开头记录请求游标，结尾只审计本用例新增请求；使用前序候选用例 + 当前用例组合验证顺序隔离。

# question

- 用户明确决定项目删除 Outbox/purge 调度不修，本任务不得扩张到该问题。

# todo

- [x] 固化 JSON Schema 假 provider 失败测试
- [x] 实现固定结构响应合同
- [x] 修复候选图请求历史隔离
- [x] 运行单元、组合、单项与完整 DB E2E 回归
- [x] 同步测试文档与长期记忆

# result

- fake provider 新增 `json_schema` 分支：剧情结构请求返回 `info.structured` 和空 `parts`；未带该格式的旧文本响应保持不变。
- 固定结构单测先 3/4 失败，修复后 4/4；E2E 环境测试 36/36、prepare 3/3、E2E typecheck 和全仓 typecheck 通过。
- 候选图顺序组合修复前 1/2，按请求游标只检查新增请求后 2/2。
- 两个原剧情结构失败组合 2/2；完整 DB E2E 15/15、file E2E 4/4。
- 没有修改生产 OpenCode、数据库或项目删除流程。
