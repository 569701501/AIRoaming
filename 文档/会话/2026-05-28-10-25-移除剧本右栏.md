# finding

- 项目事实源明确项目工作区为左侧公共“对话框”、右侧当前步骤主文档或工作内容；第 1 步“剧本”主工作区是剧本文档编辑器。
- `文档/01_愿景与产品/当前UI信息架构.md`、`文档/00_索引/AI上下文入口.md`、`文档/02_架构与契约/核心数据模型.md` 和 `文档/03_模块梳理/模块总览与依赖.md` 仍把“当前章节信息面板”描述为当前体验或已实现能力。
- 代码中 `apps/web/src/components/workbench/ProjectWorkbenchView.vue` 仍常驻渲染 `ScriptOutlinePanel`；`ScriptOutlinePanel.vue` 展示“故事主线 / 出场角色 / 场景列表”，与用户新产品判断冲突。

# web_search

未搜索。

# goal

用户要求先改文档：项目内“当前剧本”是写剧本的区域，右侧显示“主角 / 场景 / 主线”的信息面板不要了。

# approach

将本轮决策写成产品口径：第 1 步“剧本”不再常驻展示最右侧“当前章节信息”面板；当前剧本区域只负责写作、编辑、保存和完成当前章节正文。主线、角色、场景整理后置到用户主动触发的 `AI 分析当前章节`、剧情结构步骤或局部结果。

# question

# todo

- [x] 读取事实源与相关文档
- [x] 定位“当前剧本右侧信息面板”相关描述
- [x] 更新文档口径
- [x] 更新长期记忆与本次结果

# result

已先完成文档更新，未改业务代码。当前产品事实源已明确：剧本页不再常驻展示“当前章节 / 故事主线 / 出场角色 / 场景列表”的最右侧信息面板；旧完成记录 `2026-05-27_当前章节信息面板.md` 已标记为 `superseded`。代码中 `ScriptOutlinePanel.vue` 和 `ProjectWorkbenchView.vue` 仍待后续实现移除。

验证：`git diff --check` 通过；`文档/记忆/MEMORY.md` 为 35 行，符合 500 行以内要求。
