# finding

- M5 已完成；D2-A2-1 提交 `1f22861`、D2-A2-2 提交 `077762d`、D2-A3-1 提交 `9087115`；当前总 Handoff HEAD 为 `34af053`。
- D2-A2-2 将 7 个旧 clear/import/reset/pending 入口退役，blockedIds 由 6 降至 5。
- D2-A3-1 将 7 个旧 Story/Storyboard/Preflight facade 入口退役，复用 G2 modern CAS API，blockedIds 由 5 降至 4。
- P4 D2-A3-2A 已完成 Character identity DB slice：`extract_characters`、`update_character` 直写 Character 并 refresh，legacy workspace 隔离通过；已提交 `e80b8ee`、`f2b4d15`。
- P4 同一切片已补齐 `queue_character_reference`：DB 持久 GenerationTask、Character source freeze、sourceSetSealedAt 与幂等 replay；队列切片代码尚待独立提交。
- P4 已补齐公开 `queue_scene_reference`：ChapterScene source projection、持久 task、幂等 replay；SceneVisual worker 使用 fake handler 完成 staged→ready 证据。
- P4 已补齐 DB `lock_candidate`：CandidateLockRevision 线性事务、Shot current pointer 与同候选 replay；Candidate.status 不写非法 locked 值。
- P4 已补齐 DB `complete_chapter_images`：current preflight + 全镜 current lock 后以 Chapter CAS 推进 `images_done`。
- P4 已将三个同步/批量参考图旧入口合规 retired，统一 replacement 为持久 queue + worker；Character delete 仍受 Outbox 约束。

# web_search


# goal

按总 Handoff 连续执行 D2 至 M6，阶段性内部复核、独立提交；最终停在 `ready_for_real_cutover_authorization`，不接触真实数据、真实凭据、真实 Keychain 或真实切换。

# approach

优先复用 G2 版本仓储和持久任务 substrate；旧文件式入口若会绕过 CAS/事实源则稳定退役，不复制第二套写模型。每个切片 fresh SQLite + 临时 workspace 验证后再提交。

# question


# todo

- [x] P0/P1/P2/P3 完成并独立提交
- [x] P4 Character identity slice 完成并通过 364 项全量回归
- [x] P4 task/source freeze queue slice（worker/staging/CharacterVisual/SceneVisual 待后续）
- [x] P4 character worker/staged Asset/CharacterVisual/preview-final confirm + SceneVisual worker + public scene queue/source projection
- [x] P5 Character delete DB intent（物理清理由 P8 Outbox 完成）
- [x] P4 CandidateLock
- [x] P5 CandidateLock/complete images；旧参考图入口已 retired
- [x] P6 Layout/Export DB-only：LayoutWorkingCopy/Revision、ExportRevision/Artifact、asset package
- [ ] P7 Dialogue runtime
- [ ] P8 Outbox consumer
- [ ] P9 final importer/verifier
- [ ] P10 M6 tooling/rehearsal

# result

截至本次记录：P5 Character delete intent 已完成并提交 `a2c46a3`；P6 Layout/Export DB-only 已实现，P6-LAYOUT-EXPORT-01 与 project DB integration 28/28 通过，capability blockedIds 从 4 降至 3。物理 Character 清理、Dialogue runtime、Project delete/Outbox 仍未完成；下一步进入 P7，不能跳到 P9/M6。
