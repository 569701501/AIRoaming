# finding

- 用户指出 persistence 的真实构成为：实现 11710 行、测试 6045 行、CLI 439 行；仓库机械复算完全一致。
- 自建 review-attestation/review-bundle/review-check 为 2696 行生产代码、2699 行测试；reviews 目录另有 33 个文件/3051 行证据，合计形成约 8446 行审查机制与留痕。
- “独立 Reviewer”没有外部身份、密钥或签名边界；`reviewerId` 和 `independentFromWorker: true` 只是同一工作区 JSON 中的自声明，SHA/CAS 只能证明文件一致性，不能证明真正独立审查。
- 计划把 G2 开始条件绑定到 G1-10 `WIT-01`，导致必须先完成 G1 大多数切片；没有时间盒、复杂度上限或超时降级路径。
- 执行顺序倒置：r5 先 2/2 签收 manifest，之后才展开 Schema 并首次运行真实 Prisma validate，立即暴露 106 个错误，摘要变化又迫使 r6/r7 重审。
- r1～r7 反复围绕 review protocol、digest/source freeze 和 writer 安全返工；当前 r7 的 P1 仍是 review/writer manifest TOCTOU，而不是用户路径或 DB Repository 行为。
- 生产运行时代码没有 `PrismaClient` 接线；Project/Chapter/Script DB-only 垂直切片仍 pending，G2 无实现提交。数百测试的主要反馈信号是 schema/manifest/DSL 正确性，不是数据库真实业务读写。
- 纠偏执行已删除自签 review/CAS 生产关键路径，生成正式 `0001～0008` migration tree，并通过公开 `ProjectsService` 打通 Project/Chapter/Script 创建、草稿、完章和重启读回。
- 最终复核额外发现并关闭启动期 migration ledger P1：缺 0008、真实 P3018、无 ledger 或账本漂移时均在业务加载前拒绝；损坏 current chapter 指针也不再静默 fallback。

# web_search


# goal

在已完成失控诊断后开始执行纠偏：移除自签 review/CAS 写门禁，生成真实 migration tree，完成 Project/Chapter/Script DB-only 垂直切片，并解除 G2 对完整 G1 witness 的错误前置依赖。

# approach

按 `$deep-think` 分阶段执行：C0 固化 ADR 与范围；C1/C2 简化派生链并生成 migration；C3 完成最小业务读写；C4 同步文档和聚合门禁；C5 分离静态与运行复核。默认 file 模式不变，所有 DB 验证使用隔离临时根。

# question


# todo

- [x] 完成失控诊断并形成责任与纠偏结论
- [x] 固化 ADR、纠偏任务计划和复杂度预算
- [x] 移除自签 review/CAS 生产门禁并保留直接校验
- [x] 生成并验证正式 migration tree
- [x] 完成 Project/Chapter/Script DB-only 重启回读
- [x] 同步 G1/G2 文档、修复聚合门禁并完成复核

# result

- 根因不是测试比例过高，而是主代理把交付任务错误升级为自建合规证明系统，并把这套没有真实信任隔离的自审门禁放上关键路径。
- 次要根因是全量 44 模型 big-bang、先审查后运行真实 Schema、G2 对完整 G1 witness 的过度串行依赖，以及缺少时间盒/复杂度预算。
- 已按纠偏方向完成 C0～C5：生产自签门禁移除；正式 8 段 migration 可 fresh deploy/no-pending；Project/Chapter/Script 最小 DB 垂直切片和重启读回可运行；启动时精确核验 migration artifact/ledger。
- 完整门禁通过：定向 2 files/8 tests、server 25 files/162 tests；整仓 shared 15、server 162、E2E env 31、prepare 3、Playwright 4 全绿；Scrutiny 与隔离 Runtime/User Review 均通过。
- 本任务没有迁移真实数据，也没有宣称完整 G1/G2 完成。下一步从 G2 Script Working Copy 与正式发布事务开始，必须补 expected rowVersion/CAS、并发冲突、失败回滚和迟到写隔离。
