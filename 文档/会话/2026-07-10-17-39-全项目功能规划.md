# finding

- 项目已有七步章节流水线，并已于 2026-07-09 至 2026-07-10 形成竞品对照复核、提示词资产盘点和轻画布建议。
- 候选图干净底图相关业务改动已在本会话期间进入 `118e3c7`；本轮只新增研究、决策和会话文档，没有修改功能代码。
- 当前真实成熟度分三层：结构/分镜等前半段可用，候选图真实闭环但质量门未收口，排版/合成/ZIP 等交付能力仍是骨架。
- 真实 Grok 样例的主要问题已转为场景参考图资格和多图输入污染；继续叠加通用 negative prompt 不能替代素材治理。
- 通用任务驻留内存，取消/重试/恢复不足；项目、章节、镜头和工作流状态存在漂移；Prisma 尚未接管主业务数据。
- D1 已有真实 `comicFormat` 字段，但当前创建时隐藏且默认竖向条漫，进入剧本步骤后还能修改，与用户新决策冲突。
- 市面主流顶层形态更适合收敛为条漫/页漫；四格是布局模板而非同层阅读容器。
- 用户最终确认 D1 使用“漫画版式”：放在现有创建项目弹窗，默认空且必选，只提供竖向条漫/分页漫画，创建后不可直接修改；四格归入布局模板。
- 用户再次澄清：项目库中的“创建项目”按钮、`CreateProjectModal.vue` 弹窗及创建后进入剧本阶段的路径都已经存在；G3 只在现有弹窗补“漫画版式”字段和全链路约束，不新增入口、页面或创建向导。
- 用户指出 D3 的真实返修风险：A 图进入下游后换 B 图。代码确认当前换锁只改 `lockedCandidateId`，已有 layout placement 仍可能指向 A，且导出后没有换图路径。用户最终确认采用不可变定稿修订、影响预览、布局来源 stale、旧导出保留和旧任务来源隔离。
- D3 实施契约补充：取消定稿创建 `action=clear` 修订，不无痕清空；source/impact digest 使用排序后的规范 JSON；提交事务只新增修订和更新当前指针，不改写旧布局/导出。
- D45-1 已确认 A：V1 画格使用矩形/圆角矩形，但允许自由移动、缩放、旋转、重叠和调整层级；梯形、斜切、任意多边形和贝塞尔节点后置。
- D45-2 已确认 C：漫画版式与文字书写方向是两个维度；V1 在竖向条漫/分页漫画中均支持 `horizontal-tb/vertical-rl` 与同文字框字符范围富文本。
- D45-3 已确认 A：V1 气泡使用普通、思考、喊叫/爆发、旁白四类预设 + 单可调尾巴，整体可自由变换；连接气泡、多尾巴和任意轮廓后置。
- D45-4 已确认 A：V1 支持临时多选、批量变换/对齐/分布/层级/锁定/隐藏，但不保存一般 Group；气泡内部和画格—图片所属仍持久保存。
- D45-5 已确认 A：V1 桌面浏览器完整编辑；手机只做条漫/页漫真实预览、可读性、切片边界和预检问题查看，不修改 Working Copy 或创建 LayoutRevision。
- D45-6 已确认 A：成稿编辑默认收起 AI 助手，保留可展开抽屉和步骤独立对话；AI 建议先形成可预览、可放弃的 `PendingEditorCommandSet`，用户确认后才进入可撤销 Editor Command。
- 用户要求继续 D7；既定方向仍为“立即全量数据库化”，当前进入迁移、切换、回滚和旧数据处置的逐项规划。
- D7 代码盘点确认：Prisma 6.19.3 只有 6 个未接线模型，无 migration、数据库文件或业务 CRUD；真实主数据仍由 ProjectRepository Map + workspace JSON/Markdown 承载，任务、对话和多类 pending 状态仍在内存。
- 当前 workspace 只读样例为 1 项目、2 章、82 文件、约 54 MB、67 Asset、12 Character、15 Shot、27 Candidate；现有素材/角色/候选引用完整，但 55 个唯一 taskId 中 54 个缺完整 artifact。
- D73 已确认 A：关系核心 + 版本化 Json + 可重建投影；正式保存必须让权威文档、投影和 current 指针同事务一致，投影不可独立写入。
- D74 代码核验确认：SettingsService 当前会把四类 provider key 明文写入 gitignored JSON；对话 key 通过 localhost `/auth` 交给 OpenCode。用户明确：OpenCode 自己的本地文件保存文本 key 正确；图片模型由后端直接调用，图片 key 必须后端安全保存，不能在任何接口、数据库、普通 JSON、日志、任务或 artifact 中暴露明文。
- D75 代码核验确认：TasksService 使用 Map，任务创建后立即 void 调 worker，maxAttempts=1；候选图与角色/场景图使用 Promise 串行链，无 lease、heartbeat、backoff、自动重试和重启恢复。
- 用户确认 D75=A：SQLite `GenerationTask/TaskAttempt` + 单进程持久 worker/lease；任务按 at-least-once 执行并强制幂等、来源校验、重启恢复、协作取消，所有图片任务统一并发 1，BullMQ/Redis 后置。
- 用户确认 D76=A：55 个旧 taskId 全部保留；完整旧证据导入为只读 `legacy_imported`，其余创建不可执行 `legacy_stub`，以独立 provenance 状态表达缺失，不根据文件存在伪造成功历史。
- D7 已新增 ADR-0012 并完成最终文档/决策页复核；独立页与总页在默认和窄屏均无横向溢出，控制台无 warning/error。
- G1 复核确认不能只迁旧 Prisma 六模型：DB-only 必须同时接管对话/工具结果、所有 pending、SecretStore metadata、持久任务、文件 Outbox、迁移 provenance 和删除恢复。
- G1 Schema 已获用户确认，按八组展开 44 个明确模型；ProjectWorkflow/项目制作进度改为从章节里程碑、current 指针和后续 freshness 派生，不把 workflow.json 导入为第二个可写事实。
- G1 采用 RFC 8785 JCS + SHA-256 统一复杂 Json/source/manifest digest；当前 Prisma 6.19.3 不升级 major，SQLite enum/跨字段状态/不可变行由定制 migration SQL 的 CHECK/trigger 和 sqlite_master 合同测试保护。
- 最终 importer 只读 maintenance/停机生成且 pre/post source manifest 一致的脱敏 snapshot；bridge release 启动后的对话/pending/旧任务终态由同进程 MaintenanceCoordinator 生成无秘密 runtime bundle。bridge 启动前已在旧内存且未落盘的状态无法恢复，必须由用户处理或明确放弃并写 `PRE_BRIDGE_RUNTIME_UNOBSERVABLE`。
- G1 文件一致性采用 temp -> staged+Outbox -> 同文件系统 rename -> ready，任务用 claimToken fencing + TaskConcurrencySlot；未知中断的图片/LLM 外部调用默认 needsReview，不盲目重放。
- 旧 metadata 与 Asset 字节混在同一项目目录，不能 chmod 整树；切换时只把 JSON/Markdown/task artifact/shared index 移入独立只读 metadata 档案，保持 Asset storageKey 可写。
- `PersistenceState.firstBusinessWriteAt` 是完整 file-only 回滚终点；非空后只能回滚兼容 DB 应用或恢复协调应用 backup。
- G2 代码复核确认：已完成剧本再次保存不会同步发布 ScriptVersion；剧情结构/分镜正式编辑会原地修改 confirmed 内容；分镜修改还会清空 preflight/candidates/layout 并倒退章节状态；Preflight 只按 storyboard ID + updatedAt 判断来源。
- G2 已采纳方案选择不增加新模型：Script 使用 Chapter working fields，Story/Storyboard 复用 pending version 做 copy-on-write，Preflight 使用 live preview + 不可变 revision；confirmed 内容不可原地修改。
- G2 freshness 固定为 `current/stale/historical/pending`，只从 current/pending 指针、来源 ID、`sourcePolicyVersion` 和规范摘要派生；未知来源用 stale + reason code，不存第二套可写真值。
- dirty Script 或 active Story/Storyboard pending 不改变旧正式链，旧导出仍可查看，但 NewWorkGate 会阻止新下游任务；新上游正式确认后旧下游才派生 stale。
- StoryDocument V2 排除 SceneVisual/时间戳/chapterTitle，StoryboardDocument V2 排除候选锁定/生成状态/时间戳/chapterTitle；Preflight 聚合 current storyboard、角色/场景视觉、Asset sha、comicFormat、artStyle 和 policyVersion。
- G2 长任务完成同时校验 claimToken、active target、target rowVersion、current 来源和 sourceDigest；迟到结果只登记 historical，不更新 pending/current。
- G4 代码复核确认：Shared 仍以 `Candidate.status=selected/locked` 和 `Shot.lockedCandidateId` 表达定稿；Server 旧 lock API 原地改 Candidate/Storyboard；Web 在 `images_done/layout_done/exported` 后禁止生新候选；LayoutExport/AssetPackage 仍从旧指针/状态读入并覆盖骨架产物。
- G4 proposed 开发契约已收口：A→B→clear→A 使用线性不可变修订，`previousRevisionId` 不可分叉；preview/commit 使用 expected revision + JCS impact digest；丢响应只在 previous/action/target 精确匹配时幂等返回，后续决策必须冲突。
- G4 将 lock set 结构状态 `complete/incomplete/unresolved` 与来源 `current/historical/legacy_unresolved` 分轴；replace/clear 不改旧 Layout/Export/Asset/current pointers/milestone，只派生 stale。`ExportRevision.completionApplicability` 表示完成登记时结论，之后 freshness 查询派生。
- G5 代码复核确认：现有 `LayoutExportWorkspace` 仍是一镜一页卡片，`LayoutExportService` 的所谓 PNG 导出只复制每页第一张候选源图；Shared 无 crop/文字/气泡/修订，前后端也尚未安装画布、富文本或正式渲染依赖。
- G5 技术路线不在文档阶段提前锁库：交互内核只做 adapter，LayoutDocument 是唯一事实源；E0 首选验证“专用 HTML/SVG 渲染场景 + 固定版本 Chromium”，并与受控字体的 SVG/resvg 路线对照。
- 用户补充的 9 项文档冲突全部核实并修正：ADR-0007 分镜字段、五级角色、`/script` 路由、章节级候选路径、`image_preflight` 对话 scope、版本化角色图、22 字段 Asset 和两份章节编号已统一；关联扫描还修正了场景图路径、素材目录树和旧 PNG 示例。
- 代码仍有一项单独 TODO：`buildStoryboardPrompt` 写 `over_the_shoulder`，而 Shared 类型与 normalize 合法值为 `over_shoulder`；本轮未获代码开发授权，只完成协议警示和任务登记。

# web_search

- 直接漫画产品：Dashtoon、LlamaGen、Anifusion 共同强化生成后的画格、气泡、编辑、预览和导出；无限画布不是 P0 必需。
- 生产系统邻居：Jellyfish、LocalMiniDrama、LumenX 强调实体资产、shot preparation、关键帧、持久任务、失败恢复和工程导出。
- Prompt/模型：OpenAI 明确文字、一致性与布局仍有限制；Runway Gen-4 不支持 negative prompt；Promptfoo 强调样例、矩阵评测和持续反馈。
- 重要来源：
  - https://github.com/Forget-C/Jellyfish
  - https://github.com/xuanyustudio/LocalMiniDrama
  - https://github.com/alibaba/lumenx
  - https://llamagen.ai/releases
  - https://anifusion.ai/all-features/
  - https://developers.openai.com/api/docs/guides/image-generation
  - https://help.runwayml.com/hc/en-us/articles/35694045317139-Gen-4-Image-Prompting-Guide
  - https://www.promptfoo.dev/docs/intro/
- D1 形态来源：看漫画官方投稿明确“条漫、页漫不限”；Clip Studio 新建入口区分 Webtoon/Comic 并把模板分开；Tapas 支持固定宽度不限高度；KDP 使用固定布局漫画。
  - https://www.kanman.com/about/409275.html
  - https://help.clip-studio.com/en-us/manual_en/210_file/Creating_a_New_Canvas.htm
  - https://help.tapas.io/hc/en-us/articles/1260802028970-Series-Basics-How-to-publish-a-comic-episode-on-Tapas
  - https://kdp.amazon.com/en_US/help/topic/G9GSTY4LTRT39D4Z
- D4/D5 专业工具：Clip Studio 与 MediBang 都把画格、文字和气泡保持为可编辑结构；Clip Studio 进一步提供画格裁切/吸附、气泡尾巴、竖排/ruby、条漫手机预览和切片导出。
  - https://help.clip-studio.com/en-us/manual_en/540_comic/Frames_and_Panels.htm
  - https://help.clip-studio.com/en-us/manual_en/540_comic/Balloons.htm
  - https://help.clip-studio.com/en-us/manual_en/540_comic/Webtoons.htm
  - https://medibangpaint.com/en/tutorial/pc/create-comics/
- D4/D5 AI 漫画工作台：Dashtoon、Anifusion 与 LlamaGen 公开展示生成后画格/文字/气泡编辑、模板、属性面板、历史或导出预览；Anifusion 特别将 Comic Creator 与 Infinite Canvas 分成不同工具。
  - https://insiders.dashtoon.com/dashtoon-studio-august-2024-release/
  - https://anifusion.ai/all-features/
  - https://llamagen.ai/releases
- D4/D5 轻量设计路径：Canva 与 Adobe Express 优先让用户从模板/AI 初稿出发，再用少量明确动作重排画格、裁切换图、改文字和气泡。
  - https://www.canva.com/create/comic-strips/
  - https://www.canva.com/features/design-grid/
  - https://www.adobe.com/express/create/comic-strip
- D4/D5 结论：首版应是“条漫/页漫有限成稿容器 + 高自由对象编排”，而不是无限白板或专业矢量/绘画软件。
- D7 Prisma/SQLite：Prisma migration history 必须进入版本控制；SQLite Json 从 Prisma 6.2 起支持但高级过滤有限；SQLite 事务为 Serializable；interactive transaction 不应包含网络或慢操作；SQLite/PostgreSQL migration SQL 不兼容；Prisma 不提供可依赖的自动数据 down migration。
  - https://www.prisma.io/docs/orm/v6/prisma-client/queries/transactions
  - https://www.prisma.io/docs/orm/prisma-migrate/getting-started
  - https://www.prisma.io/docs/orm/prisma-migrate/understanding-prisma-migrate/migration-histories
  - https://docs.prisma.io/docs/guides/database/data-migration
  - https://www.prisma.io/docs/orm/reference/database-features
  - https://www.prisma.io/docs/orm/v6/prisma-migrate/understanding-prisma-migrate/limitations-and-known-issues
  - https://www.sqlite.org/wal.html
  - https://www.sqlite.org/backup.html
- D74 系统凭据与 OpenCode：Apple Keychain、Windows Credential Locker、Linux Secret Service 都是系统级小秘密存储；OpenCode 官方文档说明 provider credential 默认持久化到 `auth.json`；常见 Node 封装 `atom/node-keytar` 已归档，不能直接当成长期实现保证。
  - https://developer.apple.com/documentation/security/keychain-services
  - https://learn.microsoft.com/en-us/windows/apps/develop/security/credential-locker
  - https://specifications.freedesktop.org/secret-service/latest/
  - https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/cli.mdx
  - https://github.com/atom/node-keytar
- D75 BullMQ：BullMQ 建立在 Redis 上，适合多进程/多机器 worker；官方说明最坏情况为 at-least-once，stalled job 可能重新执行，任务仍必须幂等。
  - https://docs.bullmq.io/
  - https://docs.bullmq.io/guide/workers/stalled-jobs
  - https://docs.bullmq.io/patterns/idempotent-jobs
- G0 E2E 工具：Playwright 官方支持启动多个本地 webServer、隔离 page/context/request fixture、用 project dependencies 做 setup/teardown，并通过 trace 查看 DOM、网络和时间线；Vitest Browser 更适合浏览器组件测试且仍建议 Playwright provider。因此 G0 选择“Vitest Service + 独立 Playwright API/Chromium”。
  - https://playwright.dev/docs/test-webserver
  - https://playwright.dev/docs/test-fixtures
  - https://playwright.dev/docs/test-global-setup-teardown
  - https://playwright.dev/docs/best-practices
  - https://vitest.dev/guide/browser/
- G1 官方复核：Prisma SQLite 的 Json/Enum 当前可用，但 enum 不是 SQLite 数据库级强制；migration SQL 可定制，`migrate deploy` 不做 drift 检测；SQLite 只支持 Serializable，长事务不应包含网络/文件；`integrity_check` 不检查 foreign key，必须另跑 `foreign_key_check`；WAL 仍只有一个 writer并要求正确 checkpoint/backup。首版因此固定当前 Prisma 6.19.3、定制 CHECK/trigger、不依赖 partial-index preview、默认不启用 WAL。
  - https://www.prisma.io/docs/orm/v6/overview/databases/sqlite
  - https://www.prisma.io/docs/orm/prisma-migrate
  - https://www.prisma.io/docs/cli/migrate/deploy
  - https://www.prisma.io/docs/orm/v6/prisma-client/queries/transactions
  - https://www.sqlite.org/pragma.html
  - https://www.sqlite.org/foreignkeys.html
  - https://www.sqlite.org/wal.html
  - https://www.sqlite.org/backup.html
- G1 复杂 Json 摘要采用 RFC 8785 JCS，保证对象键排序、数组顺序、数字/字符串序列化和 UTF-8 输出可重复。
  - https://www.rfc-editor.org/rfc/rfc8785.html
- G5 渲染技术复核：W3C Writing Modes 定义 `vertical-rl/text-orientation`；Konva 官方建议 Vue 应用保存业务状态而非节点 JSON；Playwright 版本绑定特定 Chromium 且支持截图/PDF；resvg 可关闭系统字体并加载指定字体输出 PNG。这些只支持 E0 候选，不等于已通过项目原型。
  - https://www.w3.org/TR/css-writing-modes-3/
  - https://konvajs.org/docs/vue/Save-Load.html
  - https://playwright.dev/docs/browsers
  - https://playwright.dev/docs/api/class-page
  - https://github.com/thx/resvg-js

# goal

- 基于项目文档、真实代码现状和 2026 年当前竞品资料，重新整理全项目已具备、部分具备、缺失和不应建设的能力。
- 先产出一份可逐项讨论和决策的 HTML；用户确认方向后，再写详细开发方案与分阶段开发文档。
- 本轮不进入功能开发。
- D7 子阶段已收口：D71–D76 全部确认并形成 ADR-0012；仍不进入开发。
- G1 开发级文档目标已完成：细化 M0–M6、Schema/旧数据映射和迁移验收；仍不进入开发，等待继续 G2 文档或用户复核。
- G2 开发级文档目标已完成并获用户确认：主方案、精确契约字典、active ADR-0013 和验收清单已形成；仍未开发，下一份文档为 G3 D1 漫画版式入口。
- G3 开发级文档目标已完成并获用户确认：主方案、契约/旧值迁移字典和验收清单均已采纳；复用现有创建入口与弹窗，仍未开发。
- G4 开发级文档目标已完成并获用户确认：主方案、定稿/影响预览契约字典和验收清单已进入 accepted；仍未开发，下一步进入 G5 开发级文档。
- G5 开发级文档已完成并获用户确认：LayoutDocument/命令、Working Copy/LayoutRevision、stale 解决、受控字体、确定性 RenderScene、一个 layout_publication 多 Artifact 出版版本与 200 项唯一验收标识均已收口；仍未授权开发。
- 用户决定素材包方案先不写，G6/G7 后置；当前范围改为复核 G0–G5 是否足以进入开发准备，不删除第七步导航或既有素材包骨架。
- G0–G5 完备性复核已完成：内容达到可实施级；用户逐阶段用“继续”确认前一阶段，并在本轮澄清确认意图，因此 G0–G5 均为 accepted；当前阶段闭环到 `layout_publication/layout_done`，ZIP/`exported` 属于后置 G6/G7。
- 完备性复核的 R5 一致性修订已完成：用户列出的 9 项文档问题全部清零，且通过反向契约检查；不把尚未修复的代码枚举拼写误报为文档遗留。

# approach

- 先用 `grill-with-docs` 校验术语、边界与关键产品分叉，再用 `$deep-think` 完成跨模块研究、证据留痕和复核。
- 以用户任务而非竞品功能数量为主轴，采用“吸收 / 改造后吸收 / 暂缓 / 明确不做”四类判断。
- 推荐三轨并行：静态漫画发布闭环为近期主线，生产可靠性和 AI 质量系统补必要底座，轻漫剧等待产品决策。
- 用户要求后续一个一个细化；D1、D3、D4/D5 已收口，仍不进入功能开发。
- D4/D5 使用 `$deep-think` 正式规划：现有排版代码、官方市场资料、六项首版边界与 ADR-0011 已完成复核；下一项按依赖建议进入 D7。
- D7 使用 `$deep-think` 正式规划：切换、引擎、关系/Json、密钥分治、持久任务底座与旧任务迁移均已确认，下一阶段只能在详细开发文档完成后进入实现。
- G1 继续使用 `$deep-think`：先以当前代码/真实 workspace/官方资料收口 44 模型与切换状态机，再分别写主方案、Schema 映射、测试 runbook；不把 G2–G6 业务功能提前塞入数据库阶段。
- G2 使用 `$deep-think`：以 Working Copy/confirmed/current、SourceSnapshot/FreshnessResolver、NewWorkGate/TaskApplicabilityGuard 四组边界收口，不新增通用版本表或失效表，不把 G4 CandidateLock 和 G5 Layout 实现提前纳入。
- G3 使用 `$deep-think`：先审计现有创建弹窗、共享 DTO、服务、DB-only Schema、旧值读取和下游枚举耦合，再以“扩展现有弹窗、运行时严格、迁移时兼容、数据库硬锁”收口；不重做项目入口或七阶段导航。
- G4 使用 `$deep-think`：以 ADR-0010 + G1/G2/G5 边界收口修订状态机、两阶段影响检查、SQLite 线性并发、lock set/source freshness 和迟到任务隔离；G4 不提前实现 G5 画布换图命令。
- G5 使用 `$deep-think`：先用 E0 门禁验证交互内核、横竖排富文本、受控字体与浏览器/服务端渲染一致性，再按容器/命令、对象、文字气泡、来源版本、真实出版导出拆分；ZIP 保持在 G6。

# question

- D2 已无待决策项：复核确认“AI 分步生成、用户确认后推进”已经实现；未来自动跨步骤和批量调度继续后置。
- G5 已确认：默认页/条漫 profile、画格所属图片复合关系、离开只 autosave、active Shot 可见门禁、单一 layout_publication 和 E0 候选路线均已冻结；不需要在原型前锁定具体画布库。
- G6/G7 已无当前待决策项：素材包 V2、ZIP 下载和七阶段 `exported` 总验收明确后置。
- 开发前仅剩一个动作：用户另行明确授权开始开发；方案确认不等于开发授权。

# todo

- [x] 恢复项目事实源与真实实现清单
- [x] 复核现有两份 HTML 的证据、时效性和推理边界
- [x] 补充当前竞品与提示词工程一手资料调研
- [x] 形成能力缺口、价值、依赖、风险与阶段矩阵
- [x] 产出可讨论的决策 HTML 并完成静态与浏览器复核
- [x] 逐项获取用户决策（D2 自动调度详情与 D6/R5 均按用户意见后置）
- [x] 围绕现有七步链路形成真实能力缺口、升级顺序与完整端到端验收总文档，不重复建设阶段框架、不纳入自动调度
- [x] 用户确认总方案后，完成 G0 开发级方案与七阶段行为用例矩阵
- [x] 用户确认继续后，完成 G1 D7 主方案、Schema/旧数据映射和迁移执行验收清单；整套文档完善前不进入实现
- [x] 用户继续后完成 G2 上游版本、来源摘要、freshness、迟到任务和返修验收开发级文档
- [x] 用户确认 G2 核心规则并激活 ADR-0013
- [x] 用户继续后编写 G3 D1 漫画版式入口开发级文档
- [x] 用户继续后编写 G4 候选定稿修订、影响预览与返修验收开发级文档
- [x] 用户继续后编写 G5 成稿编辑器、LayoutDocument、确定性渲染与出版导出开发级文档
- [x] 复核 G0–G5 文档完备性，建立阶段性验收，并将 G6/G7 从当前开发前置中移除
- [x] 将 AI 质量与提示词工程登记为独立大功能提议任务
- [x] 确认并正式记录 D1 漫画版式决策
- [x] 确认并正式记录 D3 候选返修决策
- [x] 完成 D4/D5 市场调研和首版边界提议
- [x] 获取并记录 D45-1 画格轮廓自由度决策
- [x] 解释漫画版式/文字书写方向差异，并记录 D45-2 选择 C
- [x] 获取并记录 D45-3 气泡结构自由度选择 A
- [x] 获取并记录 D45-4 一般对象分组与图层深度选择 A
- [x] 获取并记录 D45-5 移动端编辑边界选择 A
- [x] 获取并记录 D45-6 成稿步骤 AI 助手占位选择 A
- [x] 完成 D7 Prisma/内存/workspace/设置/素材真实数据盘点
- [x] 形成 D7 目标事实源、M0–M6 迁移方案和配套决策页
- [x] 获取并记录 D71 切换模式选择 A
- [x] 获取并记录 D72 首版数据库引擎选择 A
- [x] 获取并记录 D73 关系表与复杂文档边界选择 A
- [x] 获取并记录 D74 修正版密钥分治决策
- [x] 获取并记录 D75 首版任务执行底座决策
- [x] 获取并记录 D76 缺失旧任务历史迁移口径
- [x] 核实并修复用户补充的 9 项正式文档一致性问题，完成反向扫描和 R5 交接

# result

- D1、D3、D4/D5、D7 已收口；D2 A 已复核为现有实现，自动调度细节明确后置。下一阶段应为现有七步链路的缺失能力和完整 E2E 验收编写开发文档，而不是重新实现“分步生成、用户确认后推进”。
- 两份 HTML 决策页已同步当前口径：D2 标记为“现有基线、自动调度后置”，D7 后续步骤改为“现有七步链路缺口与验收文档”。
- 代码与真实数据复核：七阶段状态机和每阶段用户推进动作均存在；真实第 1 章已到候选图，15 镜头已有 27 张候选但 0 张锁定，未实际进入排版/素材包。测试 79 项与三包类型检查通过，仍缺完整七阶段 E2E。
- 已完成 `2026-07-10_七阶段能力缺口与升级顺序.md` 和 `七阶段完整链路验收基线.md`：开发依赖为 G0 行为刻画 → G1 D7 DB-only → G2 上游版本/freshness → G3 D1 → G4 D3 → G5 D4/D5 → G6 素材包 V2 → G7 完整 E2E。
- 已完成 `2026-07-11_G0七阶段行为刻画与E2E测试骨架方案.md` 和 `G0七阶段行为用例矩阵.md`。G0 选择 Vitest Service characterization + 独立 Playwright API/Chromium，测试必须使用临时 workspace 和 loopback fake provider；不把当前排版复制源图、目录素材包等错误成功语义写成绿测。用户随后确认继续 G1 文档，整套文档完善前仍不进入实现。
- 用户继续后已完成并确认 G1 文档：`2026-07-11_G1数据库事实源与DB-only切换开发方案.md`、`2026-07-11_G1数据库Schema字典与旧数据映射.md`、`G1数据库迁移执行与验收清单.md`。方案补齐 44 模型、snapshot/runtime bundle、SecretStore、持久任务 fencing、Asset/Outbox、metadata 封存、firstBusinessWriteAt 回滚边界和 G0 witness 接替；本轮只写文档，当前 schema、业务代码、数据库和真实 workspace 均未修改。
- 用户继续后已完成并确认 G2 文档：`2026-07-11_G2上游版本链与Freshness开发方案.md`、`2026-07-11_G2版本来源与Freshness契约字典.md`、`ADR-0013_上游版本链与派生Freshness.md`、`G2上游版本链与失效验收清单.md`。方案保持 G1 44 模型，以 Working Copy、不可变正式版本、来源 policy/digest 和派生 freshness 取代原地覆盖/清历史；ADR-0013 已激活，本轮仍未开发。
- 用户继续后已完成并确认 G3 文档：`2026-07-11_G3漫画版式入口与不可变约束开发方案.md`、`2026-07-11_G3漫画版式契约与旧值迁移字典.md`、`G3漫画版式入口与锁定验收清单.md`。用户澄清的“已有创建入口，只缺漫画版式”已落实为复用现有弹窗与跳转；runtime strict、migration-only mapper、普通 PATCH 409、SQLite trigger 和 legacy adapter 边界已经写清。三份文档已进入 `accepted`，本轮仍未开发。
- 用户继续后已完成并确认 G4 文档：`2026-07-11_G4候选定稿修订与返修开发方案.md`、`2026-07-11_G4候选定稿与影响预览契约字典.md`、`G4候选定稿返修验收清单.md`。方案补齐线性 previous 约束、精确 replay、Working Copy 临时 binding 投影、lock set/source applicability 分轴、Export 完成时适用性、已导出后继续生候选和 G4/G5 边界。文档已进入 `accepted`，本轮仍没有修改代码、Schema、数据库或真实 workspace。
- 用户继续后已完成并确认 G5 文档：`2026-07-11_G5高自由成稿编辑器开发方案.md`、`2026-07-11_G5LayoutDocument与编辑命令契约字典.md`、`2026-07-11_G5确定性渲染与出版导出契约.md`、`G5成稿编辑器与确定性导出验收清单.md`。方案拆为 M0–M8，采用严格 LayoutDocument、受控画格图片复合关系、Working Copy/显式 Revision、字体 Asset、stale preview/commit、专用 RenderScene 与一个 publication 多 Artifact 版本；E0 前不锁技术库。未修改代码、Schema、数据库、依赖或真实 workspace。
- 用户将 G6 素材包 V2/ZIP 与 G7 七阶段总验收后置。已新增 `2026-07-11_G0至G5开发文档完备性复核.md`，把当前阶段性终点固定为 current `layout_publication`/`layout_done`，并同步产品范围、七阶段路线、验收基线、执行路线和 G0–G3 Handoff。复核未发现 G0–G5 需要新增的核心方案；G0–G5 均已确认，只剩单独开发授权。
- 用户列出的 9 项事实源冲突已经全部核实并修复；相关 JSON、章节编号、路径和交叉引用通过静态复核。唯一关联代码问题是 Prompt 中 `over_the_shoulder` 与合法 `over_shoulder` 不一致，已登记待开发，未越权修改代码。
