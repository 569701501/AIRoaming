# finding

- D2-A0 已提交 `a940166`，当前工作树干净，下一步是 D2-A1。
- `SettingsService` 当前把文本 key、OpenAI/豆包/Grok 图片 key 都放在 `workspace/settings/app-settings.json`，运行时直接从内存返回明文给 provider。
- Prisma 已有 `AppPreference`、`ProviderConfig`、`CredentialMetadata` 三个目标 metadata 模型；当前业务 SettingsService 没有注入 Prisma，也没有 SecretStore。
- 文本 key 的正式所有权是 OpenCode 本地 auth；图片 key 必须由后端 SecretStore 保存。DB/workspace/API/日志/task/artifact/普通备份不得出现图片 key。
- 现有 `credential-redactor.ts` 只用于迁移/快照脱敏；`ImageProviderService` 继续通过同步 runtime getter 取值，但 getter 已改为 SecretStore-backed cache。

# web_search

# goal

- 完成 D2-A1 的首个可验证切片：SecretStore 契约与 fake store、图片凭据运行时读取边界、非秘密 settings metadata 写入准备和安全证据。
- 不实现 D2-A2 公开项目写、不执行真实系统 Keychain/Secret Service、真实 workspace 清理或 M6。

# approach

- 先把 SecretStore、SecretString、fingerprint、递归 redaction 和 fake store 做成独立可测试模块。
- SettingsService 在 file 模式保留兼容读路径但禁止新增图片明文写回；DB 模式以 CredentialMetadata/ProviderConfig/AppPreference 为事实源。
- 图片 provider 只通过 SecretStore 读取 key，公共 DTO 只返回 configured/fingerprint，不返回 keyPreview。
- 所有迁移/清理只在临时 fixture 中验证，真实平台 adapter 继续 fail-closed/未授权。

# question

- 系统凭据库 adapter 的具体库仍需在本切片后冻结；当前先交付契约、fake store 和生产无明文回退门禁。

# todo

- [x] 完成 Settings/SecretStore 代码探索与调用地图
- [x] 编写五份 D2-A1 施工资料
- [x] 实现首个 A1 代码切片与安全测试
- [x] 运行验证、复核、提交并更新长期记忆

# result

- D2-A1 已完成首个安全切片：新增 `SecretString`、`SecretStore`、fake/unavailable adapter；fake root 0600/0700、symlink/路径安全、opaque `airoaming:image:v1:<uuid>` ref 与 fingerprint。
- SettingsService 已禁止图片 key 写入 JSON/DB/API，旧图片明文在 fake store 可用时迁移并脱敏；文本 key 仅当前进程供 OpenCode 同步，DB metadata 重启可读。
- 新增 9 项定向安全测试；server 全量 51 files/350 tests、server/web typecheck、Prisma validate 和 diff check 通过。
- A1 明确不实现真实平台凭据库；DB 图片凭据 clear/replace 在 Outbox consumer 完成前拒绝 `SETTINGS_SECRET_CLEAR_REQUIRES_OUTBOX`，SEC-10 因本切片无 task/artifact/log 写入路径记为 N/A。
