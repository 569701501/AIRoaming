# finding

- 设计口径已明确：用户选中某个灵感种子后，应直接触发 `generate_script_from_seed`，生成固定格式「章节剧本」并写入当前章节，不应只停留在聊天回复。
- 当前代码已有后端写入链路：`tryHandleScriptInspiration -> createGenerateScriptFromSeedToolResult -> ProjectsService.writeChapterDraftFromAI`。
- 可能漏触发的点在选择表达和显式 UI：后端此前主要识别“选第 N 个/第 N 个”，前端灵感卡片只展示内容，没有显式“生成章节”按钮。
- 已增强后端识别：支持裸数字/中文数字以及更口语化的选择表达；识别到像选择但无法确定时，会提示用户回复“选第 1 个/第 2 个/第 3 个”或点按钮。
- 已增强前端：灵感种子卡片新增“生成章节”按钮，直接发送 `intent: generate_script_from_seed`。

# web_search


# goal

确认并修复灵感种子选择后的写入链路：用户在 AI 输出 3 个灵感后选择某一个，应直接调用 `generate_script_from_seed`，生成固定格式「章节剧本」并写入当前章节文档。

# approach

使用 `$grill-with-docs`：以代码和文档事实源核对当前设计。设计已明确“选中灵感 = 写入章节”，因此本次直接增强识别和 UI 显式选择入口。

# question


# todo

- [x] 核对当前选择灵感的识别逻辑
- [x] 增强文本选择识别
- [x] 为灵感卡片增加显式写入按钮
- [x] 运行类型检查并记录结果

# result

已完成。验证命令：

```text
corepack pnpm --filter @airoaming/server typecheck
corepack pnpm --filter @airoaming/web typecheck
git diff --check
```

结果均通过。
