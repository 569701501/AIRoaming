# finding

- 当前 HEAD 为 `65c90fe`，工作树干净；`4972d8e` 之后没有 M4 生产代码变化，只有证据文档同步和 12 张旧截图删除提交。
- M4 验收清单仍保留“截图删除未提交、当前工作树仅有截图删除”的陈旧描述，需要在签字前修正。
- 当前 HEAD 重新执行 server 全量测试为 47 个测试文件、303 项测试通过；迁移集成文件 58/58 通过。
- workspace typecheck、G1 manifest/schema/migration、Prisma validate、`git diff --check` 均通过；`0c3295b..HEAD` 未修改 `schema.prisma` 或 migration tree。
- M5 总文档已有 backup/restore、capability、SecretStore 和 activate 边界，但缺少 Luna 可直接领取的分切片任务书、精确允许修改路径与每片退出证据。
- M4 已按当前 HEAD 正式通过，没有 P0/P1 实现缺口；截图删除已在 `65c90fe` 单独提交，验收材料已消除陈旧描述。
- M5 已建立 A0 capability registry、A1 coordinated backup、A2 restore、A3 rehearsal 四个独立切片，文档状态为 `ready_for_development`。
- M4 full importer 不创建伪 full run，而是保留 16 条独立 run；因此 M5 coordinated backup 必须验证 full-import report + decisions artifact + 16 条 ledger run，不接受单条 succeeded run 代表全量成功。

# web_search


# goal

正式复核并签署 M4；若无 P0/P1 缺口，将阶段推进到 M5，并补齐可交付给 5.6 Luna 独立执行的 M5 施工资料。M5 只允许临时根开发和演练，不授权 final import、真实 workspace、production activate 或 M6。

# approach

先以当前 HEAD 重跑 M4 门禁并修正陈旧证据，再按 G3-M 五份施工资料、G1 backup/cutover 契约和当前代码入口拆分 M5。M4 审查与 M5 施工编排分开留痕。

# question


# todo

- [x] 重跑 M4 全量测试和静态门禁
- [x] 修正 M4 验收材料并正式签字
- [x] 审查 M5 前置契约与当前代码入口
- [x] 建立 M5 分切片任务包与 Luna handoff
- [x] 文档一致性复核并准备提交

# result

M4 已正式验收通过，M5 已推进到 `ready_for_development`。Luna 的开工入口是 M5-A0；M5 代码、Runtime Review、SecretStore、final/pre-cutover 和 M6 仍未完成。
