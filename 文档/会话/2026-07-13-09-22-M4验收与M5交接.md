# finding

- 当前最终 HEAD 为 `68bb694`；M4 基线为 `65c90fe`，M5 依次提交 A0 `91a450c`、A1 `b767e30`、A2 `0f20cff`、A3 `68bb694`。
- M4 验收清单仍保留“截图删除未提交、当前工作树仅有截图删除”的陈旧描述，需要在签字前修正。
- 当前 HEAD 重新执行 server 全量测试为 47 个测试文件、303 项测试通过；迁移集成文件 58/58 通过。
- workspace typecheck、G1 manifest/schema/migration、Prisma validate、`git diff --check` 均通过；`0c3295b..HEAD` 未修改 `schema.prisma` 或 migration tree。
- M5 总文档已有 backup/restore、capability、SecretStore 和 activate 边界，但缺少 Luna 可直接领取的分切片任务书、精确允许修改路径与每片退出证据。
- M4 已按当前 HEAD 正式通过，没有 P0/P1 实现缺口；截图删除已在 `65c90fe` 单独提交，验收材料已消除陈旧描述。
- M5 A0～A3 四个切片已实现并通过验收：capability registry、coordinated backup、sealed restore、临时根重启/API rehearsal；文档状态为 `completed`。
- M4 full importer 不创建伪 full run，而是保留 16 条独立 run；因此 M5 coordinated backup 必须验证 full-import report + decisions artifact + 16 条 ledger run，不接受单条 succeeded run 代表全量成功。

# web_search


# goal

正式复核并签署 M4，随后完成 M5 A0～A3 的临时根 backup/restore/restart/API 演练。M5 不授权 final import、真实 workspace、production activate 或 M6。

# approach

先以当前 HEAD 重跑 M4 门禁并修正陈旧证据，再按 G3-M 五份施工资料、G1 backup/cutover 契约和当前代码入口拆分 M5。M4 审查与 M5 施工编排分开留痕。

# question


# todo

- [x] 重跑 M4 全量测试和静态门禁
- [x] 修正 M4 验收材料并正式签字
- [x] 审查 M5 前置契约与当前代码入口
- [x] 建立 M5 分切片任务包与 Luna handoff
- [x] 文档一致性复核并准备提交
- [x] 实现并提交 M5-A0 capability registry
- [x] 实现并提交 M5-A1 coordinated backup
- [x] 实现并提交 M5-A2 sealed restore
- [x] 完成 M5-A3 重启/API smoke、全量测试与 G1 门禁

# result

M4 已正式验收通过。M5 已完成：server 全量 49 files/314 tests、workspace typecheck、G1 manifest/schema/migration、Prisma validate 与 diff check 通过；Runtime/User Review 已完成。M5 只证明临时根演练，不代表 capability 全绿、SecretStore、final/pre-cutover 或 production activate 完成。
