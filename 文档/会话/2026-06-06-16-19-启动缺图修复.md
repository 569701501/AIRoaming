# finding

- 前端构建失败来自 `apps/web/src/components/projects/ProjectCommandPanel.vue` 对 `../../assets/project-library/hero-banner.png` 的静态导入无法解析。
- `git status --short` 显示 `apps/web/src/assets/project-library/hero-banner.png` 为删除状态，且 `git ls-files` 确认该图片原本是受 git 跟踪的项目库 banner 资源。

# web_search


# goal

恢复误删图片，使项目启动或前端构建不再因为静态资源缺失失败。

# approach

- 只恢复缺失的受跟踪图片文件，不改动其它业务代码或用户已有改动。
- 恢复后运行前端构建验证。

# question


# todo

- [x] 复现启动相关构建错误并定位缺失资源
- [x] 恢复缺失图片
- [x] 运行验证命令

# result

- 已通过 `git restore -- apps/web/src/assets/project-library/hero-banner.png` 恢复误删的项目库 banner 图片。
- `pnpm --filter @airoaming/web build` 已通过，Vite 能正常解析 `ProjectCommandPanel.vue` 中的图片导入并产出 `dist/assets/hero-banner-*.png`。
