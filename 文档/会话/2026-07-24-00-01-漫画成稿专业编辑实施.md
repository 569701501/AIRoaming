# finding

- 已确认用户授权按 2026-07-23 proposed 方案直接实施，不再逐项请求确认；现有漫画成稿测试数据允许重建。
- 工作树已有上一轮评估文档和长期记忆变更，另有并行会话记录；实施必须保留这些改动，不覆盖无关文件。
- Shared 的 V1/V2 Revision、双摘要 Preflight、Publication、同 Shot 全 appearance 来源替换与 RichText 保护已完成，37 个测试文件、247 个测试通过。
- migration 必须兼容“V1 出版任务在升级前排队、升级后完成”的状态；历史行不回填双摘要。
- 服务端 V2 composition evidence 必须追溯到成功并已应用的 `layout_compose` 任务和原始 lock 集，不能仅相信 Working Copy 自报摘要。
- 本仓库必须使用 `corepack pnpm` 读取固定 pnpm 9.15.4；裸 pnpm 7.12.1 不兼容当前 lockfile。
- V2 Publication source policy 已精确绑定 schema 与 `layout_export` consumer；worker 只相信数据库 sealed source rows 并独立重建摘要。
- Web 已引入 Konva 纯交互适配器和 P2 气泡/富文本/SFX/图层入口，Konva 私有状态不落盘。

# web_search


# goal

实施漫画成稿专业编辑能力吸收方案：先完成 P0 的 V2 Revision/Preflight/Publication、双摘要、来源替换与权威预览，再完成 Konva Web 交互适配器和现有气泡/富文本/SFX/图层能力显性化，并通过自动化与真实用户路径复核。

# approach

- 使用 `$deep-think` 建立正式任务包，按 Orchestrator → Worker → Scrutiny Review → Runtime/User Review 执行。
- P0 是发布硬门；P1.1/P2 仅在 P0 契约冻结后并行。
- 不进入未经新 ADR 定义的 V3 滤镜、文字特效或新气泡轮廓。

# question


# todo

- [x] 恢复事实源并写正式实施计划
- [x] 完成 P0
- [x] 完成 P1
- [x] 完成 P2
- [x] 完成文档、自动化和真实运行复核
- [x] 写完成记录与长期记忆

# result

- P0/P1/P2 已完成：V2 Revision/Preflight/Publication 双摘要、同 Shot 来源替换、权威预览、Konva 交互适配器和现有专业编辑能力全部进入正式链路。
- 历史 V1 不改写、在途 V1 publication 可跨 migration 完成；V2 restore 只允许保持/升级 schema，不允许降级。
- 最终验证：Shared `247`、Web `31`、Server `777` 项测试通过，Web build、Server/E2E typecheck、DB-only V2 页面闭环和 diff check 通过。
- 独立 Scrutiny Review 无阻断项；Runtime/User Review 为 `PASS（with observations）`。P3/P4 的真实素材审美、滤镜/网点/新轮廓等不在本轮范围。
