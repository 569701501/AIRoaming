# finding

- A4 真实生产入口是 `generateScriptFromOutlineWithAI -> buildScriptFromOutlinePrompt`，读取已确认项目大纲、目标章节卡、相邻章节卡和必要的上一章正式全文。
- A4 首次输出同时经过 Shared `creative.chapter-draft/1.0` 严格 Markdown 解析和 `assertP3P5ChapterDraftQuality`；格式或质量失败最多修复一次。
- `buildScriptFromSeedPrompt` / `generateScriptFromSeedWithAI` / `createGenerateScriptFromSeedToolResult` 已没有调用者，属于旧“选中灵感后直接写第 1 章”死路径，与当前“先生成并确认大纲”流程冲突。
- `generate_script_from_seed` 仍存在于 Shared、前端和迁移兼容中，是历史数据枚举，本轮不删除。

# web_search


# goal

将 A4 `script-chapter-drafting` 的生产 Prompt、P3/P5 质量重写与格式修复归入现有 Skill，让真实 A4 路径读取 Skill 资产。保持显式单章触发、六区块 Markdown、密封来源、一次修复上限和 A5 待确认流程不变。

# approach

- Skill references 保稳定角色、P3/P5 方法、输出语义和修复指令。
- Shared 继续保固定章节 Markdown 格式与 strict parser；TypeScript 只注入精确来源和执行 Validator。
- 移除不可达的旧“灵感直出第 1 章”Prompt 和私有方法，保留历史枚举与数据兼容。
- 增加 Skill 修复分流和提示词来源防回流测试。

# question


# todo

- [x] 完成 `script-chapter-drafting` 生产与修复资产
- [x] 改造 A4 真实路径并移除不可达旧 Prompt
- [x] 补充回归与来源卫生测试
- [x] 完成全量验证、静态/运行复核与文档留痕

# result

A4 主生成、P3/P5 质量重写和格式修复已统一读取 `script-chapter-drafting/references/`；旧“灵感直出第 1 章”私有死路径已移除，历史协议兼容值保留。页面、六区块字段、显式单章触发、来源密封、一次修复上限和 A5 不变。Skill 校验、定向 58/58、类型检查、构建和 Server 全量 124 files / 737 tests 通过；未调用真实文本模型或图片服务。

用户随后要求提交代码；提交范围按当前工作区的完整 Prompt 改造闭环处理，包括图片 Prompt 专业 V2、OpenCodeAI Skill 单一事实源迁移、P1/P2/A4 归位及对应测试和正式留痕，不推送远端。
