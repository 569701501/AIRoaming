# finding

- 项目已接入 Grok 图片生成，默认模型为 `grok-imagine-image-quality`；本次需求按“新增 Grok 文本对话模型”处理。
- 文本对话统一走 OpenCode Runtime；前端模型下拉读取 `/api/ai-runtime/models`，服务端从 OpenCode `/config` 动态返回 provider/model。
- 设置页当前只有一个“OpenCode 对话”凭证槽位，服务商候选尚无 xAI/Grok。

# web_search

- xAI 官方 2026-07-16 文档确认当前通用文本模型为 `grok-4.5`，支持 Responses API、Chat Completions 和 function calling；官方 Base URL 示例为 `https://api.x.ai/v1`：`https://docs.x.ai/developers/grok-4-5`。
- OpenCode 官方 provider 文档确认原生支持 `xai` provider，可通过 xAI API Key 接入并在统一模型目录中选择 Grok：`https://opencode.ai/docs/providers`。

# goal

在不发起付费模型调用的前提下，为项目对话模型增加可配置、可选择的 Grok 文本模型，并完成静态与自动化验证。

# approach

先核对 xAI 与 OpenCode 官方现行模型/provider 配置，再沿现有设置、OpenCode 配置、模型下拉和调用链做最小兼容改动。

# question


# todo

- [x] 核对官方 Grok 文本模型 ID 与 OpenCode provider 配置
- [x] 实现 Grok 文本模型配置和选择
- [x] 补充测试与项目文档
- [x] 完成验证并更新长期记忆

# result

- 设置页新增 `xAI Grok 对话`；选择后预填 `xai / grok-4.5 / https://api.x.ai/v1`。
- 不新增 Grok 专用调用分支，继续复用 OpenCode auth、模型列表和消息透传。
- 修复切换文本 provider 且未填写新 Key 时旧密钥指纹仍残留的问题。
- 两个目标测试文件共 12 项通过；Web/Server typecheck、生产构建和 `git diff --check` 通过。
- 浏览器实际验证设置页存在 xAI 选项，选择后 provider/model/baseUrl 分别为 `xai`、`grok-4.5`、`https://api.x.ai/v1`；未保存设置、未调用付费模型。
