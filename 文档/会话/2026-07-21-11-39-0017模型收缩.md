# finding

- `0017_script_dual_flow_source_state` 新增 9 张表和 20 个约束触发器，均由已有剧本 B1～B5 或 AI/import pending 来源密封生产路径直接读写。
- 标准数据库中这些表当前为空只能说明尚无对应持久数据；fresh SQLite 仓储集成和 DB-only B1～B5 浏览器路径已证明能力可达。
- 旧 `script-import.util.ts` 及两层 `analyzeScriptImport/importScriptToChapters` 没有控制器、对话、迁移或恢复调用，已由现行严格导入链完整替代。

# web_search


# goal

核对 0017 的 9 张表、20 个触发器及相关兼容代码，删除已完成且无生产读写价值的最小安全集合；不得破坏当前 DB-only 双流程。

# approach

先建立逐表生产读写和用户路径证据矩阵，再从无调用、无恢复责任、无契约责任的兼容代码中实施删除；数据库对象仅在确认整个能力退役时删除。

# question


# todo

- [x] 完成 0017 表、触发器和生产调用矩阵
- [x] 确定并删除最小安全死代码集合
- [x] 完成契约、服务端和用户路径回归
- [x] 完成静态与运行复核并同步事实源

# result

- 保留 0017 的 9 张表与 20 个 trigger，不修改 Schema 或已部署 migration。
- 删除旧启发式整本导入的 465 行生产代码和 212 行测试，Server TypeScript 降至 77,683 行。
- 聚焦 contract/repository/capability 9/9、DB 主集成 42/42、Shared 38/38、全量 766 项均有通过证据，DB-only B1～B5 Chromium 1/1 通过。
