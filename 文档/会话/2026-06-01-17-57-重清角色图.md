# finding

- 当前任务队列为空，没有运行中的图片任务。
- 当前项目 `b224e22c-9cdf-4195-851c-6bae207789c8` 下有 5 张角色预览图，均位于 `assets/characters/*/visual-v001/preview.webp`。
- 需要同步清理 `shared/assets.json` 和 `shared/characters.json`，否则页面会残留图片引用。

# web_search


# goal

清除当前已生成的角色预览图，让用户重新按产品流程生成角色图。

# approach

删除项目角色图片目录下的图片文件；删除角色图片资产记录；重置角色的图片引用、确认状态、定稿引用和视觉版本，保留角色文本信息。

# question


# todo

- [x] 检查任务队列和现有角色图
- [x] 删除角色图文件并重置引用
- [x] 验证清理结果

# result

- 已清理项目 `b224e22c-9cdf-4195-851c-6bae207789c8` 下 5 张角色预览图。
- 已从 `shared/assets.json` 删除 5 条角色图片资产记录。
- 已重置 5 个角色的图片引用、预览确认、定稿引用和 `visualVersion`，保留角色文本信息。
- 已触发后端开发进程重载，并通过 workbench API 验证：`assets` 为 0，所有角色图片引用为空。
