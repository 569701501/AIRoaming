# finding

- 已有剧本 B1～B5 已提交为 `18cdceb`；剩余明确增强项是长稿分层、后台恢复和失败项重试。
- 0017 import batch 状态足以复用，不需要新增 ChapterPlan 或数据库表。
- 长稿分片必须使用隔离模型 session，否则同一对话历史会再次累积到上下文上限。
- 空闲 Worker 不轮询 SQLite；启动恢复一次，目录确认/重试主动唤醒，批次内自动排空。

# web_search


# goal

完成超长稿分层分析、后台可恢复导入批次和失败章节单独重试，保持既有 B1～B5 用户语义与页面字段。

# approach

- 长稿按稳定 block 分块，使用同一严格分析契约做叶子分析和分层合并。
- 专用 worker 读取 0017 queued item，服务重启时恢复中断项；目录确认快速返回。
- 页面轮询批次投影，失败项单独重试。

# question


# todo

- [x] 长稿分层分析
- [x] 后台批次和恢复
- [x] 查询/重试 API 与 UI
- [x] 自动测试、复核、文档和提交

# result

- 已完成长稿分层、目录确认快速返回、后台恢复、页面轮询和失败章重试。
- Shared 153/153、Server 594/594；DB-only B 路线、A 路线和 SQLite 锁竞争回归均通过。
- Skill、产品、架构、模块、测试、完成记录和双 Review 已同步。
