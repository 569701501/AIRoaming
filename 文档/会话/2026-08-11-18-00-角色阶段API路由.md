# finding

- 本次任务：为 `CharacterStageService`（已实现，2026-08-11 16:50 完成）注册 5 个 HTTP 路由，属于进行中任务 `文档/05_执行与记录/任务记录/2026-08-11_角色图生成与阶段管理/`。
- 定妆接口（Task 4）已注册 2 个路由：`POST :projectId/characters/:characterId/anchor-candidates`、`POST :projectId/characters/:characterId/confirm-anchor`（projects.controller.ts:169-183，经 ProjectsService 门面委托，ADR-0005 模式）。
- 响应统一走 `ok()` 信封：`{ success: true, data: ... }`（apps/server/src/http.ts）。
- 项目无 class-validator / ValidationPipe，校验是手动模式（服务内抛 BadRequestException + 大写错误码）。
- ID 格式不统一：projectId=uuid、characterId=`char_<uuid>`、stageId=uuid、chapterId 可能是 `projectId_chapter_NNN`。字符集均为 `[A-Za-z0-9_-]`，不能按纯 UUID 校验。
- `CharacterStageService` 当前 4 个方法（getCharacterStages/updateCharacterStage/deleteCharacterStage/regenerateStagePreview）未按 projectId/characterId 作用域校验——跨项目/跨角色可凭 stageId 操作，需要收紧以满足"错误处理完整"验收。
- ProjectsService 已注入 characterRef 等子服务；CharacterStageService 依赖 ProjectStore/ImageProviderService，无循环依赖风险，可直接注入。
- `@airoaming/shared` 走 dist（main: ./dist/index.js），改 DTO 后必须 `pnpm --filter @airoaming/shared build`。
- 本机 `~/.airoaming/data/db/airoaming.sqlite` 存在（DB-only 标准入口），可做真实运行验证。
- Node 20 无 `node:sqlite`：backup/migration 相关 spec 是预存在环境失败（与本任务无关）。

# web_search

无。

# goal

- projects.controller.ts 新增 5 路由：POST/GET `:projectId/characters/:characterId/stages`、PATCH/DELETE/`POST .../stages/:stageId/regenerate`。
- projects.service.ts 新增 5 个门面委托方法（ADR-0005）。
- 参数校验：projectId/characterId/stageId 格式校验 + Body 手动校验（visualDelta 必填非空、可选字段类型）。
- 错误处理完整：角色不存在（PROJECT_CHARACTER_NOT_FOUND）、阶段不存在（CHARACTER_STAGE_NOT_FOUND）、跨项目/跨角色越权访问 404。
- 验收：typecheck + 单元测试全绿；真实 DB 模式启动服务，5 个路由 + 错误路径全部可访问。

# todo

- [ ] 新增 shared DTO `UpdateCharacterStageRequest`
- [ ] 新增 id-validation.util.ts（路由 ID 格式 + Body 校验）+ spec
- [ ] character-stage.service.ts 4 个方法按 projectId/characterId 作用域收紧 + spec 更新
- [ ] projects.service.ts 门面委托 5 方法
- [ ] projects.controller.ts 注册 5 路由
- [ ] typecheck + 单测 + 真实 DB 模式 curl 验证
- [ ] 文档留痕（progress.md、会话记忆、MEMORY.md）
