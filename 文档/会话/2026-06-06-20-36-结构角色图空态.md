# finding

- 用户反馈：剧情结构已经生成，但右侧“本章角色图”显示“本章角色还没有进入角色库”，不符合“按当前章角色匹配角色库，已有图拿进来，没图自动生成”的口径。
- 当前项目 `1f1a57ce-8d37-47f9-97f6-bf407f993208` 后端数据正常：`structure.json` 有 8 个本章角色，`shared/characters.json` 也有 8 个项目角色；其中 4 个已有 `preview_front` 角色图，1 个生成失败，1 个正在生成，2 个排队。
- `GET /api/projects/{projectId}/workbench` 返回的 `snapshot.characters/assets` 也正常，说明后端同步角色和自动生图任务已生效。
- 前端问题点：左侧对话确认剧情结构走 `dialogue.tool_result.created -> applyToolResultChapterUpdate`，只调用 `applyStoryStructureUpdate`，没有刷新 `snapshot.characters/assets/tasks`；右侧按钮确认结构此前已经会 `refreshActiveProjectRuntime()`。

# web_search


# goal

修复左侧对话确认剧情结构后，右侧角色图片列表仍显示空态的问题。

# approach

- 不改后端同步逻辑，因为后端和 workbench API 已正确返回角色和资产。
- 在前端处理 `confirm_story_structure` 工具结果时，应用结构更新后刷新当前项目运行态，让角色库、资产和任务队列同步到当前页面。

# question


# todo

- [x] 对照当前项目 `structure.json`、`characters.json`、`assets.json`
- [x] 对照 workbench API 返回
- [x] 定位左侧对话工具结果路径缺少刷新
- [x] 修复前端 store
- [x] 运行前端类型检查、构建和 diff 检查

# result

已修复 `apps/web/src/stores/workbench-store.ts`：当 `confirm_story_structure` 工具结果成功并带回 `storyStructure` 时，前端在 `applyStoryStructureUpdate` 后调用 `refreshActiveProjectRuntime()`。后续用户通过左侧对话确认剧情结构，也会刷新角色库、角色图资产和任务队列，不再停留在旧快照空态。

验证：`corepack pnpm --filter @airoaming/web typecheck`、`corepack pnpm --filter @airoaming/web build`、`git diff --check` 通过。web build 仅有 Vite chunk 体积提示。
