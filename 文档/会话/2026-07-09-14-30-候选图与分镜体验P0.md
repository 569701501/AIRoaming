# finding

## 候选图与分镜体验 P0（深思熟虑执行记录）

### 候选图现状（后端已就绪，前端缺 UI）
- 后端 image-candidate.service.ts：生成是 push（追加 L181），不覆盖旧候选
- 5 态枚举已定义：generated/selected/locked/rejected/superseded（dto.ts L1065）
- candidate.taskId 已落盘 —— 天然批次标识，无需新字段
- prompt 拼在 store 私有函数 buildCandidatePositivePrompt，从不暴露
- 候选只存 promptDigest（sha1 摘要），不存原文
- 候选结果更新依赖 runtime 整快照轮询（refreshActiveProjectRuntime L388）

### 分镜现状
- StoryboardWorkspace.vue 完全不读 snapshot.candidates，无缩略图
- 镜头列表普通 v-for，无拖拽；删除时已有 map(order=index+1) 重排范式（L350）
- order 字段存在但弱；后端按 order 排序但不重排值
- 无单镜头重写链路（P1）
- 无批量重编号、无 reorder API（整体 PATCH 可复用）
- 分镜改动会让候选图失效（storyboard.service.ts L238-241）

### 拖拽实现：原生 HTML5 而非 vuedraggable
- vuedraggable 因 pnpm store 版本冲突（v11 vs v3）装不上
- 改用原生 drag/drop：dragIndex/dragOverIndex ref + onDragStart/Over/Drop/End
- 线性列表够用，零依赖、零回滚风险

# goal

P0 6 项任务全部完成：候选批次分组、5态视觉、prompt可见、分镜缩略预览、拖拽重排+重编号、批量生成。

# todo

- [x] 阶段1-任务A：候选批次语义（按 taskId 分组）
- [x] 阶段1-任务B：候选5态UI（批次折叠+状态色+digest角标）
- [x] 阶段1-任务C：prompt可见（提取纯函数+预览折叠区）
- [x] 阶段2-任务D：分镜缩略预览
- [x] 阶段2-任务E：拖拽重排+批量重编号（原生HTML5）
- [x] 阶段2-任务F：批量生成全章未锁镜头
- [x] typecheck 三包 + server 48 test 全绿
- [x] 文档：task_plan/progress/findings + 完成记录
- [ ] 候选状态持久化API（后置）
- [ ] 运行时人工复核（用户验证）
