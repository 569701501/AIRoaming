# finding

- 标准本地实例为 `http://localhost:5173`，后端为 `4310`；浏览器中的“阿肃”角色图生成窗口可正常提交。
- 提交后任务队列由 0 变为 1，角色卡显示“生成中”；约十余秒后队列回到 0。
- 完成后角色卡出现可点击的“阿肃 角色图”、重新生成入口和“定稿”状态；打开预览可见真实角色图片。

# web_search


# goal

在用户明确授权一次真实付费调用后，通过已打开页面生成一张阿肃角色图，并确认任务、图片显示与页面状态是否成功。

# approach

仅提交一张角色图；观察任务队列、角色卡状态和最终图片预览，不追加生成其他角色图或三视图。

# question


# todo

- [x] 提交阿肃角色图生成
- [x] 等待任务结束
- [x] 核验角色图实际可见
- [x] 保留页面结果供用户查看

# result

- 真实角色图生成成功。
- 任务队列归零，页面无失败提示；阿肃角色图已经显示并可打开预览。
- 本次只触发 1 次图片生成，没有生成三视图或其他角色图。
