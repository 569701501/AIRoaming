# finding

本次任务是 `structure-story-parse` 阶段，只生成当前章节剧情结构 JSON。剧情结构是分镜上游输入，不是章节剧本正文或分镜稿；角色卡和场景卡为本章结构卡。

# web_search

未使用网络搜索。

# goal

为《全职猎人：暗黑大陆篇》第 1 章《黑鲸启航》生成当前章节剧情结构 JSON。必须包含 `synopsis`、`direction`、`characters`、`scenes`、`beats`、`notes`；角色卡必须含 `level` 和 `entityType`，不得输出 `projectCharacterId`；节拍粒度粗于分镜，不输出镜头语言或图片 Prompt。

# todo

- [x] 读取项目记忆与剧情结构边界
- [x] 生成当前章节剧情结构 JSON
