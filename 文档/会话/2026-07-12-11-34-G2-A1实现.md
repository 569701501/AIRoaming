# finding

- G1 正式迁移树原来严格只允许 0001–0008；A1 新增 0009 后，G1 tree/ledger 仅对已知 0009 做兼容过滤，未知额外项仍拒绝。
- 0009 不新增表/列，fresh deploy 后正式对象为 2 个 partial unique index + 14 个 `trg_g2_*` trigger；TEMP preflight guard 已清理。
- Server 已有 PrismaService、G1 source-set seal 和 G1 Preflight JSON scope；A1 只补 G2 关系门禁，不复制 digest/长 sourceProjection 校验。

# web_search

# goal

完成 G2-A1 数据库 Overlay / Repository substrate，给后续 B/C1/D1 提供可执行的 migration、ledger、事务重试、错误映射和 Chapter 版本查询基座；不宣称 G2 总体完成。

# approach

- 手写固定路径 `0009_g2_version_freshness_overlay/migration.sql`，用 direct contract 检查对象数量、fresh deploy、无 schema 漂移和 guard 清理。
- 保留 G1 八迁移历史不变；增加独立 G2 九行 ledger helper，A1 不把完整 G2 capability 混入旧 API。
- 事务 runner 只做完整 `$transaction` 重试；错误 mapper 将 P2002/AIR_G2/条件更新失败转稳定领域码；query repository 一次读取 Chapter 四层指针和 history。

# question

- 完整 Script/Story/Storyboard/Preflight command 与 G2 ledger capability 何时接入，由后续阶段根据 B/C1/D1 的 runtime evidence 决定。

# todo

- [x] 0009 overlay 与 direct contract
- [x] G2 ledger、transaction runner、error mapper、query/repository contracts
- [x] fresh migration、typecheck、full regression
- [x] scrutiny/handoff/completion docs

# result

- Shared 34/34、Server 171/171 测试通过；全仓 typecheck、G1 manifest check、git diff --check 通过。
- 已生成 A1 task plan/progress/findings/scrutiny/handoff 和功能完成记录；下一步进入 G2-B1 Script repository。
