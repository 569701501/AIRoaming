# finding

- 当前对话框已实现左侧消息 UI、快捷建议、OpenCode 流式输出和模型选择透传链路，但模型选择控件未展示，附件、停止生成、应用/插入、章节作用域线程和对话持久化未实现。
- 当前后端对话线程仍按 `projectId + stepKey` 存在进程内 Map，prompt 已读取 `WorkbenchSnapshot.currentChapter`，但只看已保存章节正文。
- `ProjectWorkflow` 已接入，可作为后续对话框 placeholder、快捷建议、阶段上下文和按钮状态的主输入。
- 用户已确认采用该方向，并要求写入正式文档。
- Aurora 可参考点应分层迁移：先迁移 workflow 驱动提示、当前文档上下文、可打开/定位文档、临时附件上下文和初始 prompt payload；暂缓迁移 show_file 动态标签、完整附件处理、agent 直接写文件和复杂多工具闭环。

# web_search


# goal

使用 `$deep-think` 讨论项目对话框下一步设计，重点评估 AuroraPlatformWeb 的哪些对话/灵感文档交互点值得 AI漫游参考；本轮只沟通方案，不写业务代码。

# approach

先对齐当前事实：AI漫游已有 Chapter、ProjectWorkflow、左侧公共对话框、OpenCode 流式输出，但还没有应用/插入闭环和章节作用域线程。再基于 Aurora 调研拆分可借鉴点、暂缓点和不适合点。

# question


# todo

- [x] 读取当前对话与 workflow 相关文档/代码
- [x] 梳理 Aurora 可参考点
- [x] 给出 AI漫游对话框分阶段建议
- [x] 记录本次沟通结论

# result

本轮只做设计沟通和文档收口，不改业务代码。已将确认方向写入 `文档/04_方案与决策/2026-05-27_剧本对话功能再设计方案.md`，并把状态改为 `active`。最终方向：对话框先参考 Aurora 的“workflow 驱动对话语境”和“左侧对话推动、右侧文档承载事实”，但保持 AI漫游约束：AI 回复先进入前端未保存草稿，保存仍由章节保存接口完成。下一步优先做 M1：按 `projectId + stepKey + chapterId` 隔离线程、发送时带当前编辑器未保存草稿、按 workflow 展示快捷建议、恢复模型选择、给 assistant 回复增加复制/插入光标/追加末尾动作。
