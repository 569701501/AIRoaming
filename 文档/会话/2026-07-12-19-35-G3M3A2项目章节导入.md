# finding

- M3-A1 已提交 `053c74f`，Prisma ledger 和 `db:audit` 已可用。
- A2 在同一 ledger port 上实现 Project/Chapter shadow slice；目标 ID 由 sourceKey 摘要生成，业务实体写入单个全局 shadow transaction。
- decisions artifact 在 Project INSERT 前校验；未决议项目不插入，其他项目可以继续 shadow。
- A2 已通过 44 文件/239 测试、typecheck、G1 三项门禁和 diff check；`db:import --kind final` 保持 fail-closed。
- A3 已开始：`ScriptOutlineShadowImporter` 复用 A2 目标实体，导入 `script-outline.md/json` 与 `script.versions/script-vNNN.md`，更新 current 指针并遵守 G2 rowVersion/working-copy 约束。
- A3 已通过 44 文件/240 测试、typecheck、G1 三项门禁、final fail-closed CLI 和 diff check，待提交后转入 pending/revision 与 Story/Storyboard 后续切片。
- A4 已开始：`ScriptPendingRevisionShadowImporter` 导入 `script-pending.json` 与 `script.revisions/latest.json`；未导入 Dialogue 时不写入旧 FK，原引用只保留在来源证据。
- A4 已通过 44 文件/241 测试、typecheck、G1 三项门禁、final fail-closed CLI 和 diff check，待提交后进入 Story/Storyboard importer。
- A4 已提交 `1e121d2`。
- A5 已实现 `StoryShadowImporter` 与 `--slice story`：structure.json → StoryVersion + Scene/Beat projections；source resolved 时按 pending→projection→confirmed→current，source unresolved 时写 blocker 且不插入伪 confirmed。
- A5 集成测试覆盖 resolved/current/replay 与 unresolved blocker 两项；Storyboard/Shot 仍是下一切片。
- A6 已实现 `StoryboardShadowImporter` 与 `--slice storyboard`：storyboard.json → StoryboardVersion + Shot/Projection；source 绑定 current Story，角色 token 非空但未导入时拒绝。
- A6 集成链路和 server 全量 44 files/244 tests 通过，待提交；后续是 Character/Asset/Visual、Preflight 及 verifier 等切片。
- A7 已实现并提交 `b337c88`：`CharacterShadowImporter` 与 `--slice characters` 导入 shared/characters.json，Story V2 旧角色 ID 同步映射到稳定 Character target，视觉指针保持空。
- A8 已实现 `AssetShadowImporter` 与 `--slice assets`：shared/assets.json → Asset 元数据；无物理文件证据时保持 staged，不创建 CharacterVisual/SceneVisual。
- A8 集成测试已通过 11 项（A2-A8 链路），全量回归、G1 门禁通过并提交为 `0bf84d6`。
- A9 已实现 `AssetVisualShadowImporter` 与 `--slice asset-visuals --workspace-root`：校验物理文件 sha256/bytes/MIME/图片尺寸，安全落盘并 promote ready，建立 CharacterVisual/SceneVisual/current 指针。
- A9 定向集成已通过（2 个 ready Asset、1 个 CharacterVisual、1 个 SceneVisual、replay 零新增），全量回归、G1 门禁通过并提交为 `58d84eb`。
- A10 已实现 `PreflightShadowImporter` 与 `--slice preflight`：只接受与当前 Storyboard documentDigest 匹配的 V2 source snapshot；旧 V1/ID-only 或来源不完整记录 `PREFLIGHT_SOURCE_UNRESOLVED` blocker，不写伪造 PreflightRevision。A10 定向 2 项、全量 server 44 files/249 tests、typecheck、G1 三项门禁通过，已提交。
- A11A 已实现 `TaskShadowImporter` 与 `--slice tasks`：完整 input/output 任务导入 `legacy_imported`，缺 output 导入不可执行 `legacy_stub`，固定不 claim/retry，A11A 定向 1 项、全量 server 44 files/250 tests、typecheck、G1 三项门禁通过，当前待提交。
- A11B 已实现 `CandidateShadowImporter` 与 `--slice candidates`：验证 Candidate 的 Shot/Task/Asset 同 scope，旧 selected/locked 转为 generated 历史，不创建 current CandidateLockRevision；A11B 定向 1 项、全量 server 44 files/251 tests、typecheck、G1 三项门禁通过，已提交。
- A11C 已实现 `CandidateLockShadowImporter` 与 `--slice candidate-locks`：从 sealed snapshot 原始 storyboard 的 `lockedCandidateId` 恢复 `legacy_import` lock revision，写入后更新 Shot current pointer；仅 Candidate.status=locked 不触发锁定，replay 不重复。定向测试、typecheck、全量 44 文件/252 tests、G1 三项门禁和 diff check 均通过。
- 当前基线已切到 `0c3295b`：release Schema identity 与 G1 package closure 收窄已完成；M4 verifier 已改用 release identity，保持 `in_progress`，不重复修改。
- A12 已实现 `LayoutShadowImporter` 与 `--slice layout`：旧 `layout/layout.json` 进入 `LayoutWorkingCopy` 的 `legacy_chapter_layout_v1` envelope；完整来源保存 lock-set digest，来源不足只标 unresolved，不设置 currentLayout。A12 定向测试、typecheck、全量 45 文件/256 tests、G1 三项门禁和 diff check 均通过。
- A12 已提交 `9e79deb`。A13 已实现 `ExportShadowImporter` 与 `--slice exports`：旧 exports 按章节/目录恢复 `ExportRevision` 历史，manifest 保存规范化 digest，统一 `legacy_unresolved`，不创建 ExportArtifact/current；A13 定向测试、typecheck、全量 45 文件/257 tests、G1 三项门禁和 diff check 均通过，待提交。
- A14 已实现 `ProviderShadowImporter` 与 `--slice providers`：从脱敏 `settings.redacted.json` 恢复 ProviderConfig、CredentialMetadata、AppPreference 非秘密元数据；ProviderConfig 保持 disabled，CredentialMetadata 保持 unconfigured/configured=false/secretRef=null/fingerprint=null。A14 定向测试、typecheck、全量 45 文件/258 tests、G1 三项门禁和 diff check 均通过，待提交；Dialogue runtime bundle 仍是 M0 不可观察骨架。
- A15 已实现 `DialogueShadowImporter` 与 `--slice dialogue`：maintenance 支持对话 runtime provider；runtime bundle 明确 `captured=true` 时导入 ConversationThread/Message/DialogueToolResult 和 closed DialogueRuntimeSession，assistant running 收敛 failed；deferred bundle 不创建对话实体。A15 定向测试、typecheck、全量 45 文件/259 tests、G1 三项门禁和 diff check 均通过，待提交；pending Dialogue artifact/read-model 仍后置。

# web_search


# goal

完成 G3-M3-A2，并继续推进 G3-M3-A3/A4/A5：从 sealed snapshot 导入 Project/Chapter、Script/Outline、pending/revision 和 Story 结构，记录 ImportedEntitySource，提供按 slice 选择的 `db:import --kind shadow`。

# approach

先解析并验证全部 Project/Chapter 计划，再在一个 Prisma transaction 中写入可导入项目；决议缺失只形成 blocker 并跳过该项目，数据约束失败则整批回滚并将 run 标为 failed。

# question


# todo

- [x] 实现 Project/Chapter 计划解析与稳定 ID
- [x] 实现 decision_required / resolved / blocked 分支
- [x] 接入 ImportedEntitySource 与 shadow CLI
- [x] 完成 A2 全量验证、文档复核与提交
- [x] 完成 A3 Script/Outline 验证与文档复核
- [x] 提交 A3 并开始下一切片
- [x] 开始 A4 pending/revision 导入
- [ ] 完成 A4 验证、文档复核与提交
- [x] 完成 A4 验证与文档复核
- [x] 提交 A4 并开始下一切片
- [x] 实现 A5 Story shadow importer
- [x] 实现 A6 Storyboard/Shot importer
- [x] 实现 A7 Character importer
- [x] 实现 A8 Asset 元数据前置导入
- [x] 实现 A9 物理文件证据与 CharacterVisual/SceneVisual 导入
- [x] 补齐 Preflight shadow 导入与来源 blocker
- [x] 补齐 Task 历史 legacy_imported/legacy_stub 导入
- [x] 补齐 Candidate 历史元数据与三方 scope 校验
- [x] 补齐 CandidateLock/Layout/Export 等后续实体
- [x] 实现 CandidateLock 直接证据导入与 replay 测试
- [ ] 完成 A11C 提交
- [ ] 继续 M3 verifier/backup/activate
- [x] 补齐 A12 LayoutWorkingCopy shadow 导入
- [x] 补齐旧导出证据导入
- [x] 补齐 Provider/settings 元数据导入
- [x] 补齐 captured Dialogue runtime 导入
- [ ] 完成 M4 双 fresh shadow/API/Asset 等价验收（保持 in_progress）

# result

A2 已完成验证、文档复核并提交为 `36203c8`；A3 已完成代码、验证、交接并提交为 `5d8e9d2`；A4 已完成代码、验证、交接并提交为 `1e121d2`；A5 Story 已提交为 `fb6e9d4`；A6 Storyboard 已提交为 `1a579c7`；A7 Character 已提交为 `b337c88`；A8 Asset 元数据导入已通过全量门禁并提交为 `5d26fa5`；A9 物理资产与 Visual 导入已通过全量门禁并提交为 `b203647`；A10 Preflight blocker/ready 导入已完成验证并提交；A11A Task 历史导入已提交；A11B Candidate 历史导入已提交；A11C CandidateLock 直接证据导入已提交；A12 LayoutWorkingCopy 导入已提交为 `9e79deb`；A13 旧导出证据导入已提交为 `ca5c449`；A14 Provider/settings 元数据导入已提交为 `47c7680`；A15 captured Dialogue runtime 导入已完成验证，待提交。M4 仍保持 `in_progress`。
