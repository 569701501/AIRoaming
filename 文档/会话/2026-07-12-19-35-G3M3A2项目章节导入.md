# finding

- M3-A1 已提交 `053c74f`，Prisma ledger 和 `db:audit` 已可用。
- A2 在同一 ledger port 上实现 Project/Chapter shadow slice；目标 ID 由 sourceKey 摘要生成，业务实体写入单个全局 shadow transaction。
- decisions artifact 在 Project INSERT 前校验；未决议项目不插入，其他项目可以继续 shadow。
- A2 已通过 44 文件/239 测试、typecheck、G1 三项门禁和 diff check；`db:import --kind final` 保持 fail-closed。
- A3 已开始：`ScriptOutlineShadowImporter` 复用 A2 目标实体，导入 `script-outline.md/json` 与 `script.versions/script-vNNN.md`，更新 current 指针并遵守 G2 rowVersion/working-copy 约束。
- A3 已通过 44 文件/240 测试、typecheck、G1 三项门禁、final fail-closed CLI 和 diff check，待提交后转入 pending/revision 与 Story/Storyboard 后续切片。
- A4 已开始：`ScriptPendingRevisionShadowImporter` 导入 `script-pending.json` 与 `script.revisions/latest.json`；未导入 Dialogue 时不写入旧 FK，原引用只保留在来源证据。
- A4 已通过 44 文件/241 测试、typecheck、G1 三项门禁、final fail-closed CLI 和 diff check，待提交后进入 Story/Storyboard importer。

# web_search


# goal

完成 G3-M3-A2，并继续推进 G3-M3-A3：从 sealed snapshot 导入 Project/Chapter 基础实体、项目大纲和章节 ScriptVersion，记录 ImportedEntitySource，提供按 slice 选择的 `db:import --kind shadow`。

# approach

先解析并验证全部 Project/Chapter 计划，再在一个 Prisma transaction 中写入可导入项目；决议缺失只形成 blocker 并跳过该项目，数据约束失败则整批回滚并将 run 标为 failed。

# question


# todo

- [x] 实现 Project/Chapter 计划解析与稳定 ID
- [x] 实现 decision_required / resolved / blocked 分支
- [x] 接入 ImportedEntitySource 与 shadow CLI
- [x] 完成 A2 全量验证、文档复核与提交
- [x] 完成 A3 Script/Outline 验证与文档复核
- [x] 提交 A3 并开始下一切片
- [x] 开始 A4 pending/revision 导入
- [ ] 完成 A4 验证、文档复核与提交
- [x] 完成 A4 验证与文档复核
- [x] 提交 A4 并开始下一切片

# result

A2 已完成验证、文档复核并提交；A3 已完成代码、验证、交接并提交为 `5d8e9d2`；A4 已完成代码、验证、交接并提交为 `352248e`。
