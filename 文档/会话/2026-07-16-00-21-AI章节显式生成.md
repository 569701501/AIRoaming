# finding

- 双流程共享输出契约与来源状态基础已完成；AI 路线尚未接入真实对话显式生成链路。
- 生产对话仍使用旧三区块大纲、宽松包装解析和 legacy pending；已完成的 Shared 严格契约与 0017 AI 来源密封尚未被真实消费。
- 现有界面只显示待确认稿前 200 字，并保留“继续下一章”；需改为现有正文区完整只读查看，且完成后只留“进入本章剧情结构”。

# web_search


# goal

实现 AI 创作路线当前章节的显式生成链路：只有用户在当前章对话中明确要求生成时才触发，正确读取章节卡、前章正式正文和项目级上下文，产生可采用或丢弃的 AI pending；切换章节绝不自动生成，页面现有内容字段不变。

# approach

- 先核对对话意图、Skill、工具、运行时与 Working Copy 既有边界。
- 复用已完成的 `createAiChapterPending` 来源密封能力，不另建第二套 pending。
- 以后端显式意图和来源校验作为真实门禁，不依赖页面切换事件。

# question


# todo

- [x] 核对事实源与当前对话/生成链路
- [x] 冻结显式生成的输入、输出、阻断与来源契约
- [x] 实现 Skill/Prompt、对话编排与仓储接线
- [x] 补单元、集成与用户路径验证
- [x] 完成文档、复核与记忆
- [x] 独立提交

# result

- AI 创作 A2～A5 已接入真实生产链：严格灵感、严格大纲/章节卡、明确单章生成、密封来源、完整查看、采用/丢弃、Working Copy 和完成正式版本。
- 大纲确认本身不生成章节；裸“继续”、章节切换和批量生成均不会创建 AI pending。
- 完成本章停留当前章，只按确认大纲下一张章节卡增加下拉入口；用户切换后仍需在对话框明确生成。
- 页面沿用原有大纲、章节正文和 StoryStructure 内容字段，没有新增 ChapterPlan 或常驻字段面板。
- DB-only 浏览器用户路径通过；全量 Server 仅有一个无关 5 秒超时波动，目标用例单独重跑通过；全部静态门禁通过。
- 已有剧本 B1～B5 的生产接线仍为下一实施包。
