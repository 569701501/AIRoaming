# finding

- 当前已有独立 QA-only 的分镜 Beat 语义评测器，已在两套 V2.3 真实分镜上重复运行，但样本题材和缺陷类型仍偏少。
- 生产分镜 Prompt 已回滚并冻结在 V2.3；本任务不继续修改生产 Prompt，而是先扩充固定评测样例和重复运行能力。
- 当前仓库没有分镜语义评测固定样例集，也没有针对多样例串行重复运行与一致性汇总的入口。

# web_search


# goal

建立覆盖多题材、不同 Beat 密度和四种语义状态的固定分镜语义评测样例集，并提供串行重复运行与一致性汇总，判断现有评测方法在不同缺陷类型上的稳定性；不影响生产页面、Prompt、Schema、DTO、数据库或媒体服务。

# approach

- 使用 `$deep-think` 建立正式任务记录、分阶段实施和双重复核。
- 固定样例保存 StoryStructure、Storyboard 与人工预期，不依赖临时目录或业务数据库。
- 先做严格样例契约和确定性测试，再做真实文本模型重复评测。
- 批量运行保持 QA-only、串行、结果写入调用方指定文件。

# question


# todo

- [x] 冻结固定样例集契约与覆盖矩阵
- [x] 实现严格样例加载、比较和汇总
- [x] 增加批量串行重复评测入口
- [x] 完成真实文本模型复测
- [x] 完成全量确定性验证
- [x] 完成双 Review、文档、记忆和提交

# result

完成 5 类题材、14 Beat、四态覆盖的分镜语义固定样例集，并新增严格加载、人工预期比较、重复稳定汇总和串行批量 CLI。真实 `self/gpt-5.5` 全集已完成两轮：有效报告预期一致 98.1%、重复稳定 95.8%；动作样例定向两轮重试为 100%，唯一语义波动在抽象关系状态。生产 Prompt 继续保持 V2.3，页面、Schema、DTO、数据库、确认流程和媒体服务均未改变。Server 定向测试、类型和构建通过；全仓并发唯一无关备份测试固定 5 秒超时，隔离 1/1 和完整文件 40/40 通过，已正式记录为既有测试稳定性债。
