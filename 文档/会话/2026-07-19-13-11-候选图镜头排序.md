# finding

- 已在项目 `project_4f1daf...` 的候选图工作台稳定复现：页面顺序为 `6,1,7,5,12,8,2,9,11,3,10,4`。
- 同一次 `/workbench` 响应中，`snapshot.storyboard.storyboardJson.shots` 正确保持 `1..12`，但 `snapshot.shots` 已变成上述乱序；问题发生在服务端工作台投影，不是正式分镜数据损坏，也不是 CSS 视觉重排。
- `CandidateLockRepository.workbench()` 查询活动 Shot 时使用 `orderBy: { id: "asc" }`；Shot ID 是稳定身份而非叙事顺序。随后 `toWorkbenchShot()` 虽从正式 Storyboard 文档恢复出正确 `shot.order`，但数组没有再按该字段排序。
- `ProjectsService.getWorkbenchSnapshot()` 在 DB 模式用 Candidate workbench 的 shots 覆盖顶层 `snapshot.shots`；`ImageCandidatesWorkspace` 又直接按该数组 `v-for`，因此 UUID 字典序原样出现在页面。

# web_search


# goal

定位候选图工作台“本章镜头”列表乱序的根因；以真实页面和数据链路为证据，确认正确排序事实源与受影响范围。

# approach

按页面复现、接口数据核对、前端派生排序与后端查询顺序逐层缩小问题。

# question


# todo

- [x] 在真实候选图工作台复现乱序并记录实际顺序
- [x] 核对正式 Storyboard Shot.order、接口返回顺序和前端渲染顺序
- [x] 定位根因并提出可验证的修复方案
- [x] 实施服务端根修复与回归测试

# result

已完成根修复：`CandidateLockRepository.workbench()` 在补齐正式镜头内容后按 `Shot.order` 升序返回，未在前端掩盖服务端错误。新增真实 SQLite 回归用例，强制正式镜头顺序与 Shot ID 字典序相反，修复前稳定失败、修复后通过。数据库完整链路 41 项、候选图相关 8 项和服务端类型检查通过；真实项目接口及浏览器页面均确认镜头按 `1..12` 展示，未调用图片生成。
