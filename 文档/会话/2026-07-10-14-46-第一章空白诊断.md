# finding

- 真实项目 `3c91668b-03db-4022-a9cd-5b130205c14f` 的第一章页面空白不是前端渲染问题：`chapters/chapter-001/script.md` 为 0 字节，`chapter.json.sourceText` 为空，workbench API 也返回空；页面控制台无 warning/error。
- 完整正文仍在 `chapters/chapter-001/script.versions/script-v001.md`，大小 19,205 字节，SHA-256 为 `9e993ca43a5cb5057c3bf90a9646a1575cbecbe944b5b2adbb9d57afe84c27f3`。
- `chapter.json.currentScriptVersionId = chapter_001_script_v001`，`structure.json.sourceScriptVersionId` 也绑定同一版本，确认该版本是第一章正式历史正文，可用于恢复。
- `ProjectRepository.readChapterFromWorkspace` 先读取 `script.md`，空文件仍被当成有效 sourceText；读取 scriptVersions 后没有在“非 draft + current version 非空”时回退，因此接口和页面继续显示空。
- 当前状态保留 scriptVersions、currentScriptVersionId、lastScriptRevision、story structure 和 storyboard，只把 sourceText 变空；该形态与 `saveChapterDraft` 覆盖最接近。其非空校验发生在 `stripChapterScriptName` 之前，标准化后没有二次校验，存在原始输入非空但清洗后为空的写入路径。
- 缺少历史 HTTP 写入日志，无法证明当时是哪次请求触发；不能把该推断写成已证实的唯一触发操作。

# web_search


# goal

恢复第一章完整正文，保持结构、分镜与候选图不变，并修复版本加载自愈和规范化后空覆盖缺口。

# todo

- [x] 对比磁盘正式文件、chapter metadata、workbench API 与页面状态
- [x] 搜索 script versions、revision 与剧情结构绑定证据
- [x] 定位加载器空文件不回退当前版本的缺口
- [x] 排查可能产生“保留版本但 sourceText 为空”的写入路径
- [x] 用户确认后先备份现状并恢复第一章正式正文
- [x] 红绿修复标准化后二次非空校验和版本自愈加载
- [x] 复核第一章页面、结构/分镜绑定与全量测试

# result

- 恢复前归档保存于 `workspace/recovery-backups/2026-07-10-150041-chapter-001-before-recovery.tar.gz`。
- 第一章 `script.md` 已从当前版本恢复为 19,205 字节，SHA-256 与 `script-v001.md` 同为 `9e993ca43a5cb5057c3bf90a9646a1575cbecbe944b5b2adbb9d57afe84c27f3`。
- 真实页面显示第 1 章正文与 `6885 字`；结构、分镜版本引用和 23 条候选记录保持不变。
- 新增仓储自愈测试 1 条、章节写入防空测试 2 条；shared 15 项、server 64 项测试通过，三包类型检查和构建通过。
- 无历史 HTTP 写入日志，原始触发请求仍无法唯一证明；代码缺口已经按可复现行为封堵。
