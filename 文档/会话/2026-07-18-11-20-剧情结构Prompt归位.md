# finding

- 当前正式剧情结构字段与页面展示已经稳定，本轮不改页面字段、数据库结构、确认方式或下游分镜接口。
- 对话路径已有完整的剧情结构生成、固定质量校验与一次定向修复，但创作规则仍硬编码在 `dialogue-prompt.util.ts`。
- 后台 `story_parse` 任务仍使用一套简化 Prompt，直接要求模型生成 `StoryDocumentV2` 和数据库关联字段，未复用对话路径的质量门与修复链。
- `StoryVersionRepository` 已支持 `unresolved-story-character:` 占位引用，可由后端在正式落库时解析或创建项目角色，因此 AI 无需生成 `projectCharacterId`。

# web_search


# goal

把剧情结构生成规则迁入 `apps/server/opencodeAI/skills/structure-story-parse`，并让对话路径与后台 `story_parse` 任务共用同一套 Skill Prompt、质量检查和一次修复逻辑；保持现有页面字段与用户确认流程不变。

# approach

- Skill 只负责从精确的已确认章节剧本中提取语义结构，不生成数据库 ID。
- 对话路径继续产出待确认预览；后台任务把同一语义输出转换为现有 `StoryDocumentV2`，由后端补齐本地 ID 和待解析角色引用。
- 项目大纲只作背景，不能覆盖章节正文事实；固定章节场景必须一一对应，并由现有质量检查器验证。

# question


# todo

- [x] 创建并验证 `structure-story-parse` Skill。
- [x] 统一对话与后台任务的 Prompt 装配入口。
- [x] 补齐语义结构到 `StoryDocumentV2` 的后端转换和测试。
- [x] 完成全量验证、正式文档、复核和长期记忆更新。

# result

- 剧情结构 Prompt 已归位新 Skill，对话与后台持久任务共用同一生产方法和修复链。
- 页面字段、确认流程、Schema 和任务协议保持不变；AI 不生成系统 ID。
- 目标回归、类型检查、构建和 Skill 校验通过；全量并发的两个历史 5 秒超时项隔离复跑通过。
- 未调用真实模型或付费媒体服务。
