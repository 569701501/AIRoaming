# finding

- 文档已定义 `Chapter` 是项目内一等工作单元，完成本章应写入章节剧本版本、推进 `script_done`，并创建或进入下一章。
- 代码已存在 `WorkbenchSnapshot.chapters/currentChapter`、章节保存/完成 DTO，但后端 API、前端章节列表和章节路由尚未实现。
- 当前工作台路由以 `/projects/:projectId/:step` 表达步骤位置，需要新增 `/projects/:projectId/script/:chapterId` 表达章节定位。

# web_search


# goal

实现剧本页前端章节列表、`/projects/:projectId/script/:chapterId` 路由、保存当前章节草稿、完成本章并进入下一章。

# approach

沿用既有章节事实源：`Chapter` 是项目内一等工作单元，`WorkbenchSnapshot.chapters/currentChapter` 是当前章节读取主契约。后端补章节列表、读取、保存草稿、完成本章 API；前端用章节列表切换当前章节，编辑器保存走章节草稿接口，完成本章推进状态并导航到活跃章节。

# question


# todo

- [x] 梳理章节路由/API/组件现状
- [x] 实现章节 API 与前端章节列表
- [x] 接入章节路由、保存草稿、完成本章
- [x] 同步文档记忆并验证

# result

已实现章节列表、`/projects/:projectId/script/:chapterId`、章节级保存草稿和完成本章。验证通过：`corepack pnpm typecheck`、`corepack pnpm --filter @airoaming/web build`、`git diff --check`、临时服务真实 API 验证和 Vite 首页响应检查。本次未做浏览器截图级视觉验收。
