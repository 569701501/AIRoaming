# finding

- A/B 双流程与长稿后台重试已在提交 `5820a7b` 完成。
- 正式契约的下一项是 P6：多文件跨边界、最终 JSON 极限和重启中断集成回归。
- 本轮不改产品流程、页面字段、数据库结构或 StoryStructure。

# web_search


# goal

补齐三个 P6 固定回归并完成聚焦、全量、类型、构建、静态复核和留痕。

# approach

- 模型边界：多文件跨章节范围、最终合并连续无效时 fail-closed。
- 状态边界：真实临时 SQLite，关闭并重建 Nest 应用上下文后恢复同一章节。

# question


# todo

- [x] 多文件跨边界测试
- [x] 最终 JSON 截断测试
- [x] 新应用实例中断恢复集成测试
- [x] 全量验证、双 Review、文档
- [x] 提交

# result

- 三项 P6 回归完成，不需要修改生产代码。
- 聚焦 6/6、Shared 153/153、Server 单 fork 597/597、typecheck 与 build 通过。
- 静态复核通过；运行复核使用真实 SQLite 与新应用实例，未冒充 OS kill 或浏览器证据。
