# finding

- 用户明确要求只操作 Codex 自带的隔离浏览器，不操作用户 Chrome。
- 正式 DB-only 服务当前运行在 `http://localhost:4310`，前端运行在 `http://127.0.0.1:5173`。
- 删除前项目列表只有一个项目：`测试`，ID 为 `09065fa1-f414-42d3-bb04-cff9108f8aa6`。
- 本次最终覆盖 G0～G6：剧本、剧情结构、分镜、出图准备、候选图、排版出版和素材包。
- 工作区已有 7 张旧验收截图的未提交改动，本任务不得覆盖或混入。
- 新项目第 1 阶段已完成真实文本生成并形成正式剧本；重复点击“完成本章”会额外生成相同 ScriptVersion，确认为幂等与按钮状态双重缺陷。
- 新项目 `雨夜末班车·真实验收` 已从剧本连续推进到素材包；正式章节为 `雨夜点名`，含 11 个分镜镜头。
- DB Asset 投影缺少 `createdAt`，导致场景重生成后前端无法稳定选中最新参考图；已补充公开排序时间并让迁移语义测试忽略该展示字段。
- 相同来源的 preflight 重复确认原会新增相同 Revision；已改为重放，页面 freshness 改读正式 workflow 状态。
- Runware 旧 FLUX.1 + IP Adapter 路径触发提示词长度和 Adapter 装载失败；候选图已迁到 FLUX.2 Dev 原生多参考图输入，最多 4 张，真实 11 镜批量生成通过。
- DB-only 素材包迁移漏掉剧本、结构、分镜、preflight、候选索引、角色索引和正式出版图；已从数据库正式版本与 layout_publication 重建完整包，并修正第 7 阶段提前显示完成的问题。

# web_search


# goal

- 用 Codex 隔离浏览器删除旧项目，新建一个竖向条漫项目，从第 1 阶段推进到 G0～G6 的真实页面闭环。
- 允许调用真实文本模型与真实图片 Provider；至少完成一次真实图片生成，并核对任务、Asset、来源和最终出版物。
- 以人类测试员方式反复点击、测试门禁和异常提示；发现阻断问题时定位、修复并回到页面复验。

# approach

- 先建立删除前基线，再用页面执行删除与新建。
- 每一步同时核对 UI、正式版本/状态、任务与文件证据；pending 不能当作 current。
- 正常路径之外至少覆盖创建表单、等待步骤、出图准备阻断、候选定稿门禁和排版预检。
- 完成后执行静态复核、运行复核并形成完成记录。

# question


# todo

- [x] 删除唯一旧项目并确认项目列表为空
- [x] 新建竖向条漫项目并完成正式剧本
- [x] 生成、检查并确认剧情结构
- [x] 生成、检查并确认分镜
- [x] 处理出图准备阻断并确认 preflight
- [x] 真实生成候选图并逐镜定稿
- [x] 完成排版、预检与出版，核对产物
- [x] 导出并逐文件核对完整素材包
- [x] 修复发现的问题并复验
- [x] 完成静态复核、运行复核和文档收口

# result


- 旧项目已从用户可见项目库和 workspace 删除；最终 DB purge 顺序 bug 已修复并有回归保护。修复前留下的不可见 deleting 壳记录未绕过 trigger 强行处理，保留安全备份并作为已接受残留。
- 新项目 `雨夜末班车·真实验收` 已完成七阶段真实页面闭环；Script、Story、Storyboard、Preflight、11 个 CandidateLock、LayoutRevision、LayoutPublication 与 AssetPackage 均有正式数据库来源。
- 已修复重复剧本发布、最新参考图排序、重复 preflight、Runware 候选多参考路径、DB 素材包缺失内容和素材包阶段提前完成共 6 项问题。
- 正式出版物为 1080×21120 长图加 3 张切片；新素材包 `pkg_1d287311` 共 25 个唯一文件，含 11 张锁定候选图、正式出版图、核心 JSON/Markdown 和角色索引，manifest 与磁盘逐项一致。
- 真实视觉质量仍有明显问题：Runware 对中文漫画提示的画风、场景和人物一致性不稳定，部分画面留白、日式偏移、伪文字或多余人脸；流程可用不等于视觉质量已达发布标准。
- 完整后端套件首轮 752/757；3 个并发 5 秒超时与 2 个迁移语义差异均已串行复测通过。前后端类型检查和 Web 生产构建通过，浏览器 console 无 error/warn。
