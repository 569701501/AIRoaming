# finding

- 用户授权并要求按无排期 Handoff 连续执行 C5～C7、首笔 DB-only 写、R2 OBS-01～10，观察通过后继续 G4/G5；不授权删除 backup/archive、down migration、G6/视频链路。
- C5/C6/C7、首写与 file guard 已通过，C7 evidence=`sha256:987d9a9466c220544ea010b6d74ead34971b3b2eb1188388bb3a4ba66c6a1452`。
- OBS-06 根因是项目 purge 的 SQLite `ON DELETE SET NULL` 隐式 Chapter 更新被 G2 rowVersion trigger 拒绝。新增 0011 三事实守卫和显式 pointer teardown 后，真实项目 purge 通过。
- OBS-07 原 coordinated backup 只支持 shadow。新增 `db-only-coordinated`，分离当前 release schema identity 与历史 cutover lineage identity；sealed backup、verify-only、fresh materialize 与当前应用读回通过。
- OBS-08 发现两个真实读取缺口：迁移 Asset 应按 DB `storageKey` 读取；Workbench chapterId 在 DB 模式应是纯读取而不是修改 current pointer。两项均有失败回归、修复和真实目标复核。
- R2 最终目标/备份/恢复 DB digest 均为 `sha256:cab0b96d88dc24a7e87925aea6bc04441d0f8db0e76fac5537ce4ab64c49d739`；1 项目、2 章节、67/67 Asset 可读，integrity=`ok`、FK=0。
- OBS-09 只修改 archive 副本，原 archive/目标 DB/运行态不变。OBS-10 检查 427 文件、4 SQLite，秘密命中 0，symlink 0。
- R2 修复提交：`62da892`、`0be5621`、`7ddeb21`、`a90f546`。最终 DB 回归 36/36、全仓 typecheck、server build 通过；此前 server 全量 493/493 通过。
- G4-A 已由 `79dc806` 完成：Shared CandidateLock 闭集/DTO/parser、0012 线性 revision/CAS overlay、12 段发布 ledger、legacy direct-evidence importer 与旧 Candidate `selected/locked` runtime 权威清理。
- G4-A 验证：Shared 46/46，G4 overlay/runtime/DB persistence 46/46，legacy/full/final importer 76/76，类型/构建/Prisma/G1 检查通过。全量 502 项中 3 项只在并行重负载下触发局部 5 秒 timeout，空闲定向复跑 3/3 通过。
- G4-A Review：Scrutiny=`passed`，Runtime=`passed_isolated`；真实用户路径待 G4-C～F，当前进入 `G4_B_IN_PROGRESS`。
- G4-B 已由 `9cd599a` 完成：纯状态机/replay、严格 lock set codec、known-answer digest、Working Copy dependency projector、Layout/Export freshness、统一 impact resolver/digest。
- G4-B 验证：定向 Shared 8/8、Server 22/22；完整回归 Shared 54/54、Server 519/524，5 个旧慢测隔离复跑 10/10；Shared/Server typecheck/build 通过。
- G4-B Review：Scrutiny=`passed`，Runtime=`passed_isolated`。
- G4-C 已由 `179be50` 完成：DB-only preview/commit/history/favorite/reject/restore/complete、事务内 replay/impact/CAS、双 writer 与旧 Server lock 权威入口删除。
- G4-C 验证：fresh SQLite/真实 HTTP 定向通过；Shared 54/54；Server 520/525，5 个并行超时旧用例隔离 5/5；类型、构建、Prisma validate 通过。
- G4-C Review：Scrutiny=`passed`，Runtime=`passed_isolated`；当前进入 `G4_D_IN_PROGRESS`。
- G4-D 已由 `894d1e8` 完成：Workbench/ProductionState source summary、工作流 needs_update、stale/unresolved/digest 下游事务门禁、迟到任务 historical fence、新 Candidate 隔离与 restart。
- G4-D 验证：定向 16/16、G4-B 纯规则 22/22、Shared 54/54；Server 全量检查点 14 个旧慢测并行 timeout 隔离 56/56，写 owner 清单遗漏修复后 3/3；类型/构建/Prisma/G1 检查通过。
- G4-D Review：Scrutiny=`passed`，Runtime=`passed_isolated`。
- G4-E 已由 `3826611` 完成：DB Workbench 权威刷新、Web 两阶段定稿/更换/clear、收藏/废弃/历史、409 重新 preview 不自动 commit、排版 stale 摘要；Server 533/533、Shared 54/54、DB-only Playwright 1/1；Scrutiny=`passed`，Runtime=`passed`。
- 当前进入 `G4_F_IN_PROGRESS`，继续 migration、完整 E2E、restart/backup restore 与总体 G4 Review。

# web_search


# goal

完成 R2 DB-only 观察与双 Review，并按总 Handoff 继续 G4/G5；不设置日期排期，不越过用户禁止边界。

# approach

- 每个 blocker 先复现、补失败回归、做最小兼容实现、跑定向/全量门禁，再在真实目标上复核。
- cutover evidence 保持绑定历史 release；新修复使用独立 clean compatible release，不重签旧 AUTH/evidence。
- R2 通过后同步唯一入口、矩阵、架构契约和长期记忆；G4-A～E 已关闭，按 TDD/诊断循环进入 G4-F migration/E2E/总体复核。

# question


# todo

- [x] 完成 C5/C6/C7、首写和 file guard。
- [x] 完成 OBS-01～10。
- [x] 完成 R2 Scrutiny 与 Runtime/User Review。
- [x] 同步 R2 当前入口、验收矩阵、架构契约和长期记忆。
- [x] 完成 G4-A Shared + Schema overlay。
- [x] 完成 G4-B。
- [x] 完成 G4-C。
- [x] 完成 G4-D。
- [x] 完成 G4-E。
- [ ] 完成 G4-F。
- [ ] 完成 G5-M0～M8 并停在用户签收。

# result

R2 已达到 `DB_ONLY_OBSERVATION_PASSED`，没有剩余切换 blocker。G4-A～E 已通过并提交 `79dc806`、`9cd599a`、`179be50`、`894d1e8`、`3826611`；backup/archive 保留，未执行 down migration，未回退 file-only，未进入 G6/视频链路。当前进入 G4-F。
