# finding

- 角色定妆任务属于进行中任务 `文档/05_执行与记录/任务记录/2026-08-11_角色图生成与阶段管理/`（阶段 4：交互式定妆）。
- 前置事实：`ProjectCharacter.anchorAssetId?: string` 已定义在 `packages/shared/src/dto.ts`；`Character.anchorAssetId` 已入 Prisma schema（migration 0021）；`GenerateAnchorCandidatesRequest`/`ConfirmAnchorRequest` DTO 已存在；`CharacterStageService`（DB 模式专用）已实现并注册，参考图优先级 = 上一阶段图 > anchorAssetId > primaryReference > preview。
- `project-repository.service.ts` 的 `databaseCharacterToLocal` 原本不映射 `anchorAssetId`（DB 读取会丢字段），本次补上。
- `ImageProviderService.generateImage` 原本不支持 seed；Runware 请求体支持 `seed` 字段，本次透传（其他 provider 不传，依赖其随机采样保证候选差异）。
- 文件模式并发写 project.json 会竞争：3 张候选先生成 buffer，再统一落盘一次。
- 候选资产 meta 用 `kind=anchor_candidate` + `characterId` + `seed`；`normalizeCharacterReferenceKind` 对未知值返回 "none"，候选不会被当成可用参考图。
- 错误码约定：角色不存在 `CHARACTER_NOT_FOUND`、资产不存在/不属于角色 `ASSET_NOT_FOUND`（本任务要求；角色库其他接口用 `PROJECT_CHARACTER_NOT_FOUND`）。
- Node 20 无 `node:sqlite`，13 个 backup/migration 相关 spec 是预存在环境失败（296 个可运行测试全过）。

# web_search

无。

# goal

- 在 CharacterReferenceService 新增 `generateAnchorCandidates(projectId, characterId, input)`（count 默认 3、customPrompt 可选；prompt = customPrompt ?? appearance ?? 角色名+角色定位；并发出图不同 seed）与 `confirmAnchor(projectId, characterId, assetId)`（校验归属 → 写 anchorAssetId → 刷新缓存 → 返回角色）。
- 错误处理：角色不存在 CHARACTER_NOT_FOUND；资产不存在或不属于角色 ASSET_NOT_FOUND；DB 模式走事务 + refreshProjectFromDatabase。
- 验收：3 张图不同 seed；anchorAssetId 正确落库；旧项目 anchorAssetId 为 null 降级。

# todo

- [x] 阅读任务文档与相关代码（dto、schema、repository、image-provider、stage service）
- [x] character-reference.service.ts 新增两个方法 + 私有辅助（file/DB 双模式）
- [x] project-repository.service.ts 映射 anchorAssetId
- [x] image-provider.service.ts generateImage 支持可选 seed（runware 透传）
- [x] projects.service.ts 门面委托 + projects.controller.ts 两个端点（anchor-candidates / confirm-anchor）
- [x] 新增 character-reference.service.spec.ts（13 个用例，file/DB 双模式）
- [x] typecheck + 相关测试全绿（296 个可运行测试通过；13 个 node:sqlite 环境失败为预存在）
- [x] 文档：核心数据模型.md、progress.md、会话记忆、MEMORY.md

任务完成：实现角色定妆接口。改动文件：character-reference.service.ts（核心）、project-repository.service.ts、image-provider.service.ts、projects.service.ts、projects.controller.ts、新增 character-reference.service.spec.ts。风险：runware 之外 provider 不显式传 seed（依赖 provider 随机采样）；定妆候选未接 Web UI（后续阶段）；候选资产无独立清理逻辑（未选中候选长期保留在项目资产中）。
