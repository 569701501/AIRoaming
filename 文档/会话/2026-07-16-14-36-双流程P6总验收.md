# finding

- A+ 两条路线、P1～P5 和导入首组 P6 已完成；正式契约明确后续不再扩张页面、批量生成或新字段，只做评测、Prompt 迭代和体验修正。
- 七个模型阶段已经有严格正反输出 fixture，但公开触发测试分散：当前专门覆盖 A4、A5、B3 确认，缺少把 A2、A3、B1/B2 与非模型阶段一起串成一张固定触发矩阵。
- 外部调研文档的 P6 仍写“4 个 Skill 全部”，当前正式体系实际为 5 个公开 Skill、7 个模型阶段，需要纠正。

# web_search


# goal

完成 AI 创作与已有剧本两条路线的 P6 最终一致性验收：锁住 5 个公开 Skill / 7 个模型阶段的正反触发、输出契约和非模型确认边界，复跑两条 DB-only 用户路径，不新增产品能力。

# approach

- 新增一份集中式 P6 契约测试，只复用现有意图函数和 Shared 严格 fixture，不新建第二套 Schema。
- 核对 Skill、动态 Prompt、运行编排和严格 parser 的一一对应关系；只修真实不一致。
- 复跑 AI 创作与已有剧本 DB-only Chromium 路径，完成静态和运行双复核。

# question


# todo

- [x] 建立 P6 五 Skill / 七阶段触发矩阵
- [x] 修正文档中的 Skill 数量与最终验收口径
- [x] 完成聚焦、类型、构建和两条 DB-only 用户路径
- [x] 双 Review、完成记录、长期记忆和提交

# result

- 五个公开 Skill、七个模型阶段、正反触发和 Prompt 禁止项已形成固定 P6 回归。
- 修复章节页轮询覆盖新 OpenCode 会话号的 DB-only 并发竞态，新增先红后绿回归。
- Server 643/643、AI 路径重复 5/5、双路线 2/2、类型/构建/Skill 校验全绿。
- 页面字段、StoryStructure、数据库 Schema 和用户流程均未改变。
