# finding

- G0～G5 已有正式完成记录，终态为 `G0_G5_COMPLETE`。
- 路线图和部分产品/UI文档仍保留实现前的旧状态，需要以现有代码、完成记录和验收文档为证据逐项复核，不能把 G6 素材包或轻量视频混入本轮。
- 当前工作区存在用户已有的文档与截图修改；本任务只修改明确属于本轮复核记录和确认过期的事实源，不覆盖无关改动。
- P1 文档审计已完成：产品、UI、模块、路线图和 G1～G4 验收总状态已同步到 `G0_G5_COMPLETE`；旧 G3-M 阻塞交接已标为 superseded。
- G0～G5 顺序回归完成：Shared 115/115，Server 单 worker 568/568，file Chromium 4/4，G2 DB-only 2/2，G4 Chromium 1/1，G5 DB-only 8/8，render/migration/typecheck/build/Prisma 均通过。
- 默认并发 Server 全量有两条 backup/restore 用例在固定 5 秒边界超时；隔离复跑和单 worker 全量通过，记录为测试稳定性观察，不判定为功能失败。

# web_search


# goal

复核并写清 G0～G5 文档进度，然后按 G0→G5 顺序执行测试并留证；素材包、轻量视频、删除 backup/archive、down migration 均不在本轮范围。

# approach

- 先以代码、测试入口、阶段完成记录和验收清单交叉核对文档。
- 只修正有充分证据的过期状态，不把可选增强项误写成 G0～G5 必须项。
- 测试按阶段顺序执行；每一阶段记录命令、结果和失败处理，不按日期排期。

# question


# todo

- [x] 建立本次任务三件套并完成文档事实审计。
- [x] 修正 G0～G5 进度文档。
- [x] 顺序执行 G0～G5 测试。
- [x] 完成静态复核、运行复核和最终留痕。

# result

已完成。G0～G5 文档状态已与事实源对齐，顺序回归通过；G6 素材包、G7 和轻量视频继续延期。本轮没有修改业务代码、真实业务数据库或 backup/archive，也没有执行 down migration。
