# finding

- 当前剧本对话已经接入 OpenCode 流式输出，后端 prompt 会读取已保存的 `WorkbenchSnapshot.currentChapter`。
- 当前对话线程 key 是 `projectId + stepKey`，章节工作流下会让不同章节共享同一条剧本对话。
- 前端 store/API 有模型列表读取和发送透传链路，但 `ProjectDialoguePanel.vue` 模板当前没有渲染可见模型选择控件。
- 当前未实现停止生成、上传、应用到剧本、插入光标、结构化 suggestion 和对话持久化。
- 右侧编辑器当前未保存草稿不会随对话请求传给后端，AI 只看见已保存章节正文。

# web_search


# goal

梳理当前“剧本对话”的实际功能状态，并重新设计它在章节剧本工作流中的产品边界、上下文、用户动作和后续开发步骤。

# approach

使用 `$deep-think`：先读取事实源和代码，还原当前能力；再区分现状、缺口、设计原则和阶段方案；必要的稳定结论写入 `文档/`。

已形成设计方向：剧本步骤对话应先升级为 `projectId + stepKey + chapterId` 作用域；请求携带当前编辑器草稿和可选选中文本；AI 回复先提供复制、插入光标、追加末尾等安全动作，应用只改前端未保存草稿，保存仍由章节保存接口完成。

# question


# todo

- [x] 读取事实源和当前代码
- [x] 梳理当前剧本对话功能
- [x] 形成设计建议
- [x] 更新任务记录/方案文档
- [x] 执行文档静态校验

# result

已新增 `文档/04_方案与决策/2026-05-27_剧本对话功能再设计方案.md`，并同步更新相关索引、产品、架构和模块文档。验证：`git diff --check` 通过；关键词检查确认新方案、章节作用域和模型选择控件差距已被事实源覆盖。
