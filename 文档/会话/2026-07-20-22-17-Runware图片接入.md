# finding

- 当前设置页已有 OpenAI、豆包、Grok 三套图片 provider，前端只读取配置状态/指纹，后端通过 `SecretStore` 保存图片密钥。
- 图片网关位于 `apps/server/src/projects/image-provider.service.ts`；候选图会先经过 `candidate_reference_plan_v1`，禁止静默遗漏必需角色/场景参考。
- 工作区已有未提交的 Grok 默认 1K 改动，本任务保留并避免覆盖。
- Runware 官方 REST 入口为 `https://api.runware.ai/v1`，请求体是任务数组；FLUX.1 Schnell 模型 ID 为 `runware:100@1`，适合低成本文生图；FLUX.2 Dev `runware:400@1` 使用 `referenceImages` 做图片精修；FLUX.1 Dev `runware:101@1` 支持 FLUX IP-Adapter。
- Runware 账号已登录；已有旧 `airoaming` key 只显示掩码，已创建新的 `airoaming-local-20260720` 专用 key，并通过设置页保存到本机 Keychain。明文未进入仓库、文档、普通设置或 API 响应。
- 实际保存时发现 macOS `security -w` 需要 TTY，旧执行器会挂起；现已改为 Expect 私有 TTY + fd 3 传密，真实 Keychain roundtrip 和设置页保存均通过。

# web_search

- 搜索主题：Runware 认证、FLUX.1 Schnell、image-to-image、IP-Adapter。
- 关键结论：Header 使用 `Authorization: Bearer <API_KEY>`；REST 请求为 `POST https://api.runware.ai/v1`；Schnell=`runware:100@1`，约 `$0.0013/1024x1024`；FLUX.2 Dev=`runware:400@1`，单图编辑使用 `referenceImages`；带多参考的候选图需使用支持 IP-Adapter 的模型，FLUX.1 Dev=`runware:101@1`，允许 `runware:56@1..4`。
- 来源：Runware Authentication、FLUX.1 Schnell API、FLUX.2 Dev API/Examples、FLUX.1 Dev API、IP Adapters 官方文档。

# goal

- 设置页新增 Runware 图片生成选项，可安全保存/切换 Runware key。
- 无参考图片生成默认使用 FLUX.1 Schnell；单图精修使用 FLUX.2 Dev `referenceImages`；多参考候选图完整传入 FLUX.1 Dev + IP-Adapter，不违反必需参考覆盖契约。
- 不进行真实付费生图；通过 mock 请求合同、类型检查、构建和真实设置页完成运行验收。

# approach

- 复用现有 `ProviderConfig + CredentialMetadata + SecretStore`，不新增数据库表。
- `modelId` 继续表示 Runware 无参考草稿模型，默认 `runware:100@1`；带参考候选图使用固定兼容管线 `runware:101@1 + runware:56@1`，在设置页说明而不是误导为同一模型。
- 本次不新增 inpainting/LoRA/ControlNet UI；只为当前已有 `generateImage/editImage/generateCandidateImage` 三个入口提供真实 Runware 适配。

# question


# todo

- [x] 探索设置、密钥和图片生成链路
- [x] 核对 Runware 官方协议并创建专用 key
- [x] 实现 Shared/设置页/后端 Runware provider
- [x] 补请求合同与密钥安全测试
- [x] 把 key 写入本地 SecretStore 并切换 provider
- [x] 完成静态复核、真实设置页复核和文档留痕

# result

Runware 已作为第四个图片 Provider 完整接入并设为当前 provider。设置页真实保存成功，密钥只进入本机 Keychain；无参考草稿走 Schnell，单图精修走 FLUX.2 Dev，多参考候选走 FLUX.1 Dev + IP-Adapter。Shared 167/167、Server 822/822、类型检查和构建全部通过，本轮付费图片调用为 0。inpainting/FLUX Fill、LoRA/ControlNet 留待独立任务；FLUX.2 Dev 商用前需复核当时有效许可。
