# finding

- 指定 Working Copy 只读预览可正常打开，11/11 张图片均加载成功，镜头 sourceShotId 顺序与正式分镜 1～11 一致。
- 正式分镜共有 8 个对白镜头、16 条 voice line、3 个旁白镜头；当前成稿只有 1 个气泡和 1 个 SFX，`automation.dialogueBindings=[]`。
- 唯一气泡“是谁……在雨里？”的 `sourceShotId`、`speakerCharacterId` 均为空，且该文本不存在于正式分镜；它不是正式对白的可靠投影。
- 第 6～9 格及第 11 格可见站牌、工牌、手机等 AI 乱码；第 2、3、4、7、8、11 格与正式分镜关键动作或场景证据存在明显偏差。
- 首格气泡内文字被压成约 135px 宽、229px 高的窄列，气泡位于双人脸部之间且尾巴归属不清。
- 浏览器 fullPage 长截图会重复拼接首屏，是截图工具伪影；DOM 实际是 11 个不同 canvas 和 11 个不同 asset，并非页面重复渲染。

# web_search


# goal

以真实读者视角核对用户指定的漫画只读预览是否内容正确、可读且可作为成稿。

# approach

- 逐格查看 11 张实际锁定素材。
- 对照正式 Storyboard 的 dialogue、caption、coreAction 与当前 Layout Working Copy。
- 只诊断和评分，不修改测试数据或成稿。

# question


# todo

- [x] 打开指定只读预览
- [x] 核对 11 张图片和页面结构
- [x] 对照正式分镜对白、旁白与动作
- [x] 给出严重度结论

# result

结论：当前页面技术展示正常，但内容不是正确漫画成稿。主要阻断是正式对白/旁白几乎全部漏排、唯一气泡无来源绑定且改写剧情，以及多格 AI 乱码和镜头语义偏差。未修改代码或数据。
