# finding

正在盘点项目当前完成度。已确认项目有完整的 `文档/` 事实源、长期记忆、功能完成记录与任务记录；当前产品主链路是「剧本 -> 剧情结构 -> 分镜工作台 -> 出图准备 -> 候选图工作台 -> 排版导出 -> 素材包」。

`文档/01_愿景与产品/MVP范围与路线图.md` 将 MVP 拆为 M1「章节剧本到分镜」、M2「分镜到漫画图候选」、M3「漫画排版与导出」。代码侧 `ProjectWorkbenchView.vue` 已有真实组件：剧本、剧情结构、项目角色库、分镜、出图准备；候选图、排版导出、素材包仍进入统一 `step-placeholder`。`projects.controller.ts` 已提供项目、章节、角色、资产文件、剧情结构、分镜、出图准备 API；未看到候选图锁定、漫画排版导出、素材包导出的业务 API。共享契约已定义完整 7 步 workflow 和相关任务类型。

本地 workspace 存在样例项目 `workspace/projects/6e0b5e54-8289-4adb-8367-f10f81f8317b`，其 `workflow.json` 显示当前第 1 章已完成剧本、剧情结构、分镜，当前 active 步骤为 `image_preflight`；候选图、排版导出、素材包为 waiting。样例项目已有 `script.md`、`structure.json`、`storyboard.json` 和多张角色参考图资产，但没有 `preflight.json`、候选图、排版导出物。

# web_search


# goal

回答用户“项目做到什么程度了”，基于文档、代码结构和可运行性检查给出当前完成度、已完成模块、未完成模块、主要风险和下一步建议。

# approach

先读项目事实源与最近完成记录，再对照源码入口、路由、API、共享契约和构建结果，最后给出分阶段进度判断。

# question


# todo

- [x] 读取长期记忆和文档入口
- [x] 读取产品路线、架构契约、模块梳理和最近完成记录
- [x] 对照代码入口、路由、API 与关键组件
- [x] 运行基础验证命令
- [x] 汇总完成度、风险和下一步

# result

截至 2026-06-23，项目处于「M1 基本完成，M2 前半段进行中」：项目库、设置、项目工作区、章节剧本、AI 对话、剧本导入/灵感/大纲/章节生成、剧情结构、角色库、角色图任务、分镜工作台、出图准备检查单均已有实现和落盘契约；候选图工作台、候选图选择锁定、漫画排版导出、素材包仍未形成真实业务闭环。验证命令 `corepack pnpm typecheck` 和 `corepack pnpm build` 均通过，build 仅有 Vite chunk 大小警告。
