# finding

- 当前工作区存在一组未提交的 OpenCode/剧情结构改动；本次审计只读代码与运行数据，不触碰这些文件。
- SQLite DB-only 切换已经完成：标准数据库处于 `db_only`，17 个 migration 全部成功，`integrity_check=ok`，无外键违规。
- 当前是 53 张业务表、242 个 trigger；“194 个 trigger”只是 0008/G1 的历史基线。现有 trigger 中 239 个只拒绝非法写，3 个还承担任务 claim/heartbeat/finish 的跨表状态物化。
- 后端 89,426 行包含 26,514 行测试和 24,584 行不在正常启动静态依赖图中的迁移、生成、备份与 CLI；从 `main.ts` 静态可达的生产代码约 38,328 行。前端 `apps/web/src` 为 23,147 行。
- G1 Schema 生成器连测试共 12,360 行，不被正常服务运行时导入；发布身份已只依赖 SQLite、Prisma Schema 与有序 migration checksum。它是待满足稳定发布周期条件后退役的迁移脚手架，不应继续扩建。
- 0017 新增的 9 张双路径剧本来源/导入表当前全部为空，并带来 20 个 trigger，是继续投入前应优先复核的数据模型。
- 文档共 1,648 个 Markdown、127,776 行；至少两份仍标为 active 的概览文档已与当前 DB-only/持久任务事实冲突，存在维护成本和误导风险。

# web_search


# goal

核实“53 张表、194 个 trigger、后端 8.9 万行、前端 2.3 万行”的统计口径与形成原因，追踪 SQLite 迁移和 Schema 代码生成器现状，并判断现有 trigger 是否必要。强制验收：文档、代码、迁移 SQL、运行 SQLite 四方交叉取证；给出保留、合并/替代、删除候选和优先级，不直接修改生产实现。

# approach

先建立可复现统计口径，再追踪 Schema/trigger 的定义与生成链，最后按完整性、并发/事务、派生缓存、兼容迁移和重复防御分类审查。

# question


# todo

- [x] 读取事实源与历史迁移决策
- [x] 复算代码、表、trigger 规模
- [x] 核实 Schema 生成器与运行数据库一致性
- [x] 评估 trigger 的必要性和删减空间
- [x] 输出静态复核与最终结论

# result

完成只读审计，未修改代码、Schema、migration 或运行数据库。结论是 SQLite 迁移已成功，后端总行数主要被测试和未退役的迁移期工具放大；trigger 不能整体删除，但应先复核 0017 空模型，再按每批 5～10 个迁移重复约束，三个任务状态物化 trigger 最后处理。详细证据见对应任务记录和完成记录。
