# finding

- 实施包 1 已提供严格的 `import-analysis/1.0`、`import-fidelity/1.0`、章节 Markdown 与来源引用解析器，本任务必须直接复用，不能另建一套格式。
- 当前正式正文由不可变 `ChapterScriptVersion` 表达；`ChapterScriptPending` 仍只有一套结构，未区分 AI 草稿、导入草稿及其精确来源。
- 当前 `PendingDialogueArtifact` 是对话候选临时态，不能代替不可变原稿、已确认拆章目录和导入批次。
- `ChapterScriptPending` 当前采用或丢弃都可执行；新导入待确认稿必须改为只允许“确认章节”，不得进入 Working Copy 或丢弃。
- 0011 之后采用前向 migration + 小契约；本任务不修改已冻结的 G1 0001～0010 生成器。

# web_search


# goal

实施双流程第二包：建立不可变原稿与稳定 block 引用、导入分析候选、已确认拆章目录、导入批次/章节项、忠实度报告，以及 AI/导入两类 pending 的精确来源绑定；保持现有页面内容字段和下游 StoryStructure 不变。

# approach

- 原稿、分析、目录、批次、报告使用各自明确的领域产物，避免把导入分析塞进对话临时态。
- `ChapterScriptPending` 增加类型和密封来源集合；AI 与导入 pending 使用不同来源策略，但仍复用同一正文内容结构。
- 拆章目录整体确认后创建一个批次与全部章节项；本阶段只建设状态和仓储能力，Prompt/对话/UI 接线留给后续实施包。
- 导入 pending 只能由“整理完成 + 忠实度无硬问题”的批次项产生，并禁止采用或丢弃。

# question


# todo

- [x] 冻结领域模型、状态机和数据库约束
- [x] 实现 Shared 来源快照、目录与 pending 来源投影
- [x] 实现 0017 migration、Prisma 模型和仓储
- [x] 补状态 DTO、门禁与集成测试
- [x] 完成全量验证、双重复核和文档
- [x] 形成独立提交

# result

- 0017、9 个来源/状态模型、双流程仓储、两类 pending 来源密封、导入直接确认和项目协调删除已完成。
- 页面内容字段和 StoryStructure payload 未改变；生产 Prompt、对话工具和页面接线留给后续包。
- Shared 152/152、Server single-fork 573/573、typecheck/build/Prisma/G1 发布门均通过。
- Scrutiny=`passed`，Runtime/Repository=`passed_db_isolated`。
