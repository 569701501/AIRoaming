# 会话：UI 样式重构参考图对照梳理（2026-07-21 22:32）

# finding

- 参考图在 `文档/ui参考设计图/`，11 张，文件名对应页面；候选图工作台/角色库/导出素材包 3 张带「待定」最后做。
- 前端页面全部在 `apps/web/src/components/`：`projects/`（项目库、创建项目弹窗）、`settings/AppSettingsView.vue`、`workbench/`（剧本=ScriptDocumentEditor+ScriptChapterList+ProjectDialoguePanel、剧情结构、分镜、出图准备、候选图、排版导出 LayoutExportWorkspace 2772 行、素材包、角色库）、`layout/`（AppShell/TopBar/AppSidebar）+ `workbench/WorkbenchStageRail.vue`（7 阶段条）。
- 路由是 stub 模式：AppShell 按 route 渲染对应视图；项目路由内不显示 AppSidebar。
- 参考图是 AI 生成稿，功能/按钮有编造（如项目库统计卡、创作流程条与项目 7 阶段不符、单镜头「已确认」、排版页无 m6 控制中心）。结论：只取样式，功能以代码为准。
- 品牌名图上不统一（绘界漫画 vs AI 漫游），项目 TopBar 固定「绘界漫画」。
- 版式枚举仅 2 个：vertical_scroll 竖向条漫 / paged_comic 分页漫画（packages/shared/src/comic-format.ts）。
- 关联任务目录：`文档/05_执行与记录/任务记录/2026-07-21_全页面视觉改版/`（上轮视觉稿任务，status review）。
- 对照产出：`文档/05_执行与记录/任务记录/2026-07-21_全页面视觉改版/ui参考图对照梳理.md`（逐页 可参考/不可照抄/必须保留 三栏 + 实施顺序）。

# web_search

（无）

# goal

按 `文档/ui参考设计图/` 逐页重构前端样式；先完成对照梳理（本次），用户确认后按 壳→项目库→创建项目→设置→剧本→剧情结构→分镜→出图准备→排版导出→3 个待定页 的顺序实施。功能/字段/状态机不许随图改动。

# todo

- [x] 读完 11 张参考图
- [x] 读完对应页面组件并对照
- [x] 输出对照梳理文档
- [x] 二轮：用 output/ui-redesign/current/ 12 张真实截图逐元素复核并修正（候选图三卡字段真实对应、排版已有属性/图层tab、剧本 markdown 裸露、广播纯声音占位、出图准备 6 项检查）
- [x] 用户确认 5 条决策（候选三卡直接用/m6 收抽屉按 AI 建议/剧本保留编辑/纯声音占位采纳+英文 prompt 用户自改/出图准备卡片化待定）
- [x] 首轮生产实现：项目库、创建项目卡片单选、剧本标题渲染+中文技能卡+去ID、设置掩码、DB条弱化、分镜镜头卡、排版 m6 抽屉、角色库波形占位、候选三卡、素材包统计卡
- [x] 验证：typecheck 双端通过、server 757 tests 通过、11 页重截复核（output/ui-redesign/implemented/）
- [ ] 出图准备检查卡卡片化（用户待定）
- [ ] 角色库卡片化（大预览+三视图横条+本章相关/全部 tab，待定页）
- [ ] 素材包「查看技术清单」（待定新功能，复制保存位置已做）

## 夜间轮次（2026-07-22 01:10 完成）

计划：`夜间精修计划.md`（含 10 页差异清单）。已执行：
项目库真实进度条（顺带修复 styles-premium 旧规则 display:none 隐藏 .progress-row）、设置本机保存提示行、剧本徽章 pill+快捷胶囊恢复（真实 intent）、结构/分镜「进入下一步」按钮（goStoryboard/goPreflight → selectStep）、分镜新增镜头顶置、候选镜头行缩略图、角色库定稿已锁定/纯声音徽章、素材包复制保存位置。vue-tsc 过，11 页重截复核过。
注意：创建项目弹窗曾踩两个 CSS 坑——样式块误嵌 @media、`.format-thumb i` 特异性压背景；改 scoped 样式后必须实机截图复核。
- [ ] 残留：layout e2e 在本环境 G2_DB_MODE_REQUIRED(file) 失败为既有环境问题；server 需重启使对话去 ID 生效
