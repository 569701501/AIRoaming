# finding

- M5-A4 已完成并提交；当前进入 D2，不能跳到 M6。
- D2-A0 要求把 8 个聚合 capability 拆到每个 `assertDatabaseOperationSupported()` 调用点。
- 当前代码共有 36 个调用点，集中在 `ProjectRepository` 与 `ProjectsService`；现有 registry 只记录聚合项。
- 36 个调用点均为写门禁；`generation_task_create` 在 DB 模式走 G2 task guard，其余调用点没有对应的 DB 公开写证据。

# web_search

# goal

- 完成 D2-A0：建立操作级 capability registry、覆盖性测试和五份 Luna 可执行施工资料。
- 保持未证明公开写路径为 fail-closed，不实现 D2-A1 SecretStore 或后续 D2 切片。

# approach

- 为每个门禁调用登记 capability、owner、source symbol、读写状态和证据 ID。
- 用源码扫描测试确认 registry 覆盖全部 36 个调用点，避免新增门禁无记录。
- 聚合 capability 的激活阻断计算同时检查操作级写状态与证据。

# question

# todo

- [x] 扩展 capability registry 与 CLI 输出
- [x] 增加 36 个调用点的覆盖性测试
- [x] 编写并复核 D2-A0 五份施工资料
- [x] 运行 targeted/full 验证
- [x] 提交并更新长期记忆

# result

D2-A0 已完成实现和复核：操作级 registry 覆盖 36 个门禁调用点；targeted 5/5、server full 49 文件/341 测试、typecheck、CLI report/check、diff check 均通过。当前仍 fail-closed，下一步只能在本切片提交后进入 D2-A1。
