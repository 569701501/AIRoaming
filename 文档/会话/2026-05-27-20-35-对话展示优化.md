# finding

- 用户反馈：AI 对话输出内容会持续撑开页面高度，希望参考 AuroraPlatformWeb 对思考、技能和工具调用的展示方式。
- 当前 `ProjectDialoguePanel.vue` 直接把 assistant `content` 放进消息气泡，`dialogueNotice` 单独显示工具结果摘要；消息列表虽有 `overflow-y: auto`，但外层工作台缺少可靠的 `min-height: 0` / viewport 高度约束时仍可能被长内容撑开。
- 当前前端没有结构化展示 `dialogue.tool_result.created`，也没有将 skill/tool/thinking 类内容折叠。
- AuroraPlatformWeb 的 `AgentChat/ChatStream.vue` 会把 reasoning 和 tools 从正文中拆出来，放进可折叠过程区；工具卡片使用紧凑行、状态图标、展开详情，消息容器使用 `min-h-0`、`overflow-auto` 和局部最大高度约束。
- 本次实现将 `DialogueThread.toolResults` 作为共享契约，后端记录工具结果，前端按 `messageId` 挂到 assistant 消息；对话正文、思考提示、技能/工具卡片分离展示。

# web_search


# goal

修复对话面板长输出撑开页面问题，并参考 AuroraPlatformWeb 做结构化展示：普通回复可滚动，工具/skill 调用以紧凑事件块呈现，长内容折叠或限制高度。

# approach

先查 AuroraPlatformWeb 的消息组件和样式，再在 AI漫游中收敛为不引入复杂依赖的实现：固定工作台视口高度、消息区独立滚动、assistant 内容预格式化但限制宽高、工具结果进入线程 `toolResults` 并在对应 assistant 消息内以事件卡片展示。

# question


# todo

- [x] 阅读 AuroraPlatformWeb 对话消息/工具展示实现
- [x] 修复当前工作台和对话面板高度约束
- [x] 实现工具/skill/thinking 类事件块展示
- [x] 运行类型检查并做页面验证

# result

- 已完成对话过程展示与高度约束：`DialogueThread` 增加 `toolResults`；工具结果进入线程而不是临时 notice；前端展示思考行、技能/工具过程卡片和局部滚动正文；项目模式下 app shell/workbench/dialogue panel 都增加高度约束。
- 验证通过：shared build、server typecheck、web typecheck、web build、`git diff --check`；临时启动 `4321/5174` 做页面截图验证，截图保存到 `文档/06_测试与验收/截图/2026-05-27_对话过程展示.png`。
