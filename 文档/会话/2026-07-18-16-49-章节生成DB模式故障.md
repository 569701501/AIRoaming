# finding

- 用户在当前章节明确触发 `script-chapter-drafting` 后收到 `G2_DB_MODE_REQUIRED`。
- 章节生成 A4 会在模型调用前调用 `ScriptWorkflowSourceRepository.getAiChapterGenerationContext()`，该来源密封能力只允许 DB mode。
- 当前 4310 开发服务返回的项目列表只包含文件模式项目；根目录 `pnpm dev` 不设置 `AIROAMING_PERSISTENCE_MODE=db`，`PrismaService` 未设置时默认 `file`。
- 当前项目“测试”有已确认大纲 4040 字，第 1 章正文为空、无 pending、无正式版本；因此报错不是用户提示中的章节状态问题。
- 用户明确纠正数据库已经改造完成。复核确认 schema、0017 migrations 和 DB-only 路径确实已完成；问题是当前 shell/4310 服务未设置 mode 和 URL，普通启动没有接回既有 DB-only 运行配置。
- 之前完成验收的 `target-data/app.db` 属于隔离运行根，当前工作根没有该文件或连接配置。撤回“创建长期 SQLite”的说法，后续只修复既有 DB-only 启动接线并保护误入 file workspace 的新项目。
- 标准 `pnpm dev` 已改为 DB-only fail-closed 入口：默认稳定根 `~/.airoaming/`，先核验 SQLite 文件、migration ledger 和 db_only activation；缺库返回 `LOCAL_DB_RUNTIME_DATABASE_MISSING`，不会创建空库或回退 file。
- A4 的 `G2_DB_MODE_REQUIRED` 已改为直接说明运行实例未连接 DB-only，明确章节内容没有问题且没有发起模型调用。
- 当前 workspace 盘点为 2 项目、21 文件、约 2.32 MiB；`~/.airoaming` 无既有运行 DB，测试临时 DB 不能复用。
- 用户已明确授权停止旧服务、备份 workspace、使用现有 migration/importer 迁入稳定 DB-only 实例，并只做一次章节文本生成。
- 迁移前 workspace 已逐字节备份，源/备份文件数、字节数和聚合摘要一致；正式 C0～C7 cutover 成功，两个项目均进入 `/Users/liyadong/.airoaming/data/db/airoaming.sqlite`。
- 标准 DB-only 服务中的一次 `xai/grok-4.5` A4 请求成功生成来源密封的 AI pending；正式正文和正式 ScriptVersion 仍为空，页面采用/丢弃确认门保持有效。
- 激活后图片任务、候选图和 Asset 均为 0；没有调用图片服务。

# web_search


# goal

复现并修复当前开发启动进入 file mode、导致 AI 创作章节生成在模型调用前被 `G2_DB_MODE_REQUIRED` 阻断的问题；必须保留 DB-only 版本链和来源密封约束，不能通过移除数据库门禁绕过。

# approach

按 diagnose 流程：先以运行中服务和定向测试构建失败反馈环，再验证启动环境、数据库激活状态和章节前置状态；优先修复正式开发启动方式，不把 A4 降级回文件模式。

# question



# todo

- [x] 确认运行中服务的持久化模式和用户项目状态
- [x] 确认当前不存在可直接复用的长期 SQLite
- [x] 建立启动模式回归测试
- [x] 修复默认开发启动与错误提示
- [x] 完成构建、定向测试和当前数据只读盘点
- [x] 取得停写、备份、迁移与切换授权
- [x] 导入当前两个项目并验证 A4 真实文本生成
- [x] 完成单实例全量回归、双 Review、Handoff 与长期记忆收口

# result

- 根因已关闭：普通 `pnpm dev` 不再静默回退 file mode，而是连接并预检稳定 DB-only 实例。
- 两个项目已通过既有 migration/importer 和 C0～C7 迁移、激活并从 DB API 验证可读。
- 真实章节文本生成 1 次成功，形成 4033 字左右的待确认草稿；未采用、未发布正式章节、未生成图片。
- Server 125 files / 752 tests 全绿，类型检查和全项目构建通过；服务已在 4310/5173/4396 保持运行。
