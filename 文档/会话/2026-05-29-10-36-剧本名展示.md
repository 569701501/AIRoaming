# finding

- 用户指出生成第一章正文里出现 `剧本名称` 不符合预期；剧本名字应显示在章节下拉框右侧。
- 当前共享格式 `packages/shared/src/script-format.ts` 仍把 `剧本名称：` 放在「章节剧本」模板里，`ensureChapterScriptName` 还会在 AI 写入兜底时插入该字段。
- 长期记忆和方案文档此前也把 `剧本名称` 记录为章节剧本固定块，需要改成项目级显示口径。

# web_search


# goal

将 `剧本名称/剧集名称` 从章节正文中移出，作为项目级标题显示在章节下拉框右侧；章节正文只保留章节标题和章节内容。

# approach

- 保留对旧正文 `剧本名称：` 的解析能力，用于从旧文件恢复 `Project.storyTitle`。
- 新生成、改写、导入和保存的章节正文不再包含 `剧本名称：` 行。
- 前端在章节选择器右侧展示 `snapshot.project.storyTitle`。
- 更新文档和长期记忆，避免后续 AI 再把剧本名称写回章节正文。

# question


# todo

- [x] 探索章节标题栏和章节格式代码
- [x] 修改共享章节格式和后端写入兜底
- [x] 修改顶部标题栏展示剧本名
- [x] 更新文档和记忆
- [x] 验证类型检查

# result

- 已将 `剧本名称/剧集名称` 调整为项目级标题，显示在章节下拉框右侧。
- 「章节剧本」正文格式移除了 `剧本名称：` 行；AI prompt、OpenCode skill、导入/改写规则已同步。
- 后端新增章节正文清理：AI 写入、保存草稿、完成章节、导入格式兜底和 workbench 返回都会移除旧的 `剧本名称：` 行，同时仍可解析旧字段同步 `Project.storyTitle`。
- 验证通过：shared/server/web typecheck，接口烟测确认当前章节正文返回中不含 `剧本名称`。
