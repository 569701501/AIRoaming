# finding

- `script-chapter-editing` 真实生产入口是 `tryHandleChapterDraftUpdate -> rewriteChapterDraftWithAI -> buildChapterEditingPrompt`。
- 运行前按用户要求确定唯一最高 P4 层级；当前编辑器草稿是保护基线，第 N 章还读取第 N-1 章当前正式全文及版本摘要。
- 首次输出执行 `creative.chapter-edit/1.0` 严格解析；源稿可解析时执行 `assertP4LayeredRevision`，有前章正式全文时执行 `assertP5RevisionContinuity`。
- 格式、P4、P5 失败共用一次修复总上限，第二次仍失败不写 AI pending；写入前还复核前章版本与当前 pending 状态。
- 当前主 Prompt、P4/P5 质量重写和格式修复仍在 TypeScript；现有 Skill 只有 `SKILL.md`，尚无 references 和 `agents/openai.yaml`。

# web_search


# goal

把章节修订主 Prompt、四种 P4 层级说明、P4/P5 质量重写和格式修复归入现有 `script-chapter-editing` Skill，并让真实生产链读取这些资产。保持触发、六区块格式、P4/P5 固定校验、一次修复、来源围栏和 A5 待确认流程不变。

# approach

- Skill references 保存主修订模板、四层修订合同、前章连续性说明和三类修复模板。
- Shared 继续负责章节 Markdown 严格格式；Server 继续负责层级分类、Validator、动态来源注入和 pending 写入。
- 增加来源卫生和 P4/P5/格式修复分流测试，不调用真实模型或付费媒体服务。

# question


# todo

- [x] 完成 Skill 资产与发现元数据
- [x] 改造真实主生成和一次修复路径
- [x] 补充回归与来源卫生测试
- [x] 完成全量验证、双复核与文档留痕

# result

章节修订主 Prompt、四层 P4 合同、有/无前章连续性规则及 P4/P5/格式修复已统一读取 `script-chapter-editing/references/`。页面、六区块格式、分类、Validator、一次修复、版本围栏和 A5 pending 不变。Skill 校验、定向 65/65、类型检查、构建和 Server 全量 124 files / 738 tests 通过；真实模型和付费媒体调用为 0。
