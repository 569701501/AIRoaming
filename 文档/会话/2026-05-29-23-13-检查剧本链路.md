# finding

- 已启动 `$deep-think` 检查：目标是核对当前剧本 AI 链路、项目 workspace 文件、AI 生成的大纲与章节剧本是否按约定落盘。
- 已读取事实源：`文档/README.md`、`文档/00_索引/AI上下文入口.md`、`文档/00_索引/写作规范与留痕规则.md`、`文档/04_方案与决策/2026-05-27_剧本对话功能再设计方案.md`、`文档/02_架构与契约/核心数据模型.md`。
- 代码链路确认：`generate_inspiration_seeds` 只生成 3 个灵感种子并放入 pending 状态/对话工具结果，不写章节；`generate_script_outline_from_seed` 调用 `saveScriptOutlineFromAI` 写 `script-outline.md/json`；用户确认后 `generate_script_from_outline` 调用 `writeChapterDraftFromAI` 写当前章节 `script.md` 和 `script.revisions/latest.json`。
- 实际项目 `workspace/projects/b224e22c-9cdf-4195-851c-6bae207789c8` 已存在项目级大纲文件、章节 1 剧本文档、章节元数据和 AI 写入记录。
- `script-outline.md` 已包含 `基础信息 / 主要角色 / 情节概要`，且 `script-outline.json` 状态为 `confirmed`。
- `chapters/chapter-001/script.md` 已包含固定「章节剧本」六段格式，章节标题同步为 `chapter.json.title = 第 1 章：别相信他`，最近一次写入来源为 `generate_script_from_outline`。
- `chapters/chapter-002/script.md` 当前为空，符合“确认大纲后只生成当前一章”的设计；第 2 章只是章节文件占位/草稿，并未由本次 AI 链路生成正文。
- 发现一个旁路不一致：`project.json.status` 文件中仍是 `draft`，但 `GET /api/projects` 会根据当前章节正文推导为 `story_ready`；这说明项目列表状态不是从 `project.json.status` 原样读取，后续可修正文件元数据同步。

# web_search


# goal

检查当前链路是否符合：用户创建项目后，AI 生成的剧本大纲、章节剧本内容和固定格式需要保存到项目目录下的稳定文件中，不能只留在对话气泡或内存状态。

# approach

- 先从文档和代码确认设计链路与写入入口。
- 再从 `workspace/projects` 检查真实项目目录和文件内容。
- 最后对照固定格式判断已落盘、未落盘或格式不完整的部分。

# question


# todo

- [x] 梳理剧本 AI 链路代码
- [x] 检查真实项目 workspace 文件
- [x] 对照大纲和章节剧本固定格式
- [x] 输出结论和风险

# result

- 结论：当前核心链路已经把项目级剧本大纲和第一章章节剧本写入项目文件；格式也基本符合当前固定契约。
- 风险：灵感种子和对话历史仍是运行时/对话结果，不是稳定项目文件；`project.json.status` 与 API 推导状态存在不一致。
