# finding

- 用户真实截图中顶部第 3 步“分镜工作台”几乎不可见且不可点击，当前章节无法按正常流程进入下一阶段。
- 服务端权威 Workflow 对该步骤返回 `needs_confirmation`；共享契约允许该状态，但 `WorkbenchStageRail.vue` 只允许 `done/active` 点击，CSS 也没有 `needs_confirmation/needs_update` 样式。
- 当前章节已有正式 StoryVersion 和一份待确认 StoryboardVersion；旧剧情结构对话仍显示“待确认”，用户发送“确认采用当前剧情结构”会被误判为重新生成确认。
- 5 个场景 SceneVisual 已正式入库，但 DB read model 未加载 ChapterScene/SceneVisual，页面仍从不可变 StoryVersion 文档里的 `referenceAssetId` 取图，因此没有回显。

# web_search


# goal

修复真实项目七阶段流程阻断：第 3 步必须正常显示、可点击并进入待确认分镜；正式/待确认状态不能被旧对话误导；已生成场景图必须回显。通过自动化回归和用户当前项目浏览器路径复核。

# approach

- 先建立可重复的 DB E2E 失败信号，再修改前端阶段状态映射。
- 场景图通过 DB read model 做只读投影，不修改不可变 StoryVersion。
- 对话状态只展示当前权威产物状态，清理旧 pending 过程卡对已确认产物的误导。
- 最后在用户当前项目验证：剧情结构 → 分镜待确认 → 确认分镜 → 出图准备。

# question


# todo

- [x] 建立并运行 `needs_confirmation` 阶段栏回归测试，确认修复前失败。
- [x] 修复阶段栏可见性、可点击性与状态样式。
- [x] 修复 DB SceneVisual 到剧情结构页面的只读投影。
- [x] 核对并修复正式产物与旧对话 pending 状态冲突。
- [x] 执行类型检查、定向测试和真实浏览器全路径验证。
- [x] 完成静态复核、运行复核和正式留痕。

# result

已完成。用户当前项目已按页面正常按钮路径从剧情结构进入 12 镜待确认分镜，确认后解锁并进入出图准备；5 个场景图已正确识别，未调用图片 Provider。剩余业务阻塞为哑巴、商队多人、红心棺、商队众人 4 个出镜角色尚未定稿。
