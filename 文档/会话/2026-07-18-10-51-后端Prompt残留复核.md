# finding

- 主 Skill 读取链 fail-closed，没有静默硬编码 fallback。
- 生产残留共四类：持久 `shot_generate` 独立 Prompt、provider 参考图职责、分镜 JSON 示例语义、Skill 外画风/版式 Prompt 词汇。
- 非生产还有 P6 evaluator Prompt；邻接范围还有剧本、导入和剧情结构旧 Prompt。

# web_search


# goal

复核分镜、角色/场景参考图、候选图稳定创作提示词是否仍在后端残留第二份可编辑事实源，并区分合法动态装配/校验文本与应迁移的创作正文。

# approach

- 以 ADR-0017 和三个 Skill 为基准建立提示词特征集合。
- 反向扫描 `apps/server/src`、配置、测试与其他运行目录。
- 沿实际生产调用链检查加载、渲染、provider 编译和 fallback。
- 只读复核；如发现残留，先记录证据和影响，不在 Scrutiny Review 中直接修改。

# question


# todo

- [x] 建立残留判定标准和扫描范围
- [x] 执行静态文本与调用链扫描
- [x] 检查 fallback、默认值与 provider profile
- [x] 复核运行测试并形成结论

# result

静态复核不通过，需要修复。三个 Skill 主模板和加载器已接线，但“唯一事实源”尚未闭合。5 个离线测试文件、34/34 通过；未调用真实模型或图片 provider。
