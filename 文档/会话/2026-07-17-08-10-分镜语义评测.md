# finding

- V2.3 已成为生产基线；真实 AI 样本仍出现 `beat_01` 已挂载但 outcome 中“旧机器刹车声引向站台尽头”未明确进入镜头的问题。
- 当前 `assertStoryboardQuality` 只做结构、引用、beatId 覆盖、双轨对白一致和正式台词来源等高确定性检查，不评估 `summary/outcome` 的语义可观察性。
- 语义覆盖不适合加入生产确定性硬门，否则会增加每次生成的模型调用、延迟、成本和误拦截；更适合作为 P6 A/B 与回归评测。

# web_search

- 本轮沿用已完成的 Promptfoo/模型 rubric 分层调研，不新增网络搜索。

# goal

新增 QA-only 的 Beat `summary/outcome` 语义评测工具：严格输出逐 beat 状态和镜头证据，用真实 V2.3 两项目验证；不得接入用户生成链路、页面、数据库、自动修复或自动确认。

# approach

- 模型只判断 `covered/partial/missing/contradicted`，不打总分、不改写分镜、不建议新剧情。
- 服务端根据严格报告和输入事实计算 `pass/warning/fail`，不接受模型自报总状态。
- CLI 读取 structure/storyboard JSON，使用现有 deny-all OpenCode 文本运行时，结果只写指定评测文件。

# question


# todo

- [x] 冻结评测契约并建立失败测试。
- [x] 实现 Prompt、严格 parser 和 QA CLI。
- [x] 用 AI 创作、导入两个 V2.3 输出真实评测。
- [x] 完成双 Review、正式留痕、记忆和提交。

# result

已完成 QA-only Beat 语义评测工具和两路线各两次真实文本模型复测。AI 样本稳定复现 4 个非完整 Beat，导入样本稳定复现 2 个并出现 2 个单次附加判断；因此报告用于 P6 A/B 诊断，不接生产硬门。生产分镜、页面、Schema、数据库和确认流程未改变。
