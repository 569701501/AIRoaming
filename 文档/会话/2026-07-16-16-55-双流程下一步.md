# finding

- A+ 的 AI 创作与已有完整剧本两条上游路线已经完成生产接线、P1～P6 固定门和真实模型浏览器验收。
- 两条路线都在已确认 `ChapterScriptVersion` 汇合，现有页面字段和 `StoryStructure` payload 未改变。
- 当前剧情结构运行时已有生成、预览、确认流程，但提示词主要约束 JSON 形状和字段说明，尚未纳入上游同等级的内容覆盖、忠实提取和固定质量回归。
- DB Workbench 的章节正文是 Working Copy；剧情结构现已在生成前要求 clean，并按 current ScriptVersion 读取正式正文，避免结构内容与来源版本错绑。

# web_search


# goal

在不新增页面字段、不改变 A/B 双流程和现有 StoryStructure 数据结构的前提下，确定并完成双流程之后最有价值的下一步。

# approach

- 不再扩展上游流程、ChapterPlan 或公开 Skill。
- 优先检查汇合后的剧情结构阶段，补齐其 Prompt、固定校验和真实来源边界。

# question


# todo

- [x] 核对双流程完成状态和现有后续约束
- [x] 审查剧情结构现有 Prompt、解析器和测试缺口
- [x] 实施最小且可验证的下一步
- [x] 完成自动验证、文档留痕和长期记忆更新

# result

- 剧情结构生成已接入正式正文来源、逐场覆盖、人物/场景/beat 引用完整性和一次定向修复。
- 页面、字段、A/B 双流程和 StoryStructure payload 均未改变。
- 聚焦 17/17、Server 652/652、类型/构建和 DB-only Chromium 1/1 通过。
