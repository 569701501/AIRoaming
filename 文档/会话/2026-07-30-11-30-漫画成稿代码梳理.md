# 2026-07-30 漫画成稿功能代码梳理

# finding

- 漫画成稿=第 6 步,内部 stepKey `layout_export`,路由 slug `/layout`,手机只读预览路由 `/projects/:projectId/layout/preview`。
- 三层结构:
  - Shared `packages/shared/src/layout/`(29 模块):LayoutDocument V1(画布/画格/文字/气泡/自由图/阅读顺序)+ V2(automation:对白绑定、保护 scope、V2→V1 投影)、V1/V2 命令协议、规则排版 rule-composer、视觉排版 visual-composer、预检 preflight、出版 publication、CAS Working Copy、正式版本 revision、换源 source-replacement、受控字体 font。
  - Server `apps/server/src/projects/`:11 个 layout service(working-copy/versioning/renderer/publication(+worker)/composition(+worker)/visual-analyzer/font/pending-command/legacy-converter/source-freshness)。渲染为 pngjs+fontkit 纯 TS。
  - Web:`LayoutExportWorkspace.vue` 3901 行主编排器 + `LayoutKonvaInteractionLayer.vue`(Konva 直接操纵)+ `LayoutRichTextEditor.vue` + `LayoutCanvasVisualPreview.vue` + `composables/layout-editor-session.ts`(自动保存/冲突恢复,无 undo)+ `layout-composition-session.ts`(只发 initial 排版)。
- 用户可见:首次自动排版→画布手调(拖拽/多选/右键菜单/裁切/文字/气泡/图层/阅读顺序/模板/页面设置)→自动保存→预检→文字 review→导出(页漫 PNG+PDF/条漫切片+长图);来源 stale 提醒、换源、手机只读预览、旧排版转换。
- 底层保留但 UI 不暴露:full/scoped_reflow、5 种排版 intent、保护 scope 管理、Undo 历史结构、Pending 命令(手机 AI 两段式)、版本历史/恢复、导出历史列表、出版 profile 自定义。
- 已退役:UI 上 Undo/立即保存/二次智能调整/建议对比/版本历史/四步出版(ADR-0022 基础版收缩);V1 文档栈仅兼容读取。
- 2026-07-26 评估结论:数据链路产品级,短板在画布直接操纵(盲拖、无画布文字编辑、无 undo)与第三套蓝色私有主题(--le-accent:#4f8cff)。P0 已修 Delete 删除/缩放 0.1-0.8/text 死代码清理。P1 后置:拖拽实时预览、画布双击编辑文字、主题并入项目视觉、有限本地 Undo。

# web_search

(无)

# goal

用户准备重新设计漫画成稿功能。已确认:流程保持「自动排版 → 手动调整 → 导出」不变;**只改编辑器 UI/交互**(不动 Shared 协议、排版/导出后端);形态为「极简画布 + 浮动工具条」;四类核心操作=移动/缩放画格、挪气泡/改遮挡、改文字内容、调裁切范围。

已启动 deep-think,任务目录:`文档/05_执行与记录/任务记录/2026-07-30_漫画成稿编辑器极简重构/`(task_plan D1~D8 决策待用户确认)。

# todo

- [x] 读记忆与相关文档
- [x] 探索三层代码实现(Shared/Server/Web)
- [x] 输出功能总表(用户可见 vs 底层保留 vs 已退役)
- [x] 与用户确认重新设计的方向和范围
- [x] M0:建任务目录、写设计计划(D1~D8,用户已确认)
- [x] M1 布局骨架 / M2 浮动工具条 / M3 画布直接操纵 / M4 本地 Undo / M5 测试重写
- [x] M6:Scrutiny Review 通过(3 条残留风险)、ADR-0023、完成记录、MEMORY 更新
- [ ] 等用户 Runtime/User Review(真实浏览器按 task_plan §6 七条验收)

# 最终总结

漫画成稿编辑器极简重构(deep-think M0~M6)已完成开发与静态复核:画布优先 + 浮动工具条 + 拖拽实时预览 + 双击就地改字 + 有限本地撤销 + 主题并入项目视觉。全部门禁绿(单测 777/777、web 契约 55/55、e2e db 18/18)。冒烟改动已恢复。剩余:用户真实路径验收。关键长期经验已写入 MEMORY.md 顶部条目。
