# finding

- SH-01～SH-09 的核心运行证据可独立复核：release commit clean，A/B 16/16 succeeded，逐 slice verify 全通过，source 仅新增授权文件，secret scan=0，SH-09 restore verify 通过。
- SH-10 预审为 `changes_requested`：缺少真实 plan/plan identity/shadow gate；执行记录混淆 G1 baseline digest 与实际 release schema identity；私有证据根和 DB/报告文件权限不满足 0700/0600；正式 G1 清单仍为 not_run；table-count 缺 sealed pre-SH09 checkpoint；1 个 warning 未经人工接受；证据根缺 canonical index。
- 用户已提供责任角色、维护窗口、settings/credential 分支、warning accepted 和 migration reviewer 标识；原值写入仓库外 0600 私有记录。私有 plan=`sha256:d08b7e3aa2561c556ad25348d6b9dbcd08f487a1c428233b59763fc9df0412da`、review packet=`sha256:a28ab7e1a59a9b8ba26a89e6522bd235ec0ad2176085b12a72574a3bc20f35fd` 已生成并通过正式 plan service 同字节实现及独立摘要/权限/空路径校验。
- 用户随后明确确认绑定上述实际 plan/review digest；final SH-10 evidence=`sha256:b0d58efef766f8dc4dc2d57f14566f9187fbaf0b798d09e65001d14629518e21`，gate=`sha256:e5d150ae57baa4578b07d03a8e1bfdd508531695bb6c53c60cd1f5e040439d3c`，生产 reader 与 2/2 定向 spec 通过。
- 用户随后要求继续执行；按“只执行 C0”的边界生成一次性随机 0600 maintenance token，冻结 release C0 返回 `CUTOVER_C0_OK`，C0 evidence=`sha256:3444ae2d4b20fae8b5f01a7c0955aefdc8d80f6c46886f7d029f6322f9a3ba11`，Scrutiny 和 3 files/17 tests 通过。
- 用户已明确确认 AUTH-C1 原文，授权 C1～C4（含 C3 凭据验证/预置），明确未授权 C5/C7。私有 AUTH-C1 绑定 C0 evidence=`sha256:3444ae2d...9a3ba11`，authorization digest=`sha256:d310d5fe...e9472eb3`。
- C1/C2 已成功：C1 gate=`sha256:0bc1d582...9d69d0`、C2 gate=`sha256:3b1254e5...ea1680`；C3 fail-closed 为 `CUTOVER_SETTINGS_START_STATE_MISMATCH`，因为真实 source settings 已是 `already_sanitized`，而冻结 plan 仍声明 `legacy_plaintext_requires_two_phase/prestage_legacy`。未写 C3/C4 evidence，未触碰真实 Keychain。

# web_search

# goal

完成 R0-B SH-10、R1-C0 及经授权的 R1-C1/C2；处理 C3 settings 起点契约阻塞，不伪造明文或篡改已绑定 plan；C5/C7 仍禁止。

# approach

按 `$deep-think` 的 Worker→Scrutiny→Human Decision Review 分离口径执行 SH-10 与 C0；C0 只读证据通过后，保持 AUTH/C1～C7 为空。

# question

- 需要用户/发布责任人决定：按真实 source 的 `already_sanitized/verify_existing` 重新生成 plan 并重跑 C0，还是恢复/确认 legacy 明文起点；两者都会改变 plan digest，不能在当前 AUTH-C1 上继续。

# todo

- [x] 独立复核 SH-01～SH-09 核心证据
- [x] 输出 SH-10 changes_requested 预审记录
- [x] 修复证据身份与正式 SH 状态
- [x] 收紧外置证据权限并建立 canonical index
- [x] 补可重算 C/D 表计数 checkpoint
- [x] 整理 warning disposition 与真实 plan 人工字段
- [x] 生成并校验 digest-bound 私有 plan/review packet
- [x] 人类确认实际 plan/review digest
- [x] 基于该确认记录并复核 SH-10 gate
- [x] 执行 C0 只读检查并完成复核
- [x] 获取独立 AUTH-C1 人工授权并绑定 C0
- [x] 执行 C1～C2
- [ ] 解决 plan/source settings 起点不一致，重新绑定 plan/C0
- [ ] 继续执行 C3～C4

# result

R0-B SH-10 与 R1-C0 已完成；用户 AUTH-C1 已绑定并授权 C1～C4。C1、C2 已通过；C3 因 source settings 实际为 `already_sanitized` 而冻结 plan 声明 legacy plaintext 起点，按 fail-closed 停止，目标 DB/data root 已清理，证据链止于 C2。当前停止点为 `blocked_before_c3_settings_start_state`；AUTH-C5/C7 未生成。
