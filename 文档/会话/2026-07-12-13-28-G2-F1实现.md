# finding

- 新增 `TaskApplicabilityGuardService`，复用 NewWorkGate，输出 current/historical、reasonCodes、productionState。
- Guard 无 DB 写权限，不修改 current；现有 TasksService 仍为内存 Map，persistent worker/claim/lease/history 未实现。

# web_search


# goal

- 先落地 G2 任务完成前的 applicability 安全边界，不把旧内存队列冒充持久 worker。

# approach

- `evaluate` 复用四类 NewWorkGate；image task 额外校验 expected Preflight sourceDigest。
- 不增加表、不接管 TasksService，不改变现有 file-mode 行为。

# question


# todo

- [x] TaskApplicabilityGuardService
- [x] fresh SQLite current/historical evidence
- [x] scrutiny/handoff/完成记录

# result

F1 已完成；下一步仍是 persistent worker、claim/lease/recovery、task history 和 capability switch。
