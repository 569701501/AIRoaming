# finding

- 当前分支为 `codex/g0-test-safety-net`，M6-A1 代码最新提交为 `3661939`；最终文档收口后再提交。
- D2 至 M6 总状态记录为 P0～P12 全部完成；M6 实现提交为 `c07ec8c`，文档收口提交为 `f9276f1`。
- M6 证据为临时根/fake SecretStore/隔离 execute rehearsal，未执行真实 workspace、真实数据库、真实 Keychain、真实 provider、真实停写或真实 cutover。
- M6 隔离链和负例已完成；最新 server 全量为 61 files/425 tests 全绿，capability CLI 为 8 个聚合 capability、`blockedIds=[]`；这些结果证明隔离工程证据，不等于真实切换已授权。
- `app:backup --kind pre-cutover` 已绑定 final run/ready/maintenance/current identity；restore 支持 pre-cutover，activate 只接受验证通过的 pre-cutover bundle。
- `db:activate` 只调用 restore verify-only，没有校验 backup 与本次 final run、source/effective identity、`ready_for_activation` 状态一致；单测中的 restore 是 fake。
- `m6-c0-c7.rehearsal.spec.ts` 已改为真实临时 SQLite、final importer、pre-cutover backup/materialize restore、关闭态 API read、activate 和首笔业务写，并持久化 C0～C7 evidence；仅 SecretStore/provider 为 fake。
- `PrismaService.runBusinessTransaction` 尚未覆盖所有业务写入路径；多处服务仍直接调用 `$transaction`，因此首笔业务写时间与 ready/recovery 写入门禁不能覆盖全系统。
- 隔离矩阵项现均为 `passed`；真实授权、真实根、真实 Keychain/provider、OBS-01～10 继续未执行，最终状态为 `ready_for_real_cutover_authorization` 但真实切换仍 no-go。
- G4 验收仍是规划态，G5 只能在 G4 后进入；在修正 M6 验收缺口前不应执行真实切换，也不建议直接宣称进入 G4 正式交付。

# web_search


# goal

复核当前开发完成度，并确定下一步应进入真实切换授权、观察期，还是继续后续功能开发。

用户随后要求把复核结论写成可直接交给 5.6 Luna 连续执行的 M6-A1 施工文档；必须覆盖实现契约、测试、文件函数、复核和停止点。

# approach

按 `$deep-think` 的 Scrutiny Review 角色只读核对 git、总 Handoff、M6 证据、路线图与后续验收状态；不执行真实切换。

转入 Orchestrator 角色建立 `2026-07-13_M6-A1真实切换验收补强` 任务目录，提供五份施工资料和 task/progress/findings 三件套；同步撤回旧 M6 当前通过状态。

# question


# todo

- [x] 核对 git、M6 总状态与复核资料
- [x] 核对总路线图及 G4/G5 实际状态
- [x] 输出下一步建议与授权边界
- [x] 编写 Luna 五份施工资料和 deep-think 三件套
- [x] 修正旧 M6/总 Handoff 的当前状态和事实源索引
- [x] 校验文档路径、测试 ID、阶段顺序和 diff 格式
- [x] 执行 A1-3 业务写边界收口并独立提交
- [x] 执行 A1-4 真实临时 SQLite C0～C7 综合 rehearsal
- [x] 完成 server/workspace 回归与构建门禁
- [x] 完成 A1-5 Scrutiny/Runtime Review、负例补强、最终文档和独立提交；隔离矩阵无 `not_run` 行

# result

本次继续执行 M6-A1：A1-1/A1-2 已落地 pre-cutover final/ready backup、restore kind 分支、closed runtime bundle、activate identity 校验和 durable C0 evidence；A1-3 已将生产业务写统一收口到 `runBusinessTransaction`，独立提交 `c969bb9`；A1-4 已用真实临时 SQLite 串起 C0～C7，综合 spec 不再使用 fake Prisma/fake restore，代码提交 `b4b4a18`。A1-5 已形成 Scrutiny Review、隔离 Runtime Review 和功能完成记录；server 全量 61 files/412 tests、workspace typecheck、server/web build、Prisma/G1 checks、capability report 和 diff check 均通过。测试矩阵中未运行的故障注入、crash-resume、路径和安全负例仍不能伪造为 passed；真实切换授权仍禁止。

- 复核结论：D2/M5 主体能力、final importer 与 M6 工具骨架已经完成；M6 真实切换验收没有完成，当前应判定为 `M6 tooling complete / real cutover no-go`。
- 下一阶段固定为 `M6-A1 真实切换验收补强`：实现并绑定 pre-cutover backup、强化 activate 绑定校验、把 mock rehearsal 替换为真实临时 SQLite 全链路、持久化 C0～C7 证据、统一所有业务写边界、验证停写条件，并回填 G1 正式验收清单。
- M6-A1 完成并经 Scrutiny/Runtime Review 后，才可重新判定 `ready_for_real_cutover_authorization`；真实 C0～C7 仍需用户单独授权。G4/G5 不属于本次补强范围。
- Luna 当前唯一入口为 `文档/05_执行与记录/任务记录/2026-07-13_M6-A1真实切换验收补强/handoff.md`；A1-0～A1-5 可连续执行、逐阶段独立提交，最终停止在授权申请边界。
- 施工包纠正了 C2/C4 时序：C2 只生成最终源 snapshot；pre-cutover DB backup 必须在 C4 final succeeded + ready 后生成。ReadyCoordinator 的 boolean 假证据必须退役。

# final_update

- M6-A1 最终门禁：server 61 files/425 tests；workspace typecheck、server/web build、Prisma/G1、capability CLI、diff check 均通过。
- 当前交付边界：`ready_for_real_cutover_authorization`；不得自动执行真实 `db:activate --execute`、真实停写、真实 C0 或进入 G4/G5。
- 直接证据范围已在 test_matrix/review 文档中标明：ACT-06 为 DB-only resume，BK-02 为 missing final run + shadow state，PATH-01 为 symlink/overlap；这些不扩张为真实切换证据。
