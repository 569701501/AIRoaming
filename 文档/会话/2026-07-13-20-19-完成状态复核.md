# finding

- 当前分支为 `codex/g0-test-safety-net`，HEAD 为 `f9276f1`，检查开始时工作树干净。
- D2 至 M6 总状态记录为 P0～P12 全部完成；M6 实现提交为 `c07ec8c`，文档收口提交为 `f9276f1`。
- M6 证据为临时根/fake SecretStore/隔离 execute rehearsal，未执行真实 workspace、真实数据库、真实 Keychain、真实 provider、真实停写或真实 cutover。
- M6 定向门禁复跑为 6 files/12 tests 全绿，capability CLI 为 8/36、`blockedIds=[]`；这些结果只能证明当前工具和 capability registry，不足以证明真实切换安全。
- `app:backup --kind pre-cutover` 仍被实现明确拒绝；restore 只接受 `backupKind=coordinated`、`runKind=shadow`，尚无绑定 final run/ready state 的 pre-cutover bundle。
- `db:activate` 只调用 restore verify-only，没有校验 backup 与本次 final run、source/effective identity、`ready_for_activation` 状态一致；单测中的 restore 是 fake。
- `m6-c0-c7.rehearsal.spec.ts` 使用 fake Prisma/fake restore/marker 文件模拟 C0～C7，没有串起真实临时 SQLite、final importer、pre-cutover backup/materialize restore、关闭态 API 回滚和真实业务写。
- `PrismaService.runBusinessTransaction` 尚未覆盖所有业务写入路径；多处服务仍直接调用 `$transaction`，因此首笔业务写时间与 ready/recovery 写入门禁不能覆盖全系统。
- G1 正式验收清单中的 SH、C0～C7、RB、OBS 等关键项仍为 `not_run`/未勾选；当前文档声明 `ready_for_real_cutover_authorization` 与实际证据不一致。
- G4 验收仍是规划态，G5 只能在 G4 后进入；在修正 M6 验收缺口前不应执行真实切换，也不建议直接宣称进入 G4 正式交付。

# web_search


# goal

复核当前开发完成度，并确定下一步应进入真实切换授权、观察期，还是继续后续功能开发。

用户随后要求把复核结论写成可直接交给 5.6 Luna 连续执行的 M6-A1 施工文档；必须覆盖实现契约、测试、文件函数、复核和停止点。

# approach

按 `$deep-think` 的 Scrutiny Review 角色只读核对 git、总 Handoff、M6 证据、路线图与后续验收状态；不执行真实切换。

转入 Orchestrator 角色建立 `2026-07-13_M6-A1真实切换验收补强` 任务目录，提供五份施工资料和 task/progress/findings 三件套；同步撤回旧 M6 当前通过状态。

# question


# todo

- [x] 核对 git、M6 总状态与复核资料
- [x] 核对总路线图及 G4/G5 实际状态
- [x] 输出下一步建议与授权边界
- [x] 编写 Luna 五份施工资料和 deep-think 三件套
- [x] 修正旧 M6/总 Handoff 的当前状态和事实源索引
- [x] 校验文档路径、测试 ID、阶段顺序和 diff 格式

# result

本次继续执行 M6-A1：A1-1 已落地 pre-cutover final/ready backup、restore kind 分支、closed runtime bundle 和 ReadyCoordinator 真实证据校验；A1-2 已开始，activate 只接受匹配的 pre-cutover bundle，并新增带摘要/身份绑定的 durable C0 evidence。临时 SQLite 定向验证已通过：backup/restore 35/35、ready 3/3、activate/cutover 7/7；server 全量 60 files/408 tests 通过。阶段提交为 `e9912ca`。A1-3/A1-4/A1-5 仍未完成；禁止真实切换授权。

- 复核结论：D2/M5 主体能力、final importer 与 M6 工具骨架已经完成；M6 真实切换验收没有完成，当前应判定为 `M6 tooling complete / real cutover no-go`。
- 下一阶段固定为 `M6-A1 真实切换验收补强`：实现并绑定 pre-cutover backup、强化 activate 绑定校验、把 mock rehearsal 替换为真实临时 SQLite 全链路、持久化 C0～C7 证据、统一所有业务写边界、验证停写条件，并回填 G1 正式验收清单。
- M6-A1 完成并经 Scrutiny/Runtime Review 后，才可重新判定 `ready_for_real_cutover_authorization`；真实 C0～C7 仍需用户单独授权。G4/G5 不属于本次补强范围。
- Luna 当前唯一入口为 `文档/05_执行与记录/任务记录/2026-07-13_M6-A1真实切换验收补强/handoff.md`；A1-0～A1-5 可连续执行、逐阶段独立提交，最终停止在授权申请边界。
- 施工包纠正了 C2/C4 时序：C2 只生成最终源 snapshot；pre-cutover DB backup 必须在 C4 final succeeded + ready 后生成。ReadyCoordinator 的 boolean 假证据必须退役。
