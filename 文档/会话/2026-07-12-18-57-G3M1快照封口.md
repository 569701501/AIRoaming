# finding

- 当前基线为 `e2caa13`，工作区干净；G3-M0 maintenance gate 已通过 MNT-01～06。
- M1 只处理 closed runtime bundle 的文件封口、workspace snapshot 的 pre/post manifest、路径安全、设置脱敏和 SEALED 发布；不读取 importer、不实现决议/导入/备份/激活。
- 现有 workspace 根由 `WorkspacePathService` 解析，但 snapshot 必须接收显式绝对 workspaceRoot/stagingRoot，拒绝 symlink 与 staging 越界。

# web_search


# goal

- 完成 G3-M1 snapshot/runtime bundle 完整切片并留下可交给 Luna 的验证证据。
- 强制验收：SNP-01～06、runtime bundle/redaction、server typecheck、server 全量测试、G1 三项 check；不修改 G3 enum/0010。

# approach

- 先把 runtime bundle 作为 sealed 输入校验：kind/schema/payloadDigest、0600 文件和无 token/apiKey。
- snapshot 只扫描允许的 workspace 相对文件；settings 不复制原文，改为脱敏 JSON；pre/post manifest 必须逐字一致后才发布 SEALED。
- 所有 digest 排除时间、绝对根和自身 digest；输出目录使用临时目录 + fsync + 原子 rename。

# question


# todo

- [x] 建立 G3-M1 任务记录并读取 snapshot 事实源
- [x] 实现 runtime bundle 文件封口与 canonical digest 校验
- [x] 实现 snapshot pre/post manifest、path guard、redaction 与 sealed publish
- [x] 补齐 SNP/runtime 测试与 db:snapshot CLI
- [x] 静态复核、全量验证并提交 G3-M1

# result

G3-M1 已实现并完成验证，待提交本轮代码和文档；下一切片为 G3-M2 decision codec，sealed snapshot 不能直接导入。
