# finding

- 用户报告项目 `d14f801d-5d35-4cb1-8021-600d39ec477b`、章节 `d14f801d-5d35-4cb1-8021-600d39ec477b_chapter_001` 的漫画成稿预览存在文字遮挡角色。
- 既有事实源已记录：该章 5 次 layout 任务均为 `rule_fallback`，11 个镜头均出现 `visual_analysis_not_configured`；当前 Working Copy 曾测得 19 个气泡中 7 个与来源画格相交。
- 本轮目标是重新核对真实页面、当前文档、任务与配置链路，区分数据问题、预览缩放问题和运行时配置问题；只诊断，不直接修改代码或成稿。
- 当前真实预览为 Working Copy `layout_wc_080dd45e-7623-4079-a5dc-cd913a9221b7` v66，9 段、11 画格、19 个对白/旁白对象；文档几何重新计算得到 7/19 与来源画格相交。
- 页面复核确认第 1、2、6、9 段存在画格内文字；第 6 段压住主角头发，第 9 段三条对白覆盖人物躯干、手、手机/雨伞区域。预览 DOM 与文档坐标按统一比例对应，控制台无错误，排除预览缩放或渲染错位。
- 最近 5 个 `layout_compose` 任务输入均冻结 `visualAnalysisProvider=null`，输出均为 `analysisMode=rule_fallback`；每次 11/11 分析都是 `visual_analysis_not_configured`，仍以 `succeeded`、`needs_review=0`、92.48 分结束。
- 设置公开状态的文本模型 `configured=true` 只由指纹存在决定；当前默认文本 Provider 为 `self/grok-4.5`、owner=`opencode`、无 `secretRef`。四个图片 Provider 虽有 SecretStore 凭据，但与文本配置的指纹和 base URL 可恢复匹配数为 0，因此当前进程的文本 runtime key 为空。
- source projector 只根据 runtime `apiKey` 决定是否写入视觉 Provider；为空即冻结 null。analyzer 随后不调用模型，直接产生规则回退。
- fallback 契约要求视觉区域为空；composer 却无条件保留画格内候选。评分把气泡面积至少 82% 处于来源画格内当成关联正确，空 subjects 又让主体遮挡保持通过；hard gate 不检查气泡几何、主体遮挡或 direct-usable rate，导致错误版面稳定放行。
- 行为违反 ADR-0019 与智能成稿契约的“保守安全布局”意图，同时暴露正式 hard-gate 契约本身缺少无视觉证据时的安全条件。

# web_search


# goal

- 在用户给出的真实预览页复现文字遮挡，并用页面、数据库、任务输出和代码证据确认根因与影响范围。
- 给出明确诊断结论、风险等级和修复顺序，不在未授权情况下改代码或覆盖当前成稿。

# approach

- 使用真实预览页截图和 DOM / 文档坐标构造反馈信号。
- 核对当前 Working Copy、最近 layout 任务的 analysis mode、warning、视觉分析记录和运行时配置状态。
- 沿 source projector → visual analyzer → rule composer → quality score → preview renderer 逐层验证四个假设。

# question


# todo

- [x] 复现并标记当前预览中的遮挡位置
- [x] 核对当前成稿与最近 5 次任务是否均为规则回退
- [x] 核对视觉凭据为何没有进入任务来源投影
- [x] 核对 fallback 放置规则与质量门误放行条件
- [x] 输出诊断结论、修复优先级和复核记录

# result

- 用户给出的四层根因判断全部成立；另补充确认 hard gate 本身遗漏了气泡几何与主体遮挡条件。
- 本轮只读检查页面、数据库、任务输出、设置链路和代码，没有修改生产代码、数据库或当前 Working Copy。
- 证据与完整结论保存在 `文档/05_执行与记录/任务记录/2026-07-23_排版文字遮挡复核/`。
