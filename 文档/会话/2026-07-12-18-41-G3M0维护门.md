# finding

- 当前基线已提交为 `0dbf93d`，工作区干净。
- G3-M0 的范围是维护状态机、统一 mutation lease、五类参与者、loopback/token 管理入口和不泄密的 runtime bundle 骨架；不实现 importer、backup/restore、activate 或真实 workspace 迁移。

# web_search


# goal

- 在现有 G3-core 基线上完成 G3-M0 maintenance gate，并留下可交给 Luna 的测试与交接证据。
- 强制验收：MNT-01..06、server typecheck、server 全量测试、G1 三项检查；不修改 G3 enum/0010。

# approach

- 以独立 MaintenanceModule 提供全局 coordinator/controller，业务服务在写入口取得 coordinator lease。
- 参与者状态和 bundle 只表达 M0 可观察事实；未接入的细节显式标记为 unobservable，不伪造迁移完成。

# question


# todo

- [x] 建立 G3-M0 任务记录并读取维护门相关事实源
- [x] 实现 MaintenanceCoordinator、participants 与 loopback 管理入口
- [x] 实现 runtime bundle 骨架与启动/模块接线
- [x] 补齐 MNT 测试、运行验证并更新交接记录

# result

G3-M0 已实现并完成验证，待提交本轮代码与文档；后续应切换到 M1，不得把 M0 bundle 当作 snapshot/importer 输入。
