# 角色阶段管理前端 UI（阶段2）

## finding

- 阶段1（定妆）完成记录：`文档/05_执行与记录/功能完成记录/2026-08-11_角色定妆前端UI.md`；后端任务：`文档/05_执行与记录/任务记录/2026-08-11_角色图生成与阶段管理/`。
- 后端 5 个阶段接口已在 `apps/web/src/services/api.ts:449-488`：listCharacterStages / createCharacterStage / updateCharacterStage / deleteCharacterStage / regenerateCharacterStage。
- `CharacterStage` DTO（`packages/shared/src/dto.ts:826`）可选字段后端序列化为 `undefined`（非 null）：name/fromChapterId/toChapterId/previewAssetId/finalAssetId。
- 后端 `regenerateCharacterStage` 返回 `{ stage, previewAsset: WorkbenchAsset | null }`（character-stage.service.ts:360），previewAsset 为 null 表示无参考图未生成垫图；`previewAssetId` 无外键，删除阶段时后端显式回收 character_stage_* 资产。
- 后端列表按 stageOrder 升序返回（service.ts:283）。
- `finalAssetId` 目前无任何端点写入（预留字段），阶段2 UI 不展示"已定稿"状态。
- 弹窗 z-index 约定：角色库 70、预览大图 90、定妆弹窗 95、阶段管理 96（更高）。
- ProjectCharactersWorkspace 的 readonly 模式（ProjectCharactersModal 内嵌）隐藏操作按钮；定妆按钮对纯声音角色隐藏。
- 项目约定：全站禁用 window.confirm，二次确认用自定义弹窗（MEMORY）。

## goal

实现阶段2：CharacterStageManager.vue（弹窗+列表+空状态+骨架屏+预览放大+删除二次确认+重新生成）+ CharacterStageForm.vue（创建/编辑表单，visualDelta 必填验证）+ ProjectCharactersWorkspace.vue 集成（阶段管理按钮，Layers 图标）。类型检查通过。

## todo

- [x] CharacterStageManager.vue 创建
- [x] CharacterStageForm.vue 创建
- [x] ProjectCharactersWorkspace.vue 集成按钮
- [x] 删除二次确认 + 重新生成预览
- [x] typecheck 验证
- [x] 功能完成记录 + MEMORY 更新

## 总结

- CharacterStageManager.vue（Teleport z-index 96）：骨架屏/空状态/卡片列表（渐变边框，stageOrder 升序）、预览缩略图点击放大（z-index 97）、编辑/删除/重新生成预览、删除二次确认自定义弹窗（红色按钮，无 window.confirm）、表单视图切换、加载失败重试、移动端单列。
- CharacterStageForm.vue：名称（可选）/起始章节下拉（"从头"+章节列表）/外观变化描述（必填 textarea 验证）；创建走 createCharacterStage（返回 previewAsset），编辑走 updateCharacterStage；清空字段传空串（后端归一为 null）。
- api.ts 修正：createCharacterStage 响应类型补 `previewAsset: WorkbenchAsset | null`（后端实际返回，原类型缺失）。
- ProjectCharactersWorkspace.vue：row-title 新增紫色「阶段管理」按钮（Layers 图标），非 readonly 且非纯声音角色显示（与去定妆一致）。
- 验证：pnpm -r typecheck 三包通过；web 55/55；vite build 成功；真实浏览器冒烟（localhost:5173 + 4310 DB 实例）：打开弹窗→空状态→表单→必填验证→创建成功回列表→删除二次确认→清理，零 JS 错误；测试后 DB 无残留（character_stages=0，character_stage 资产=0）。
