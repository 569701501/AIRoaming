# finding

- 双路线固定无头 Chromium 测试已通过，但该测试使用受控假模型，只证明交互、状态和落库流程。
- 本轮需要新建隔离项目，通过真实浏览器与真实 OpenCode 模型配置检查生成质量和两条路线的实际可用性。
- 使用独立 SQLite 和 workspace 启动应用，真实模型列表可用；本轮实际调用 `self/gpt-5.5`，未读取或修改现有项目数据。
- AI 创作测试项目 `35c1d2ea-e7c6-4791-9859-94c708724a81` 已完成两章近未来悬疑条漫的真实生成验证：大纲确认不自动生成正文，第 2 章切换不自动生成，只有用户明确输入“生成当前章节”后才生成。
- 第 2 章正确承接第 1 章正式正文中的离线芯片、旧代号“鸢尾”等信息，P5 连续性在本样例中有效。
- AI 路线发现 `C1-REAL-01`：章节生成完成后，右侧已经展示 `pendingSourceText`，但立即点击“采用草稿”提示“没有待确认章节草稿”；刷新页面后采用正常。代码侧原因是对话工具结果只调用 `applyChapterUpdate` 更新 snapshot，没有同步 DB 模式下独立的 `scriptPendingSuggestion`；采用动作读取后者。
- 第 1 章正式版本形成后可进入剧情结构。真实模型生成 6 个角色、4 个场景、9 个剧情节拍；确认后 StoryVersion 绑定精确的 `sourceScriptVersionId`，章节状态为 `structured`，分镜工作台正确解锁。
- 已有剧本测试项目 `443a162a-40f6-4906-85df-2446cdbcbe0c` 通过页面粘贴 1647 字、22 个稳定 block 的两章完整剧本，系统正确路由到已有剧本分析并保存不可变原稿版本。
- 已有剧本路线发现 `C1-REAL-02`：真实模型返回 `excludedRanges[0].sourceRanges`，严格解析器要求单数 `sourceRange`，因此 B2 失败并安全拒绝创建候选目录、章节映射和导入批次。默认空白章节未被覆盖。
- `C1-REAL-02` 的契约本身明确，但运行时示例只给出 `excludedRanges: []`，没有展示条目形状；Skill 也只列顶层字段。这使模型容易沿用其他结构中的复数 `sourceRanges`。
- `C1-REAL-01` 已修复：DB-only 章节生成/修订工具结果不再只局部 patch 章节快照，而是统一刷新当前项目运行态，使 pending 正文和采用/丢弃动作 DTO 同步可用。浏览器回归已改为真实页面输入“生成当前章节”后不刷新立即点击“采用草稿”。
- `C1-REAL-02` 已修复：B2 Prompt 与 `script-import-normalize` Skill 明示 `excludedRanges[].sourceRange` 单数条目结构、category 闭集和空数组规则；真实 `self/gpt-5.5` 已成功生成 2 章分析候选。
- 真实复验又暴露两类 B4 Prompt 边界：验证器曾把有原文证据的“压迫氛围”等辅助字段归纳误判为新增剧情；materialize 曾把磁带标签文字重分类为人物对白。现已分别明确“可证据支持的结构化归纳允许”和“标签/录音/档案等必须保持载体”的规则，并补 Prompt 回归。
- 最终真实导入批次为 `completed`：两章均形成 `origin=import` 正式版本，pending 清零；第一章在第 2 次尝试通过，第二章在修正规则和真实硬问题重试后第 4 次通过，证明失败项可单章重试且不会修改已确认章节。

# web_search


# goal

在不影响现有项目的前提下，先完成真实浏览器双路线验收，再修复 `C1-REAL-01` 草稿状态同步和 `C1-REAL-02` 导入分析字段契约问题；补回归测试并重新执行真实用户路径。

# approach

- 使用独立临时数据库和工作目录启动本地应用。
- 创建两个互不污染的新测试项目，分别覆盖 AI 创作和已有剧本导入。
- 优先验证正式章节形成；条件允许时继续验证进入剧情结构的汇合点。
- 用户已授权修复。修复阶段使用诊断闭环：先建立失败测试，再最小修改 Web 状态同步与 B2 Prompt/Skill 契约，最后重跑定向、全量与真实浏览器路径。

# question


# todo

- [x] 启动隔离的本地应用并确认真实模型可用
- [x] 新建 AI 创作测试项目并完成核心路径
- [x] 新建已有剧本测试项目并执行核心路径（B2 发现真实阻断）
- [x] 验证正式章节进入剧情结构的汇合点
- [x] 整理结论和失败样例
- [x] 为 `C1-REAL-01` 增加“页面生成后不刷新立即采用”的浏览器回归
- [x] 为 `C1-REAL-02` 增加 `excludedRanges[].sourceRange` Prompt 契约回归
- [x] 完成状态同步、B2 字段形状和 B4 忠实度边界修复
- [x] 重跑真实浏览器双路线并更新最终结论
- [x] 执行类型检查、构建、单元测试和完整 DB-only Playwright 矩阵

# result

最终真实模型浏览器验收结论为 `ACCEPTED`。

- AI 创作路线：在新建隔离项目中，大纲确认不偷跑正文；用户明确输入“生成当前章节”后真实模型生成 pending；不刷新立即点击“采用草稿”成功，正文进入可编辑 Working Copy，pending 清零。
- 已有剧本路线：页面粘贴完整两章原稿后，真实模型成功生成观察性大纲和 2 章目录；用户一次确认目录后系统创建全部章节入口并后台逐章整理/验证；失败章可单独重试；两章最终均由用户逐章点击“确认章节”形成正式 `origin=import` 版本，并各自允许进入剧情结构。
- 忠实度门在真实样例中同时证明了正确放行和正确拦截：合理氛围归纳不再误判；“要求交出”被写成“阻止交出”、标签文字被改成人物对白仍会阻断。
- 隔离数据证据：导入批次最终 `completed`，2 个 item 均 `confirmed`，2 个正式版本均 `origin=import`，pending 数为 0；AI 项目 Working Copy 为 `dirty`、正文 5825 字符、pending 数为 0。
- 自动回归：typecheck、E2E typecheck、build、E2E prepare 通过；DB-only Playwright 完整矩阵 11/11 通过。全量单元测试首次 641/643，两个与本改动无关的备份测试因并行负载触发固定 5 秒超时，隔离复跑 2/2 通过。
- 真实浏览器使用独立 SQLite/workspace 和两个新项目；现有应用、现有项目数据库与用户正在运行的 OpenCode 服务未被修改。测试临时目录和隔离进程已清理。
