# finding

- 用户报告候选图工作台点击“生成候选图”后报错；页面仅显示 `Internal server error`，任务表没有新增 `image_generate`，因此失败发生在任务创建阶段，未调用付费图片 Provider。
- 使用生产 SQLite 一致性副本复现同一任务，准确异常为 `Prisma P2003`，发生于 `generationTaskSource.create()`。
- 当前确认 Preflight `aa01...` 于 2026-07-19 12:27 创建，仍引用旧 `scene_01` 场景视觉 `ef64...` / Asset `2a28...`；旧 Asset 因物理文件不存在于 12:37 被标记为 `missing`。
- `scene_01` 已于 13:07 成功生成新视觉 `eaec...` / ready Asset `d83d...`，但当前 Preflight 没有因此变 stale；候选图任务 Guard 继续从旧 Preflight source snapshot 密封旧 `scene_visual`，SQLite source trigger 正确拒绝 missing Asset。
- `GET production-state` 仍错误报告 `preflight.freshness=current`、`image_preflight=done`、`image_candidates.canStartTask=true`。根因是 Preflight freshness / NewWorkGate 只校验 Storyboard 版本与快照自身 digest，没有把当前角色/场景视觉 ID、Asset 状态与快照重新比对。

# web_search


# goal

定位当前真实项目候选图生成失败的准确阶段和根因，给出有证据的修复方案；未经确认不新增付费图片请求。

# approach

读取页面错误、任务队列记录、服务端日志、任务输入与生成规格，区分门禁失败、参考资产失败、模型配置失败和供应商请求失败。

# question


# todo

- [x] 捕获页面现有报错与失败任务
- [x] 核对任务输入、尝试记录和服务端日志
- [x] 定位根因
- [x] 视觉素材变化/缺失时让 Preflight stale
- [x] 任务创建前返回受控 409，且不新增任务
- [x] 补 shared、DB 集成、全量回归与真实页面无付费验证

# result

已完成根因修复：production-state 与 NewWorkGate 共同基于当前出镜角色、引用场景、Asset 可用状态和画风重建 live Preflight source snapshot；视觉变化会让旧 Preflight stale，候选图任务在落库前返回受控 409。重新确认最新出图准备后允许任务入队。Shared 167、Server 770、Web 类型检查、DB 集成与真实浏览器路径通过；未调用图片 Provider，未生成图片。用户真实页面已停留在第 4 步“出图准备”，等待用户自行确认。
