# finding

- `story/story_draft.source.txt` 当前仍在 `ProjectsService` 中被读取、创建和写入。
- 当前章节模型已经稳定为 `chapters/{chapterSlug}/script.md`，继续维护 `story/` 会形成双事实源错觉。
- 文档事实源中仍有“旧草稿兼容读取”和 `story/story_draft.source.txt` 说明，需要同步移除。

# web_search


# goal

移除 `story/` 旧兼容路径，让章节剧本只以 `chapters/{chapterSlug}/script.md` 为事实源。

# approach

代码上停止读取、创建和写入 `story/story_draft.source.txt`；文档上改为明确 `story/` 已废弃，新项目不再生成，当前章节脚本是唯一剧本文本入口。

# question


# todo

- [x] 扫描旧兼容引用
- [x] 修改后端读写逻辑
- [x] 更新事实源和完成记录
- [x] 运行验证

# result

已移除 `story/story_draft.source.txt` 旧兼容路径：

- 后端不再读取、创建或写入 `story/story_draft.source.txt`。
- 新建项目只生成 `project.json`、`chapters/chapter-001/chapter.json`、`chapters/chapter-001/script.md` 以及 `assets/`、`tasks/`、`exports/` 等目录。
- `WorkbenchSnapshot.story.id` 的兜底值从 `story_draft` 改为 `chapter_script_draft`，避免继续沿用旧命名。
- 已更新 AI 上下文入口、核心数据模型、素材文件契约、模块总览、长期记忆和完成记录。

验证：

- `corepack pnpm typecheck` 通过。
- `git diff --check` 通过。
- 临时 server 创建项目后确认不再生成 `story/` 目录。

遗留风险：如果磁盘上已有旧 `story/` 目录，本次不会主动删除用户文件；只有旧 `story/story_draft.source.txt` 而没有章节目录的早期项目也不再自动迁移。
