# finding

- 当前代码已经有 6 步路由和 `WorkbenchSnapshot.stages`，但后端原先硬编码只有第 1 步 active，前端流程栏也只允许第 1 步点击。
- 章节状态已经具备 workflow 推导基础：`draft -> project_story`、`script_done -> story_structure`、`structured -> storyboard`、`storyboard_done -> image_candidates`、`images_done -> layout_export`、`layout_done/exported -> asset_package`。
- 章节作用域任务已经强制 `chapterId`，因此 workflow 可以先作为项目级流程视图，聚合当前章节下一步和每步证据路径，不替代 `Chapter`。
- 适合借鉴 Aurora 的是“固定 workflow 状态文件 + 快照驱动 UI + 后续对话框消费状态”；不适合照搬的是完整 Docker/OpenCode 工具链和 agent 直接改文档。

# web_search


# goal

把 AuroraPlatformWeb 的 workflow 思路结合到 AI漫游现有章节/步骤拆分中；本轮先处理 workflow，不展开对话框应用/插入闭环。

# approach

先读取当前步骤、章节、任务和工作台快照契约，再决定 workflow 应作为项目级状态源、章节级进度还是两者结合。优先复用现有 6 步与 `chapterId` 方向，不照搬 Aurora 的完整运行时。

# question


# todo

- [x] 梳理现有步骤、章节和任务契约
- [x] 设计 AIRoaming workflow 最小模型
- [x] 更新共享契约与后端快照/落盘
- [x] 更新前端流程栏读取 workflow 状态
- [x] 同步文档与验证

# result

已接入轻量 `ProjectWorkflow`：共享契约定义 6 步、状态、scope 和完成标准；后端按当前章节状态生成 `WorkbenchSnapshot.workflow` 与兼容 `stages`，并写入 `workspace/projects/{projectId}/workflow.json`；前端流程栏按 workflow 状态显示并只允许进入已完成步骤或当前 active 步骤。本轮未继续改对话框应用/插入闭环。验证通过：`corepack pnpm typecheck`、`corepack pnpm build`、`git diff --check`，并用现有后端接口检查了 workbench snapshot 与本地 `workflow.json`。
