# finding

- M6-A1 已在提交 `e195cb3` 收口隔离证据；R0-A 已补齐主要 production entry 代码与隔离门禁，但真实 domain runner chain/故障回滚/双 Review 未通过，当前总体状态为 `production_entry_changes_requested / real_cutover_no_go`。
- G1 正式验收清单的真实 C0～C7、SH-10 人工签署与 OBS-01～10 仍未执行；旧 `real_cutover_handoff.md` 和总执行状态仍保留撤回时期的过期状态，不能直接作为运行入口。
- G4 候选定稿返修与 G5 成稿编辑器仅有 accepted 开发文档，功能实施仍未开始；按现有依赖必须在真实 DB-only 切换与观察期后再推进。
- 用户已授权开始执行上方 R0-A；当前仍只在临时根/注入 fake executor 下开发和测试，不访问真实路径、Keychain、数据库或执行停写。
- `MacOSKeychainSecretStore.put()` 使用 `security ... -U -w`，`-w` 为最后选项；secret 仅通过 child stdin 的两行 password/confirmation 进入。生产 adapter 不接收 keychainPath，隔离 smoke 通过临时 HOME 的 disposable 默认 keychain 完成。
- C0 与 AUTH-C1 顺序已纠偏：plan 先冻结，C0 无授权只读落证，AUTH-C1 只能绑定 C0 passed evidence，随后才允许 C1～C4。
- runner 链验证出 plan 的数据库路径必须位于 targetDataRoot 内才能满足 backup path safety；已统一 PlanService 与 BackupService 约束，并由 C3 创建空 0600 SQLite 文件后执行 migration。

# web_search

- 2026-07-14 核对 macOS `security add-generic-password -w`：本机 `security add-generic-password -h` 明确要求 `-w` 为最后选项才能进入提示；Apple `getpass(3)` 说明优先读 `/dev/tty`，不可访问时才回退标准输入。因此 `-U -w` 参数顺序代码证据已充分，但服务进程的真实 TTY/pipe 行为仍需获授权的隔离 macOS smoke 验证。参考：Apple Security 帮助与 `getpass(3)` man page。


# goal

实现 R0-A production entry，并在双 Review 前停住；不执行真实切换。

# approach

按 `$deep-think` 先做源码/CLI 与契约差异复核，再生成五份主资料和三件套；同步旧状态文档。R0-A 只允许 Luna 在临时根实现生产入口，包含 Keychain 进程边界、严格 evidence/activate 和 production runner；R0-B/R1/R2 继续等待新授权。

# question

- R0-A 完成后，仍需用户决定是否授权 R0-B 的真实只读发现、release-specific shadow 和 SH-10；本轮没有该授权。

# todo

- [x] 核对 M6-A1 最终状态
- [x] 核对真实切换与观察期未完成项
- [x] 核对 G4/G5 前置顺序
- [x] 给出下一步建议和停止边界
- [x] 核对 production final/ready/activate/cutover CLI 真实可执行性
- [x] 编写五份主施工资料和 deep-think 三件套
- [x] 同步总状态、G1、路线图与 M6-A1 isolated/production 边界
- [x] 完成文档静态复核与最终交付
- [x] 实现非 argv Keychain boundary、credential verifier、settings deferred/prestage
- [x] 实现 strict runtime profile、plan/evidence store、activate required flags
- [x] 接入 db:cutover C0～C7 domain action 入口（C4 含 settings CAS 脱敏）
- [x] 补齐 C7 completion seal/reconcile、strict runtime profile、CLI 前置拒绝与证据负例
- [x] 完成 R0-A 定向 88 测试、服务端全量 453 测试和静态门禁
- [x] 抽取 `cutover-runner.service.ts`，补齐 C1/C3/C4/C5/C7 注入 seam 与故障注入（14 spec、106 tests 定向通过）
- [x] 补齐两个 fresh domain C0～C7 runner chain（server 全量 69 spec、471 tests）
- [x] 独立复核前的 production SecretStore/path safety 审计与 RCUT-SEC-08、RCUT-SEC-11/12、RCUT-PATH-01/02/03、RCUT-EVD-09、RCUT-C0-01、RCUT-C3-ROLLBACK 修复（定向 14 spec、106 tests；SecretStore/runner 19 tests；全量 69 spec、473 tests）
- [x] 完成独立 Scrutiny/Runtime Review（Runtime `passed_isolated`；Scrutiny `changes_requested`，保留 Keychain 平台 smoke gate；SH gate 已降级为 R0-B 真实前置）

# result

R0-A 上一轮独立复核已完成；随后发现并修正 macOS `security` 参数顺序（`-w` 必须最后），增加显式 disposable `keychainPath` 注入与 `RCUT-SEC-12`，并修正 disposable probe 为只读 reserved-account lookup（missing 视为可达；生产默认仍 default-keychain）。修复后 SecretStore/runner 定向 19 tests、服务端全量 69 spec/473 tests 通过，typecheck、server/web build、Prisma、G1、capability `blockedIds=[]`、`git diff --check` 全部通过。代码风险已关闭，但真实服务进程到 Keychain 的 TTY/pipe 行为仍待获授权隔离 smoke，因此 Scrutiny 继续保留平台证据门。

已关闭 C3 失败后的新建 DB/dataRoot 残留；C0 现在强制校验绑定 plan identity 的 shadow gate、SH-01～SH-10 evidence digest、MigrationReport digest、人工 reviewer 和 gateDigest。SH gate 已降级为 R0-B 真实 gate，P1-C 已关闭；P1-A 的参数顺序和 disposable path 已关闭，但真实服务进程 TTY/pipe 行为仍待获授权隔离 smoke。真实系统操作次数为 0，未生成 AUTH，不进入 R0-B/R1/R2。
Luna 重审已确认：`-w` 参数顺序代码问题关闭，但仍需获授权 disposable-keychain 的 put/get/delete/probe 端到端 smoke 才能关闭平台证据门；当前禁止真实 Keychain，继续停止。
最终版又增加显式绝对 `keychainPath` 注入并通过 RCUT-SEC-12；Luna 重跑全量 69 spec/473 tests（151.45s，exit 0），Scrutiny 仍仅保留真实 macOS TTY/pipe smoke 门，Runtime 为 `passed_isolated`。
随后修正显式 disposable 路径的 probe：改为只读 reserved-account lookup，不再给 `default-keychain` 传 positional；定向 2 spec/19 tests 通过，Luna 独立检查确认无新增阻塞。

2026-07-14 获授权执行 disposable Keychain 隔离 smoke：临时 HOME + 临时 keychain 下 put/get/delete/probe 全部通过，fingerprint 匹配，删除后返回 `SECRET_STORE_ENTRY_MISSING`，secret 不出现在 argv/stdout/stderr；真实默认 keychain 与搜索列表前后不变，临时 HOME/keychain 已删除，未生成 AUTH。最新服务端全量为 69 spec/472 tests，typecheck、server/web build、Prisma、G1、capability `blockedIds=[]`、diff check 全部通过。

早期错误的路径参数探测曾把路径当作 `-w` 密码，短暂创建合成 probe 条目；已按精确 account/service 删除并复核默认 keychain/search list 不变，未使用真实凭据。随后移除 keychainPath API。平台 smoke 证据已补齐，Luna 已基于最新工作树独立复核并将 Scrutiny 更新为 `passed`、Runtime 保持 `passed_isolated`；真实 C0～C7、SH gate、AUTH、真实凭据和真实数据仍禁止执行。
