# finding

- 上一轮累计完整删除 27 个文件、11,769 行，并移除旧 reset/整本导入/重复 cutover 协调器。
- 本轮重建 package script、Nest 装配、生产 import、Web API、CLI、恢复/迁移和测试引用后，又完整删除 4 个孤儿文件，并收缩旧候选锁、同步参考图、测试专用 facade、失效 readiness wrapper、共享旧别名及前端无调用方法。
- 当前累计完整删除 31 个代码文件；工作树代码路径为 `+848/-14,358`、净减 13,510 行。Server TypeScript 76,376 行，其中生产 52,146、测试 24,230；Web TS/Vue 22,730 行；Shared 14,042 行。
- fresh SQLite 仍为 53 张业务表、242 个有效 trigger、17 个 migration；trigger 承担关系同域、不可变历史、状态迁移、租约、Outbox 与激活门禁，不属于生成器遗留。
- 零生产引用复核剩余项均有明确责任：测试夹具/契约、隐式 `toJSON`、worker 测试注入，以及项目删除最终 purge。后者虽必须保留，但标准启动未找到通用 Outbox 消费与 purge 调度，是独立运行时接线风险。
- DB E2E 全矩阵初次 12/15：两个剧情结构失败来自本轮前既有的 OpenCode `info.structured` 改造与旧假服务返回格式不一致；候选图用例单跑通过，整套失败来自假服务请求历史未按测试隔离。三项均非删代码回归，随后获用户授权修复测试设施并复跑 15/15。

# web_search


# goal

复核上一轮是否还有清理遗漏；对生产入口、CLI、Nest 注入、前端调用、测试专用和恢复职责建立闭合证据，发现明确死代码则继续收缩，不能安全删除的项给出具体阻塞原因。

# approach

先做入口和导入图审计，再核对 file-mode 分支及历史恢复责任；只处理无生产、恢复、历史读取和有效测试责任的代码。

# question

- 用户明确决定暂不修复项目删除 Outbox 消费与最终 purge 调度；该项作为已接受风险，不进入后续修复范围。

# todo

- [x] 重建所有生产与离线入口清单
- [x] 扫描零生产调用和仅测试调用文件
- [x] 复核 file-mode 路由/前端 fallback/对话分支
- [x] 复核迁移、备份恢复和历史只读兼容
- [x] 清理明确遗漏并完成验证，输出保留理由与独立风险

# result

按“无生产入口、无动态装配、无恢复责任、无有效测试责任”四项同时成立的判死标准，明确死代码已经清完。未删除项不是遗漏：file runtime 兼容需整体退役决策，migration/trigger/backup/importer 是现行恢复和数据库不变量，项目删除 purge 是尚缺运行时调度的必要实现。

验证：工作区 typecheck/build、E2E typecheck、Prisma validate、diff check 通过；Shared 167/167、Server 755/755、file E2E 4/4。DB E2E 的 3 个非清理失败已在 E2E 假服务/断言层修复，完整矩阵 15/15，未改动生产 OpenCode 工作。

后续决策：项目删除调度缺口暂不修；OpenCode 固定结构假服务合同和候选图请求历史隔离已修复。
