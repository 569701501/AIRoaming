# finding

- 标准启动已固定为 DB-only，首次业务写已发生；file runtime 仅允许迁移、恢复或测试显式启用。
- 本轮需区分可删除的日常 file 写入口、仍承担历史读取/迁移输入的 adapter，以及仍承担协调恢复的 cutover 组件。
- 项目级 reset/impact-preview 无前端真实调用，DB 模式只会拒绝；旧目录清理助手只由该链使用，已整体删除。
- callback 版 `CutoverCoordinator` 无生产入口，已被 `DbCutoverService + CutoverEvidenceStore + cutover-runner` 替代；真实 C0-C7 演练已迁至正式证据格式。
- snapshot/final importer/ready/activate/backup/restore/archive、file bridge guard 与 Asset 文件写入仍承担迁移、恢复或运行责任，保留。

# web_search


# goal

审计标准 DB-only 启用后残留的 file-mode 写入口与一次性 cutover 编排，删除无生产入口、无迁移恢复责任、无历史读取价值的最小安全集合。

# approach

先建立入口、调用方、运行模式、恢复责任和测试责任矩阵；只删除证据闭合的遗留代码，不修改历史 migration、标准数据库或只读 archive/backup。

# question


# todo

- [x] 读取切换与恢复事实源，建立候选清单
- [x] 核对生产启动、CLI、Nest 注入和测试调用
- [x] 删除最小安全集合并更新文档
- [x] 完成静态、集成和适用的运行复核

# result

完成：连续清理累计完整删除 27 个文件、11,769 行，并删除存量文件中的旧入口/类型/门禁；无数据库变化。静态复核 `passed`，隔离运行复核 `passed_isolated`。全量并发唯一 5 秒恢复用例超时，隔离复跑通过。
