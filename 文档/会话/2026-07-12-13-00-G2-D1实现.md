# finding

- C1 Story current 已可作为 D1 source；0009 Storyboard confirm trigger 要求无 Story pending、Script clean、无 ChapterScriptPending。
- `StoryboardVersion`、`Shot`、`StoryboardShotProjection` 和 `StoryboardShotCharacter` 已在 G1 schema 中存在；V2 codec 与 stableShotId 已在 Shared。
- D1 已实现 DB-only pending CRUD、stable Shot create/replay、projection、confirm/retire、API facade/routes；未实现 shot worker/Candidate/Preflight/history/capability switch。

# web_search


# goal

- 完成 Storyboard 手工 Working Copy/confirm 路径并验证 source gate、stable Shot、projection、retire、CAS/replay。

# approach

- Board pending source 固定绑定 Story current id/digest；stable Shot 使用 `g2-shot-v1` 命名空间算法。
- confirm 先重建 pending projections，再 formalize Board、切 current pointer，并对 current projection 差集做 active→retired。

# question


# todo

- [x] D1 Shared contract
- [x] StoryboardVersionRepository
- [x] API facade/routes
- [x] fresh SQLite/source gate/stable Shot/replay/retire tests
- [x] scrutiny/handoff/docs

# result

D1 已完成并通过全量验证；下一阶段为 E1 ProductionState/Workflow/NewWorkGate，shot worker、Preflight 和 history 仍未实现。
