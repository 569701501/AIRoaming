# finding

- `buildCharacterReferencePrompt`（`apps/server/src/projects/reference-prompt.util.ts`）第 5 个参数原为 `version`，本任务在其后新增第 6 个参数 `hasReferenceImage = false`，调用侧必须显式传 `"v2"` 才能传第 6 参（`renderOpenCodePromptTemplate` 会校验模板变量必须全部由调用方提供，缺变量直接抛 `OPENCODE_PROMPT_TEMPLATE_VARIABLE_MISSING`）。
- 模板唯一事实源：`apps/server/opencodeAI/skills/image-reference-generate/references/`，6 个角色模板（preview/final × v1/v2 + creature/group）均新增 `{{ADDITIONAL_NOTES}}` 行（位于性格气质与 promptFragment 之间）。
- 调用侧：`character-reference.service.ts` 两处（`generateCharacterReference` 的 file 路径、`queueCharacterReference` 的 DB 路径）；`image-prompt-baseline.util.ts` 基线编译不传该参（默认 false，基线 fixture 无参考图概念）。
- 有参考图判定：`anchorAssetId || primaryReferenceAssetId || previewReferenceAssetId`（新增私有方法 `hasCharacterReferenceImage`）。
- `ReferencePromptDefaults.appearance` 默认值 = "根据项目风格补全，但保持简洁稳定"。
- 本机 Node 20.20.2 无 `node:sqlite`，`project-db-persistence.integration.spec.ts` 与 `document-library.integration.spec.ts` 为预存在环境失败（stash 验证与改动无关）。

# web_search

无。

# goal

- 有参考图时角色提示词简化为 `参考所提供的角色图片，保持面部特征、发型和整体气质一致`，appearance 降级为 `补充细节：{appearance}`（ADDITIONAL_NOTES）。
- 无参考图时提示词保持原样（APPEARANCE = appearance || 默认值，ADDITIONAL_NOTES = ''）。
- 验收：单测覆盖有图/无图场景；typecheck + 相关测试全绿。

# todo

- [x] 修改 buildCharacterReferencePrompt：新增 hasReferenceImage 参数 + APPEARANCE/ADDITIONAL_NOTES 逻辑
- [x] 6 个角色模板新增 {{ADDITIONAL_NOTES}}
- [x] character-reference.service.ts 两处调用传入 hasReferenceImage（anchorAssetId || primaryReferenceAssetId || previewReferenceAssetId）
- [x] reference-prompt.util.spec.ts 新增 4 个用例（有图+有 appearance / 有图无 appearance / 无图原样 / 定稿有图）
- [x] typecheck + 单测全绿（12/12；projects 目录 266 passed，2 个 node:sqlite 环境失败为预存在）
- [x] 留痕：会话记忆、功能完成记录、MEMORY.md

任务完成：实现参考图优先提示词。改动：reference-prompt.util.ts（核心）、6 个模板 md、character-reference.service.ts（2 处调用 + 判定 helper）、reference-prompt.util.spec.ts（+4 用例）。风险：creature/group 模板同样使用人形化的参考图文案（"面部特征、发型"），任务文案为全局指定；image-prompt-baseline 未感知参考图（fixture 无此维度）。
