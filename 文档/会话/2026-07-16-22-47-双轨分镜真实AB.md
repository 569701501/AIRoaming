# finding

- V1 精确代码基线为 `c3fe684`，V2 生产提交为 `5551271`。
- A/B 将创建 AI 创作、已有剧本导入两个新逻辑项目，并在正式结构确认后复制数据库，保证同路线只有分镜 Prompt 不同。
- 图片持久任务 worker 全程关闭；不调用图片、视频、TTS 或字幕 provider。
- AI 基线项目 `dfb3aa62-6447-45bf-aee4-6aeea6476149` 已形成正式 StoryVersion `f00e48ce-e070-46d8-9ab7-7b20ac8f1515`。
- 导入基线项目 `76e071bd-7e97-4ed5-8de1-06ab590c9f51` 已形成正式 StoryVersion `34835ca7-ed9d-47ee-a087-53d09e5fb42c`。
- 短粘贴原稿的导入意图可能误路由；自然格式第 1 章又因摘要元数据被 P6 误判失败。最终 A/B 改用同故事标准完整两章剧本，确保比较变量仍只有分镜 Prompt。
- 四次真实分镜均完成：AI V1 24 镜、AI V2 17 镜、导入 V1 14 镜、导入 V2 10 镜；beat 覆盖、引用和 `promptDraft` 硬门全部通过。
- V2 两条路线都明显改善漫剧时间过程，但 AI V2 首次需一次修复，且少数长镜头负载偏高；正式结论为 `MIXED / V2_DIRECTIONALLY_BETTER`。
- 四个页面保持可打开，均是待确认草稿；图片任务 worker 关闭，付费任务执行数为 0。

# web_search

- 本轮复用 2026-07-16 已完成的外部对标，不重新联网搜索。

# goal

用真实 `self/gpt-5.5` 完成双路线 V1/V2 分镜 Prompt A/B，给出可追溯结论并保留用户可查看页面。

# approach

- 先走真实页面形成两个正式 StoryVersion。
- 冻结并复制 SQLite/workspace 到 V1/V2。
- 同触发语、独立模型会话生成四份分镜，比较硬门、漫画、漫剧和双轨边界。

# question


# todo

- [x] 建立两个正式基线项目。
- [x] 准备 V1/V2 双环境。
- [x] 生成四份真实分镜。
- [x] 完成对比、双 Review 和用户可查看入口。

# result

- 已完成双路线真实文本模型 A/B；判定 `MIXED / V2_DIRECTIONALLY_BETTER`。
- V2 不回退；下一步应先限制动态单镜负载并补多题材重复样例，不立即扩 Schema。
- V1/V2 隔离页面和数据库保留给用户查看，没有替用户确认分镜，也没有调用付费图片服务。
