# finding

- 用户反馈当前项目下已有错误导入的剧本，界面清不掉。
- 实际 workspace 只有一个项目 `b224e22c-9cdf-4195-851c-6bae207789c8`，项目名“测试 1”。
- 该项目被旧导入拆成 `chapter-001` 到 `chapter-075`，内容是《火柴推理》技术/玩法文档，不是真正剧本。
- 当前 UI 只能保存当前章节草稿；没有“清空全部剧本/重置章节”能力，因此无法一次清掉多章节导入结果。
- `writeProjectFiles` 写新章节时不会删除旧章节目录，若旧目录残留，服务重启会从 workspace 把旧章节读回来。
- 已新增 `POST /api/projects/{projectId}/script/reset`，清空时删除旧 `chapters/` 和历史 `story/`，重建空白 `chapter-001`。
- 已修改整本剧本导入写入逻辑：导入替换章节集合前先删除旧 `chapters/` 目录，避免旧章节残留。

# web_search


# goal

修复“错误导入的多章节剧本清不掉”问题：提供项目级清空剧本能力，并清理当前 workspace 中的错误导入章节。

# approach

新增后端 `resetProjectScript` 能力：删除项目 `chapters/` 下旧章节目录，重建空白 `chapter-001`，同步 `project.currentChapterId/sourceText/workflow`。前端在剧本编辑器提供“清空剧本”按钮，避免用户只能清当前章节。

# question


# todo

- [x] 复现并确认当前项目有 75 个误导入章节
- [x] 实现后端清空剧本 API
- [x] 实现前端清空剧本入口
- [x] 清理当前 workspace 项目
- [x] 验证和更新文档记忆

# result

已完成。当前项目“测试 1”已重置为一个空白 `chapter-001`，`script.md` 为 0 字节，`story/` 旧目录已删除。共享 DTO、后端 API、前端按钮、模块契约和完成记录已同步。
