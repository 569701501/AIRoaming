# finding

- `ProjectDialoguePanel.vue` 空对话开场里有一个用户可见预设按钮“分析剧情”，点击后调用固定 `analysisPrompt` 发送普通对话。
- `analyze_script_import` 是上传/粘贴剧本的导入前安全分析，不等于用户要删除的“分析剧情”快捷入口，需保留。
- `ProjectStoryPanel.vue` 仍有旧的 `AI 分析剧情` 按钮，但当前剧本页主实现已换成 `ScriptDocumentEditor.vue`；为避免历史组件再次挂载露出旧入口，同步移除该按钮。

# web_search


# goal

移除 AI 对话框里的“分析剧情”入口。验收标准：用户进入空对话时不再看到“分析剧情”按钮，对话开场不再引导剧情分析，导入前分析工具结果展示不受影响。

# approach

删除对话框开场 action、固定分析 prompt 和对应发送函数；保留普通输入、灵感种子、上传/粘贴剧本整理、大纲确认和章节改写。同步产品文档，把“分析剧情/AI 分析当前章节”从剧本阶段主动作和对话框能力中移除。

# question


# todo

- [x] 定位“分析剧情”入口
- [x] 移除对话框可见按钮和固定 prompt
- [x] 移除旧剧本面板里的历史分析按钮
- [x] 同步文档和长期记忆
- [x] 运行类型检查

# result

已移除 AI 对话框空状态里的“分析剧情”快捷按钮和固定分析 prompt，同时调整对话框开场文案，不再引导剧情分析。旧 `ProjectStoryPanel` 中未挂载的 `AI 分析剧情` 按钮也已移除，避免历史组件重新出现旧入口。保留 `analyze_script_import` 导入前分析结果展示。验证通过：`corepack pnpm --filter @airoaming/web typecheck`、`git diff --check`；并确认两个相关组件内不再出现“分析剧情 / AI 分析当前章节 / analysisPrompt / sendPreset”。
