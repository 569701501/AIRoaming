# finding

- 当前创建项目后至少写入 `project.json`、`chapters/chapter-001/chapter.json`、`chapters/chapter-001/script.md`，并会创建若干空目录。
- 后端 `writeProjectFiles` 还会保留旧兼容路径 `story/story_draft.source.txt`。
- 数据库持久化接入前，`project.json`、`chapter.json` 和 `script.md` 也是服务重启恢复项目索引的本地恢复源。

# web_search


# goal

说明当前创建项目后，workspace 项目目录里的文件和目录分别代表什么。

# approach

按当前代码实际写入和文档契约解释，区分项目级、章节级、兼容路径和未来生成产物目录。

# question


# todo

- [x] 读取当前记忆、代码和素材文件契约
- [x] 汇总当前项目文件含义

# result

已向用户说明当前创建项目后的文件/目录含义；本次没有修改代码或正式契约。
