# finding

- 当前分支为 `codex/g0-test-safety-net`，工作区在盘点开始时无未提交改动；最新提交为 `96c8845 feat(g2): complete persistent generation task pipeline`。
- G0 测试安全网、G1 数据库基座最小垂直切片、G2 A0-F4 持久版本链与四类任务 worker 已有代码、测试和完成记录。
- G2 尚未完成生产 DB-only 切换；真实 provider smoke test、独立 Outbox consumer、legacy importer、capability switch 仍后置。
- G3 文档已获确认，但端到端实现尚未开始：Shared 仍是三种旧版式，创建 DTO 未要求 `comicFormat`，创建弹窗只有项目名称，普通 PATCH 仍允许修改版式，文件读取仍会默认/映射旧值；数据库已有两值 CHECK，但尚未找到版式不可变 trigger。
- G4、G5 仍是开发级方案与验收资料，现有候选图和排版页面只是旧骨架，不满足新契约。
- `文档/05_执行与记录/路线图与里程碑.md` 顶部仍写“等待开发授权”，已落后于 G0-G2 实际提交；当前状态应以完成记录、AI 上下文入口和代码为准。
- 2026-07-12 现场复跑：Shared 36/36、Server 181/181、Playwright 4/4、E2E prepare 3/3、类型检查和 G1 schema/manifest/migration 三项检查全部通过。首次把测试与类型检查并行执行时，一条 5 秒迁移用例超时；隔离复跑和随后整套复跑均通过，属于测试时限/资源争用风险，不是稳定断言回归。

# web_search


# goal

核对当前项目的真实开发进度，区分“已有旧骨架、已完成新契约、仅文档确认、尚未生产切换”，并用代码、提交和测试证据给出下一步判断。

# approach

按 `$deep-think` 的 Scrutiny Review 角色只读核对分支、提交、事实源、完成记录、G3 入口代码与数据库约束；不在本次盘点中继续实现功能。

# question


# todo

- [x] 核对分支、提交和工作区状态
- [x] 核对 G0-G2 完成记录与残留边界
- [x] 核对 G3 文档与实际代码差距
- [x] 复跑当前自动化测试与类型检查
- [x] 输出阶段结论与下一步建议

# result

当前新升级路线真实进度停在 G2 F4：G0-G2 已有实现和绿色自动化证据，G3 是下一阶段且尚未正式施工；G4/G5 仍为文档。G1/G2 的 production DB-only、真实 provider 与迁移/消费配套尚未完成，不能描述为整套生产链路完成。
