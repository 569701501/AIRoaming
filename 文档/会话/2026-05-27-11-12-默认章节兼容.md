# finding

- 章节方案要求新项目默认创建 `chapter-001`，已有开发期项目如果存在 `story/story_draft.source.txt`，应迁移或兼容读取到默认章节脚本。
- 当前 `ProjectsService` 原本只维护项目级 `sourceText`，并只写入 `project.json` 与 `story/story_draft.source.txt`。
- 章节共享契约已提供 `ChapterListItem`、`ChapterDetail` 和 `WorkbenchSnapshot.chapters/currentChapter` 过渡字段，可用于后端快照返回默认章节。
- 由于兼容读取需要访问 workspace 文件，`getWorkbenchSnapshot` 已从同步方法改为异步方法，相关项目控制器和对话服务调用点已同步改造。

# web_search


# goal

实现后端创建项目时默认创建 `chapter-001`，并兼容读取旧 `story/story_draft.source.txt` 到默认章节。

# approach

先读取章节方案、素材路径契约和当前 `ProjectsService` 实现；然后只在后端项目服务内引入本地章节模型与文件写入/兼容读取，不改前端章节列表。当前旧保存接口仍保留，并同步写章节文件，保证现有 UI 可继续工作。

# question


# todo

- [x] 读取章节相关文档与当前后端代码
- [x] 实现默认章节创建和旧草稿兼容读取
- [x] 验证类型检查与 workspace 文件行为
- [x] 更新完成记录和长期记忆

# result

已完成后端默认章节接入：创建项目时生成 `chapters/chapter-001/chapter.json` 和 `script.md`，工作台快照返回 `chapters/currentChapter`，旧 `PATCH /api/projects/{projectId}` 保存 `sourceText` 时同步写当前章节脚本。兼容逻辑已验证：当默认章节脚本缺失但旧 `story/story_draft.source.txt` 存在时，读取工作台快照会返回旧草稿，并重新写出 `chapter-001/script.md`。验证通过：`corepack pnpm --filter @airoaming/server typecheck`、`corepack pnpm typecheck`、`git diff --check`，并用临时 server 做了 HTTP 创建项目和旧草稿迁移验证。
