# finding

- 已稳定复现：5 个场景中，第 1 个「棺旁的死线」有 Asset/SceneVisual 绑定，但图片请求为 500、浏览器 `naturalWidth=0`；其余 4 张均为 2816×1584 正常图片。
- 该 Asset 在 DB 中仍为 `ready`，但 `~/.airoaming/workspace/.../scene_01/background.webp` 已不存在。
- 历史任务证据显示 2026-07-19 09:44 左右曾重生成该场景：Provider 已返回图片，随后 `assets.storage_key` 唯一约束 P2002 失败。旧 worker 先覆盖固定路径，异常清理再删除同一路径，造成旧图物理文件丢失。
- 应用工作区、现有备份和用户目录中均未发现该摘要/字节数的可恢复副本。
- 检查和修复全过程未触发新的图片生成。

# web_search


# goal

复现并定位剧情结构页场景图不显示的根因；在不重新生成图片的前提下修复，并验证已有场景资产能稳定显示。

# approach

按故障诊断闭环执行：浏览器复现 → 核对页面数据与资产请求 → 先建立失败回归测试 → 改为 Asset 独立物理路径 → 缺失状态校正与页面兜底 → 全量和浏览器回归。

# question


# todo

- [x] 在用户当前项目的剧情结构页复现并采集页面证据
- [x] 核对 DB、快照投影、资产 URL 三层数据
- [x] 建立失败回归测试并修复根因
- [x] 运行定向测试、全量测试与真实页面回归
- [x] 更新契约、完成记录、会话记录和长期记忆

# result

- 新场景视觉写入 `scenes/{sceneKey}/visuals/{assetId}/background.webp`，重生成失败不会再覆盖或删除旧图。
- 物理文件缺失返回 `404 PROJECT_ASSET_FILE_NOT_FOUND`，Asset 自动从 `ready` 校正为 `missing`。
- 剧情结构页加载失败显示“场景图文件缺失 / 重新生成”；其余图片保持正常。
- 修正 Preflight `sceneChecks.referenceAssetId` 使用真实 Asset ID。
- 新增重生成保留旧图、缺失状态校正和 Preflight ID 回归；Server 全量、类型检查、Web 构建和真实浏览器回归通过。
- 当前项目第 1 张场景图原文件不可恢复，本次未自动重生成、未产生费用。
