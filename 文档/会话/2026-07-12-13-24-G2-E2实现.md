# finding

- E2 已新增 `SourceSnapshotBuilderService`、`PreflightRevisionRepository/Service` 和 DB preview/confirm API。
- SourceSnapshot 从 current Storyboard 引用聚合 Character/Visual/Asset、ChapterScene/Visual/Asset 和 Project style；严格 V2 codec 负责 ready/block 校验。
- Preflight confirm 使用 expected source ID/digest/Chapter rowVersion，在同一事务插入不可变 revision 并切 current；Storyboard replacement 后 resolver 返回 `PREFLIGHT_SOURCE_STORYBOARD_CHANGED`。

# web_search


# goal

- 完成 G2-E2 Preflight live preview、SourceSnapshot、不可变 revision confirm，并保留明确的 worker/TaskApplicabilityGuard 非目标。

# approach

- 复用 Shared source snapshot builder 与 PreflightDocumentCodec，不复制 strict JSON/JCS。
- DB repository 负责事务、CAS、replay；旧 file-mode ImagePreflight 路径不双写。

# question


# todo

- [x] SourceSnapshotBuilder
- [x] Preflight preview/confirm repository/service
- [x] API wiring and fresh SQLite stale/replay evidence
- [x] scrutiny/handoff/完成记录

# result

G2-E2 已完成并通过全量验证。下一阶段为 F/任务 runtime；persistent worker、TaskApplicabilityGuard、history、capability switch、Candidate/Layout/Export 仍未实现。
