# finding

- 用户接受后端规模审计建议，授权移除已经完成且无用的代码。
- 本批选择退役 G1 Markdown/DSL → manifest → Prisma Schema/migration 活跃生成链；保留历史 manifest、现有 Prisma Schema、0001～0017 migrations、release identity、runtime ledger、备份恢复和必要 SQLite 语义测试。
- 当前工作区已有一组 OpenCode/剧情结构未提交改动；本任务不触碰这些文件。
- ADR-0015 的 full shadow、final import、DB-only activate、协调 backup/restore 已完成；DB-only R2、后续 G4/G5 和用户签收均已通过。用户本轮明确授权收缩，故把“稳定发布周期”门槛对 G1 生成器退役判定为满足。
- 已删除 16 个生成器生产文件、7 个专用测试和 6 个 package script；必要 trigger 语义测试改为读取冻结 manifest，并固定校验其文件 SHA-256。
- 服务端 TypeScript 从 383 文件/89,426 行降为 360 文件/78,360 行；生产代码从 247 文件/62,912 行降为 231 文件/53,770 行。
- `schema.prisma`、历史 manifest、全部 migration 的删除前后 SHA-256 完全一致；标准库仍为 `db_only`，17/17 migration 成功，完整性检查为 `ok`。

# web_search


# goal

在不改变数据库字节、Schema、trigger、运行任务协议和用户路径的前提下，删除已完成使命且无运行时依赖的 G1 活跃生成器及专用测试/脚本，并保留独立的发布身份、迁移账本和 SQLite 契约保护。

# approach

先穷举生产/测试/脚本依赖闭包；把必要 trigger 语义测试改为直接读取冻结历史 manifest；再删除生成器闭包、更新事实源并执行全量静态与运行回归。

# question


# todo

- [x] 确认退役门槛与范围
- [x] 建立精确删除清单和保留清单
- [x] 退役生成器与专用脚本/测试
- [x] 验证 Schema、migration、trigger、构建和服务测试
- [x] 完成双 Review、文档与长期记忆

# result

G1 活跃生成器已完成退役，净减少 11,066 行服务端 TypeScript；数据库、trigger 和用户路径不变。全仓 typecheck、server build、Prisma validate、43 个保留数据库契约测试和 server 129 files/790 tests 全部通过。Scrutiny Review 与后端等价 Runtime Review 均通过。
