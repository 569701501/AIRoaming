# finding

- 当前正式链路已经具备 `LayoutDocumentV2`、Working Copy、Revision、Publication、来源 freshness 与确定性渲染，底层功能完整。
- `LayoutExportWorkspace.vue` 约 3972 行，同时承载编辑器、智能重排、两级对比预览、来源返修、预检、版本历史和出版任务，普通用户主路径被技术流程淹没。
- 当前 UI 暴露整章/选区/当前页段/当前场景、四种智能意图、缩略对比与完整对比、两次预检和四步发布；其中多项是同一业务意图的重复表达。
- 正式对白完整性只在 V2 Revision/Publication preflight 强制，错误 Working Copy 仍可被只读预览展示；指定测试成稿因此出现 16 条正式对白全部未绑定、唯一气泡无来源的情况。
- 需要保留的核心不是所有现有入口，而是来源事实完整、可编辑文档、人工修改保护、可追溯版本和确定性出版。
- ADR-0021 的 reconciliation、custom confirm、opaque draft token 与后续智能建议方向已被用户否决并由 ADR-0022 取代；本轮只扩展正式 V2 preflight 与现有 Web 导出编排。
- fresh initial 已重建为 9 段、11 镜头、19 条正式绑定；真实只读预览为 19/19。
- 单一导出已形成两次 ready Publication，并在 Revision/Publication 网络歧义和并发旧响应场景下保持精确重放与终态不回退。

# web_search


# goal

实施漫画成稿基础版：首次无感自动排版、现有画布人工调整、自动保存、单一导出；正式对白和旁白不得被 AI 遗漏或改写，用户主动文字变化在导出时弹窗确认。

# approach

- ADR-0022 取代 ADR-0021 尚未实施的复杂 Facade、修复 Proposal 和后续智能建议。
- 复用现有 initial dialogue coverage、Working Copy autosave 和正式 publication，不改数据库或 LayoutDocument wire shape。
- 通过 V2 preflight 区分系统文字错误与用户主动文字变化，再由一个导出入口内收版本和出版步骤。
- 具体画布编辑工具本轮保持现状，后续单独体验和决定。

# question


# todo

- [x] 核对当前事实源与错误成稿证据
- [x] 盘点现有成稿链路和界面入口
- [x] 用户确认基础版最终范围
- [x] 实现文字差异判定
- [x] 删除二次智能调整、撤销重做和手动保存入口
- [x] 接通单一导出与文字确认弹窗
- [x] 完成自动化、真实浏览器和静态复核

# result

已完成。最终结果：

- 普通路径固定为首次自动排版一次、人工画布、自动保存和单一导出。
- 系统文字错误阻断导出；用户主动文字变化展示镜头、说话者、原文和当前文字。
- Shared 257、Web 55、Server Layout 11 项通过，三端 typecheck 通过。
- 真实项目完成 19/19 正式文字预览、两次正式导出与产物生成。

正式执行记录见：

`文档/05_执行与记录/任务记录/2026-07-24_漫画成稿基础版收缩/`
