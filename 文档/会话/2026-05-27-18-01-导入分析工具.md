# finding

- 用户要求继续做功能，并显式使用 `$grill-with-docs`。
- 当前代码中 `DialogueService.tryImportScriptToChapters` 命中整理意图后会直接调用 `ProjectsService.importScriptToChapters` 写入章节。
- `ProjectsService.importScriptToChapters` 内部仍直接调用 `parseProvidedScriptChapters`，没有导入前分析。
- `packages/shared/src/dto.ts` 当前 `DialogueToolResult.tool` 只包含 `import_script_to_chapters`、`update_chapter_draft`、`generate_script_from_seed`；`status` 只包含 `succeeded/failed`。
- 文档已经要求导入前经过 `script-import-normalize` / `analyze_script_import`，并提到失败或需要用户确认时不写章节。

# web_search


# goal

继续实现“剧本导入前分析/校验”功能，先用文档和代码事实确认工具结果契约，再进入实现。

# approach

推荐把 `analyze_script_import` 做成正式受控工具结果，而不是普通 assistant 文本：它不写章节，但会返回结构化分析、失败原因或确认需求，便于前端和后续审计统一处理。

# question

- 已确认：上传剧本后，先让 AI/系统正式检查，通过了才写入；不通过就绝不写文件。

# todo

- [x] 核对当前文档与代码契约
- [x] 等用户确认 `analyze_script_import` 结果契约
- [x] 实现共享 DTO、后端分析逻辑和前端展示
- [x] 验证并更新文档

# result

- 已新增 `ScriptImportAnalysis`、`ScriptImportChapterPlan` 和 `analyze_script_import` 工具结果契约。
- 后端导入链路改为先分析，`ready_to_import` 才写章节；`reject` 和 `needs_user_confirmation` 不写文件。
- `needs_user_confirmation` 会在当前剧本线程中临时缓存待导入文本；用户回复“确认导入/确认覆盖”后再写入，回复“取消导入”则丢弃。
- 前端收到 `analyze_script_import` 只展示提示，不刷新章节；真正写入成功后才刷新工作台。
- 已更新核心数据模型、模块总览、剧本对话设计方案和功能完成记录。
- 验证通过：shared build、server typecheck、web typecheck、git diff --check；临时后端 API 覆盖非剧本拒绝、明确章节直接导入、无章节先确认再导入。
