# finding

- `script-import-normalize` 已覆盖 B1～B5 产品边界，但生产 B2 分析、B4 章节整理、B4 忠实度验证和一次格式修复正文仍位于 `dialogue-prompt.util.ts`。
- B2 使用 `import-analysis/1.0` 严格解析，并对长稿执行连续 block 分片、分层合并与最终完整分配校验。
- B4 整理输出由 Shared 严格章节 Markdown 解析；验证输出由 `import-fidelity/1.0` 严格解析。三阶段最多各执行一次只修格式的重试。
- 目录整体确认、全部章节入口创建、逐章后台整理、失败隔离、待确认只读和逐章“确认章节”均由现有服务负责，不应迁入 Prompt 或改变。

# web_search


# goal

把已有剧本路线三份稳定生产 Prompt 和格式修复合同归入 `script-import-normalize/references/`，让真实运行路径读取 Skill，同时保持 B1～B5、页面字段、Schema、API、任务状态和用户确认方式不变。

# approach

- 使用 `$deep-think` 控制跨分析、批处理和版本围栏的边界。
- 使用 `$skill-creator` 更新现有 Skill，不新增公开 Skill。
- 分开保留 B2 分析、B4 忠实整理和 B4 忠实度验证三个模型合同；动态来源、严格 Schema 示例、解析器、Validator 和重试上限继续由代码负责。
- 仅运行离线测试，不调用真实模型或付费媒体服务。

# question


# todo

- [x] 盘点真实导入链、Prompt、解析器和测试
- [x] 新增 Skill references 与发现元数据
- [x] 接入真实构造器并增加防回流测试
- [x] 完成离线验证与正式留痕

# result

- B2 分析、B4 忠实整理、B4 忠实度验证和一次格式修复已归入 `script-import-normalize/references/`，真实构造器已接入。
- B1～B5、页面、Schema、API、批处理和逐章确认均无变化。
- Skill 校验、定向 Server 60 项、Shared 29 项、全项目 typecheck/build 和 Server 全量 739 项均通过。
- 未调用真实模型或付费媒体服务。
