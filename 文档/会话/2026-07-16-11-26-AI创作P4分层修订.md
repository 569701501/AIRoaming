# finding

- 当前 `script-chapter-editing` 已能按用户要求返回完整章节稿，但只使用宽泛 Prompt 和宽松 `ensureChapterMarkdown`，没有严格格式重试、修订层识别或未请求字段保护。
- 编辑输入是页面当前编辑器最新文本；输出继续进入现有 AI pending，用户采用后才覆盖 Working Copy。
- 当前编辑链没有完整上一章正式正文，不能把本轮描述为 P5 编辑连续性完成。

# web_search


# goal

在不新增页面字段、数据库或确认步骤的前提下，把 P4 分层修订接入现有 `update_chapter_draft`：识别连续性、发展性、场景对白、文字四层，保护未请求事实，最多修订一次，第二次失败不创建 pending。

# approach

- 最高层优先：连续性 → 发展性 → 场景与对白 → 文字。
- 低层修改必须精确保护高层事实；高层修改只允许必要的下层联动。
- 使用严格章节 parser 验证输出；当前源稿可严格解析时再执行字段级保护，legacy 非标准源稿不伪造静态对比结论。
- 不输出层级、评分或诊断字段，页面仍只看到完整章节待确认稿。

# question


# todo

- [x] 分层识别与保护 evaluator
- [x] Prompt、Skill、一次定向修订
- [x] 聚焦与全量验证
- [x] 双 Review、正式文档、记忆和提交

# result

- P4 已接入现有 `update_chapter_draft`，页面和数据库不变。
- 低层越界、无效改写、建议误触发和二次失败均已 fail-closed；合格结果仍形成用户可采用的 AI pending。
- 聚焦 35/35、Server 单 fork 626/626、DB-only Chromium 1/1、类型、构建和 Skill 校验全部通过。
