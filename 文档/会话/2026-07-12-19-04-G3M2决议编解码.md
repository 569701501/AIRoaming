# finding

- 当前基线为 `131fbc2`，G3-M0/M1 已完成；sealed snapshot 已能提供 sourceManifestDigest。
- M2 只实现独立 comic-format mapper、MigrationIssue detail/resolution codec、decision artifact digest/校验和 CLI；不调用旧 file runtime reader，不实现 importer。
- 旧值规则固定：vertical_scroll/paged_comic canonical，page_horizontal 只读自动映射，four_panel/missing/invalid 必须 decision_required，不能 trim/lowercase/fallback。

# web_search


# goal

- 完成 G3-M2 decision codec 与迁移账本最小闭环，并留下 MAP-01～08、DEC-01～04 证据。
- 强制验收：server typecheck、server 全量测试、G1 三项 check；不修改 G3 enum/0010，不写入数据库。

# approach

- mapper 只接受 JSON-safe legacy value，输出稳定 safe preview、issue code 和 layoutPresetIntent。
- decisions artifact 使用共享 `digestCanonicalJson`，排除自身 digest；严格拒绝未知字段、重复/乱序 entry、旧 source digest 和非法 intent。
- issue codec 生成稳定 detailJson/resolutionJson；report codec 只做脱敏摘要，不产生 MigrationRun 写入。

# question


# todo

- [x] 建立 G3-M2 任务记录并读取决议/账本事实源
- [x] 实现 comic-format mapper 与 issue detail/resolution codec
- [x] 实现 decisions artifact、report digest 与 check CLI
- [x] 补齐 MAP/DEC 测试并运行全量验证
- [x] 静态复核、交接记录并提交 G3-M2

# result

G3-M2 已实现并完成验证，待提交本轮代码和文档；下一切片为 G3-M3 importer run/ledger，必须在 fresh DB 上接入全量导入顺序。
