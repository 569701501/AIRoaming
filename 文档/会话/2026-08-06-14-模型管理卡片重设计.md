# 2026-08-06 模型管理卡片重设计

# finding

- 模型管理位于 `apps/web/src/components/settings/AppSettingsView.vue`（models tab），弹窗 `AddModelModal.vue`。
- 后端与 store 早已支持 `updateManagedModel`(PATCH)，旧 UI 无编辑入口，本次补上。
- 无任何测试锁模型管理 DOM（grep model-item/model-list/AddModelModal 仅命中组件自身），可自由重设计。
- 项目约定：禁用 window.confirm，二次确认用自定义 UI；本次用「点击删除→3 秒内出现确认删除？按钮」内联两步确认，避免新增弹窗组件。
- In-app browser（iab）本次会话不稳定：broker response id mismatch、截图 capture failed；改用仓库 playwright(`node_modules/.pnpm/playwright@1.61.1`)临时脚本截图，输出 `output/ui-check/`。
- dev 实例已在跑：web 5173 / api 4310，真实配置数据（xAI Grok 对话 active、DeepSeek 未配置密钥等）。

# web_search

无

# goal

重设计设置页模型管理样式：卡片式 + 显式操作（用户从 3 个方案中选定）。
验收：①激活改为显式「设为当前」按钮；②当前生效卡片一眼可辨（渐变亮边+使用中徽章）；③未配置密钥有警告态；④补编辑入口（预填+PATCH+清除密钥勾选）；⑤删除有二次确认；⑥typecheck + web 契约测试绿；⑦真实页面截图复核。

# todo

- [x] 卡片列表重构（模板+样式，两类模型 v-for 分组渲染）
- [x] AddModelModal 编辑模式（预填、隐藏类型 tab、保存修改、清除密钥勾选）
- [x] vue-tsc 通过；web 契约 55/55
- [x] Playwright 截图复核：列表/编辑弹窗/删除确认三态正常
- [x] 会话记忆与完成记录留痕
