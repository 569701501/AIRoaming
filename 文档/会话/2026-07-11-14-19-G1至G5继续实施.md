# finding

- 用户指出上一轮错误地把 G0 阶段完成当成整个目标完成；新 Goal 已明确恢复为完成 G0～G5 全部开发波次，G0 保留，从 G1 开始继续。
- 当前分支 `codex/g0-test-safety-net` 工作树干净，G0 主体提交为 `185b83c`，收口提交为 `a7430b2`。
- G1 三路只读审计确认：当前仍是 file-mode/内存态，Prisma 只有 6 个未接线模型，G1 功能实现基本为 0。
- 旧 scoped ID 在不同项目间必然重复；G1 importer 必须按 `projectId + entityType + legacyId` 稳定重键，并用 `ImportedEntitySource` 保留旧 ID/路径/摘要。
- G1 必须先补 workspace/dataRoot/fake SecretStore 三根隔离夹具；现有 G0 E2E 不能直接冒充 G1 SecretStore 验收。
- G1-0 已完成：测试进程使用 run-bound workspace/data/fake SecretStore 与临时 HOME/XDG，临时 SQLite、严格环境 allowlist、canonical/marker/symlink/runtime-state 栅栏和可信 Chromium 路径；主 Agent `test:all` 全绿且真实目录/PID 不变。
- G1-1 M0-A 首版 Schema 实施契约已展开 44 模型与 0001～0008，但双重只读审查拒绝签收：CHECK/trigger manifest 只锁名称可被空壳自证，projection/source binding/ready Asset/ExportArtifact/Outbox/Task 等存在直写绕过，legacy source nullable、DialogueToolResult.status、PersistenceState 激活边界与后续 overlay 也尚未自洽。未放行 migration SQL。
- Pass 1 已从子 Agent 额度中断点由主 Agent 接管完成：同步 G1/G2/G4/G5 事实源，引入 Task source/Layout binding 单向 seal、G1 base + stage overlay、完整 CHECK/trigger manifest schema 和受控 purge。Prisma 6.19.3 E0 临时探针证明显式 `FK OFF -> BEGIN IMMEDIATE -> rebuild -> COMMIT -> FK ON` 可行且 COMMIT 前故障原子回滚；新 SQLite 连接 FK 默认仍为 0，必须逐连接启用/验证。
- 二审后又闭合 Persistence/ever-ready Asset、Task/Attempt/Slot heartbeat+finish、Outbox/Credential/purge 与 formal projection 时序；补 `TaskSourceProjectionV1/RegistryV1` 和 Story/Storyboard V1/V2 path registry。最新仍是 44 模型/555 字段，Schema 实现仍保持 Red，第三轮双审未签前不进 Pass 2 或 migration。
- 用户已改变当前分工：后续实际实施交给 GLM 5.2，当前 Codex 任务只做尽量详细的文档—代码—验收口径复核，不继续写功能、不再启用子 Agent。
- 机械复核确认主契约仍精确包含 44 模型/555 标量字段，`git diff --check` 通过；SCH-00 tracer 保持预期 Red：模型清单通过，关键字段在 `PersistenceState` 失败（1 pass / 1 fail）。
- 文档审查发现的实施阻塞包括：Pass 2 machine manifest/双审尚未完成；总控计划引用的 `TSK-00/OTB/DEL-00/MNT/SNP/RUN/ACT/WIT` 验收 ID 在 G1 验收清单未定义；缺逐 task type 的 TaskPolicy 矩阵和五类 Outbox payload/idempotency/replay registry；G3 SQLite 示例使用了与 G1 物理契约冲突的 `Project/comicFormat`；G5 使用未在 Shared 目标枚举中的 `targetType=export_revision`，且任务协议的 ExportArtifact order 仍从 0 开始；`PREFLIGHT_SOURCE_UNRESOLVED` 既是切换 blocker 又要求 DB-only 后重确认，尚未定义切换前如何签署决议并解除 blocker。
- 非立即阻塞但必须收口的问题包括：G2 Story/Storyboard/Preflight V2 只列摘要字段而无完整 strict DTO/unknown-field 规则；G1 Schema 字典说使用 Prisma enum，实施契约却冻结为 `String + CHECK`；Prisma relation field/relationName 未声明是否属于 555 字段/manifest；Story confirmed→archived 语义与 formal status 锁死冲突；Secret clear 主方案与实施契约时序表达不同；旧“G0–G5 文档完备性复核”仍以 active 声称全部冻结，而新 G1 实施契约明确仍为 in_progress。
- 用户再次明确后续执行分工：Codex 只作为父级 Orchestrator；由单个、可持续复用的子 Agent 负责实现、测试和必要留痕，父级只检查增量 diff、测试证据和验收口径，发现问题后将精确问题退回同一子 Agent 返修，直至通过。优先降低 token 消耗，禁止无必要地重复通读全量文档、重写既有方案或并发启动多个重复审查 Agent。
- 用户随后撤销低消耗限制；因已开通 20x 额度，恢复原始完整 `$deep-think` 执行：Worker 实施与返工、主 Agent 审查、门禁处并行独立静态复核、运行/用户路径复核和正式留痕均保留。2026-07-11 已重新创建 active G0–G5 Goal，不设置人工 token budget。
- 当前第一切片 Worker 已关闭 P0/P1 文档缺口并生成 source-only Pass 2 manifest：44/555/105/210、195 CHECK binding、185 trigger binding、10 TaskPolicy、5 OutboxHandler，completeness=0；状态仅为 `ready_for_scrutiny`，双审仍 0/2，migration 继续禁用。
- 主 Agent 对首版交付发现新的 P0：base manifest 把 review gate 永久写死为 0/2，却没有 digest-bound attestation/report 和可执行到达 2/2 的通路。现已收口为 immutable base + derived gate；`g1:manifest:review-check` 从 stored artifact、fresh sources 和固定 bundle 机械计算，不回写 base。
- review round 固定为 `g1-m0a-pass2-r1`，只允许 `contract_consistency/sqlite_dsl_machine` 两名不同 Reviewer 的四个固定 sidecar/report 文件；stale/tamper/unknown/rejected/open P0/P1 与文件系统 symlink/hardlink/TOCTOU 均 fail closed。Worker 未创建任何真实 review 证明。
- 四个生产 verifier source 已进入摘要，TypeScript allowlist 为 16，连同两份 Markdown 共 18 个 sourceDocuments；review sidecar/report/spec 排除以避免循环。Schema 契约日期与状态定义校正后重新生成的 current digest 为 `sha256:5496ddcd51d62d6a4f9a5e92856e0dfd881b29d3d3e90d7ee0a024323873f39e`，真实状态仍是 0/2 pending。
- r1 两名 Reviewer 均 rejected，四份证据已原样保留；旧 digest 已 stale。current round/root 固定为 `g1-m0a-pass2-r2`，Worker 不创建 r2 Reviewer 证据。
- r2 返工将 Persistence source/effective digest 分离，r2 当轮 inventory 更新为 44/556/105/210、195 CHECK、194 trigger、10 TaskPolicy、5 OutboxHandler、44 PurgeOwnership；补齐 8 个 purge root、routing/write target、Outbox/Storyboard/Layout/Task/Credential 语义与 bundle 二次重验。
- 真实 SQLite 语义套件已实际 CREATE 44 表和 194 trigger，33 条用例通过，包括完整 Outbox lifecycle、Storyboard 空 character array、source-backed Layout seal 与 source/effective 分离 activation。
- r2 摘要源固定后曾生成 `sha256:2fef00a5c016b48784db675fb9e88778c6e073dd6e0798c0c0fdbbeb2b5d279f`，但 r2 两名独立 Reviewer 均给出 rejected；其四份证据与 r1 证据原样保留。
- current round 已切换为 `g1-m0a-pass2-r3`。r3 修复补齐 gate exactly-one ownership、10 类 frozen idempotency binding、三个 active dialogue all-state DELETE guard，以及 parent-published canonical sealed review snapshot；raw 0/1/2 无 sealed file 均不授权 gate。
- current manifest digest 为 `sha256:210e5718052872aff4059f128525c56f3eafc7594dd488bc6275b3585e328963`；non-tracer persistence `141/141`、review protocol `93/93`、SQLite DSL `36/36`、typecheck、manifest check、diff-check 全绿。r3 真实 root absent、gate `0/2 pending`、`bundleSnapshotDigest=null`，仍禁止 migration。
- r3 两名 Reviewer 最终均 rejected；父 Orchestrator 已 sealed 四份 raw，r3 五文件 SHA 与内部 bundle digest 冻结。current round 已切换为 `g1-m0a-pass2-r4`，Worker 不修改 r3 证据、不创建 r4 review root。
- r4 关闭五项 P1：suffix gate `IMP-05A` exactly-one 且 malformed/prefix fail closed；一份 rejected/open-P1 的 derived status 统一为 rejected；sealed identity 加 ctimeNs；publisher 以 rename/首次 dir fsync 区分 not-committed、committed-recovery-required、committed-cleanup-warning并提供 token-bound recovery；真实 package script 接受字面 `--` 且 first/second/stale/recovery 全经进程测试。
- `apps/server/package.json` 已纳入 source allowlist，r4 当轮 sourceDocuments=19；r4 digest=`sha256:0acc15df259d1744bb26c6b853e994dadc6c4be48fcb1d8e2c798254bc01a237`。non-tracer `150/150`、review protocol `101/101`、SQLite DSL `36/36`、gate `3/3`、typecheck、manifest check、diff-check 全绿；r4 root absent、gate 0/2 pending。
- r4 两名 Reviewer 均 rejected 并由父 Orchestrator sealed；r4 五文件 SHA 与内部 bundle digest 冻结。r5 关闭三个 P1：宽 gate-like detector 捕获 `A-01/ABCDE-01` 且不误报受控 reference；r5 当轮 source 权威口径统一为 19；真实 `rename(temp,target)` 覆盖后旧 FD `nlink=0` 只在不同 regular pathname + 旧 dev/ino/size/mtime 稳定 + `1→0` 时按旧 bytes 线性化，删除无替换和 late hardlink 均拒绝。
- r5 digest=`sha256:d981372c9051bf89fe6d01c4be94b50cfad7fd5bbe71dea36ae67dccb02e6825`。non-tracer `152/152`、review protocol `103/103`、SQLite DSL `36/36`、gate `3/3`、typecheck、manifest check、diff-check 全绿；SCH-00 保持 1 expected Red；r5 root absent、gate 0/2 pending。
- r5 两名独立 Reviewer 已 accepted 并由父 Orchestrator 完成 CAS sealing：first bundle=`sha256:f267e32886af1f91f22e0e7cda1f5803709a2088d0fedd2d08e31cd60d2eb422`，final bundle=`sha256:970c80b9511730aee257fb0eb9f18084947f991fee154cf19eb8b4720e5bb0e6`，sealed file SHA-256=`e5cc71f73a1ad418e9f8730cee9aa4a4e2108024931d2b05604de6a8aaef2953`。derived gate=`2/2 accepted true`，唯一 P2 advisory=`G1-CONTRACT-P2-MANIFEST-SOURCE-COMMENT`。
- r5 签收后曾进入 G1-1 M0-A 实施；当时冻结 manifest artifact、19 个 sourceDocuments 与 r1～r5 reviews，只允许 marker-owned temp SQLite，不接触真实数据路径。随后 Prisma 实证使该摘要进入历史。
- M0-A Schema 首次真实 `prisma validate` 暴露 r5 relation exact-unique 冲突：`Candidate.asset` 的三列 defining fields 只有严格子集 `asset_id` unique，不能在 Prisma 6.19.3 SDL 形成 1:1。migration 生成已停止，未创建 0001～0008。
- r6 最小修订将 unique 推断收紧为完整 relation fields exact unique；独立扫描确认仅影响 `Candidate.asset`。manifest/Schema 现为 `Asset.candidatesByAsset Candidate[]`，物理 `uq_candidates_asset(asset_id)` 仍保证实际 0..1；renderer 硬编码 adapter 已删除。
- SQLite connector 不支持 relation FK `map`；renderer 仍 exact-match manifest name/columns/ref/actions，SDL 只省略 `map`，物理 name 留给未来 migration/fresh SQLite 验证。
- r5 P2 JSDoc 随必要 source 变更关闭；round/root 当时轮转 r6，digest=`sha256:356d2150ec848a1e4c583d170fee0b80b136bfe3c0990faefd49fe72aeadcfb6`。r1～r5 evidence 不变。
- r6 Schema 曾确定性生成 44/556/210，`prisma validate` 与最终机械验证全部 Green；该轮随后完成双审，不再是 current。
- r6 最终结论为 Reviewer A accepted、Reviewer B rejected，父 Orchestrator 已密封整体 rejected snapshot。Reviewer B 的两个 P1 是 FK physical name 未按公式 exact 证明，以及 production Schema renderer/CLI 未受 manifest digest 绑定。
- r7 已关闭这两项 P1并完成父级增量审计：105 FK exact name + unique relation consumption；Schema/migration renderer/CLI 全部入摘要；sourceDocuments=`23 = 20 TS + 2 Markdown + package.json`，相对本地 import/require 必须 transitive closure。
- r7 Schema writer、migration pure plan/writer/checker、fresh verifier 已实现。0008 具备事务前 FK mode readback、row-count + 全列双向 EXCEPT 和 COMMIT 前 FK guard；artifact tree 拒绝 tamper/missing/extra、symlink 与 hardlink。
- marker-owned temp E0 已证明 direct SQLite 双 replay、外层事务 FK mode 负例、same-count 值改写、orphan rollback，以及真实 Prisma 6.19.3 8 success→no pending / P3018→P3009；未触碰真实数据路径。
- current round=`g1-m0a-pass2-r7`，digest=`sha256:c32dd95ab61a2d8a89c25dbab45d0f3efb7323d504f6031dc2e51e38b5943d06`；真实 r7 root、migration tree 与 stage residue均不存在，derived gate=`0/2 pending false`。
- source freeze 最终验证：Server `26 files / 253 tests`、typecheck、manifest check、Schema exact check、Prisma validate、diff-check 全绿；r1～r6 共 28 个历史文件 SHA 与 r3～r6 internal digest不变。dry E0 不冒充正式 `SCH-00～15` pass。
- 用户询问为何仍显示“第二步”；这里的 Pass 2 是 G1 M0-A 的整段摘要双审/Schema/migration阶段编号，不是只完成了两个动作。当前尚停留该阶段是因为 r7 仍需两名独立 Reviewer 和正式 materialization，而不是实现没有推进。

# web_search

- M0-A 只读安全审计核对官方文档：Prisma 6 SQLite enum 落为 TEXT，数据库 CHECK/trigger 需定制 migration SQL；`migrate deploy` 不负责数据库 drift 检测；SQLite UNIQUE 允许多个 NULL，复合 FK 父列必须匹配唯一键且子列含 NULL 会跳过约束；`integrity_check` 不包含 `foreign_key_check`。来源：Prisma SQLite connector、Unsupported database features、Migrate deploy，以及 SQLite CREATE INDEX / Foreign Keys / PRAGMA 官方文档。

# goal

继续完整 G0–G5 Goal：保留已完成 G0，从 G1 开始依次完成 DB-only、版本/freshness、漫画版式、候选定稿返修和成稿出版；每阶段必须经过 Worker 实施、自测、主审、返工、独立静态复核、运行或用户路径复核、正式留痕与提交。

# approach

- Orchestrator 不直接写功能代码；按任务阶段指派 Worker，主 Agent 审查并将问题退回原 Worker 返修。
- 高风险门禁使用相互独立的 Scrutiny Review，结论只读；未双签时不进入下一不可逆阶段。
- 当前第一切片为 r7 source freeze：Schema/migration generator 与隔离 E0 已 Green，但 r7 仍为 0/2，正式 migration tree暂停。M0-A 正式 SCH 门禁完成前不得进入 M0-B。
- base manifest 永久保持 `0/2 pending false`；审查事实只由 current digest 绑定的 derived gate/Handoff 表达。任一摘要源变化后必须重新 generate 并重新 2/2，未来 migration generator 必须调用同一 verifier。
- Reviewer 只写各自 raw pair；父 Orchestrator 用唯一 publish CLI 和 expected-previous digest CAS 生成 `review-bundle.v1.json`。无 sealed snapshot 时 raw pair 不计入 gate，Worker 不代写任何审查事实。

# question


# todo

- [x] 由 Worker 关闭 G1 P0/P1 文档缺口并生成 Pass 2 machine manifest
- [x] 主 Agent 审查 Worker 交付并打回/收口 digest-bound review gate P0
- [x] 同步正式契约与验收口径并生成 current manifest digest
- [x] 保留 r1 驳回证据并完成 Pass 2 r2 source/契约/测试返工
- [x] 生成并回填 r2 最终 current manifest digest，完成最终机械验证
- [x] 完成 r2 双审（结果为两名 Reviewer 均 rejected，并保留不可改写证据）
- [x] 完成 Pass 2 r3 Worker 修复、manifest 生成与最终机械自验
- [x] 完成 r3 双审与父级 sealing（结果为两名 Reviewer 均 rejected，五份证据不可改写）
- [x] 完成 Pass 2 r4 五项 P1 修复、manifest 生成与最终机械自验
- [x] 完成 r4 双审与父级 sealing（结果为两名 Reviewer 均 rejected，五份证据不可改写）
- [x] 完成 Pass 2 r5 三项 P1 修复、manifest 生成与最终机械自验
- [x] 两名独立 Scrutiny Reviewer 提交 r5 raw pair，由父 Orchestrator CAS sealing 并取得 current digest `2/2 accepted`
- [x] 完成 r6 exact-unique 最小契约修订、确定性 Prisma Schema 与真实 validate
- [x] 完成 r6 双审与父级 sealing（Reviewer A accepted、Reviewer B rejected，五份证据不可改写）
- [x] 完成 r7 exact FK/source closure、Schema/migration generator、隔离 SQLite 与真实 Prisma E0
- [x] 完成 r7 source freeze 最终机械验证与正式 Handoff/QA 留痕
- [ ] 两名独立 Scrutiny Reviewer 提交 r7 raw pair，由父 Orchestrator CAS sealing 并取得 current digest `2/2 accepted`
- [ ] r7 签收后 materialize 正式 Schema/migration tree并完成 `SCH-00～15`
- [ ] M0-A 父审/运行审通过后进入 M0-B

# result

r1～r6 共 28 个历史文件均不可改写并已复算 SHA；r6 整体 rejected，只绑定旧 digest。current r7 digest=`sha256:c32dd95ab61a2d8a89c25dbab45d0f3efb7323d504f6031dc2e51e38b5943d06`，23 sources、Schema/migration generator 与 marker-owned dry E0 已 Green，但 derived gate=`0/2 pending false`，正式 migration tree 与 r7 review root 均不存在。尚未完成正式 `SCH-00～15`、M0-A/M0-B/G1，也未触碰真实数据路径。
