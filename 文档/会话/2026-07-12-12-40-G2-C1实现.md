# finding

- B1 已完成 Script current/working/pending 门禁；C1 只消费 B1 的 scoped query、事务 runner、错误 envelope 和 productionState。
- `StoryVersion`、`StoryBeatProjection`、`StorySceneProjection`、`ChapterScene` 已存在；G1 trigger 要求 confirmed Story 的 scene/beat projection 与 V2 document 一致。
- Story V2 codec 已提供 strict parse/encode；Character `projectCharacterId` 必须在同 project 的 characters 表中存在。
- C1 已实现 StoryVersionRepository、StoryVersionService 和 working-copy API；确认事务包含 projections、current pointer、pending Storyboard 清理和 rowVersion CAS。
- fresh SQLite 首次触发了 0009 trigger 的 `c.chapter_id` 错误，已改为 `c.id = NEW.chapter_id`；旧 ProjectRepository 启动加载已确认 Story 时不再误报 C3 越界。

# web_search


# goal

- 完成 Story pending create/update/discard/confirm 的 DB-only 手工路径。
- 验收：CAS/replay、Script source gate、projection、重启读回和旧路径兼容。

# approach

- pending 创建绑定 current Script version/digest；clone_current 复用 current Story document，empty 使用严格 V2 空壳。
- update 在 pending pointer 仍有效时先写 document/projection，再让 Chapter rowVersion +1。
- confirm 在 pending parent 上完成 projection/formalize/pointer switch；不改 confirmed 历史内容。

# question


# todo

- [x] Shared Story API contract
- [x] StoryVersionRepository
- [x] API facade/routes
- [x] fresh SQLite/CAS/source gate/restart tests
- [x] scrutiny/handoff/docs

# result

C1 已完成：Story pending create/update/discard/confirm 手工闭环、projection、幂等重放、重启读回和全量验证通过。下一阶段为 D1 Storyboard；Storyboard/Preflight/worker/capability switch 仍未实现。
