# finding

- 复核确认 4 类生产残留：持久任务 `shot_generate` 独立分镜 Prompt、图片 provider 参考图职责、分镜 JSON 示例语义、参考图画风与漫画格式词汇。
- 三个主 Skill 已接入生产加载器；本次应在现有 Skill 内补齐事实源，而不是新增孤立 Skill。
- `shot_generate` 的数据库路径可读取正式 StoryVersion、绑定 ChapterScriptVersion 与 Project，具备复用完整分镜 Skill 的条件。

# web_search


# goal

修复 4 类生产 Prompt 残留，使分镜、参考图、候选图的稳定创作正文只保存在 `apps/server/opencodeAI/skills/`，并增加防回流测试；不调用付费图片服务。

# approach

- 复用完整分镜 Prompt 构造与固定质量门，替换持久任务的简化旁路。
- 把参考图职责、分镜示例、画风和版式 Prompt 词汇迁入对应 Skill reference。
- 后端只保留动态事实装配、引用映射、Schema 校验和 provider 传输。

# question


# todo

- [x] 修复 `shot_generate` 分镜旁路
- [x] 迁移 provider 参考图职责
- [x] 迁移分镜示例语义
- [x] 迁移参考图画风与漫画格式词汇
- [x] 增加防回流测试并完成离线验证
- [x] 更新正式文档、长期记忆和完成记录

# result

复核发现的 4 类生产残留已全部修复。三个 Skill 校验、定向测试、Server 全套离线测试、全项目类型检查和构建通过；未调用付费图片服务。P6 evaluator 与剧本/导入/剧情结构旧 Prompt 保留为后续渐进迁移项。
