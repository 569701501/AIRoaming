# finding

- 当前生产剧本格式事实主要集中在 `packages/shared/src/script-format.ts`；大纲只校验三个旧区块，章节格式只用 `includes()` 判断标签存在。
- `apps/server/src/dialogue/dialogue-text.util.ts` 对不合格章节或大纲会进行保守包装，尚未使用严格模型输出契约。
- 当前灵感解析会接受多于 3 个候选后静默截取；导入 analyze/verify 尚无目标 JSON 契约。
- 本任务只实施共享格式、严格 parser/validator 和固定 fixture 骨架，不接入动态 Prompt、数据库来源模型、页面或正式化流程。
- Shared 已新增版本化严格契约：三个 JSON/Markdown 主产物 parser、来源/行引用上下文校验、canonical serializer、七 stage 正反 fixture；旧运行时尚未切换。

# web_search


# goal

实施双流程第一包：为七个模型阶段建立可复用的共享输出契约，重点落地灵感 JSON、A3 大纲 Markdown、统一章节 Markdown、B2 导入分析 JSON、B4 忠实度 JSON 的严格解析与校验，并补定向测试。现有页面内容字段和运行流程保持不变。

# approach

- 在 `packages/shared` 新增独立、版本化的剧本工作流契约模块，先不替换旧运行时兼容函数。
- 使用无第三方依赖的 TypeScript 严格解析器，返回结构化错误；正式接线时才能据此 fail-closed。
- 用 fixture 覆盖七个模型 stage 的正反产物，避免把测试规则复制进公开 Skill。

# question


# todo

- [x] 冻结契约类型与错误模型
- [x] 实现灵感、大纲、章节、导入分析、忠实度解析校验
- [x] 补七阶段正反 fixture 与测试
- [x] 完成定向/全量验证和文档收口

# result

双流程实施包 1 已完成。Shared 新增七个模型阶段可复用的严格输出契约、canonical Markdown parser/serializer、导入来源/行引用上下文校验、忠实度硬问题辅助和七 stage 正反 fixture；未切换生产 Prompt、数据库、pending 或页面。Shared 25 files/144 tests、全工作区 typecheck/build、fixture subpath 运行导入和 diff check 通过。下一包应进入来源、状态与 pending 基础。
