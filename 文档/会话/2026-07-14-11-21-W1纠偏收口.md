---
doc_id: AIR-SESSION-20260714-W1-FIX
status: active
created: 2026-07-14
updated: 2026-07-14
owner: AI漫游项目
audience: human, ai-agent
source: W1 DB-only Web 门禁纠偏与复核
---

# finding

- W1 新增 E2E 暴露真实缺陷：已有 Storyboard/Preflight 后确认新 Story 时，Story confirm 固定写入 `milestone_status=structured`，触发 G1 单调里程碑约束并返回 500。
- 修复为已有下游时保留原里程碑；DB Workbench workflow 改从 `ChapterProductionQueryService` 读取，页面能显示 stale。
- DB E2E 已覆盖 dirty gate、并发 CAS、stale 页面、历史复制和 Story→Storyboard→Preflight；file-mode 回归保持通过。

# web_search


# goal

- 完成 W1 纠偏、独立提交、全量静态/服务端/隔离 E2E 复核，并停在 `WAIT_R0B_AUTH`。
- 禁止真实 workspace、真实数据库、默认 Keychain、真实凭据、AUTH、R0B/R1/R2。

# approach

- 只修改 W1 直接相关服务、E2E fixture、测试与任务证据；不暂存用户已有 M6/项目盘点文档。
- 通过 fresh SQLite + 正式 migration + fake provider 验证，不使用真实数据。

# question


# todo

- [x] 修复新 Story 确认时里程碑回退和 DB workflow stale 展示
- [x] 补齐 dirty/CAS/stale/history DB-only E2E 并重复 3 次
- [x] 通过 typecheck、build、server/shared 全量测试和 file-mode E2E
- [x] 独立提交 corrective slice `4fe1dfa`
- [ ] 等待用户明确 `AUTH-R0B` 后再进入 R0B

# result

- W1 corrective slice 已提交：`4fe1dfa fix(web): close g2 stale milestone gate`。
- 验证：shared 8 spec/39 tests；server 70 spec/474 tests；DB W1 6/6、file E2E 3/3；typecheck、e2e typecheck、build 均通过。
- 当前唯一下一状态：`WAIT_R0B_AUTH`。真实切换与真实凭据操作计数均为 0。
