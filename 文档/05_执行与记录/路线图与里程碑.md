# 路线图与里程碑

---
doc_id: AIR-EXEC-ROADMAP-001
status: active
created: 2026-05-23
updated: 2026-06-10
owner: AI漫游项目
audience: human, ai-agent, developer
source: MVP 范围与路线图
---

## 1. 当前阶段

当前处于 M1：章节剧本到分镜阶段，已接近收尾。剧情结构工作台第一版（2026-05-30）、分镜工作台第一版（2026-05-30，5-31 补待确认持久化）、项目角色库异步角色图（2026-06-01）均已落地，M1 验收项已全部通过；剩余主要是新角色待处理队列和 Chapter/Character 数据模型入库。项目库优先、项目工作区和 7 步主流程已经成为当前产品口径。

## 2. 阶段总览

| 阶段 | 名称 | 状态 | 核心交付 |
| --- | --- | --- | --- |
| M0 | 文档与契约成型 | completed | 文档体系、产品范围、架构、数据模型、任务协议 |
| M0.1 | 产品页面链路收口 | completed | 核心用户流程、功能清单、当前 UI 信息架构、AI 入口同步 |
| M1 | 章节剧本到分镜 | in_progress | 项目、章节列表、章节剧本输入、项目角色库、剧情结构化、分镜生成和编辑 |
| M2 | 分镜到漫画图候选 | pending | 场景模板、图片生成、候选选择和锁定 |
| M3 | 漫画排版与导出 | pending | 页面排版、PNG/PDF 导出、素材包 |
| M4 | 轻漫剧基础版 | pending | TTS、字幕、图片序列、MP4 导出 |

## 3. M0 文档与契约成型

### 目标

让项目从原始种子进入可执行状态。

### 任务

- [x] 建立文档目录体系。
- [x] 建立 AI 上下文入口。
- [x] 建立文档写作与留痕规则。
- [x] 拆分产品总览、MVP 和核心流程。
- [x] 拆分系统架构、数据模型、任务协议、素材路径契约。
- [x] 建立模块总览。
- [x] 建立功能完成记录模板。
- [x] 建立任务记录模板。
- [x] 将深思熟虑迁移为按需使用的 Codex 技能 `$deep-think`。
- [x] 清理旧 UI 试错文档，收敛当前事实源。

### 退出标准

- [x] 根目录和文档目录可读性自检通过。
- [x] 原始种子完成归档或索引。
- [x] 首份功能完成记录创建。
- [x] `$deep-think` 可在复杂任务中按需使用。
- [x] 下一版前端实现前，先完成 `功能清单与页面链路.md` 的重新设计。

## 4. M0.1 产品页面链路收口

### 目标

在继续写前端页面前，确认当前 UI 信息架构和页面链路。

### 任务

- [x] 更新 `核心用户流程.md`。
- [x] 更新 `功能清单与页面链路.md`。
- [x] 新增 `当前UI信息架构.md`。
- [x] 同步 `AI上下文入口.md`。
- [x] 同步 `产品总览.md` 和 `MVP范围与路线图.md` 中轻漫剧位置。
- [x] 同步 `模块总览与依赖.md` 的前端项目工作台 UI 模块。

### 退出标准

- [x] AI 入口不再说页面结构未定。
- [x] 当前 UI 结论明确为项目库 -> 项目工作区 -> 7 步流程。
- [x] 轻漫剧不作为独立流程进入当前 7 步 UI 主流程。
- [x] 历史归档文档不再保持 `active` 状态。

## 5. M1 章节剧本到分镜

### 目标

用户能从当前章节剧本得到可编辑分镜。

### 建议任务

- [x] 初始化应用工程。
- [ ] 建立 Project/Chapter/Character/StoryVersion/Shot 数据模型；当前 Prisma 已有 Project/StoryVersion/Shot/Candidate/Asset/GenerationTask，Chapter/Character/剧情结构/分镜仍以共享契约 + workspace JSON 文件形式存在，未入库。
- [x] 建立项目 workflow 契约和基础流程状态；已按 2026-06-02 口径将 `project_characters` 迁出主流程，并新增 `image_preflight` 出图准备步骤。
- [x] 实现项目创建。
- [x] 实现项目级过渡故事输入和保存。
- [x] 实现章节列表和当前章节保存。
- [x] 实现完成本章并创建下一章草稿；完成后优先进入本章剧情结构，角色库待处理项只提示。
- [x] 实现项目角色库常驻资产入口、角色预览图异步生成与确认、基于已确认预览图的定稿图串行生成（2026-06-01 项目角色库异步角色图）。
- [ ] 实现新角色待处理队列（剧情结构和分镜阶段发现的新角色入队，见 2026-06-02 方案与 2026-06-06 流程收口口径）。
- [x] 实现 `story_parse` 任务占位或真实调用。
- [x] 实现结构化剧情查看和编辑（2026-05-30 剧情结构功能第一版：对话生成待确认预览、确认写入 `structure.json`、字段级编辑、章节状态推进 `structured`）。
- [x] 实现 `shot_generate` 任务占位或真实调用；当前以对话工具 `generate_storyboard` / `confirm_storyboard` 形式落地，真实章节回归生成 16 镜。
- [x] 实现分镜列表和分镜详情编辑（2026-05-30 分镜工作台第一版，5-31 补待确认分镜持久化；镜头缩略预览、拖拽重排、单镜头重写仍未做）。

### 验收

- [x] 输入当前章节剧本后能保存为 `ChapterScriptVersion` 或等价章节草稿（章节级保存草稿与完成本章写入章节剧本版本）。
- [x] 能生成结构化剧情 JSON（确认后写入章节 `structure.json`）。
- [x] 能生成至少 8 条分镜（2026-05-30 真实章节回归生成 16 镜待确认预览）。
- [x] 分镜字段可编辑并保存（`PATCH /projects/:projectId/chapters/:chapterId/storyboard`）。
- [x] 功能完成记录已写（剧情结构功能第一版、分镜工作台第一版、项目角色库异步角色图等）。

## 6. M2 分镜到漫画图候选

### 目标

用户能为分镜生成多个漫画图候选并锁定结果。

### 建议任务

- [ ] 建立 SceneLocation 模型，并让出图准备和候选图读取已确认项目角色库。
- [x] 实现出图准备：检查本章出镜角色绑定、角色参考图、场景和画风参考。
- [x] 建立 Asset/Candidate/GenerationTask 模型（workspace 文件态 Candidate + Asset；GenerationTask 进程内）。
- [ ] 实现场景模板编辑和角色参考图注入。
- [ ] 实现分镜提示词生成（当前直接用 promptDraft / 构图描述进入 image_generate）。
- [x] 实现图片生成任务（真实 `image_generate` worker + ImageProviderService）。
- [x] 实现候选图列表。
- [x] 实现选择、锁定（废弃/重生成 UI 后置；锁定与再生成入口已有）。

### 验收

- [x] 每个分镜能生成多个候选（candidateCount 1–6）。
- [x] 候选能追溯到任务输入（taskId / asset meta / candidates.json）。
- [x] 一个分镜只能锁定一个候选。
- [x] 锁定状态能被排版模块读取。

## 7. M3 漫画排版与导出

### 目标

用户能把已锁定候选组成漫画页面并导出。

### 建议任务

- [x] 建立 LayoutPage 模型（workspace `layout/layout.json`）。
- [x] 实现基础页面模板（MVP：一镜一页顺序页）。
- [ ] 实现格子排序和调整（拖拽后置）。
- [x] 实现 PNG 导出（复制锁定候选为 exports/pages）。
- [ ] 实现 PDF 导出（依赖后置）。
- [x] 实现素材包导出（目录包 + manifest）。

### 验收

- [x] 已锁定分镜能进入页面。
- [x] 页面导出文件注册为 Asset。
- [x] 素材包包含 manifest。

## 8. M4 轻漫剧基础版

### 目标

用户能基于已锁定漫画图导出基础 MP4。

### 建议任务

- [ ] 建立 VideoDraft/VideoSegment 模型。
- [ ] 实现图片序列时间轴。
- [ ] 实现 TTS 任务。
- [ ] 实现字幕生成。
- [ ] 实现 BGM 引用。
- [ ] 实现 FFmpeg MP4 导出。

### 验收

- [ ] MP4 能播放。
- [ ] 字幕与 TTS 对齐。
- [ ] 视频 Asset 可追溯到源分镜。

## 9. 状态维护规则

- 每完成一个任务，更新本文件对应勾选项。
- 每完成一个功能，新增完成记录。
- 如果阶段目标变化，更新 `MVP范围与路线图.md` 并视情况新增 ADR。
