# 路线图与里程碑

---
doc_id: AIR-EXEC-ROADMAP-001
status: active
created: 2026-05-23
updated: 2026-08-10
owner: AI漫游项目
audience: human, ai-agent, developer
source: MVP 范围与路线图
---

## 1. 当前阶段

当前阶段 **G0～G5 已完成，M3C 全部里程碑（M0～M6）已完成**。

G0 安全网、S0/W1、R0-A/B、SH-10、v5 C0～C7、首写边界、R2 OBS-01～10、G4-A～F、G5-M0～M8 技术验收和最终用户签收均已完成；production status=`completedThrough=C7`，evidence=`sha256:987d9a94...6a1452`，G4=`G4_PASSED`，G5=`G5_COMPLETE`，总体=`G0_G5_COMPLETE`。唯一状态入口为 `文档/05_执行与记录/任务记录/2026-07-14_G0至G5剩余连续施工/luna_current_handoff.md`。

G0～G5 基础交付没有回退。2026-07-22 用户已确认的连续开发目标（在 G5 底座上把第 6 步升级为“零配置智能成稿 + 同一编辑器精修”）已交付完毕：当前产品具备持久 `layout_compose`、V2 Working Copy、首次自动成稿、同一编辑器手调、整章当前/新排法对比、保留/应用/Undo 和条漫/页漫浏览器路径。执行入口为 `文档/05_执行与记录/任务记录/2026-07-22_智能成稿编辑器重构/handoff.md`。

**M6 已于 2026-07-23 完成，但其中的 scoped 智能调整随即于 2026-07-24 被 ADR-0022 收缩掉。**此前“待实施 / 暂走规则降级”的表述已过期。

保留下来的部分：真实视觉分析已接线，`apps/server/src/projects/layout-composition-source-projector.service.ts:302` 在存在 AI key 时注入多模态 provider，`apps/server/src/projects/layout-visual-analyzer.service.ts:340` 走真实结构化分析并校验角色映射越界，仅在未配置或分析失败时逐 Shot 规则回退。分析结果用于 `textSafeRegion` 避让人物与视觉重点；无可靠证据时用保守规则位置，不得宣称看见画面主体。

被删除的部分：full/scoped reflow、选区/页面/场景范围选择、四种 intent、MiniPreview 与建议对比、应用/放弃、撤销重做、AI Drawer、四步出版控制中心。已确认前端代码中不存在这些入口。当前漫画成稿只有四项用户能力：首次无感自动排版 → 人工画布编辑 → 自动保存 → 单一导出；AI 只在没有 Working Copy 时执行一次。

完成记录见 `文档/05_执行与记录/功能完成记录/2026-07-23_真实视觉分析与局部智能调整.md`，最终口径以 `文档/04_方案与决策/ADR-0022_漫画成稿基础编辑与单次自动排版.md` 为准。

人工 A/B 保持 0/2 并按用户决定转为可选研究，部分气泡美观作为已接受风险；“代表性验收章节至少 80% 画格无需移动或缩放”仍无实测证据，是 M3 唯一未达成的验收项。

2026-07-25 起工作重心转向主流程之外的输入侧与配置侧，均已落地：长篇原著资产库（只读方案评估）、文稿库（M1～M3 完整交付）、模型管理与对话运行时衔接、模型凭证统一重构。详见第 10 节。

G6 素材包 V2/ZIP 与轻漫剧仍不在当前目标范围。

G6 素材包 V2/真实 ZIP 与 G7 七阶段总验收已于 2026-07-11 后置。第七步导航和旧目录包骨架保留，但不作为当前 G0–G5 开工或完成前提。详细结论见 `文档/04_方案与决策/2026-07-11_G0至G5开发文档完备性复核.md`。

## 2. 阶段总览

| 阶段 | 名称 | 状态 | 核心交付 |
| --- | --- | --- | --- |
| M0 | 文档与契约成型 | completed | 文档体系、产品范围、架构、数据模型、任务协议 |
| M0.1 | 产品页面链路收口 | completed | 核心用户流程、功能清单、当前 UI 信息架构、AI 入口同步 |
| M0.2 | G0–G5 开发级方案与实施 | completed | G0–G5 方案、实现、复核与签收完成；G6/G7 后置 |
| M1 | 章节剧本到分镜 | completed_g2 | DB-only、不可变版本、freshness 与任务链已完成 |
| M2 | 分镜到漫画图候选 | completed_g4 | 候选生成、线性定稿修订、影响预览、返修门禁和历史已通过 |
| M3A | 漫画排版与出版 | completed_g5 | G5 M0～M8 技术验收与最终用户签收通过 |
| M3C | 智能漫画成稿重构 | completed | M0～M6 complete；零设置工作台、同一编辑器、整章预览、真实视觉分析、scoped 智能调整与 V2 出版均已落地。剩余缺口只有 80% 画格实测证据 |
| M3B | 素材包 V2 与 ZIP | deferred | G6 后置；现有目录包不算完成版 |
| M5 | 输入侧与配置侧能力 | completed | 文稿库（拆章/编码/只读浏览）、模型管理、凭证统一；不属于七步主流程 |
| M4 | 轻漫剧基础版 | deferred | TTS、字幕、图片序列、MP4 导出 |

下文 M1–M3 的勾选项主要记录 2026 年 5–7 月第一版骨架历史；它们不能覆盖 G0–G5 文档中更严格的 current/accepted 口径。

## 3. M0 文档与契约成型

### 目标

让项目从原始种子进入可执行状态。

### 任务

- [x] 建立文档目录体系。
- [x] 建立 AI 上下文入口。
- [x] 建立文档写作与留痕规则。
- [x] 拆分产品总览、MVP 和核心流程。
- [x] 拆分系统架构、数据模型、任务协议、素材路径契约。
- [x] 建立模块总览。
- [x] 建立功能完成记录模板。
- [x] 建立任务记录模板。
- [x] 将深思熟虑迁移为按需使用的 Codex 技能 `$deep-think`。
- [x] 清理旧 UI 试错文档，收敛当前事实源。

### 退出标准

- [x] 根目录和文档目录可读性自检通过。
- [x] 原始种子完成归档或索引。
- [x] 首份功能完成记录创建。
- [x] `$deep-think` 可在复杂任务中按需使用。
- [x] 下一版前端实现前，先完成 `功能清单与页面链路.md` 的重新设计。

## 4. M0.1 产品页面链路收口

### 目标

在继续写前端页面前，确认当前 UI 信息架构和页面链路。

### 任务

- [x] 更新 `核心用户流程.md`。
- [x] 更新 `功能清单与页面链路.md`。
- [x] 新增 `当前UI信息架构.md`。
- [x] 同步 `AI上下文入口.md`。
- [x] 同步 `产品总览.md` 和 `MVP范围与路线图.md` 中轻漫剧位置。
- [x] 同步 `模块总览与依赖.md` 的前端项目工作台 UI 模块。

### 退出标准

- [x] AI 入口不再说页面结构未定。
- [x] 当前 UI 结论明确为项目库 -> 项目工作区 -> 7 步流程。
- [x] 轻漫剧不作为独立流程进入当前 7 步 UI 主流程。
- [x] 历史归档文档不再保持 `active` 状态。

## 5. M1 章节剧本到分镜

### 目标

用户能从当前章节剧本得到可编辑分镜。

### 建议任务

- [x] 初始化应用工程。
- [x] 建立 Project/Chapter/Character/StoryVersion/StoryboardVersion/Shot 数据模型；G1～G3 已完成 44 模型、正式 migration、DB-only 切换与 file guard。
- [x] 建立项目 workflow 契约和基础流程状态；已按 2026-06-02 口径将 `project_characters` 迁出主流程，并新增 `image_preflight` 出图准备步骤。
- [x] 实现项目创建。
- [x] 实现项目级过渡故事输入和保存。
- [x] 实现章节列表和当前章节保存。
- [x] 实现完成本章并创建下一章草稿；完成后优先进入本章剧情结构，角色库待处理项只提示。
- [x] 实现项目角色库常驻资产入口、角色预览图异步生成与确认、基于已确认预览图的定稿图串行生成（2026-06-01 项目角色库异步角色图）。
- [x] G0～G5 必需角色处理闭环已完成：剧情结构/分镜角色可进入项目角色与出图准备门禁；更完整的待处理队列 UI 属于非阻塞体验增强，不计入阶段完成条件。
- [x] 实现 `story_parse` 任务占位或真实调用。
- [x] 实现结构化剧情查看和编辑（2026-05-30 剧情结构功能第一版：对话生成待确认预览、确认写入 `structure.json`、字段级编辑、章节状态推进 `structured`）。
- [x] 实现 `shot_generate` 任务占位或真实调用；当前以对话工具 `generate_storyboard` / `confirm_storyboard` 形式落地，真实章节回归生成 16 镜。
- [x] 实现分镜列表和分镜详情编辑（2026-05-30 分镜工作台第一版，5-31 补待确认分镜持久化；镜头缩略预览、拖拽重排、单镜头重写仍未做）。

### 验收

- [x] 输入当前章节剧本后能保存为 `ChapterScriptVersion` 或等价章节草稿（章节级保存草稿与完成本章写入章节剧本版本）。
- [x] 能生成结构化剧情 JSON（确认后写入章节 `structure.json`）。
- [x] 能生成至少 8 条分镜（2026-05-30 真实章节回归生成 16 镜待确认预览）。
- [x] 分镜字段可编辑并保存（`PATCH /projects/:projectId/chapters/:chapterId/storyboard`）。
- [x] 功能完成记录已写（剧情结构功能第一版、分镜工作台第一版、项目角色库异步角色图等）。

## 6. M2 分镜到漫画图候选

### 目标

用户能为分镜生成多个漫画图候选并锁定结果。

### 建议任务

- [x] 以 `ChapterScene` + `SceneVisual` 落地场景身份与视觉版本，并让出图准备、SourceSnapshot 和候选任务读取已确认来源；不再要求平行 `SceneLocation` 真值。
- [x] 实现出图准备：检查本章出镜角色绑定、角色参考图、场景和画风参考。
- [x] 建立 SQLite `Asset`/`Candidate`/`GenerationTask`/`TaskAttempt`/`OutboxEvent` 模型与持久 worker。
- [x] G0～G5 必需的角色/场景参考来源注入已通过 PreflightRevision、SourceSnapshot 和任务输入实现；独立场景模板编辑器是后置增强。
- [x] 候选任务使用版本化 prompt spec、`promptDraft`/构图描述、来源摘要和尺寸策略生成；独立提示词编辑器不是阶段硬门。
- [x] 实现图片生成任务（真实 `image_generate` worker + ImageProviderService）。
- [x] 实现候选图列表。
- [x] 实现选择、锁定（废弃/重生成 UI 后置；锁定与再生成入口已有）。

### 验收

- [x] 每个分镜能生成多个候选（candidateCount 1–6）。
- [x] 候选能追溯到任务输入（taskId / asset meta / candidates.json）。
- [x] 一个分镜只能锁定一个候选。
- [x] 锁定状态能被排版模块读取。

## 7. M3 漫画成稿与导出

### 目标

用户无需先理解模板或参数，就能把正式分镜与已锁定候选生成完整可编辑漫画初稿；必要时在同一编辑器精修并导出。

### 建议任务

- [x] 旧骨架：建立 LayoutPage 模型（workspace `layout/layout.json`）。
- [x] 旧骨架：一镜一页顺序页；该语义不能作为 G5 成稿完成标准。
- [x] G5：实现画格、裁切、层级、文字、气泡、Working Copy 与 LayoutRevision。
- [x] 旧骨架：复制锁定候选到 exports/pages；该输出不能作为正式 PNG 合成通过。
- [x] G5：实现确定性 `layout_publication`、真实 PNG、页漫可选 PDF 和条漫切片。
- [x] M3C-M0：冻结 10 组/12 变体、69 镜/59 文字项的 Storyboard/lock/Asset/font corpus，生成真实 renderer 现状基线、逐项评分和双人复核模板。
- [x] M3C-M1：实现 strict `LayoutDocumentV2`、V1 WC 保守升级、V2→V1 临时投影、dialogue binding/protection、command actor、语义气泡命令与单批 inverse；真实 renderer gate 保持 green。
- [x] M3C-M2：实现对白归一化/覆盖账本、NarrativeGroup、页漫原子分页、条漫可变段落、规则 crop/自动气泡/阅读顺序与 strict V2 composition；12 变体真实 renderer 达到 69/69 镜头、59/59 文字项、0 改写、0 矩形 overflow。
- [x] M3C-M3 自动预筛：实现 strict 视觉分析/fallback、三套稳定候选、视觉 crop、shape-safe 气泡、人物尾巴、页节奏评分和生产 renderer 语义对齐；自动画格 69/69、气泡 59/59。
- [x] M3C-M3 退出：自动硬门与用户轻量复核完成；A/B 0/2 保持真实但转为可选研究，部分气泡美观风险由编辑器与后续智能预览承接。
- [x] M3C-M4：实现 `layout_compose` 来源密封任务、V2 WC、幂等、恢复、失败留痕、不可变应用凭证和独立 apply。
- [x] M3C：Shared 已实现叙事分组、页漫分页/条漫分段、三候选、非破坏裁切、阅读顺序与确定性质量硬门/评分。
- [x] M3C：Shared 已实现对白/旁白稳定 ID、原文不改写、100% 覆盖、自动气泡/说明框和可信人物尾巴，并已接 Server 规则降级运行路径。
- [x] M3C-M5：把 V2/command actor/字段级保护接入 Web；full reflow 进入当前稿/新排法 Pending 预览，可保留、应用和一次 Undo。
- [x] M3C-M6：接入真实视觉 Provider（2026-07-23 完成；未配置 AI key 或分析失败时逐 Shot 规则回退）。
- [x] M3C-M6 的 scoped 智能调整（场景/页面/段落/选区）已实现，但**随即于 2026-07-24 按 ADR-0022 从页面删除**：普通页面不再提供二次智能调整、范围/intent、建议对比与应用/放弃。视觉分析本身保留，用于 `textSafeRegion` 避让。
- [x] M3C：普通首次进入只显示单一成稿进度和失败恢复，成功后进入现有一等编辑器；不新增参数向导或第二套编辑器。
- [x] 旧骨架：目录包 + manifest；不等于可下载素材包。
- [ ] 后置 G6：实现素材包 V2、真实 ZIP、下载、PackageRevision 和摘要审计。

### 验收

- [x] 旧骨架可让已锁定分镜进入一镜一页。
- [x] G5 publication 产物按新契约注册为不可变 Asset/Artifact 并通过真实渲染验收。
- [x] M3C M5 浏览器路径首次进入零配置；固定语料已证明全部 active Shot、对白和旁白完整映射、原文不改写且无 overflow。
- [x] M3C M6 真实视觉分析已接线并在 E2E 覆盖条漫选区预览/应用/Undo/释放保护与页漫当前页预览/放弃；人工保护用例通过。
- [ ] M3C 代表性验收章节中至少 80% 画格无需移动或缩放即可继续出版。**未取得实测证据**：人工 A/B 为 0/2 且已转为可选研究，部分气泡美观列为已接受风险。这是 M3 唯一未达成项，需真实章节抽样才能判定。
- [ ] 后置 G6 的 ZIP 素材包包含可验证 manifest；旧目录 manifest 不算完成。

## 8. M4 轻漫剧基础版

### 目标

用户能基于已锁定漫画图导出基础 MP4。

### 建议任务

- [ ] 建立 VideoDraft/VideoSegment 模型。
- [ ] 实现图片序列时间轴。
- [ ] 实现 TTS 任务。
- [ ] 实现字幕生成。
- [ ] 实现 BGM 引用。
- [ ] 实现 FFmpeg MP4 导出。

### 验收

- [ ] MP4 能播放。
- [ ] 字幕与 TTS 对齐。
- [ ] 视频 Asset 可追溯到源分镜。

## 9. M5 输入侧与配置侧能力

### 目标

七步主流程之外的支撑能力：把长篇原著文本变成可用的章节输入，把模型与凭证变成用户可自助管理的配置。这些能力不进入七步导航。

### 任务

- [x] 长篇原著资产库只读方案评估（2026-07-25）。结论“有条件可行”，未改功能代码；阻断项为大文件上传、全书自动 AI、Repository/Workbench 全量加载、七处章节下拉、非剧本步骤路由丢章。是否实施待定。
- [x] 文稿库 M1～M3（2026-08-04）：`DocumentWork`/`DocumentChapter` 模型、`/documents` 与 `/documents/:id` 路由、上传 .txt/.md 自动拆章、只读原文浏览、重命名与删除。
- [x] 拆章引擎 `packages/shared/src/document-splitter.ts`：三层规则（章节号变体/卷识别含卷章同行/空行分块兜底），连续性 warning，确定性幂等。
- [x] 编码识别 `packages/shared/src/document-encoding.ts`：UTF-8 严格校验 → gb18030 兜底。
- [x] 模型管理（2026-08-05）：设置页「模型管理」tab，对话/图片两个区块，`models[]` + active 指针 CRUD，预置 6 项。
- [x] 对话运行时衔接（2026-08-05）：默认对话模型跟随 `activeTextModelId`，对话面板模型选择器，消息气泡显示所用模型。
- [x] 模型管理卡片式重设计（2026-08-06）。
- [x] 模型凭证统一重构（2026-08-10）：`CredentialService` 集中凭证读写，DB 模式凭证跨重启恢复，DB 模式只读、写入统一由 `SettingsService` 处理。
- [x] 图片生成运行时接入 `activeImageModelId`（原 S3）：`settings.service.ts:267` `getRuntimeImageProviderSettings()` 先解析选中的图片模型，协议按 providerId 关键字推断（doubao/grok/runware，默认 openai），凭证两层回退＝模型自身凭证 → 同 providerId 固定槽位；无选中模型时才整体回退固定槽位。**注意：`2026-08-05_对话与图片模型多选切换/progress.md` 第 48 行仍写"未实施"，与同文件第 82 行及代码矛盾，以代码为准。**

### 验收

- [x] 真实《凡人修仙传》1.txt（GBK）2574 章、2.txt（UTF-8）1396 章，合计 3970 章全部识别、0 断裂、14 卷组。
- [x] 真实浏览器冒烟：上传 → 自动跳详情 → 1396 章按 100 章分桶 → 分组折叠/章节切换/重命名/删除全通过，零页面错误。
- [x] DB 模式与文件模式凭证均可保存、读取并跨重启恢复（`settings.service.spec` M-08/M-12）。
- [x] `pnpm -w typecheck` 通过；settings + credential 测试 40/40（2026-08-10 复跑）。

### 已知限制

- DB 模式下固定槽位镜像出的模型行不可在模型管理删除（`MANAGED_MODEL_FIXED_SLOT_DELETE_FORBIDDEN`）。固定槽位与 `models[]` 在数据模型层双轨仍并存，未按原计划废弃；UI 层已于 2026-08-06 移除对应 tab，固定槽位降级为用户不可见的后端兼容层，两侧仍由 `syncActiveTextModelWithAIKey` / `syncActiveImageModelWithProvider` 双向同步。
- 凭证重构原计划的「`settings.service.ts` 减少 300–500 行」未达成，实际净 +323 行；阶段 4（删同步逻辑）与阶段 5（settings.json v3）均未执行，前置条件都是固定槽位退役。偏差明细见 `文档/05_执行与记录/任务记录/2026-08-10_模型凭证统一重构/执行计划.md`。

## 10. 状态维护规则

- 每完成一个任务，更新本文件对应勾选项。
- 每完成一个功能，新增完成记录。
- 如果阶段目标变化，更新 `MVP范围与路线图.md` 并视情况新增 ADR。
- **完成记录写了但本文件未同步，等于断档。**2026-07-25～08-10 的四轮工作曾出现该情况（完成记录齐全，路线图停留 7-23），2026-08-10 已回填。
