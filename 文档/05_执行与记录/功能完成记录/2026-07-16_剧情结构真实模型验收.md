---
doc_id: AIR-COMPLETION-20260716-STORY-STRUCTURE-REAL-MODEL
status: completed
created: 2026-07-16
updated: 2026-07-16
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: 剧情结构真实模型验收任务记录、隔离数据库与浏览器用户路径
---

# 剧情结构真实模型验收

## 功能摘要

使用真实 `self/gpt-5.5` 和两个隔离新项目，完成 AI 创作正式章节与已有剧本导入正式章节的剧情结构生成、预览、确认和下游解锁验收。两条路线均为 `passed_real_model`，未发现需要修改产品代码的缺陷。

## 影响范围

- 只新增验收与留痕文档。
- 不修改页面字段、StoryStructure Schema、数据库结构、确认节点或公开 Skill。
- 不改变 A1～A5、B1～B5 已确认流程。

## 用户路径结果

| 路线 | 正式正文 | 结构结果 | 确认后状态 |
| --- | ---: | --- | --- |
| AI 创作 | 6446 字，用户采用 AI 草稿后完成本章 | 3 角色 / 7 场景 / 15 beats | `structured`，分镜解锁 |
| 已有剧本 | 第 1 章 1016 字，`origin=import` | 3 角色 / 1 场景 / 6 beats | `structured`，分镜解锁 |

已有剧本项目还验证了：

1. 原稿分析结果展示 4 个带边界证据的章节候选。
2. 用户整体确认目录一次后，后台整理并验证全部 4 章。
3. 用户可自由切换并只读查看待确认全文。
4. 只确认第 1 章后，其余 3 章仍保持 `pending_ready`，不会批量形成正式版本。
5. 待确认导入章不提供保存、采用、丢弃或手动整理入口。

## 数据与协议检查

- AI 创作 StoryVersion 精确绑定 ScriptVersion `c8643c58-ea0b-496d-be2b-904bc839b60b`。
- 已有剧本 StoryVersion 精确绑定 import ScriptVersion `9f53cd70-df6a-4721-a192-e93f465d1aaa`。
- 两个 `sourceScriptVersionId = currentScriptVersionId` 均为真。
- 隔离 SQLite `PRAGMA integrity_check` 返回 `ok`。
- 没有新增或修改协议字段。

## 修改文件

- `文档/05_执行与记录/任务记录/2026-07-16_剧情结构真实模型验收/`
- `文档/05_执行与记录/功能完成记录/2026-07-16_剧情结构真实模型验收.md`
- `文档/06_测试与验收/自动化测试体系.md`
- `文档/会话/2026-07-16-17-46-剧情结构真实验收.md`
- `文档/记忆/MEMORY.md`

## 验证结果

- Runtime/User Review：`passed_real_model`。
- Scrutiny Review：`passed`。
- 浏览器控制台：error/warn 0。
- 页面与数据库状态一致；两章均确认结构并解锁分镜。
- 本轮没有产品代码改动，因此不重复运行已在前序质量门提交中全绿的 Server 652 项自动化；固定门自动证据继续由 `2026-07-16_剧情结构固定质量门.md` 承担。

## 已知边界

- 原稿明确使用“第一场～第四场”标题，模型据此提出 4 章；这是用户确认前的候选判断，不代表所有输入都会采用相同粒度。
- 本轮各验证一个真实模型样例，不等同于所有题材、超长稿和艺术质量的穷尽评测。
- AI 正文正式版本显示 `origin=user` 是因为用户采用 AI pending 后点击完成；AI 来源仍由 revision、对话消息和工具结果保留。

## 后续建议

继续维持“自动固定反例 + 少量真实模型样例”的双层验收方式。只有出现真实误杀或漏拦时，才把具体失败样例转成最小固定回归；不要为了样例评分增加页面字段或新的确认节点。
