---
doc_id: AIR-G3-M3-A9-DONE-001
status: active
created: 2026-07-12
updated: 2026-07-12
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: A9 任务记录与 SQLite 集成证据
---

# G3-M3-A9 物理资产与视觉 shadow 导入完成记录

## 功能摘要

校验 snapshot 中 Asset 物理文件的真实摘要，将文件安全落到目标 workspace；满足 G1 ready 条件后导入 CharacterVisual、SceneVisual，并恢复 current visual 指针。

## 影响范围

- 新增 `apps/server/src/migration/asset-visual-shadow-importer.ts`。
- `db:import` 新增 `--slice asset-visuals --workspace-root <absolute-path>`。
- 物理证据单独写入 `AssetPhysicalEvidence` 来源账本。

## 修改文件

- `apps/server/src/migration/asset-visual-shadow-importer.ts`
- `apps/server/src/migration/db-import.cli.ts`
- `apps/server/src/migration/project-chapter-shadow-importer.integration.spec.ts`
- G3 handoff、A9 任务记录与会话记忆。

## 验证命令与结果

- A9 定向集成通过：2 个 ready Asset、1 个 CharacterVisual、1 个 SceneVisual、两个 current 指针、replay 零新增。
- 全量 server 回归：44 个测试文件、247 项测试通过。
- typecheck 通过；G1 manifest/schema/migration 三项门禁通过；缺 workspaceRoot 返回 `MIGRATION_WORKSPACE_ROOT_INVALID`；`db:import --kind final` 返回 `MIGRATION_FINAL_IMPORT_NOT_READY`；`git diff --check` 通过。

## 已知风险

- 当前仅覆盖角色预览视觉和结构场景 referenceAssetId；Candidate/Lock、Preflight、Task、Layout/Export、Dialogue 仍未导入。
- 目标 workspace 文件复制与 DB promote 之间仍需由后续 verifier/恢复流程覆盖崩溃补偿。

## 后续建议

下一步进入 Preflight source snapshot 与旧 Candidate/Lock 导入；所有新任务仍只能引用 ready Asset 和 available Visual。

## 提交

本记录对应提交：`b203647`。
