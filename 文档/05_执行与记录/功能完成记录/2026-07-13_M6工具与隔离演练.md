---
doc_id: AIR-M6-CUTOVER-TOOLING-COMPLETE-001
status: completed
created: 2026-07-13
updated: 2026-07-21
owner: AI漫游项目
audience: human, ai-agent, qa
source: M6 任务目录与 G3-M 施工包
---

# M6 工具与 C0-C7 隔离演练完成记录

## 功能摘要

完成 DB-only 激活工具、首笔业务写事务边界、file bridge 保护、metadata-only archive 和临时根 C0-C7 顺序演练。真实切换不在本记录授权范围内。

2026-07-21 生命周期更新：早期 callback 版 `cutover-coordinator.service.ts` 已被正式 `DbCutoverService`、`CutoverEvidenceStore` 与 `cutover-runner.service.ts` 完整替代并删除；真实隔离演练已改用正式证据格式。本记录中的旧文件名仅表示当时交付历史。

## 影响范围

- 新增 `apps/server/src/migration/db-activate.service.ts`、`db-activate.cli.ts`、`cutover-coordinator.service.ts`、`metadata-archive.service.ts`。
- `PrismaService` 增加业务事务边界；ProjectRepository 三条公开 DB 写路径接入该边界。
- 新增 file bridge guard 与 M6 定向/临时根测试。

## 协议变化

- `db:activate --format json ... --gate ACT-08 --dry-run|--execute`。
- dry-run 不更新 PersistenceState；execute 只做 `ready_for_activation -> db_only + activatedAt`。
- 首次 DB-only 业务事务设置 `firstBusinessWriteAt`；首写后 file-only 启动受保护。

## 验证

- M6 定向 5 files/12 tests：通过。
- 服务端全量 59 files/403 tests：通过。
- workspace typecheck、web build、Prisma validate、G1 manifest/schema/migration、git diff check：通过。
- 临时根 C0-C7：8/8 通过，未使用真实根、凭据、Keychain、provider 或正式停写。

## 已知风险与后续

正式 pre-cutover backup、正式停写、真实 `db:activate --execute`、观察期和真实 RB-01～06 必须由用户另行 go/no-go 授权；本记录不宣称真实切换完成。
