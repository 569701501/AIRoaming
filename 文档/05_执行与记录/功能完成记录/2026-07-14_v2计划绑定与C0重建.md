---
doc_id: AIR-RCUT-V2-C0-DONE-001
status: active
created: 2026-07-14
updated: 2026-07-14
owner: AI漫游项目
audience: human, ai-agent, release-owner
source: R1 C3 settings 起点修复、v2 SH-10 与 C0 证据
---

# v2 计划绑定与 C0 重建完成记录

## 功能摘要

在不修改项目代码、不篡改旧证据的前提下，把错误的 legacy settings 起点修正为真实的 `already_sanitized/verify_existing`，完成新的 digest-bound SH-10 gate 与只读 C0 证据。

## 影响范围

- 新建仓库外 v2 私有计划与证据身份；旧 plan/gate/AUTH/C0～C2 保持不变。
- 更新 R0-R2 任务进度、发现、授权清单、G1 验收清单和长期/会话记忆。
- 未修改 `apps/`、`packages/`、`tests/`，未改变数据库、schema、协议或业务数据。

## 关键结果

| 项目 | 结果 |
| --- | --- |
| settings 起点 | `already_sanitized`，credential action=`verify_existing` |
| v2 plan | `sha256:675bb346...6e185af` |
| v2 SH-10 gate | `sha256:be1209a7...047414` |
| v2 C0 | `CUTOVER_C0_OK` |
| v2 C0 evidence | `sha256:e173a8e0...262cf1` |
| 当前状态 | `blocked_waiting_v2_auth_c1` |

## 验证

- 冻结 release 生产 plan/gate/evidence reader 全部通过。
- evidence 链只有 C0，私有目录 0700、文件 0600。
- 5 files / 24 tests 全通过。
- `git diff --check` 通过，代码目录无 diff。
- target DB、snapshot、runtime bundle、credential expectations、AUTH-C1 均未生成；未调用 Keychain。

## 已知风险

- 旧 identity 的 C1/C2 不能接续 v2；新 AUTH-C1 后必须从 C1 顺序执行。
- C1 停写与 C3 Keychain 只读验证仍是独立人工授权边界，本记录不构成授权。

## 后续建议

由用户使用 `already_sanitized` AUTH-C1 文本，明确绑定 v2 C0 evidence=`sha256:e173a8e0...262cf1`；授权前保持停止。
