---
doc_id: AIR-COMPLETE-G4-C-001
status: completed
created: 2026-07-15
updated: 2026-07-15
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: G4-C code commit 179be50 与阶段 Review
---

# G4-C 候选定稿事务与 API 完成记录

## 功能摘要

新增 DB-only 候选定稿 preview/commit/history、收藏、废弃/恢复和完成命令；定稿采用 impact digest + expected revision + Shot pointer CAS，两写者竞争、丢响应重放和影响变化均 fail-closed。

## 影响范围

- Shared：history/preference DTO，commit reason null 契约修正。
- Server：CandidateDecisionService、CandidateLockRepository、错误映射与新 Controller 路由。
- 旧入口：删除旧公开 lock 路由和旧 DB 直接 revision/pointer 写实现；file-mode 兼容逻辑保留。

## 修改文件

- `packages/shared/src/candidate-lock.ts`
- `apps/server/src/projects/candidate-decision.service.ts`
- `apps/server/src/projects/candidate-lock.repository.ts`
- `apps/server/src/projects/candidate-lock-error.ts`
- `apps/server/src/projects/projects.controller.ts`
- `apps/server/src/projects/projects.service.ts`
- `apps/server/src/projects/image-candidate.service.ts`
- 对应 Shared、Server 与数据库集成测试。

## 数据或协议变化

- 无 Schema 变化；复用 G4-A 0012 overlay。
- 新增 `/shots/{shotId}/candidate-lock/preview`、`/shots/{shotId}/candidate-lock`、history、favorite、rejection 路由。
- complete 的 DB 写权威收口到 CandidateDecisionRepository；旧 response projection 只读。

## 验证结果

- Shared 54/54。
- Server 520/525；5 个并行超时旧用例隔离复跑 5/5。
- G4-C fresh SQLite/HTTP 定向 1/1；类型、构建、Prisma validate、diff check 通过。

## 已知风险与后续

- Web 仍待 G4-E 改用新两阶段 API；G4-D 先补 Workbench/ProductionState 和下游 freshness 门禁。
- G4-F 执行完整 API 历史链、restart/backup restore 和浏览器用户验收。
