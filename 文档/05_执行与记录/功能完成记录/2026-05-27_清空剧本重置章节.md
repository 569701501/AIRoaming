# 清空剧本重置章节

---
doc_id: AIR-EXEC-COMPLETION-20260527-SCRIPT-RESET
status: active
created: 2026-05-27
updated: 2026-05-27
owner: AI漫游项目
audience: human, ai-agent, developer
source: 用户反馈“项目下其实是有剧本的，我清不掉”
---

## 1. 功能摘要

剧本编辑器新增“清空剧本”入口。用户确认后，后端会删除当前项目旧章节目录，重建空白 `chapter-001`，并刷新工作台当前章节。

## 2. 背景

旧导入逻辑曾把非剧本文档按 `1`、`2`、`3` 等标题拆成大量章节。用户在前端只能清空当前章节文本，无法一次删除项目下所有错误章节；同时后端写新章节时如果不删除旧 `chapters/` 目录，服务重启后旧目录还会被 workspace 扫描恢复。

## 3. 影响范围

- 新增项目级清空剧本 API：`POST /api/projects/{projectId}/script/reset`。
- 清空时删除旧 `chapters/` 目录和历史 `story/` 目录，重建空白 `chapter-001`。
- 整本剧本导入写入章节前会先清理旧章节目录，避免被替换掉的旧章节残留。
- 前端剧本编辑器新增“清空剧本”按钮，确认后刷新章节列表并跳回第 1 章。
- 当前 workspace 中项目“测试 1”的错误导入章节已清理为一个空白第 1 章。

## 4. 修改文件

```text
packages/shared/src/dto.ts
apps/server/src/projects/projects.controller.ts
apps/server/src/projects/projects.service.ts
apps/web/src/services/api.ts
apps/web/src/stores/workbench-store.ts
apps/web/src/components/layout/AppShell.vue
apps/web/src/components/workbench/ProjectWorkbenchView.vue
apps/web/src/components/workbench/ScriptDocumentEditor.vue
文档/02_架构与契约/核心数据模型.md
文档/03_模块梳理/模块总览与依赖.md
文档/04_方案与决策/2026-05-27_剧本对话功能再设计方案.md
```

## 5. 数据或协议变化

新增响应 DTO：

```ts
interface ResetProjectScriptResponse {
  chapter: ChapterDetail;
  chapters: ChapterListItem[];
}
```

清空后本地目录目标状态：

```text
workspace/projects/{projectId}/project.json
workspace/projects/{projectId}/workflow.json
workspace/projects/{projectId}/chapters/chapter-001/chapter.json
workspace/projects/{projectId}/chapters/chapter-001/script.md
```

其中 `script.md` 为空文本，`project.currentChapterId` 为 `chapter_001`，章节状态为 `draft`。

## 6. 验证结果

已执行：

```text
corepack pnpm --filter @airoaming/shared build
corepack pnpm --filter @airoaming/server typecheck
corepack pnpm --filter @airoaming/web typecheck
```

结果：通过。

使用现有本地服务验证：

```text
POST /api/projects/b224e22c-9cdf-4195-851c-6bae207789c8/script/reset
```

结果：

- 项目从 75 个误导入章节重置为 1 个空白章节。
- `chapters/chapter-001/script.md` 为 0 字节。
- `story/` 旧目录已删除。
- `WorkbenchSnapshot` 返回 `chapterCount = 1`、`currentChapter = chapter_001`、`status = draft`。

## 7. 已知风险

- 清空剧本是破坏性动作，当前前端使用浏览器确认框防误触；后续可升级为项目内确认弹窗，并显示将删除的章节数量。
- 当前尚无章节回收站或撤销清空能力；正式用户数据接入前应补版本回退或回收站策略。

## 8. 后续建议

1. 在清空确认弹窗中展示章节数量和最近更新时间。
2. 后续接数据库时，把“替换整本剧本前先删除旧章节集合”固化为事务边界。
