---
doc_id: AIR-D2-A2-1-COMPLETE-001
status: completed
created: 2026-07-13
updated: 2026-07-13
owner: AI漫游项目
audience: human, ai-agent, qa
source: A2-1 implementation, scrutiny review, runtime review
---

# D2-A2-1 非破坏性公开写闭环完成记录

## 功能摘要

数据库模式已接通项目 metadata、按序建章、AI ChapterScriptPending/revision、Script Outline draft/confirm；G2 Script Working Copy/Pending/Publish 已成为 Web DB 模式的正式写链。file mode 保留旧 API，DB 模式旧写入口 fail-closed。

## 影响范围

- 新增 `ProjectScriptCommandRepository` 和 runtime command ID helper。
- ProjectRepository/ScriptVersionService 在 DB mutation 后刷新 identity map。
- WorkbenchSnapshot 新增 `versioningCapability`；Web store/API 具备 DB/file 双模式 Script 路由。
- capability registry 仅将 5 个 A2-1 operation 标为 implemented；两个聚合项仍 partial，blockedIds 仍为 6。

## 修改文件

主要代码位于 `apps/server/src/projects/`、`apps/server/src/projects/versioning/`、`apps/server/src/dialogue/`、`apps/web/src/services/api.ts`、`apps/web/src/stores/workbench-store.ts` 和 `packages/shared/src/`；未修改 schema、migration、G1 generator 或 trigger。

## 验证命令与结果

- `vitest run` server 全量：54 files / 360 tests PASS。
- A2-1 fresh SQLite 主链：14/14 PASS；能力/来源 guard 定向：13/13 PASS。
- workspace `typecheck` PASS；Web `build` PASS。
- `prisma validate` PASS。
- `g1:manifest:check`、`g1:schema:check`、`g1:migration:check` PASS。
- `git diff --check` PASS。

## 已知风险

- A2-2 clear/import/reset 尚未实现；character/story/storyboard/preflight 其余公开写仍保持阻塞。
- SQLite 现有 check constraint 要求 pending/revision 的 thread、message、toolCall 全存或全空；缺失 Dialogue FK 时实现三者全 null，不伪造 Conversation 行，待 D2-A5 runtime 接入后再补完整 provenance。
- 未触碰真实 workspace、真实数据库、真实 provider、真实 Keychain 或凭据；当前不是 production-ready。

## 后续建议

连续总 Handoff 下一阶段进入 D2-A2-2，先设计章节退役/影响预览/备份语义，再实现 clear/import/reset 和 legacy operation 退役，不得直接物理删除历史或让 blockedIds 提前减少。
