---
doc_id: AIR-COMPLETE-20260716-SCRIPT-IMPORT-B1-B5
status: completed
created: 2026-07-16
updated: 2026-07-16
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: 2026-07-16_已有剧本生产接线任务记录
---

# 已有剧本 B1～B5 生产路线完成记录

## 功能摘要

已有完整剧本路线已从旧 file-mode 分析/整本覆盖，切换为可追溯的 DB-only B1～B5 生产链：不可变原稿 → 观察性分析 → 拆章目录一次确认 → 全部章节逐章整理与忠实度验证 → 全文只读查看 → 逐章直接确认正式版本。

## 影响范围

- Shared：导入确认 DTO、批次结果、来源 kind 和对话工具结果。
- Server：严格 B2/B4 Prompt、意图识别、原稿/候选/map/batch 仓储、批次编排、专用确认接口。
- Web：分析与批次结果卡、项目级决策线程、import pending 只读动作、逐章确认与刷新恢复。
- Skill：`script-import-normalize` 改为 B1～B5 严格边界。
- 测试：单元、fresh SQLite 集成、E2E fake provider 和 DB-only Chromium 路径。

## 数据与协议变化

- 未新增数据库 migration；复用 `0017_script_dual_flow_source_state`。
- `ChapterPendingSourceText.kind` 区分 `legacy | ai | import`。
- 新增导入章节专用确认请求/响应和批次结果 DTO。
- import pending 不经过 Working Copy；确认事务直接形成 `ChapterScriptVersion(origin=import)`。
- StoryStructure payload 和页面内容字段保持不变。

## 主要修改文件

- `packages/shared/src/dto.ts`
- `apps/server/src/dialogue/script-dialogue.service.ts`
- `apps/server/src/dialogue/script-import-batch.service.ts`
- `apps/server/src/dialogue/dialogue-prompt.util.ts`
- `apps/server/src/projects/script-workflow-source.repository.ts`
- `apps/server/src/projects/projects.controller.ts`
- `apps/web/src/components/workbench/ProjectDialoguePanel.vue`
- `apps/web/src/components/workbench/ScriptDocumentEditor.vue`
- `apps/web/src/stores/workbench-store.ts`
- `tests/e2e/web/script-import-existing-flow.spec.ts`
- `apps/server/opencodeAI/skills/script-import-normalize/SKILL.md`

## 验证结果

- `corepack pnpm --filter @airoaming/shared test`：26 files / 152 tests passed。
- Server single-fork 全量：100 files / 590 tests passed。
- `corepack pnpm typecheck:e2e`：passed。
- `corepack pnpm test:e2e:prepare`：3/3 passed，内部 env 34/34。
- DB-only Chromium 已有剧本路径：1/1 passed。
- DB-only Chromium AI 显式单章回归：1/1 passed。
- `corepack pnpm -r typecheck`、`corepack pnpm -r build`：passed。
- `script-import-normalize` Skill quick validation：passed。

## 已知风险

- B4 当前同步运行于目录确认请求，章节很多时等待时间较长。
- 失败项已隔离持久化，但没有用户可见的重试/继续入口。
- 超长稿不被服务端主动截断，但尚无分层上下文方案；超过模型上下文时会真实失败，不伪造覆盖率。

## 后续建议

优先补可恢复后台 worker、失败项重试和超长稿分层分析。任何增强都必须保留当前产品语义：目录整体确认一次、章节逐章确认、导入草稿只读、页面内容字段不变。

## 后续状态更新

以上三个增强已在 `2026-07-16_已有剧本长稿后台与重试.md` 完成；本记录中的“同步等待、无重试、无长稿分层”只描述 B1～B5 首次接线完成时的历史状态。
