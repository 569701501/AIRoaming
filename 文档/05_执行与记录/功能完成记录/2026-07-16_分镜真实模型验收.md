---
doc_id: AIR-COMPLETION-20260716-STORYBOARD-REAL-MODEL
status: completed
created: 2026-07-16
updated: 2026-07-16
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: 分镜 S3 任务记录、隔离数据库、浏览器与自动化回归
---

# 分镜真实模型验收

## 功能摘要

使用真实 `self/gpt-5.5` 和两个隔离新项目，完成 AI 创作与已有剧本导入两种正式 StoryVersion 的分镜生成、固定门、待确认预览、用户确认和版本来源验收。两路均为 `passed_real_model`，证明在当前样例中可以共用同一分镜 Prompt 和确认流程。

## 影响范围

- 分镜页面字段、Storyboard Schema、数据库结构和确认节点保持不变。
- 修复 AI 章节 pending 的对话来源作用域错误。
- OpenCode 文本生成会话统一禁用文件、命令和其他系统工具，新旧会话都受保护。
- 增加相应回归、任务留痕和测试基线说明。

## 用户路径结果

| 路线 | 正式结构 | 分镜结果 | 质量门表现 | 确认后状态 |
| --- | --- | --- | --- | --- |
| AI 创作 | 6 scenes / 12 beats | 24 shots | 首次通过 | `storyboard_done` |
| 已有剧本 | 3 scenes / 7 beats | 12 shots | 首次空对白画格失败，一次修复通过 | `storyboard_done` |

两个项目确认前均无正式 StoryboardVersion、章节仍为 `structured`、出图准备禁用；确认后才发布正式版本并解锁下游。

## 数据与协议检查

- AI：Script `0996d782...` → Story `ee2bdc0f...` → Storyboard `76a1d0fd...`。
- import：Script `1e1bb4a9...` → Story `c889b0f4...` → Storyboard `6c229ede...`。
- 两路 beat 覆盖均为 100%，未知/倒序引用与 `promptDraft` 禁止内容均为 0。
- SQLite `integrity_check=ok`；没有 migration 或协议字段变化。

## 修改文件

- `apps/server/src/ai-runtime/opencode-runtime.service.ts`
- `apps/server/src/ai-runtime/opencode-runtime.service.spec.ts`
- `apps/server/src/projects/script-workflow-source.repository.ts`
- `apps/server/src/projects/script-workflow-source.repository.integration.spec.ts`
- `apps/server/src/dialogue/dialogue-text.util.ts`
- `apps/server/src/dialogue/dialogue-text.util.spec.ts`
- `文档/04_方案与决策/2026-07-16_分镜及后续提示词改造顺序.md`
- `文档/05_执行与记录/任务记录/2026-07-16_分镜真实模型验收/`
- `文档/06_测试与验收/自动化测试体系.md`
- `文档/会话/2026-07-16-21-05-分镜真实模型验收.md`
- `文档/记忆/MEMORY.md`

## 验证结果

- Runtime/User Review：`passed_real_model`。
- Scrutiny Review：`passed`。
- 聚焦回归：3 files / 7 tests 通过。
- Server typecheck：通过。
- Server 全量：114 files / 681 tests；并发 2 项既有 5 秒超时，隔离重跑 2/2 通过。
- 浏览器控制台：两个项目均 error/warn 0。

## 已知边界

- 两个真实文本样例不等同于跨题材稳定率和商业质量穷尽评测。
- 本轮没有授权或调用真实图片 provider；角色/场景一致性、构图和图片审美仍未验收。
- 两个重型 Server 测试仍有并发固定超时稳定性债。

## 后续建议

下一步是 S4 真实图片评测，但必须由用户单独授权 provider、模型与费用范围。评测应固定镜头集并按 OpenAI、Doubao、Grok 分开记录，不能把不同模型混为一个总分。
