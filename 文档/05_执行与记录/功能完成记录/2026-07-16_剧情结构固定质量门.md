---
doc_id: AIR-COMPLETE-20260716-STORY-STRUCTURE-QUALITY
status: completed
created: 2026-07-16
updated: 2026-07-16
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: A+ 双流程汇合点、剧情结构运行时审查与固定回归
---

# 剧情结构固定质量门完成记录

## 功能摘要

AI 创作和已有剧本导入形成正式章节后，共用的剧情结构生成阶段已增加正式正文来源密封、场景/人物/beat 完整性检查和一次定向修复。页面字段、生成/预览/确认动作和 StoryStructure 数据结构保持不变。

## 影响范围

- Server Prompt：明确正文事实源、大纲禁止补写、逐场覆盖和引用规则。
- Server 编排：首次格式或质量失败只重做一次，第二次失败不产生待确认结构。
- Server 来源门：DB Working Copy 必须 clean，按 current ScriptVersion 读取正式正文。
- 测试：新增质量工具、Prompt 和服务编排固定正反回归；复跑现有 DB 浏览器确认链。

## 修改文件

- `apps/server/src/dialogue/dialogue-prompt.util.ts`
- `apps/server/src/dialogue/story-structure-dialogue.service.ts`
- `apps/server/src/dialogue/story-structure-quality.util.ts`
- `apps/server/src/dialogue/story-structure-quality.fixture.ts`
- `apps/server/src/dialogue/story-structure-quality.util.spec.ts`
- `apps/server/src/dialogue/story-structure-dialogue.service.spec.ts`
- `apps/server/src/dialogue/dialogue-creative-prompt.spec.ts`
- `文档/02_架构与契约/2026-07-16_双流程来源与状态契约.md`
- `文档/06_测试与验收/自动化测试体系.md`

## 数据与协议变化

- 无数据库 migration、Schema、API、DTO、页面字段或 StoryStructure payload 变化。
- 没有新增公开 Skill；A1～A5、B1～B5 和两路正式汇合点不变。
- DB 模式读取正式正文的约束增强：dirty Working Copy、剧本 pending 或 current 版本不一致时，在模型调用前停止。

## 验证结果

- 聚焦测试：3 files / 17 tests passed。
- Server 全量：107 files / 652 tests passed。
- `pnpm typecheck`：passed。
- `pnpm typecheck:e2e`：passed。
- `pnpm build`：passed；仅保留既有 Web 大 chunk 警告。
- DB-only Chromium `W1-E2E-05`：1/1 passed，run ID `g0-83878-mrnaizws-466a9c3e`。
- `git diff --check`：passed。

## 复核结论

- Scrutiny Review：通过。改动仅位于剧情结构 Prompt、内部校验、正式来源读取和测试；未改变页面、数据模型或双流程。
- Runtime/User Review：通过。现有页面生成预览、确认结构、角色同步和进入分镜的路径保持可用。

## 已知风险

- 高置信规则不能判断节奏、情绪力度、人物弧或商业质量；这些仍由模型和用户判断。
- 历史非固定 Markdown 章节无法执行逐场一一对照，只执行结构自身的非空、枚举和引用校验；当前固定格式章节执行完整覆盖门。
- 真实模型仍可能在一次修复后失败；系统会停止而不是放宽正文忠实边界。

## 后续建议

保持当前页面和字段不变。后续只根据真实模型的误杀、漏拦或正文覆盖事故增加最小反例，不增加评分面板或新的结构产物。
