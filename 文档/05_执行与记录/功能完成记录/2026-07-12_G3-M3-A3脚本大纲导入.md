---
doc_id: AIR-G3-M3-A3-DONE-001
status: completed
created: 2026-07-12
updated: 2026-07-12
owner: AI漫游项目
audience: human, ai-agent, luna
source: A3 任务记录与 SQLite 集成证据
---

# G3-M3-A3 Script/Outline shadow 导入完成记录

## 功能摘要

在 A2 Project/Chapter shadow 之后导入项目级剧本大纲和章节不可变 ScriptVersion，并恢复数据库 current 指针。

## 影响范围

- 新增 `ScriptOutlineShadowImporter`，只读取 sealed snapshot，复用 decisions digest 和 Prisma migration ledger。
- `db:import` 增加显式 `--slice script-outline`；默认 `project-chapter` 行为保持兼容，`final` 继续 fail-closed。
- currentScriptVersionId 更新遵守 G2 working-copy shape 与 rowVersion 触发器；同库 replay 不重复插入或推进指针。

## 修改文件

- `apps/server/src/migration/script-outline-shadow-importer.ts`
- `apps/server/src/migration/project-chapter-shadow-importer.integration.spec.ts`
- `apps/server/src/migration/db-import.cli.ts`
- `apps/server/src/migration/migration-report.ts`

## 数据与协议变化

- `script-outline.md/json` 映射为 `ProjectScriptOutline` version 1；confirmed 状态必须有 confirmedAt。
- `script.versions/script-vNNN.md` 映射为 `ChapterScriptVersion`，origin 固定为 `import`，正文使用规范化 Script digest。
- 来源账本使用稳定 `ProjectScriptOutline`/`ChapterScriptVersion` sourceKey 和 `complete` provenance；缺失 current 指针只计 warning。

## 验证

A3 集成链路、server 全量 44 文件/240 测试、typecheck、G1 manifest/schema/migration check、final fail-closed CLI 和 diff check 通过，命令明细见任务目录 `evidence/commands.md`。

## 已知风险

本记录不代表 G3-M3 full importer 完成。`script-pending.json`、`script.revisions/latest.json`、Story/Storyboard/Preflight、Task、Asset、Candidate、Layout、Dialogue、db-verify、backup、activate 和两轮 shadow 等价仍未实现。

## 后续建议

下一切片补齐 Script pending/revision 证据导入，再进入 Story/Storyboard importer；继续沿用 Project subtree 单事务和来源账本。
