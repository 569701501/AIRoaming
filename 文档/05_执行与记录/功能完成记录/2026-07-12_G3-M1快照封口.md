---
doc_id: AIR-G3M1-COMPLETE-001
status: completed
created: 2026-07-12
updated: 2026-07-12
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: G3-M1 snapshot/runtime bundle 实现
---

# 功能摘要

完成 G3-M1 sealed runtime bundle 与 workspace snapshot 封口：验证 runtime bundle、双 manifest、settings 脱敏、路径安全、pre/post 稳定性和原子 sealed publish。

# 影响范围

- 新增 `apps/server/src/migration/` 的 snapshot、redactor、runtime bundle 文件服务、CLI 和测试。
- `apps/server/package.json` 新增 `db:snapshot`，并同步 G1 machine manifest。
- 未改变 Prisma Schema、G3 enum 或 migration 0010。

# 数据/协议变化

- 新增 `airoaming_snapshot_manifest_v1`、`airoaming_snapshot_transforms_v1`、`airoaming_snapshot_sealed_v1` 文件 envelope。
- snapshot 输出包含 source/snapshot manifest、transforms、redacted settings、runtime bundle、payload 和 `SEALED`。

# 验证

- SNP/runtime 定向测试 14/14。
- server 全量 38 files / 209 tests。
- typecheck 与 G1 manifest/schema/migration checks 全部通过。

# 已知风险

- M1 redactor 不是 SecretStore；未知秘密采用有限规则 fail-closed。
- snapshot 仍未进入 decision/import/verify/backup/activate，不能作为 production cutover 证据。

# 后续建议

进入 G3-M2：实现 comic-format decision codec、issue detail/resolution schema、decisionsDigest 和旧 run 不可变校验。

