---
doc_id: AIR-COMPLETE-20260719-WORKFLOW-BLOCK
status: completed
created: 2026-07-19
updated: 2026-07-19
owner: AI漫游项目
audience: human, ai-agent, developer
source: 用户真实页面故障、任务记录与回归证据
---

# 功能摘要

修复 DB-only 七阶段工作区的三类真实阻断：待确认/需更新阶段不可进入、已确认产物仍被旧对话显示为待确认、已生成场景图不回显。用户当前项目已从剧情结构正常进入并确认分镜，随后进入出图准备。

# 影响范围

- 顶部七阶段流程栏状态映射和样式。
- 剧情结构/分镜历史对话过程卡的当前状态显示。
- DB Workbench 剧情结构场景参考图只读投影。
- 剧情结构重复确认的幂等和无 pending 失败提示。

# 修改文件

- `apps/web/src/components/workbench/WorkbenchStageRail.vue`
- `apps/web/src/components/workbench/ProjectDialoguePanel.vue`
- `apps/server/src/projects/project-repository.service.ts`
- `apps/server/src/dialogue/story-structure-dialogue.service.ts`
- 对应 Server 与 E2E 回归测试。
- `文档/01_愿景与产品/当前UI信息架构.md`

# 数据或协议变化

- 无数据库 Schema 变化。
- 无任务协议变化。
- 无页面正式字段变化。
- StoryVersion 保持不可变；`referenceAssetId` 只存在于 Workbench DTO 投影。

# 验证结果

- Web、Server、E2E 类型检查：全部通过。
- Server：44/44 通过。
- DB Web/API E2E：2/2 通过。
- file-mode 阶段栏 E2E：1/1 通过。
- 真实浏览器路径：剧情结构 → 分镜工作台 → 确认分镜 → 出图准备，通过。

# 已知风险

- 用户当前项目仍需定稿 4 个出镜角色才能进入候选图，这属于出图准备业务门禁，不是流程栏故障。
- 本次未生成图片，也未验证付费候选图生成链路。

# 后续建议

在剧情结构页完成哑巴、商队多人、红心棺、商队众人的形象定稿；返回出图准备确认门禁清零后，再由用户明确触发候选图生成。
