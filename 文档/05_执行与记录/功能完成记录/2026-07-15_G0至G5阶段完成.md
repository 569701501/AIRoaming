---
doc_id: AIR-DONE-20260715-G0-G5
status: completed
created: 2026-07-15
updated: 2026-07-15
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: G0～G5 连续施工任务、阶段 Review、用户最终签收
---

# G0～G5 阶段完成记录

## 功能摘要

本轮 G0～G5 已完成技术实施、静态复核、运行复核与最终用户签收。最终状态为：

```text
G4 = G4_PASSED
G5 = G5_COMPLETE
overall = G0_G5_COMPLETE
```

## 完成范围

- G0：自动化测试安全网与可重复 E2E 环境。
- G1～G3：44 模型 SQLite/Prisma 基线、版本链、任务/Asset/Outbox、漫画版式和真实 DB-only 切换。
- R0～R2：影子导入、SH-10、C0～C7、首笔 DB-only 写、OBS-01～10、备份恢复与 file guard。
- G4：不可变 CandidateLockRevision、影响预览、返修、来源 freshness 与历史。
- G5：高自由成稿编辑器、Working Copy/Revision、字体/富文本/气泡、来源返修、确定性 PNG/PDF/条漫出版、手机只读、AI pending 与 legacy cutover。

## 关键提交

- G4：`79dc806`、`9cd599a`、`179be50`、`894d1e8`、`3826611`、`81c922a`。
- G5：`53b65e4`、`68b00cb`、`e93d70f`、`ec71594`、`93a58b2`、`cd35053`、`429ec69`、`d8ed6cc`、`fc9ea47`。

## 数据与协议结果

- SQLite 是唯一业务事实源；首次 DB 写后 file-only 永久禁止。
- migration 0001～0016 只向前追加；不改写历史 migration，不执行 down migration。
- LayoutDocument 是编辑、预览和渲染共同事实源；正式出版只消费 sealed LayoutRevision。
- Candidate、LayoutRevision、ExportRevision、Asset 与任务历史不可变，current/stale/historical 从指针和 digest 派生。

## 最终验证

- G5 Shared：115/115。
- G5 Server：568/568。
- G5 DB-only Playwright：8/8。
- typecheck、E2E typecheck、生产 build、Prisma validate、render gate、migration gate 与 diff check 全部通过。
- G5 Scrutiny、Runtime 与用户签收均为 `passed`。

## 用户签收

用户明确确认 G5 M0～M8 运行结果通过，并授权将 G5 和本轮 G0～G5 标记完成。

## 边界与后续

- 不进入 G6、素材包 ZIP 或视频链路。
- 不删除 backup/archive。
- 不执行 down migration，不回退 file-only。
- 本轮没有自动后续任务；新范围必须由用户另行提出。

## 2026-07-15 文档与回归复核

- 正式产品、UI、模块、路线图和 G1～G4 验收文档已再次对齐 `G0_G5_COMPLETE`，旧 G3-M 阻塞入口已标为 superseded。
- 新鲜回归结果：Shared 115/115；Server 单 worker 568/568；file Chromium 4/4；G2 DB-only 2/2；G4 Chromium 1/1；G5 DB-only 8/8；render、migration、typecheck、build 与 Prisma validate 通过。
- 默认并发 Server 全量有两条 backup/restore 用例触发固定 5 秒 timeout；隔离复跑、backup 文件 40/40 与单 worker 全量均通过。该项记录为测试稳定性债，不改变 G0～G5 完成态。
- G6 素材包、G7 与轻量视频仍为延期范围，没有在本轮实现或测试。
