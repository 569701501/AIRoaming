---
doc_id: AIR-DONE-20260720-RUNWARE
status: completed
created: 2026-07-20
updated: 2026-07-20
owner: AI漫游项目
audience: human, ai-agent, developer
source: 文档/05_执行与记录/任务记录/2026-07-20_Runware图片接入/
---

# Runware 图片接入完成记录

## 功能摘要

设置页新增 Runware 图片生成选项，并完成 Shared、Settings、SecretStore、图片网关、候选引用计划与 Prompt Profile 的端到端接入。真实 Runware 专用 key 已创建并保存到本机 Keychain，Runware 已设为当前图片 Provider。

## 影响范围

- 设置：支持 Runware 保存、清除、切换与安全状态显示。
- 图片网关：Schnell 草稿、FLUX.2 Dev 单图精修、FLUX.1 Dev + IP-Adapter 多参考候选。
- 候选引用：Runware 进入 `candidate_reference_plan_v1`，必需来源继续完整覆盖并 fail-closed。
- Prompt：Runware 拥有生产 Prompt Profile，并进入离线基线/视觉 A/B 计划；任何真实付费 A/B 仍需重新授权。
- 凭据：修复 macOS Keychain `security -w` 的 TTY 写入缺陷。

## 修改文件

主要修改：

- `packages/shared/src/dto.ts`
- `apps/server/src/settings/settings.service.ts`
- `apps/server/src/settings/secret-store.ts`
- `apps/server/src/projects/image-provider.service.ts`
- `apps/server/src/projects/candidate-reference-plan.ts`
- `apps/server/opencodeAI/skills/image-candidate-generate/references/provider-profiles.json`
- `apps/web/src/components/settings/AppSettingsView.vue`
- `apps/web/src/stores/settings-store.ts`
- 对应单元测试、离线基线与项目事实源文档

## 数据或协议变化

- `ImageProviderType` 新增 `runware`。
- `AppSettings/UpdateAppSettingsRequest` 新增 `runwareImageProvider`。
- 默认草稿模型为 `runware:100@1`，默认 API 地址为 `https://api.runware.ai/v1`。
- 单图精修固定使用 `runware:400@1` 的 `referenceImages` 合同。
- 候选参考固定使用 `runware:101@1 + runware:56@1`，每个物理槽位进入 `guideImages`。
- 无数据库 Schema 变化；继续复用 `ProviderConfig + CredentialMetadata + SecretStore`。

## 验证命令与结果

| 命令/验证 | 结果 |
| --- | --- |
| `pnpm --filter @airoaming/server exec vitest run src/projects/image-provider.service.spec.ts src/settings/macos-keychain-secret-store.spec.ts src/settings/settings.service.spec.ts` | 27/27 通过 |
| `pnpm test` | Shared 167/167、Server 822/822 通过 |
| `pnpm typecheck` | 通过 |
| `pnpm build` | 通过；既有 Web 大 chunk 警告 |
| `git diff --check` | 通过 |
| macOS Keychain 临时 put/get/delete | 通过，测试条目已清理 |
| 真实设置页与安全 API 投影 | Runware 已配置、当前选中、密码框清空、`keyPreview=null` |

首次将完整测试、构建和类型检查三套资源密集型任务并行执行时，慢迁移/备份集成测试命中 5 秒超时；改为顺序执行后全量 989 项测试全部通过。

## 已知风险

- 未执行真实 Runware 图片生成或视觉 A/B，合同通过不等于最终人物一致性质量已证明。
- FLUX.2 Dev 商业许可需在商用前按当时有效条款复核。
- inpainting/FLUX Fill、LoRA、ControlNet 尚无完整 UI/任务/素材闭环。

## 后续建议

1. 经用户单独授权后，用 1 个镜头做“小额 Schnell 4～8 张草稿 → FLUX.2 Dev 精修 → IP-Adapter 一致性”视觉验收，并设置明确请求上限。
2. 若需要局部修手/脸/道具，独立设计 mask 资产、FLUX Fill 请求和可撤销编辑流程。
3. 商用前确认模型许可；若 FLUX.2 Dev 不适用，增加可配置的商业编辑模型而不是静默替换。

