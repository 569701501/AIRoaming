---
doc_id: AIR-FEATURE-D2-A3-1-001
status: completed
created: 2026-07-13
updated: 2026-07-13
owner: AI漫游项目
audience: human, ai-agent, qa
source: A3-1 execution
---

# D2-A3-1 Story/Storyboard/Preflight 公开入口收口完成记录

## 功能摘要

将旧 Story/Storyboard/Preflight 文件式公开写入口从 DB 模式移除，统一引导到已有 G2 Working Copy、CAS、projection、current/pending、Preflight preview/confirm API。旧入口返回可审计的 409，不产生 DB 或 workspace 副作用。

## 影响范围

- 7 个 operation 变为合规 `retired`，都有 reason、replacement 与证据。
- `outline_story_storyboard_preflight` aggregate 从 partial/blocked 升为 implemented。
- capability report 保持 8 个 capability、36 个 operation，`blockedIds` 从 5 精确降为 4。
- 不修改 schema、G1 migration、正式历史、modern version repository 或 file mode。

## 修改文件

- `apps/server/src/projects/projects.service.ts`
- `apps/server/src/migration/db-capability-registry.ts`
- `apps/server/src/migration/db-capability-registry.spec.ts`
- `apps/server/src/projects/project-db-persistence.integration.spec.ts`
- 本任务目录施工资料、双复核和进度记录

## 验证命令与结果

- 定向：21/21 passed。
- server 全量：54 files / 362 tests passed。
- `corepack pnpm typecheck`：passed。
- `corepack pnpm --filter @airoaming/web build`：passed（仅既有 chunk size warning）。
- `corepack pnpm --filter @airoaming/server prisma:validate`：passed。
- server G1 manifest/schema/migration checks：全部 passed。
- `git diff --check`：passed。
- capability CLI：8/36，retired=14，`blockedIds` 精确为 `character_scene_asset_candidate_lock`、`layout_export`、`dialogue_pending_runtime`、`project_delete_outbox`。

## 已知风险与后续

角色/视觉 DB 写入仍待 P4；旧角色解析入口不会伪造 ready。下一阶段进入 D2-A3-2A Character/Scene reference 与 Asset/Visual，保持 Outbox、final importer、M6 和真实 cutover 禁止。
