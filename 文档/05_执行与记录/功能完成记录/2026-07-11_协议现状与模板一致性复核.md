# 协议、实现现状与模板一致性复核完成记录

---
doc_id: AIR-COMPLETE-20260711-DOC-CONTRACT-R6
status: active
created: 2026-07-11
updated: 2026-07-11
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: 用户补充六项问题、开发文档完备性复核 R6
---

## 1. 功能摘要

将生成任务协议、全流程实现现状、角色术语与文档模板重新对齐当前代码和已采纳决策，避免后续开发遗漏真实场景任务或把历史骨架误判为未实现/已完成。

## 2. 背景

- 关联任务：`文档/05_执行与记录/任务记录/2026-07-11_开发文档完备性复核/`
- 用户发现任务协议、现状索引、核心模型与模板目录存在 6 项缺口。
- 本轮只修文档和模板，不修改代码、Schema、数据库、依赖或真实 workspace。

## 3. 完成内容

- 生成任务协议与 Shared 统一为 10 类任务和 8 类 target。
- 为 `scene_reference_generate` 补齐受控 API、输入、输出、规范路径、Asset 回写和验收契约。
- 重建 `全流程与字段清单.md`，区分当前实现、基础骨架、已采纳未实现和后置能力。
- 将 `final_reference` 统一为“四要素角色定稿组合图”。
- 明确 `projectCharacterId` 引用 `Character.id`，当前 `ProjectCharacter` 只是文件态 DTO 名。
- 新增 ADR 模板与验收清单模板，并同步写作规范和模板索引。
- 修复深思熟虑模板对已删除旧契约路径的引用。

## 4. 影响范围

| 类型 | 内容 |
| --- | --- |
| 产品 | 不改变产品范围；G6/G7 继续后置 |
| 任务协议 | 文档补齐现有场景任务和 target 枚举，不改变运行代码 |
| 数据模型 | 只校正 Character/ProjectCharacter 与 final_reference 术语 |
| 当前现状 | 候选标为真实旧模型；排版/素材包标为基础骨架 |
| 文档治理 | 新增 ADR、验收清单模板和模板索引 |
| 真实数据 | 只读发现 4 个未登记候选文件，未删除、移动或导入 |

## 5. 主要修改文件

- `文档/02_架构与契约/生成任务协议.md`
- `文档/00_索引/全流程与字段清单.md`
- `文档/02_架构与契约/核心数据模型.md`
- `文档/04_方案与决策/ADR-0004_角色版本与定稿时机方案.md`
- `文档/04_方案与决策/ADR-0006_剧情结构卡角色ID回填方案.md`
- `文档/90_模板/ADR模板.md`
- `文档/90_模板/验收清单模板.md`
- `文档/00_索引/写作规范与留痕规则.md`
- `文档/04_方案与决策/2026-07-11_G0至G5开发文档完备性复核.md`

## 6. 数据或协议变化

运行时协议没有变化；本轮把文档修正为代码现状：

- `GenerationTaskType`：10 个值，包含 `scene_reference_generate`。
- `GenerationTaskTargetType`：8 个值，包含 `character/scene`。
- 场景任务规范路径：`chapters/{chapterSlug}/scenes/{sceneId}/background.webp`。
- `final_reference`：一张含正面半身、正面全身、侧面全身、背面全身的组合图。
- `projectCharacterId`：兼容字段名，目标实体为 `Character.id`。

## 7. 验证结果

| 验证项 | 方式 | 结果 |
| --- | --- | --- |
| 任务类型集合 | 自动提取 Shared 数组并对比协议总表、全流程表 | 10/10 一致 |
| target 集合 | 自动提取 Shared 数组并对比协议、全流程表 | 8/8 一致 |
| 场景任务契约 | 检查独立章节、target、路径、失败码与验收 | 通过 |
| JSON 与 Markdown | 全仓 JSON fenced block 解析、代码围栏闭合 | 通过 |
| doc_id | 全仓重复检查 | 通过 |
| 改动范围 | Git 状态检查 | 只有 `文档/` |
| Runtime/User Review | 本轮无运行代码、页面或真实数据变更 | 不适用 |

## 8. 已知风险

- `buildStoryboardPrompt` 仍有 `over_the_shoulder` 拼写错误，本轮未改代码。
- 场景任务运行中去重漏比较 `chapterId`，跨章节相同 `sceneId` 可能误复用，本轮未改代码。
- 当前 workspace 有 31 个候选 WebP、27 个 Candidate 记录，4 个文件未登记；本轮未修改真实数据。
- OpenCode 工具描述仍可能向用户显示旧“三视图”文案，正式契约已改为角色定稿组合图；代码/UI 文案待开发授权后统一。

## 9. 后续建议

- 开始开发后先为两个代码 TODO 添加回归测试，再修改 Prompt 与场景任务去重。
- G1 importer/Asset Outbox 必须覆盖“文件存在但无 Asset/Candidate 记录”的 orphan 场景。
- 后续新 ADR 和验收文档直接从新增模板创建。

## 10. 完成判定

- [x] 六项用户问题均有代码或文档证据并完成修订。
- [x] 文档、模板和索引已同步。
- [x] 静态验证证据已记录。
- [x] 未授权的代码和真实 workspace 未修改。
- [x] 残留风险已说明。
