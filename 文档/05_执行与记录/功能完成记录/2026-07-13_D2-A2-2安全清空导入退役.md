---
doc_id: AIR-FEATURE-D2-A2-2-001
status: completed
created: 2026-07-13
updated: 2026-07-13
owner: AI漫游项目
audience: human, ai-agent, qa
source: D2-A2-2 execution
---

# D2-A2-2 安全清空、导入与退役完成记录

## 功能摘要

将 legacy clear/import/reset 与旧 pending source 写入口正式退役。DB 模式不再执行整树写入、隐式重置或历史覆盖，而是返回可审计的 409 和逐章 CAS replacement；新增只读 impact preview 帮助调用方在执行替代动作前查看影响。

## 影响范围

- `project_chapter_script` capability 从 partial/blocked 升级为 implemented。
- 7 个 legacy operation 记录 `retired`、reason、replacement、测试证据。
- registry 的 `blockedIds` 从 6 精确降到 5；其余 capability 未改变。
- 未改变 Prisma schema、G1 migration、formal history 或 file mode 既有行为。

## 修改文件

- `apps/server/src/migration/db-capability-registry.ts`
- `apps/server/src/migration/db-capability-registry.spec.ts`
- `apps/server/src/projects/projects.service.ts`
- `apps/server/src/projects/projects.controller.ts`
- `apps/server/src/projects/project-db-persistence.integration.spec.ts`
- 本任务目录下的契约、矩阵、进度、复核和证据文档

## 验证命令与结果

- `corepack pnpm --filter @airoaming/server test -- --run --testTimeout=20000`：54 files / 361 tests passed。
- 定向 registry + project integration：20/20 passed。
- `corepack pnpm typecheck`：passed。
- `corepack pnpm --filter @airoaming/web build`：passed（仅保留既有 chunk size warning）。
- `corepack pnpm --filter @airoaming/server prisma:validate`：passed。
- `corepack pnpm --filter @airoaming/server g1:manifest:check`：passed。
- `corepack pnpm --filter @airoaming/server g1:schema:check`：passed。
- `corepack pnpm --filter @airoaming/server g1:migration:check`：passed。
- `git diff --check`：passed。

## 已知风险

物理清空和整文件导入仍不可用，这是为保护 formal history、milestone 和当前事实而作出的有意安全边界。后续如需恢复业务能力，必须新建阶段契约并沿逐章 Working Copy/Pending CAS 设计，不得重新启用隐式旧入口。

## 后续建议

进入 D2-A3-1，保持 5 个未关闭 capability blocker 原样；本阶段不自动领取 D2-A6、final importer、M6 或真实 cutover。
