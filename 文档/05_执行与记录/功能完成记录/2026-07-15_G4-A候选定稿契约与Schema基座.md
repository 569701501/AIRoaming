---
doc_id: AIR-DONE-20260715-G4-A-001
status: completed
created: 2026-07-15
updated: 2026-07-15
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: G4-A 实现提交与连续施工证据
---

# G4-A 候选定稿契约与 Schema 基座完成记录

## 功能摘要

完成 G4 候选定稿返修的 Shared 契约、严格请求 parser、SQLite 线性历史 overlay、12 段发布迁移身份、legacy importer 规则和旧 `selected/locked` runtime 语义清理。提交：`79dc8065e9cf410006be50d6b7074e6c9569e188`。

## 影响范围

- Shared Candidate/CandidateLock DTO、枚举、错误码与 parser。
- CandidateLockRevision 线性链、Shot current pointer CAS、当前定稿 Candidate 状态保护。
- DB 启动迁移 ledger 与 release schema identity。
- legacy Candidate/CandidateLock shadow import 与 verifier 版本注册。
- 服务端候选投影和现有 Web“当前定稿”展示来源。

## 数据或协议变化

- 新增 forward-only migration `0012_g4_candidate_lock_linear_history`。
- Candidate runtime 状态闭集固定为 `generated/rejected/superseded`。
- `selected` 迁移为 favorite；`locked` 不再推断 current；只有 Shot 直接证据可以创建 legacy v1。
- 当前定稿由 CandidateLockRevision + Shot current pointer 表达。

## 验证结果

- Shared 46/46；G4 overlay/runtime/DB persistence 46/46；legacy/full/final importer 76/76。
- Server 全量 502 项中 3 项在并行重负载下触发局部 5 秒 timeout，空闲环境定向复跑 3/3 通过，其余 499 项通过。
- workspace typecheck、E2E typecheck、Server/Web build、Prisma validate、G1 manifest/schema/migration check 全部通过。
- 0012 checksum=`sha256:19b28fcccac149e5994ed16b43d7d329b8db25e6696bfcba8cff0a2846672f5f`；fresh integrity=`ok`，FK=0。

## 已知风险与后续

- 本阶段没有新对外 API；G4-B～F 仍需完成 resolver、事务命令、下游门禁、Web 和完整 E2E/User Review。
- 真实 DB 尚未部署 0012；发布时必须先运行 12 段迁移，应用启动会 fail-closed 拒绝 11 段 ledger。
- backup/archive 保留；未执行 down migration、file-only 回退或 G6/视频链路。
