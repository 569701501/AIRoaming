---
doc_id: AIR-COMPLETION-20260721-REAL-FULL-FLOW
status: completed
created: 2026-07-21
updated: 2026-07-21
owner: AI漫游项目
audience: human, ai-agent, developer, qa
source: 真实全流程验收任务与 Codex 隔离浏览器运行证据
---

# 真实七阶段全流程验收完成记录

## 功能摘要

使用 Codex 隔离浏览器从空项目库创建 `雨夜末班车·真实验收`，完成剧本、剧情结构、分镜、出图准备、候选图、排版出版和素材包七阶段真实闭环。真实调用 OpenCode Go 和 Runware，生成并定稿 11 个镜头，发布 1080×21120 长图与 3 张切片，最终导出 25 文件完整素材包。

本轮共修复六类真实问题：项目删除角色视觉指针顺序、相同剧本重复发布、最新参考素材排序、相同 preflight 重复确认、Runware 候选多参考路径不可用、DB-only 素材包缺内容和阶段提前完成。

## 影响范围

- 项目删除和剧本幂等修复已在检查点提交 `c5469f4`。
- 图片生成：Runware 候选路径改为 FLUX.2 Dev 原生多参考图；安全保留第三方错误 code/parameter。
- 数据投影：WorkbenchAsset metadata 增加 `createdAt`，用于稳定选择最新重生成素材。
- 版本流程：相同 preflight 精确重放；页面 freshness 服从正式 workflow。
- 素材包：DB-only 路径从正式版本、锁定候选和 layout publication 生成完整包。
- 工作流：只有真正的 `asset_package` ExportRevision 才完成第 7 阶段。
- 测试：扩展数据库集成用例和迁移语义对照，覆盖新合同。

## 修改文件

- `apps/server/src/projects/candidate-reference-plan.ts`
- `apps/server/src/projects/image-provider.service.ts`
- `apps/server/src/projects/image-provider.service.spec.ts`
- `apps/server/src/projects/project-db-persistence.integration.spec.ts`
- `apps/server/src/projects/project-repository.service.ts`
- `apps/server/src/projects/versioning/preflight-revision.repository.ts`
- `apps/server/src/projects/asset-package.service.ts`
- `apps/server/src/projects/versioning/chapter-version-query.repository.ts`
- `apps/server/src/projects/versioning/chapter-production-query.service.ts`
- `apps/server/src/migration/project-chapter-shadow-importer.integration.spec.ts`
- `apps/web/src/components/workbench/ImagePreflightWorkspace.vue`
- `apps/web/src/components/workbench/AssetPackageWorkspace.vue`
- 本任务会话、任务记录、完成记录与长期记忆。

## 数据或协议变化

- 无数据库 Schema 和 migration 变化。
- Runware 候选图片请求从 FLUX.1 + IP Adapter 改为 FLUX.2 Dev `inputs.referenceImages`，最多 4 张参考图。
- WorkbenchAsset 的公共 `meta` 增加 `createdAt`；这是只读展示/排序字段，不改变 Asset 事实源。
- DB 素材包合同补齐 `script.md`、`structure.json`、`storyboard.json`、`preflight.json`、`candidates.json`、`layout/layout.json`、锁定图、出版物、`shared/characters.json` 和根 `manifest.json`。
- `asset_package` 步骤完成条件由章节 `exported` 改为当前 ExportRevision.kind 为 `asset_package`。

## 验证命令与结果

- `project-db-persistence.integration.spec.ts -t P6/G4-D`：1/1 通过；覆盖打包前后 workflow、核心文件集合、磁盘存在性、候选 current-final 和 manifest。
- `image-provider.service.spec.ts candidate-reference-plan.spec.ts`：21/21 通过。
- 完整 Server 套件：首轮 752/757；3 个固定 5 秒并发超时串行 9/9 通过，2 个迁移语义差异修复后串行 2/2 通过。
- Server typecheck：通过。
- Web typecheck：通过。
- Web build：通过；仅保留既有主 chunk 大于 500kB 提示。
- 浏览器运行：七阶段真实流程、刷新恢复、下载、手机只读预览和重新导出素材包均通过；console 无 error/warn。
- 新素材包 `pkg_1d287311`：manifest 25 个唯一路径，磁盘 25 个文件；11 个候选均 current-final；正式长图 sha256 为 `840927c14fd2fa28cb08365ac55dcd14c3eab021cb165a3f1bbc7286c59b0031`。

## Scrutiny Review

- 六项修复均来自真实页面或真实 Provider 失败，不是猜测性重构。
- 数据读取只使用 DB 正式 current/sealed/ready 来源；素材包没有回退到 legacy chapter 文件。
- 幂等与 freshness 由服务端正式版本保护，前端只呈现状态，不成为事实源。
- manifest 文件数、唯一性和磁盘存在性有集成断言；出版图通过 layout publication Asset 复制。

## Runtime/User Review

- 全程只操作 Codex 自带隔离浏览器，没有操作用户 Chrome。
- 删除、创建、文本生成、版本确认、参考图、候选生成、逐镜定稿、排版、出版、下载、手机预览和素材包均由真实页面动作完成。
- 正常路径、重复点击、表单门禁、来源过期、预检警告、刷新恢复和历史显示均有覆盖。

## 已知风险

- 流程已通不等于图片质量达标。当前 Runware 样例存在空背景、日式偏移、伪文字、额外人脸和身份漂移。
- 修复前删除的旧项目仍有不可见 `deleting` DB 壳记录；未绕过 trigger 强行清除，备份位于 `/private/tmp/airoaming-pre-delete-repair-20260721-1721.sqlite`。
- 通用 Outbox 调度按用户此前决定不在本轮修复。
- Web 主 chunk 仍有体积警告，属于性能/工程体验债，不阻断本轮功能。

## Handoff 与后续建议

1. 保留当前真实项目作为七阶段回归样本，不要把生成图片当作视觉金样。
2. 下一轮若提升成片质量，优先做 Provider/模型对比、参考图遵循率和人脸污染评测，不再先扩数据库或工作流。
3. 若要清除旧 deleting 壳记录，使用正式 forward-only 修复或受控维护流程，不临时拆 trigger。
4. 后续修改候选锁、布局或出版来源时，必须继续复跑素材包完整性用例。
