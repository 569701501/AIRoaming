---
doc_id: AIR-TASK-20260710-FEATURE-GAP-PROGRESS
status: in_progress
created: 2026-07-10
updated: 2026-07-10
owner: AI漫游项目
audience: human, ai-agent, developer
source: task_plan.md
---

# 进度日志

## 会话：2026-07-10

### 阶段 1：需求与事实源恢复
- **状态：** completed
- 已采取的操作：
  - 读取 `grill-with-docs` 与 `$deep-think` 技能并确认执行边界。
  - 检查仓库目录、文档清单、代码入口与 Git 工作区状态。
  - 读取文档总入口、AI 上下文、写作规则和长期记忆。
- 创建/修改的文件：
  - 本任务三件套。
  - `文档/会话/2026-07-10-17-39-全项目功能规划.md`。
- 验证结果：
  - 已确认存在两份用户指定 HTML、对应复核 Markdown 和既有任务记录。
  - 已确认工作区存在与本轮无关且未提交的业务代码改动，本轮保持不动。
- 下一步：
  - 已转入外部证据复核和能力缺口建模。

### 阶段 2：外部证据复核
- **状态：** completed
- 已采取的操作：
  - 复核 Dashtoon、LlamaGen、Anifusion 等直接漫画产品。
  - 补充 Jellyfish、LocalMiniDrama、LumenX 等 2026 年生产系统邻居。
  - 核验 OpenAI Image、Runway Gen-4 与 Promptfoo 官方文档。
- 关键结果：
  - 竞品吸收重点从“功能数量”调整为成稿编辑、实体资产、准备态、持久任务、恢复和真实交付。
  - 提示词重点从热门文案改为样例、审计、供应商能力和回归质量门。

### 阶段 3：能力缺口与取舍建模
- **状态：** completed
- 已采取的操作：
  - 对照七步 UI、服务端路由、领域契约、任务服务、Prisma schema 和真实 workspace 样例。
  - 形成 `usable / partial / skeleton / missing / deferred` 成熟度口径。
  - 将缺口组织为 G0、P0 发布闭环、P0 必要底座、P1 质量效率、P0.5 轻漫剧和明确暂缓项。
- 关键结果：
  - 当前最主要缺口是候选图后的真实成稿与交付，同时必须补任务/状态/版本和 AI 质量底座。
  - 轻漫剧不应默认挤入同一主线；是否改变需用户明确决策。

### 阶段 4：决策 HTML
- **状态：** completed
- 创建/修改的文件：
  - `文档/04_方案与决策/2026-07-10_全项目功能补全研究与决策基线.md`
  - `文档/04_方案与决策/2026-07-10_全项目功能补全决策版.html`
  - Codex 会话内可交互可视化源文件。
- 验证结果：
  - JavaScript 语法检查通过；HTML tidy 无结构错误，仅报告 HTML5 属性兼容性提示。
  - 浏览器默认宽度与约 320px 内容宽度均无横向溢出。
  - 能力选择、四视图切换和 D1 方案选择均有效；控制台无 warning/error。

### 阶段 5：逐项决策访谈
- **状态：** in_progress
- 当前事项：
  - 用户已确认 D1：创建弹窗字段使用“漫画版式”，默认空且必选，只保留竖向条漫/分页漫画，创建后不可直接修改，四格归入布局模板。
  - 用户已确认 D3：收藏不驱动下游；定稿和每次换图创建不可变修订；更换前预览影响；旧布局、旧导出和旧任务结果按来源保留。
  - D1、D3 均已形成详细方案和 ADR，尚未进入功能开发。
  - 下一项按依赖合并细化 D4/D5：高自由文字/气泡与高自由画布的首版最小核心边界。
- 创建/修改的文件：
  - `文档/04_方案与决策/2026-07-10_D1漫画成品形态入口与锁定方案.md`
  - `文档/04_方案与决策/ADR-0009_项目创建时锁定漫画版式.md`
  - `文档/04_方案与决策/2026-07-10_D3候选定稿修订与下游返修方案.md`
  - `文档/04_方案与决策/ADR-0010_候选定稿修订与下游返修.md`
  - `文档/05_执行与记录/任务记录/2026-07-10_AI质量与提示词工程/`（独立大功能提议任务包）
- D1 代码探索：
  - 现有 `comicFormat` 已贯穿共享 DTO、项目服务、出图准备、候选生成和排版导出。
  - 当前创建弹窗只有项目名称；后端默认竖向条漫；剧本步骤仍可编辑格式。
  - 现有三枚举混合了阅读容器与四格模板，方案推荐收敛为 `vertical_scroll/paged_comic`。
- D3 代码探索：
  - `ImageCandidateService.lockCandidate` 可在 `images_done` 时把同镜旧 locked 候选降回 generated，并直接改写 `Shot.lockedCandidateId`。
  - `LayoutPage.placements` 保存自己的 `candidateId`，现有换锁不会同步更新或标记 stale。
  - 导出后章节进入 `layout_done`，现有锁定接口不接受该状态，没有正式的“返修并生成新导出修订”路径。
- D3 契约补强：
  - 取消定稿不静默清空指针，而是新增 `action=clear` 的不可变修订，保持审计连续。
  - `sourceLockSetDigest` 与 `impactDigest` 均使用排序后的规范 JSON 计算，排除时间戳、数据库行顺序和本地路径。
  - 更换/取消在同一数据库事务内重算影响、插入修订并更新 Shot 当前指针，不改写旧布局和旧导出。
- 文档同步：
  - 产品流程、MVP、当前 UI、核心数据模型、素材版本契约、生成任务协议与模块边界已同步 D1/D3 接受目标，并明确当前代码仍是旧实现。
  - 全项目决策基线已把 D1/D3 标记为 `accepted`，并链接详细方案和 ADR。
  - 决策 HTML 已显示 D1/D3 已采纳，默认聚焦下一项 D4；D2/D4/D5/D7 保留已确认方向，D6 后置，Prompt 已独立建项。
  - 原“竞品设计吸收与轻画布建议”已标记为 `superseded`；竞品证据仍可复用，但“不做自由画布”不再是当前结论。
- 验证结果：
  - D1/D3 方案中引用的当前代码行为、数据对象和本地任务路径均已复核。
  - 更新后的决策 HTML 可正常加载和切换视图；D1/D3 详情分别显示 ADR-0009/ADR-0010，默认显示 D4；736px 内容宽度无横向溢出。
  - `git diff --check` 通过；当前事实源中无遗留 `terminology_review/needs_explanation/in_discussion`；新正式文档 frontmatter 与代码围栏检查通过。
  - 决策页 JavaScript 语法通过；HTML tidy 仅报告 `charset/sandbox/referrerpolicy/srcdoc` 的 HTML5 兼容提示，无结构错误。
  - 本轮只修改文档和决策页，未重跑业务测试；上一阶段的 15 项 shared、64 项 server、三包 typecheck 和 Prisma validate 结果仍有效。

#### D4/D5 高自由成稿编辑器调研

- 已用 `$deep-think` 完成独立任务包，核验现有排版页、服务、共享 DTO 和画布/渲染依赖现状。
- 已用官方资料复核 Clip Studio Paint、MediBang、Dashtoon、Anifusion、LlamaGen、Canva 与 Adobe Express，并按专业漫画、AI 漫画工作台、轻量设计编辑器三类归纳。
- 推荐边界已收敛为“条漫/页漫有限成稿容器 + 画格/图片/文字/气泡的高自由对象编排”，明确不做无限白板、绘画、画布内 inpaint 和视频时间线。
- 已形成 `2026-07-10_D4D5高自由成稿编辑器调研与首版边界.md`与配套 HTML 决策页，并完成浏览器加载、切换、选项和窄屏溢出检查。
- Scrutiny Review 通过；六项边界全部确认后，方案已由 ADR-0011 采纳，但仍不构成功能开发授权。
- 用户已确认 D45-1 选择 A：V1 画格只支持矩形/圆角矩形轮廓，但允许自由移动、缩放、旋转、重叠和层级调整；任意多边形后置。
- 用户在确认“漫画版式”与“文字书写方向”不冲突后，已确认 D45-2 选择 C：横向/竖向文字和同文字框逐字富文本进入 V1。
- D45-2 契约已明确为 `RichTextDocument -> paragraphs[] -> runs[]`，气泡内文字复用同一引擎，并把 IME、选区、粘贴清洗、范围级撤销和横竖排确定性渲染纳入验收。
- 用户已确认 D45-3 选择 A：V1 使用普通/思考/喊叫/旁白四类气泡预设 + 一条可调尾巴，气泡整体可自由变换。
- 用户已确认 D45-4 选择 A：V1 使用临时多选和批量操作，不把一般 Group 写入正式文档。
- 用户已确认 D45-5 选择 A：V1 桌面浏览器完整编辑，手机只做真实成品预览、切片边界与预检问题查看，不修改画布数据。
- 用户已确认 D45-6 选择 A：成稿编辑默认收起 AI 助手，保留可展开抽屉和步骤独立对话；AI 建议经预览、来源校验与用户确认后才转为可撤销编辑命令。
- D45-1 至 D45-6 最终矩阵为 A/C/A/A/A/A；正式提议状态已转为 `accepted`，并新增 `ADR-0011_高自由成稿编辑器首版边界.md`。
- 配套决策页已将六项全部标记为已确认，并完成最终静态检查、独立页生成和浏览器窄屏运行复核。
- 全项目决策页已同步 D4/D5“已采纳”详情，并默认聚焦 D7；浏览器验证 D4/D5 切换、D7 默认项和 736px 内容宽度无横向溢出。

#### D7 全量数据库化调研与切换提议

- 已用 `$deep-think` 建立独立任务包，读取架构、数据、任务、素材契约和 ADR-0009/0010/0011，并核验真实代码与 workspace。
- 当前 Prisma 6.19.3 只有 6 个未接线模型；仓库无 migration、数据库文件、PrismaService 或业务 CRUD，真实主数据仍在 ProjectRepository Map + workspace JSON/Markdown。
- TasksService、DialogueService、多类 pending 预览和图片串行队列仍依赖进程内 Map/Set/Promise，服务重启不能恢复。
- 当前样例 1 项目、2 章、82 文件、约 54 MB；67 Asset、12 Character、15 Shot、27 Candidate 的现有引用完整，但 55 个唯一 taskId 中 54 个缺完整 artifact。
- 已形成“数据库唯一业务事实源 + Asset/文件存大字节 + Keychain/加密存秘密 + 内存只放可重建运行态”的边界。
- 已形成 M0–M6：契约/migration、DB 垂直切片、审计导入器、影子导入、停写切换、DB-only 观察和旧路径收缩。
- 已新增 `2026-07-10_D7全量数据库化迁移与切换方案.md` 和配套决策页。
- D7 决策页与全项目决策页已完成浏览器复核：D71 选项、目标边界、M4 阶段与总页 D7 状态正常，根容器均为 736px 且无横向溢出。
- 用户连续确认 D71 选择 A：影子导入验证 + 短暂停写一次 DB-only 切换；旧 JSON/Markdown 只读封存，不做运行期双写或双向同步。
- 用户连续确认 D72 选择 A：首版本地 SQLite 单引擎，数据库文件只放应用受控本地目录；保持 PostgreSQL 可迁移，但首版不建设双引擎、双 migration 或在线同步。
- D72 落盘后只讨论 D73，推荐 A“关系核心 + 版本化 Json + 可重建投影”。
- D7 决策页和全项目决策页已同步 D71/D72 已确认、D73 当前状态；浏览器验证 D73 三个选项、独立页降级提示和 736px 无横向溢出。
- 用户确认 D73 选择 A：关系核心 + 版本化 Json + 可重建投影；权威文档、投影和 current 指针同事务提交，投影不可独立编辑。
- D74 代码与官方资料复核确认：当前设置服务会把四类 key 写入明文 settings JSON；对话 key 会同步到本机 OpenCode auth。
- 用户澄清并确认 D74 修正版 A：OpenCode 保存文本 key 正确；图片 key 因 NestJS 直接调用而必须由后端 SecretStore 安全保存，不得暴露到前端、数据库、普通 JSON、日志、任务或 artifact。
- D7 决策页和全项目决策页已同步 D71–D73 已确认、D74 当前状态；浏览器验证 D74 三个选项、独立页降级提示和 736px 无横向溢出。
- D75 代码核验确认当前 TasksService Map、直接 void worker 与两条 Promise 图片队列均无法重启恢复；BullMQ 官方资料确认 Redis/多 worker 优势，但最坏情况仍需 at-least-once 幂等保护。
- 当时只讨论 D75，并推荐 A“数据库任务表 + 单进程持久 worker/lease，BullMQ/Redis 后置”。
- D7 决策页和全项目决策页已同步 D71–D74 已确认、D75 当前状态；浏览器验证 D75 三个选项、独立页降级提示和 736px 无横向溢出。
- 用户确认 D75 选择 A：SQLite `GenerationTask/TaskAttempt` + 单进程持久 worker/lease；V1 采用 at-least-once + 幂等、过期 lease 恢复、协作取消和统一图片并发 1，不引入 BullMQ/Redis。
- 系统架构、生成任务协议、模块职责、D7 提议与任务留痕已同步 D75；D7 决策页和全项目决策页已转入最后一项 D76“缺失旧任务历史”。
- D76 推荐 A：保留原 taskId；1 个完整 artifact 正常导入，54 个缺记录 ID 创建不可执行 `legacy_stub`，独立标记 provenance 不完整，不根据产物存在伪造 `succeeded`。
- D76 决策页浏览器复核通过：独立页显示 D71–D75 已确认、D76 三个选项与 A 的无宿主回传提示；默认 736px 和窄屏 337px 内容宽度均无横向溢出。全项目页同步显示“D71–D75 已确认，待 D76”，736px 无横向溢出，两个页面控制台无 warning/error。
- 用户确认 D76 选择 A：保留全部原 taskId；有完整证据的旧记录只读导入为 `legacy_imported`，其余导入为不可执行 `legacy_stub`，不根据已有文件伪造成功状态。
- D7 的 D71–D76 已全部收口，方案状态转为 `accepted`，并新增 `ADR-0012_全量数据库化切换与持久任务边界.md`；D7 规划任务完成但未授权业务实现。
- 产品、架构、任务协议、素材契约、模块、索引、会话和长期记忆已同步 D76；当时曾建议下一项细化 D2，随后经用户质疑和代码复核确认该建议重复规划了现有能力。
- 当时的 D7 决策页与全项目决策页浏览器复核通过；其中“D2 下一项”后来被本轮复核纠正为“D2 沿用现有阶段机制”。
- 用户明确 D2 自动调度详细边界暂不规划；当时把“真实样例尚未走完链路”误写成“还需实现分步确认闭环”，现已根据代码和 workspace 证据纠正。
- 旧称“人工可控工作台”只是在解释现有的分阶段用户确认机制，不构成新的页面或功能模块；后续不再使用该名称规划开发任务。
- 当时两份决策页曾同步为“D2 详情后置、下一阶段编写七步主链路详细开发文档”；页面验证本身通过，但该产品判断随后被本轮代码复核纠正。
- 用户质疑 D2 A 是否早已实现后，重新核对共享契约、前后端状态机、七个页面动作、API 服务与真实 workspace：确认“AI 分步生成、用户确认后推进”已经是现有基线，不是待开发功能；前一轮把它与未来自动调度混为一谈，现已纠正。
- 现有代码可由 `draft -> script_done -> structured -> storyboard_done -> images_done -> layout_done -> exported` 推进，并为结构、分镜和出图准备保留正式确认记录；等待步骤在顶部流程栏禁用。真实样例当前停在 `image_candidates`，15 个镜头已有 27 张候选但 0 张锁定，尚未实际进入排版和素材包。
- 静态复核通过：`corepack pnpm test` 为 shared 15 项、server 64 项全部通过；`corepack pnpm typecheck` 三包通过。仍缺完整七阶段 E2E，且排版/素材包只是基础骨架，因此后续写“现有缺口与全链验收”文档，不重建 D2 A。
- 已建立 `2026-07-10_七阶段缺口与验收` 独立任务，完成逐阶段代码/真实数据审计、`2026-07-10_七阶段能力缺口与升级顺序.md` 和 `七阶段完整链路验收基线.md`。
- 总方案将真实工作收敛为 G0–G7：行为刻画、D7 DB-only、上游版本/freshness、D1、D3、D4/D5、素材包 V2、完整 E2E；自动调度、轻漫剧、R5 和 Prompt 详细实现不混入本链路。

## Handoff

### 完成
- 已完成事实源恢复、真实代码核验、外部资料复核、能力缺口建模、研究基线、决策 HTML，以及 D1/D3 详细方案与 ADR。
- 已完成 D4/D5 三类市场调研、六项边界决策、ADR-0011、实施顺序、决策树和配套 HTML 运行复核。
- 已完成七阶段真实缺口与升级顺序总方案，以及完整链路验收基线。

### 未完成
- 用户确认七阶段总方案后，按 G0–G7 一次一个编写开发级垂直切片文档。
- 七阶段总方案的最终静态复核和 Runtime/User Review 清单确认。
- AI 质量与提示词工程详细方案作为独立大功能后续推进。

### 证据
- `git status --short --branch`
- `rg --files 文档`
- `corepack pnpm test`
- `corepack pnpm -r typecheck`
- `corepack pnpm prisma:validate`
- 决策 HTML 的浏览器 DOM、交互、窄屏和控制台检查

### 命令记录
- 运行只读检索、现有测试、类型检查、Prisma 验证和 HTML 静态/浏览器检查；未修改业务代码。

### 发现的问题
- 既有报告部分结论已被随后实现淘汰，产品/UI/模块文档与真实代码也存在时效差。
- 场景参考素材污染、任务驻留内存、状态漂移和“复制源图式导出”是需要进入正式方案的真实约束。

### 流程遵守
- 已读取事实源：文档入口、AI 上下文、写作规范、长期记忆。
- 已更新任务记录：是。
- 未越界修改：未修改业务代码。

### 给复核者的重点
- D1 已固定项目级阅读容器；D4/D5 方案必须在条漫/页漫边界内定义高自由工具，而不是重新引入无限制项目形态。
- D3 已固定修订式返修；D7 和任务方案不能退回只改 `lockedCandidateId`、覆盖旧布局或覆盖旧导出的实现。
