# 核心数据模型

---
doc_id: AIR-ARCH-DATA-001
status: active
created: 2026-05-23
updated: 2026-05-29
owner: AI漫游项目
audience: human, ai-agent, developer
source: AI漫游 MVP 数据建模
---

## 1. 建模原则

- 中间产物必须可追溯，不能只保存最终图片或视频。
- AI 任务的输入输出必须留存，便于复跑、调试和版本比较。
- 图片、音频、视频、PDF 都统一抽象为 `Asset`。
- 生成候选与分镜之间通过 `Candidate` 连接。
- 当前锁定结果与历史候选分离，避免覆盖历史。
- 章节是项目内一等工作单元，后续剧本、结构化剧情、分镜、候选图、排版和导出优先挂到 `chapterId`。
- 项目级角色、世界观、通用场景和共享素材继续挂到 `projectId`，供多个章节复用。
- 对话框组件可以公共复用，但原始对话记录必须按项目步骤隔离。
- 跨步骤 AI 上下文只共享用户已确认的事实、保存文档和锁定产物，不共享未确认聊天假设。

## 2. 实体关系图

```text
Project
  ├─ ProjectWorkflow
  ├─ ConversationThread(step scoped)
  │    └─ ConversationMessage
  ├─ ProjectContextFact(confirmed only)
  ├─ Chapter
  │    ├─ ChapterScriptVersion
  │    ├─ StoryVersion
  │    │    ├─ StoryBeat
  │    │    └─ Shot
  │    │         ├─ Candidate
  │    │         │    └─ Asset(image)
  │    │         └─ lockedCandidateId
  │    ├─ LayoutPage
  │    │    └─ PanelPlacement
  │    └─ Asset(chapter scoped)
  ├─ Character
  │    └─ Asset(reference image)
  ├─ SceneLocation
  │    └─ Asset(background/reference)
  ├─ GenerationTask
  │    ├─ inputJson
  │    └─ outputJson
  ├─ VideoDraft
  │    └─ VideoSegment
  └─ Asset
```

## 3. Project

项目容器，承载一个作品的全部故事、分镜、素材和导出物。

```json
{
  "id": "project_001",
  "name": "月下旅人",
  "type": "comic",
  "status": "draft",
  "storyTitle": "月下旅人：第一话",
  "genreTags": ["悬疑", "都市"],
  "comicFormat": "vertical_scroll",
  "artStyle": "dark_realistic",
  "currentChapterId": "chapter_001",
  "currentStoryVersionId": "story_v001",
  "createdAt": "2026-05-23T00:00:00.000Z",
  "updatedAt": "2026-05-23T00:00:00.000Z"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 项目 ID |
| `name` | string | 项目名称 |
| `type` | enum | `comic/light_motion/mixed` |
| `status` | enum | `draft/story_ready/shots_ready/images_ready/layout_ready/exported` |
| `storyTitle` | string | 故事标题，用于项目内创作和故事版本默认标题；在第 1 步“剧本”保存 |
| `genreTags` | string[] | 题材标签或类型标签；在第 1 步“剧本”保存，项目库卡片当前不展示 |
| `comicFormat` | enum | `vertical_scroll/page_horizontal/four_panel` |
| `artStyle` | enum | `dark_realistic/semi_realistic/japanese_realistic/comic_style/cyberpunk/custom` |
| `currentChapterId` | string/null | 当前活跃章节 |
| `currentStoryVersionId` | string | 当前故事版本 |

当前开发阶段还没有接入数据库持久化，后端运行时仍维护进程内 `Project` 索引用于服务 API；项目创建、草稿保存和章节完成会同步写入本地 workspace。服务启动或首次访问项目接口时，会扫描 `workspace/projects/*/project.json` 和章节目录，反向恢复进程内项目索引，避免重启后项目库丢失。

创建项目只建立项目记录，前端创建弹窗只保留“项目名称”一个字段；`type` 使用默认漫画项目类型；`storyTitle`、`genreTags`、`comicFormat`、`artStyle` 和章节剧本在第 1 步“剧本”保存。

```text
workspace/projects/{projectId}/project.json
workspace/projects/{projectId}/workflow.json
workspace/projects/{projectId}/chapters/chapter-001/chapter.json
workspace/projects/{projectId}/chapters/chapter-001/script.md
```

当前后端创建项目时已默认创建 `chapter-001` 并写入章节目录。过渡期旧剧本保存接口仍提交 `sourceText`，但后端只同步写入当前章节 `script.md`；`story/story_draft.source.txt` 旧兼容路径已废弃，新项目不再创建、读取或写入。

当维护动作清空项目剧本，或整本剧本导入需要替换章节集合时，后端必须先删除旧 `chapters/` 目录，再按最新章节列表重建目标章节目录。不能只覆盖新章节数量内的文件，否则服务重启后旧章节目录会被 workspace 扫描恢复，造成已经清掉的章节重新出现。

剧本编辑器里的用户可见“清空”动作只作用于当前打开章节，不删除项目下其他章节。项目级清空只作为整本替换、错误导入修复或维护能力保留，不应作为普通编辑器按钮触发。

旧项目草稿保存接口：

```text
PATCH /api/projects/{projectId}
```

请求字段：

```json
{
  "name": "迷雾之城",
  "storyTitle": "迷雾之城：第一话",
  "genreTags": ["悬疑", "都市"],
  "comicFormat": "vertical_scroll",
  "artStyle": "dark_realistic",
  "description": "迷雾之城：第一话",
  "sourceText": "故事原文"
}
```

响应返回更新后的 `ProjectListItem` 和 `WorkbenchSnapshot`。

该接口为过渡兼容入口。剧本页当前章节保存应优先使用章节草稿保存接口。

`WorkbenchSnapshot` 中章节字段为剧本页读取当前内容的主契约：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `chapters` | `ChapterListItem[]` | 当前项目章节列表，用于后续章节列表和章节切换器 |
| `currentChapter` | `ChapterDetail/null` | 当前活跃章节详情；剧本编辑器和剧本步骤对话上下文应优先读取这里的 `sourceText`、`title` 和 `id`；剧本页不再常驻展示基于该字段解析的当前章节信息面板 |
| `workflow` | `ProjectWorkflow` | 当前项目工作流状态，聚合当前章节的下一步、6 步状态、完成标准和证据路径 |
| `stages` | `WorkbenchStage[]` | `workflow.steps` 的前端兼容镜像；新增消费者优先读取 `workflow` |
| `story` | `WorkbenchStory` | 过渡镜像，用于尚未迁移的旧消费者兜底；新增消费点不应把它当作章节事实源 |

## 4. ProjectWorkflow

`ProjectWorkflow` 是项目级流程事实视图，用于把“当前章节状态”和“6 步主流程”合并成 UI、对话和任务都能消费的统一状态。

当前 MVP 中，工作流状态由当前章节 `Chapter.status` 推导，并写入本地：

```text
workspace/projects/{projectId}/workflow.json
```

示例：

```json
{
  "schemaVersion": 1,
  "projectId": "project_001",
  "currentChapterId": "chapter_001",
  "currentStepKey": "story_structure",
  "steps": [
    {
      "key": "project_story",
      "label": "剧本",
      "status": "done",
      "scope": "chapter",
      "summary": "章节剧本已完成并写入版本快照。",
      "evidence": "/workspace/projects/project_001/chapters/chapter-001/script.md",
      "completionCriteria": ["当前章节 `script.md` 已保存", "用户点击完成本章并写入章节剧本版本"]
    }
  ],
  "updatedAt": "2026-05-27T00:00:00.000Z"
}
```

工作流步骤：

| key | 页面 | scope | 下一步依据 |
| --- | --- | --- | --- |
| `project_story` | 剧本 | chapter | `Chapter.status = draft` |
| `story_structure` | 剧情结构 | chapter | `Chapter.status = script_done` |
| `storyboard` | 分镜工作台 | chapter | `Chapter.status = structured` |
| `image_candidates` | 候选图工作台 | chapter | `Chapter.status = storyboard_done` |
| `layout_export` | 排版导出 | chapter | `Chapter.status = images_done` |
| `asset_package` | 素材包 | project | `Chapter.status = layout_done/exported` |

`ProjectWorkflowStep.status`：

| 状态 | 含义 |
| --- | --- |
| `done` | 当前章节已越过该步骤，可回看 |
| `active` | 当前章节下一步应推进的步骤 |
| `waiting` | 前置步骤未完成，默认不开放点击 |
| `blocked` | 存在阻塞；MVP 预留，当前后端暂不主动产生 |

设计约束：

- workflow 不替代 `Chapter`；章节产物仍以 `chapterId` 归属。
- 前端路由表达“用户正在看哪个页面”，workflow 表达“当前章节应该推进到哪一步”。
- 对话框后续应读取 workflow 生成阶段提示；剧本阶段允许 AI 通过受控工具写章节草稿，但不能因为 workflow active 就自动绕过工具/API 写文档。

## 5. Chapter

章节是项目内创作的基本推进单元。用户完成第 1 章后，可以创建或进入第 2 章继续创作。

```json
{
  "id": "chapter_001",
  "projectId": "project_001",
  "order": 1,
  "title": "第 1 章",
  "status": "draft",
  "currentScriptVersionId": "chapter_script_v001",
  "currentStoryVersionId": "story_v001",
  "sourceText": "当前章节剧本正文",
  "summary": "本章一句话主线",
  "lastScriptRevision": {
    "source": "ai_tool",
    "threadId": "thread_001",
    "messageId": "msg_assistant_001",
    "toolCallId": "tool_call_001",
    "operation": "update_chapter_draft",
    "summary": "强化当前章节紧张感和冲突推进。"
  },
  "createdAt": "2026-05-27T00:00:00.000Z",
  "updatedAt": "2026-05-27T00:00:00.000Z",
  "completedAt": null
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 章节 ID |
| `projectId` | string | 所属项目 |
| `order` | number | 章节顺序，不能只依赖文件夹名 |
| `title` | string | 章节标题，默认可为“第 1 章” |
| `status` | enum | 章节当前状态 |
| `currentScriptVersionId` | string/null | 当前剧本版本 |
| `currentStoryVersionId` | string/null | 当前结构化剧情版本 |
| `sourceText` | string | 当前章节剧本正文，MVP 可先冗余保存 |
| `summary` | string | 当前章节故事主线或一句话总结 |
| `lastScriptRevision` | `ScriptRevisionItem/null` | 最近一次 AI 通过受控工具写入本章节的来源追溯；第一版只保留最近一次 |
| `completedAt` | string/null | 当前章节剧本完成时间 |

章节状态建议：

| 状态 | 含义 |
| --- | --- |
| `draft` | 章节草稿中 |
| `script_done` | 当前章节剧本已完成，可进入剧情结构 |
| `structured` | 当前章节已结构化 |
| `storyboard_done` | 当前章节分镜已完成 |
| `images_done` | 当前章节候选图或锁定图已完成 |
| `layout_done` | 当前章节排版已完成 |
| `exported` | 当前章节已导出 |

### 4.1 ChapterScriptVersion

章节剧本版本用于保存用户对章节剧本的大改和完成本章时的快照。

```json
{
  "id": "chapter_script_v001",
  "chapterId": "chapter_001",
  "projectId": "project_001",
  "version": 1,
  "sourceText": "当前章节剧本正文",
  "sourcePath": "projects/project_001/chapters/chapter-001/script.md",
  "status": "current",
  "createdAt": "2026-05-27T00:00:00.000Z"
}
```

保存草稿可以覆盖当前章节 `script.md`。点击“完成本章，进入下一章”时，必须写入当前剧本快照，并把章节状态推进到 `script_done`。

章节 API：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/projects/{projectId}/chapters` | 返回章节列表和当前章节 ID |
| `GET` | `/api/projects/{projectId}/chapters/{chapterId}` | 返回单个章节详情 |
| `PATCH` | `/api/projects/{projectId}/chapters/{chapterId}/draft` | 保存当前章节草稿，覆盖 `script.md` |
| `POST` | `/api/projects/{projectId}/chapters/{chapterId}/complete` | 完成本章，写入 `script.versions/script-vNNN.md`，并创建或进入下一章 |
| `POST` | `/api/projects/{projectId}/chapters/{chapterId}/script/clear` | 清空当前章节剧本正文，其他章节不受影响 |
| `POST` | `/api/projects/{projectId}/script/reset` | 项目级重置剧本，删除旧章节目录并重建空白 `chapter-001`；仅用于整本替换或维护，不作为编辑器普通按钮 |

保存章节草稿请求：

```json
{
  "sourceText": "当前章节剧本正文",
  "title": "第 1 章",
  "summary": "本章一句话主线"
}
```

完成本章请求：

```json
{
  "sourceText": "当前章节剧本正文",
  "title": "第 1 章",
  "summary": "本章一句话主线",
  "createNextChapter": true,
  "nextChapterTitle": "第 2 章"
}
```

完成本章响应必须返回：

| 字段 | 说明 |
| --- | --- |
| `completedChapter` | 已完成章节，状态为 `script_done` |
| `activeChapter` | 完成后进入的章节；如无下一章且 `createNextChapter` 不为 `false`，则为新建章节 |
| `chapters` | 更新后的章节列表 |
| `scriptVersion` | 本次完成动作写入的章节剧本版本 |
| `createdNextChapter` | 本次是否创建了下一章 |

清空当前章节剧本请求无 body。响应 `ClearChapterScriptResponse`：

| 字段 | 说明 |
| --- | --- |
| `chapter` | 清空后的当前章节详情；保留章节 ID、顺序和章节目录，章节标题重置为默认 `第 N 章`，`sourceText` 为空 |
| `chapters` | 更新后的章节列表；其他章节保留 |

清空当前章节必须将该章节退回 `draft`，将 `title` 重置为默认 `第 N 章`，并清空 `sourceText`、`summary`、`currentScriptVersionId`、`currentStoryVersionId`、`completedAt` 和 `lastScriptRevision`。当前实现会移除该章节的 `script.versions/` 与 `script.revisions/`，避免服务重启后旧剧本版本重新被恢复为当前版本；后续如果接入完整回退功能，再重新设计可恢复历史。项目级 `storyTitle` 不随“清空本章”清空。

清空项目剧本请求无 body。响应 `ResetProjectScriptResponse`：

| 字段 | 说明 |
| --- | --- |
| `chapter` | 重置后的当前章节详情；固定为新的空白 `chapter_001` |
| `chapters` | 重置后的章节列表；只包含空白第 1 章 |

清空项目剧本必须同时更新 `project.currentChapterId`、`Project.sourceText` 过渡镜像和 `workflow.json`，并删除历史遗留的 `story/` 目录。该能力不等于编辑器里的“清空当前章节”。

## 6. StoryVersion

保存结构化剧情。章节模型接入后，`StoryVersion` 应归属于 `Chapter`，用于表示某个章节的一次结构化结果。

剧情结构必须按章节生成和确认。一个 `Chapter` 对应自己的 `StoryVersion` / `structure.json`；不要把整本剧本一次性生成成一个全局剧情结构。

产品语义上，「剧情结构」不是章节剧本，也不是最终分镜；它是给制作使用的结构表，用于把当前章节拆成摘要、角色、场景和剧情节拍，并作为分镜生成的上游输入。

`structureJson.characters` 和 `structureJson.scenes` 默认表示本章结构卡，只归属于当前 `StoryVersion`；它们不等同于正式项目级 `Character` 或 `SceneLocation`。当用户确认某个角色或场景需要跨章节复用时，后续再提升为项目级资产。

剧情结构的用户可见主形态是可编辑结构表，底层持久化为当前章节的 `structure.json` / `StoryVersion.structureJson`，不以 Markdown 正文作为主事实源。

`StoryVersion.status = structured` 表示用户已经确认当前结构，可以作为 `shot_generate` 的输入。AI 刚生成但未确认的结构应视为待确认草稿，不应自动触发分镜生成。

剧情结构右侧 UI 以 `structureJson` 字段渲染元素化结构表。可编辑字段应支持字段级编辑：展示态为普通文本，右上角有小笔图标；编辑态为输入框或多行输入框，失焦后写回对应字段并恢复展示态。

AI 生成但未确认的结构可以在右侧显示为待确认预览。待确认预览必须和正式结构表在状态上区分：它可以用于用户检查和修改，但不能作为 `shot_generate` 输入；用户确认后才写为正式 `StoryVersion` / `structure.json`。

剧情结构步骤的章节下拉/切换器应基于 `Chapter` 列表展示每章结构状态。`Chapter.status = structured` 且 `currentStoryVersionId` 存在时，表示该章剧情结构已完成；未确认预览不应推进 `Chapter.status`。

章节下拉应展示所有章节，但生成权限按章节状态控制：`draft` 章节不能生成剧情结构；`script_done` 章节可以运行 `story_parse` 并生成待确认预览；`structured` 及后续状态章节可以查看和编辑已有结构，如重新生成必须先提示会影响后续分镜、候选图和排版。`需更新` 是 UI 派生状态，可根据剧本版本或更新时间晚于当前 `StoryVersion` 推导，不需要新增 `Chapter.status` 枚举值。

```json
{
  "id": "story_v001",
  "projectId": "project_001",
  "chapterId": "chapter_001",
  "version": 1,
  "sourceText": "当前章节剧本正文",
  "summary": "一句话摘要",
  "structureJson": {
    "characters": [],
    "scenes": [],
    "beats": []
  },
  "status": "structured"
}
```

| 状态 | 含义 |
| --- | --- |
| `draft` | 只有原文 |
| `structured` | 已结构化 |
| `shots_generated` | 已生成分镜 |
| `archived` | 历史版本 |

## 7. StoryBeat

剧情节拍，是分镜生成的上游语义单元。

`StoryBeat` 的粒度是关键剧情事件，不是最终分镜或镜头。一个 `StoryBeat` 后续可以生成多个 `Shot`，因此不应在剧情结构阶段提前写镜头编号或镜头级构图。

`StoryBeat` 可以包含轻量 `visualFocus`，表示该剧情事件的画面重点；但不得包含镜头编号、景别、机位、构图或完整分镜稿，这些属于 `Shot`。

```json
{
  "id": "beat_001",
  "storyVersionId": "story_v001",
  "chapterId": "chapter_001",
  "order": 1,
  "sceneId": "scene_001",
  "summary": "主角进入雨夜街道",
  "characters": ["char_hero"],
  "emotionalTone": "lonely",
  "visualFocus": "雨水倒映主角孤立的身影",
  "dialogueHints": []
}
```

## 8. Character

角色模板，约束图片生成一致性。

```json
{
  "id": "char_hero",
  "projectId": "project_001",
  "name": "林舟",
  "role": "protagonist",
  "appearance": "黑发，短外套，神情克制",
  "personality": "冷静，敏感",
  "referenceAssetIds": ["asset_char_ref_001"],
  "promptFragment": "young man, black hair, short jacket"
}
```

## 9. SceneLocation

场景或地点模板。

```json
{
  "id": "scene_001",
  "projectId": "project_001",
  "name": "雨夜街道",
  "description": "潮湿的老城区街道，路灯昏黄",
  "timeOfDay": "night",
  "mood": "quiet, melancholy",
  "referenceAssetIds": []
}
```

## 10. Shot

分镜单元，是图片生成和排版的核心。

```json
{
  "id": "shot_001",
  "projectId": "project_001",
  "chapterId": "chapter_001",
  "storyVersionId": "story_v001",
  "beatId": "beat_001",
  "shotNumber": 1,
  "sceneId": "scene_001",
  "characterIds": ["char_hero"],
  "action": "林舟站在雨夜街口抬头看灯",
  "dialogue": "",
  "caption": "雨越下越密。",
  "camera": "medium shot, slight low angle",
  "composition": "character left third, street depth on right",
  "emotion": "lonely",
  "promptDraft": "comic panel, rainy old street at night...",
  "lockedCandidateId": null,
  "status": "draft"
}
```

| 状态 | 含义 |
| --- | --- |
| `draft` | 初始生成或编辑中 |
| `ready_for_image` | 可用于图片生成 |
| `image_generated` | 已有候选图 |
| `locked` | 已锁定候选图 |
| `needs_revision` | 需要重改 |

## 11. GenerationTask

所有 AI 和导出任务的统一记录。

```json
{
  "id": "task_001",
  "projectId": "project_001",
  "chapterId": "chapter_001",
  "type": "image_generate",
  "status": "running",
  "targetType": "shot",
  "targetId": "shot_001",
  "inputJson": {},
  "outputJson": null,
  "errorJson": null,
  "attempt": 1,
  "createdAt": "2026-05-23T00:00:00.000Z",
  "startedAt": "2026-05-23T00:00:01.000Z",
  "finishedAt": null
}
```

任务类型见 `文档/02_架构与契约/生成任务协议.md`。

## 12. Candidate

候选结果，通常关联一个分镜和一个图片 Asset。

```json
{
  "id": "candidate_001",
  "projectId": "project_001",
  "chapterId": "chapter_001",
  "shotId": "shot_001",
  "taskId": "task_001",
  "assetId": "asset_img_001",
  "index": 1,
  "status": "generated",
  "score": null,
  "notes": ""
}
```

| 状态 | 含义 |
| --- | --- |
| `generated` | 已生成 |
| `selected` | 当前选中 |
| `locked` | 锁定用于分镜 |
| `rejected` | 废弃 |
| `superseded` | 被替代 |

## 13. Asset

统一素材记录。

```json
{
  "id": "asset_img_001",
  "projectId": "project_001",
  "chapterId": "chapter_001",
  "type": "image",
  "mimeType": "image/png",
  "path": "projects/project_001/assets/images/shot_001/candidate_001.png",
  "width": 1024,
  "height": 1536,
  "durationMs": null,
  "sourceTaskId": "task_001",
  "metadataJson": {}
}
```

| 类型 | 说明 |
| --- | --- |
| `image` | 漫画图、参考图、背景图 |
| `audio` | TTS、BGM、音效 |
| `video` | MP4、预览视频 |
| `document` | PDF、JSON、字幕、manifest |
| `archive` | 素材包 |

## 14. LayoutPage 与 PanelPlacement

漫画排版模型。

```json
{
  "id": "layout_page_001",
  "projectId": "project_001",
  "chapterId": "chapter_001",
  "pageNumber": 1,
  "format": "vertical_comic",
  "width": 1440,
  "height": 2560,
  "placements": [
    {
      "shotId": "shot_001",
      "candidateId": "candidate_001",
      "x": 0,
      "y": 0,
      "w": 720,
      "h": 640
    }
  ]
}
```

## 15. VideoDraft 与 VideoSegment

轻漫剧基础视频模型。

```json
{
  "id": "video_draft_001",
  "projectId": "project_001",
  "name": "第一版轻漫剧",
  "status": "draft",
  "segments": [
    {
      "shotId": "shot_001",
      "assetId": "asset_img_001",
      "subtitle": "雨越下越密。",
      "ttsAssetId": "asset_audio_001",
      "durationMs": 3000,
      "transition": "fade"
    }
  ]
}
```

## 16. 对话框与项目上下文模型

对话框属于项目工作区公共能力，但数据上不采用“整个项目共用一条完整对话记录”。

### 14.1 ConversationThread

按项目步骤隔离的对话线程。章节工作流下，剧本、剧情结构、分镜、候选图、排版等章节内步骤应带 `chapterId`，避免不同章节的原始对话混在一起。

```json
{
  "id": "thread_001",
  "projectId": "project_001",
  "stepKey": "project_story",
  "chapterId": "chapter_001",
  "title": "剧本对话",
  "status": "active",
  "toolResults": [],
  "createdAt": "2026-05-25T00:00:00.000Z",
  "updatedAt": "2026-05-25T00:00:00.000Z"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 对话线程 ID |
| `projectId` | string | 所属项目 |
| `stepKey` | enum | `project_story/story_structure/storyboard/image_candidates/layout_export/asset_package` |
| `chapterId` | string? | 章节内步骤的章节 ID；素材包等项目级步骤可为空 |
| `title` | string | 线程显示名 |
| `status` | enum | `active/archived` |
| `toolResults` | `DialogueToolResult[]` | 当前线程内 AI 受控工具/技能调用结果；前端按 `messageId` 挂回对应 assistant 消息，作为过程卡片展示 |

唯一性建议：

```text
projectId + stepKey + chapterId?
```

当前代码仍是进程内线程。剧本前置阶段使用 `projectId + project_story`，用于用户提供剧本整理和灵感种子；章节内对话使用 `projectId + project_story + chapterId`，避免不同章节的原始对话混在一起。前端进入剧本页时必须先读取 workbench，解析当前 `currentChapter.id` 后再加载对话线程；如果路由没有显式 `chapterId`，默认加载当前章节线程，避免误读空的项目级线程。后续再扩展多线程、专题线程或持久化线程。

当前 `DialogueThread` DTO 会返回 `messages` 和 `toolResults`。普通 assistant 正文仍在 `ConversationMessage.content`，并作为面向用户的最终回复独立展示；工具/技能执行摘要、结构化灵感种子、导入分析、章节写入结果和修订来源进入 `toolResults`，作为过程卡片展示。前端必须让过程卡片和最终回复拥有不同视觉容器；最终回复正文自然展开，由外层对话消息区统一滚动，避免把过程日志直接堆进正文撑开页面，或让用户误以为技能输出后没有 AI 回复。

流式对话必须收敛 `ConversationMessage.status`：assistant 消息创建时为 `running`，正常完成后为 `completed`，运行时异常、浏览器刷新、页面离开或 SSE 连接中断后必须转为 `failed` 或完成态，不能长期停留在 `running`。当前实现会在 SSE 客户端断开时 abort OpenCode 请求；读取线程或新建同线程 turn 时，如果发现没有活跃流式请求的旧 `running` assistant 消息，会标记为 `failed`，错误码为 `DIALOGUE_STREAM_INTERRUPTED`。

### 14.2 ConversationMessage

对话消息记录。

```json
{
  "id": "msg_001",
  "threadId": "thread_001",
  "projectId": "project_001",
  "stepKey": "project_story",
  "chapterId": "chapter_001",
  "role": "assistant",
  "content": "我建议把第一幕冲突提前，让主角更早做出选择。",
  "model": {
    "providerId": "self",
    "modelId": "gpt-5.5"
  },
  "runtimeStatus": "completed",
  "sourceRefs": ["story_v001"],
  "createdAt": "2026-05-25T00:00:00.000Z"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `threadId` | string | 所属对话线程 |
| `projectId` | string | 冗余项目 ID，便于查询 |
| `stepKey` | enum | 消息发生的项目步骤 |
| `chapterId` | string? | 消息所属章节，章节内步骤应填写 |
| `role` | enum | `user/assistant/system/tool` |
| `content` | string | 消息内容 |
| `model` | object/null | assistant 消息使用的 `providerId/modelId` |
| `runtimeStatus` | enum | `running/completed/failed` |
| `sourceRefs` | string[] | 引用的项目文档、当前步骤产物或项目级已确认事实；当前 DTO 暂未落该字段 |

规则：

- 普通聊天消息只是聊天记录，不进入项目级事实。
- 剧本阶段 AI 写章节草稿必须产生受控工具结果和写入来源记录。
- 不提供跨步骤手动引用原始聊天消息；跨步骤来源只能来自项目级已确认事实、保存文档和锁定产物。

### 14.2.1 ProjectScriptOutline

项目级剧本大纲是灵感链路中选定灵感种子后的确认产物，保存到项目目录，不写入章节编辑器。

目标路径：

```text
workspace/projects/{projectId}/script-outline.md
workspace/projects/{projectId}/script-outline.json
```

固定 Markdown 格式：

```md
# 剧本大纲

## 一、基础信息
剧集名称：
题材风格：
剧集篇幅：
剧集章数：
剧情简介：

## 二、主要角色
角色名（定位）：

## 三、情节概要
第 1 - 2 集：
```

规则：

- `剧本大纲` 是项目级产物，供用户确认故事方向和后续漫剧集数规划。
- `情节概要` 按漫剧集数段落规划，例如 `第 1 - 2 集：...`，但不替代当前漫画创作主单位 `Chapter`。
- 用户确认大纲后，第一版只生成当前一章，不自动创建或生成多章。
- 用户说“不行”或提出修改要求时，重新生成并覆盖当前项目级大纲，直到用户确认。

### 14.2.2 DialogueToolResult

AI 通过受控业务工具产生的结果。剧本阶段当前包含导入、灵感生成、从种子生成剧本大纲、从确认大纲生成当前章和章节草稿更新。

剧本导入分两步：

1. `analyze_script_import`：先分析上传/粘贴内容是否像剧本、章节边界是否可信、是否需要用户确认；此步骤不写文件。
2. `import_script_to_chapters`：只有分析通过或用户确认后，才把剧本拆分并写入 `chapters/*/script.md`。

```json
{
  "id": "tool_result_001",
  "projectId": "project_001",
  "threadId": "thread_001",
  "messageId": "msg_assistant_001",
  "toolCallId": "tool_call_001",
  "tool": "analyze_script_import",
  "status": "needs_user_confirmation",
  "summary": "已检查上传剧本：这份内容像故事或剧本，但没有识别到明确章节边界。继续导入会先写成单个章节。请回复“确认导入”继续，或回复“取消导入”。",
  "chapters": [],
  "currentChapterId": null,
  "currentChapter": null,
  "analysis": {
    "decision": "needs_user_confirmation",
    "contentType": "story_prose",
    "reason": "这份内容像故事或剧本，但没有识别到明确章节边界。",
    "chapters": [
      {
        "order": 1,
        "title": "第 1 章",
        "boundary": "single_chapter",
        "summary": "雨夜里，主角第一次发现异常。"
      }
    ],
    "risk": "继续导入会先写成单个章节；如果要自动拆成多章，需要后续再做剧情节拍拆分。",
    "nextTool": null
  },
  "revision": null,
  "createdAt": "2026-05-27T00:00:00.000Z"
}
```

写入成功示例：

```json
{
  "id": "tool_result_002",
  "projectId": "project_001",
  "threadId": "thread_001",
  "messageId": "msg_assistant_001",
  "toolCallId": "tool_call_002",
  "tool": "import_script_to_chapters",
  "status": "succeeded",
  "summary": "已根据粘贴剧本整理并写入 3 个章节，当前打开第 1 章。",
  "chapters": [
    {
      "id": "chapter_001",
      "title": "第 1 章",
      "order": 1,
      "sourceTextPreview": "雨夜里，主角第一次发现异常。"
    }
  ],
  "currentChapterId": "chapter_001",
  "currentChapter": {
    "id": "chapter_001",
    "title": "第 1 章",
    "sourceText": "雨夜里，主角第一次发现异常。",
    "scriptPath": "projects/project_001/chapters/chapter-001/script.md"
  },
  "analysis": null,
  "revision": {
    "operation": "import_script_to_chapters",
    "summary": "根据粘贴剧本整理并写入 3 个章节。"
  },
  "createdAt": "2026-05-27T00:00:00.000Z"
}
```

灵感种子示例：

```json
{
  "tool": "generate_inspiration_seeds",
  "status": "succeeded",
  "summary": "我根据剧本灵感 skill 生成了 3 个漫画剧本方向，回复“选第 1 个”到“选第 3 个”就可以生成项目级剧本大纲。",
  "inspirationSeeds": [
    {
      "order": 1,
      "title": "AI 生成的方向标题",
      "logline": "AI 根据项目上下文和用户输入生成的一句话故事钩子。",
      "keyConflict": "AI 生成的核心冲突。",
      "visualHook": "AI 生成的画面钩子。",
      "firstChapterDirection": "AI 生成的第 1 章开场方向。"
    }
  ],
  "revision": null
}
```

剧本大纲示例：

```json
{
  "tool": "generate_script_outline_from_seed",
  "status": "needs_user_confirmation",
  "summary": "已根据方向生成项目级剧本大纲，并保存到 projects/project_001/script-outline.md。请确认是否继续生成第 1 章。",
  "scriptOutline": {
    "id": "script_outline_current",
    "projectId": "project_001",
    "status": "draft",
    "title": "AI 生成的剧集名称",
    "sourceText": "# 剧本大纲\n\n## 一、基础信息\n剧集名称：AI 生成的剧集名称\n...",
    "outlinePath": "projects/project_001/script-outline.md",
    "createdAt": "2026-05-29T00:00:00.000Z",
    "updatedAt": "2026-05-29T00:00:00.000Z",
    "confirmedAt": null
  },
  "revision": null
}
```

章节写入成功示例：

```json
{
  "tool": "update_chapter_draft",
  "status": "succeeded",
  "chapters": [
    {
      "id": "chapter_001",
      "title": "第 1 章",
      "order": 1,
      "sourceTextPreview": "强化后的章节开场正文。"
    }
  ],
  "currentChapterId": "chapter_001",
  "currentChapter": {
    "id": "chapter_001",
    "title": "第 1 章",
    "sourceText": "强化后的章节开场正文。",
    "scriptPath": "projects/project_001/chapters/chapter-001/script.md"
  },
  "revision": {
    "source": "ai_tool",
    "threadId": "thread_001",
    "messageId": "msg_assistant_001",
    "toolCallId": "tool_call_001",
    "operation": "update_chapter_draft",
    "summary": "强化当前章节紧张感和冲突推进。"
  }
}
```

事件：

| 事件 | 说明 |
| --- | --- |
| `dialogue.tool_result.created` | 受控工具已产生业务结果，前端应把结果挂入当前线程 `toolResults`；写入类工具成功后应优先用 `currentChapter + chapters` 局部合并 `WorkbenchSnapshot`、章节列表和当前章节编辑器，不重新请求完整 workbench |

规则：

- `analyze_script_import` 是正式工具动作，但不写项目文件。
- 写入类工具结果应带 `chapters` 和 `currentChapter`；`currentChapterId` 保留为路由和旧消费者兜底。
- `import_script_to_chapters` 是后端/AI 工具动作，不是 UI 主按钮。
- 附件上传和输入框粘贴只是输入方式，单独选择附件不写章节。
- 用户明确要求整理成章节，或直接粘贴长剧本时，必须先经过 `script-import-normalize` / `analyze_script_import` 这类导入前分析。
- 只有分析结果确认“内容像可导入剧本且章节边界可信”时，后端才可以触发 `import_script_to_chapters`。
- 分析失败或需要用户确认时，不写 `chapters/*/script.md`，只返回对话提示或 `failed/needs_user_confirmation` 类工具结果。
- `needs_user_confirmation` 会在当前项目级剧本线程中临时保留待导入文本；用户回复“确认导入/确认覆盖”后再写入，回复“取消导入”则丢弃。
- 低可信章节边界，例如单独成行的 `1`、`2`、`3` 或 Markdown 标题 `# 1`，不能绕过内容校验直接写入章节。
- `generate_inspiration_seeds` 调用 AI / `script-inspiration-seeding` 生成 3 个候选灵感，不写文件；后端只解析结构化结果并缓存待选择种子。
- 用户回复“选第 N 个方向”后触发 `generate_script_outline_from_seed`，生成固定格式项目级「剧本大纲」并保存到项目目录；此时不写章节正文。
- 用户确认剧本大纲后触发 `generate_script_from_outline`；章节正文由 AI / `script-chapter-drafting` 生成固定格式「章节剧本」，其中 `第 X 章：章节标题` 作为当前章节显示名同步到 `Chapter.title`；剧本名称来自项目级大纲并显示在章节下拉框右侧，不写入章节正文。
- `update_chapter_draft` 会调用 AI / `script-chapter-editing` 基于编辑器最新草稿返回完整固定格式「章节剧本」；后端负责校验、写入当前章节 `script.md`，同步正文里的章节标题，并把 `lastScriptRevision` 写入 `chapter.json` 和 `script.revisions/latest.json`。
- `import_script_to_chapters` 写入的章节正文也必须统一为固定格式「章节剧本」；最终章节剧本不输出主体列表、正式场景列表、剧情节拍、分镜剧本、镜头编号、图片 Prompt 或 JSON。

### 14.3 ProjectContextFact

项目级已确认事实，用于跨步骤共享上下文。

```json
{
  "id": "fact_001",
  "projectId": "project_001",
  "type": "character",
  "content": {
    "name": "林舟",
    "appearance": "黑发，短外套，神情克制"
  },
  "sourceType": "story_version",
  "sourceId": "story_v001",
  "status": "confirmed",
  "createdAt": "2026-05-25T00:00:00.000Z"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `type` | enum | `story/character/world/style/structure/shot/layout/asset/custom` |
| `content` | object | 已确认事实内容 |
| `sourceType` | enum | 来源类型，如 `story_version/shot/candidate/layout_page/asset` |
| `sourceId` | string | 来源对象 ID |
| `status` | enum | `confirmed/superseded/archived` |

只有以下用户动作可以把内容提升为项目级事实：

- 通过受控工具写入章节草稿。
- 保存草稿。
- 确认剧情结构。
- 锁定候选图。
- 导出或归档素材。

AI 上下文注入顺序建议：

```text
当前步骤系统提示词
  -> 项目级已确认事实
  -> 当前步骤主文档或产物
  -> 当前步骤对话记录
  -> 用户本轮输入
```

### 14.4 DialogueRuntimeSession

对话运行时映射。第一阶段采用 OpenCode 时，用于把 AI漫游业务线程映射到 OpenCode 内部 session。

```json
{
  "id": "runtime_session_001",
  "threadId": "thread_001",
  "projectId": "project_001",
  "stepKey": "project_story",
  "runtime": "opencode",
  "externalSessionId": "oc_session_001",
  "status": "active",
  "providerId": "self",
  "modelId": "gpt-5.5",
  "variant": "medium",
  "createdAt": "2026-05-25T00:00:00.000Z",
  "updatedAt": "2026-05-25T00:00:00.000Z"
}
```

规则：

- `DialogueRuntimeSession` 只负责运行时路由，不承载业务聊天内容。
- `ConversationThread`、`ConversationMessage` 和 `ProjectContextFact` 仍是 AI漫游对话与上下文事实源。
- OpenCode Session 不向前端直接暴露。
- OpenCode 原始事件必须由后端转换为 AI漫游标准 `dialogue.*` 事件。
- 如果未来替换运行时，只替换 `runtime/externalSessionId` 映射和 runtime adapter，不重写业务对话模型。

当前实现状态：

- 2026-05-26 已在 `packages/shared/src/dto.ts` 增加 `DialogueThread`、`DialogueMessageItem`、`SendDialogueMessageRequest`、`SendDialogueMessageResponse` 和 `AIRuntimeModelItem` DTO。
- 2026-05-26 已增加 `DialogueStreamEvent`，初始事件类型包含 `dialogue.message.created`、`dialogue.message.delta`、`dialogue.message.completed` 和 `dialogue.error`。
- 2026-05-27 `SendDialogueMessageRequest` 已补 `chapterId`、`intent`、`context` 和 `attachments`；`DialogueStreamEvent` 已补 `dialogue.tool_result.created`。
- 2026-05-27 后端对话线程支持 `projectId + stepKey + chapterId`；剧本前置整理仍使用 `projectId + project_story`。
- 2026-05-27 已接入 `analyze_script_import` + `import_script_to_chapters`：支持 `.txt/.md` 文本附件或输入框长文本先做导入前分析；分析通过才写入章节，失败或需要确认时不写文件；写入成功后通过工具结果事件让前端更新章节列表和当前章节编辑器。
- 2026-05-28 `DialogueToolResult` 写入类结果补充 `currentChapter`，前端在 AI 写入、清空当前章节、保存草稿和完成本章时用章节详情与章节列表做局部合并，不再为了更新编辑器重新请求完整 workbench 和项目列表。
- 2026-05-28 已接入章节级 `POST /api/projects/{projectId}/chapters/{chapterId}/script/clear`：编辑器“清空本章”只清空当前打开章节正文，保留其他章节，并将该章节退回 `draft`。
- 2026-05-27 已接入项目级 `POST /api/projects/{projectId}/script/reset`：用于清空错误导入的多章节剧本或整本替换维护，后端删除旧 `chapters/` 和历史 `story/` 目录，重建空白 `chapter-001`；整本剧本导入重写章节前也会清理旧章节目录，避免 stale chapter 被重启恢复。该能力不作为编辑器普通清空按钮。
- 2026-05-29 剧本大纲确认链路已接入：`generate_inspiration_seeds` 每次生成 3 个灵感种子；`generate_script_outline_from_seed` 生成并保存项目级 `script-outline.md`；`generate_script_from_outline` 在用户确认大纲后只写当前一章固定格式「章节剧本」；`update_chapter_draft` 继续写入固定格式「章节剧本」；`import_script_to_chapters` 对非标准输入做格式兜底，避免剧本阶段生成后续剧情结构、分镜或图片 Prompt 产物。
- 2026-05-27 `DialogueThread` 已增加 `toolResults`，用于前端把 AI 思考、技能和工具调用展示为可展开的过程卡片；工具结果不再只作为临时 notice，也不直接混入普通对话正文。
- 停止生成、对话持久化和完整版本回退仍未实现；AI 写章节第一版只保留最近一次 `lastScriptRevision`。

## 16. 全局设置

### 16.1 AppSettings

本地全局设置。当前保存到 `workspace/settings/app-settings.json`，用于首页设置页，不挂到单个项目。

```json
{
  "aiKey": {
    "providerId": "self",
    "providerName": "自定义 OpenAI 兼容",
    "modelId": "gpt-5.5",
    "baseUrl": "https://api.example.com/v1",
    "configured": true,
    "keyPreview": "sk-t...7890",
    "keyFingerprint": "8b35ae8d2893",
    "updatedAt": "2026-05-29T04:03:00.000Z"
  },
  "appearance": {
    "theme": "dark"
  },
  "settingsPath": "/workspace/settings/app-settings.json",
  "updatedAt": "2026-05-29T04:03:00.000Z"
}
```

规则：

- 前端 `GET /api/settings` 只能读取密钥配置状态、掩码和指纹，不能读取完整 API Key。
- 前端保存密钥时通过 `PATCH /api/settings` 提交；后端持久化后在 OpenCode 调用前同步到 OpenCode auth。
- `appearance.theme` 取值为 `system`、`dark` 或 `light`；前端负责应用到 `document.documentElement`。
- `workspace/settings/*` 被 `.gitignore` 排除，避免本地密钥进入仓库。

## 17. 最小数据库实现建议

MVP 第一版可以先落以下表：

1. `Project`
2. `Chapter`
3. `ProjectWorkflow`
4. `StoryVersion`
5. `Character`
6. `SceneLocation`
7. `Shot`
8. `GenerationTask`
9. `Candidate`
10. `Asset`
11. `LayoutPage`

`ChapterScriptVersion`、`StoryBeat`、`PanelPlacement`、`VideoSegment` 可先以 JSON 字段或文件快照嵌入，等查询需求明确后再拆表。

`ProjectWorkflow` 当前可由章节状态推导并写入 `workflow.json`；接数据库后可以作为项目级状态表或快照 JSON 保存，避免流程栏、对话框和任务入口各自推导。

当对话框接入真实 AI 存储时，应增加：

1. `ConversationThread`
2. `ConversationMessage`
3. `ProjectContextFact`
4. `DialogueRuntimeSession`
