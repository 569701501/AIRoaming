# AI漫游长期记忆

- 2026-08-11 角色图生成与阶段管理-完整交付(任务 `文档/05_执行与记录/任务记录/2026-08-11_角色图生成与阶段管理`,完整报告 `文档/会话/2026-08-11-22-00-角色图生成与阶段管理完整交付报告.md`):【后端 7 任务】数据模型(0021 migration:CharacterStage+Character.anchorAssetId)、DTO(CharacterStage/CreateCharacterStageRequest/GenerateAnchorCandidatesRequest/ConfirmAnchorRequest)、CharacterStageService(创建阶段+生成预览垫图+更新/删除/重生成,参考图优先级=上一阶段 final>preview>锚点>primary>preview,DB 专用)、定妆接口(generateAnchorCandidates 并发 3 张不同 seed+confirmAnchor)、参考图提示词改造(buildCharacterReferencePrompt 新增 hasReferenceImage 参数)、分镜生图集成(candidate-generation-spec.ts 阶段选择逻辑+向后兼容)、API 路由(5 阶段+2 定妆端点),测试 672/672。【前端阶段 1】CharacterAnchorModal.vue 定妆弹窗(z-index 95,3 候选 grid+点击选中/双击确认+重新生成+骨架屏/错误态)、ProjectCharactersWorkspace(琥珀色"待定妆"标签+定妆照槽位+去定妆/重设定妆按钮)、workbench-store confirmAnchor+事件链透传,测试 55/55+真实浏览器冒烟。【前端阶段 2】CharacterStageManager.vue(z-index 96 弹窗+列表/表单视图切换+预览放大 z-97+删除二次确认+重新生成预览+骨架屏/空状态/错误重试+移动端适配)、CharacterStageForm.vue(创建/编辑+visualDelta 必填验证+章节下拉)、ProjectCharactersWorkspace 紫色"阶段管理"按钮(Layers 图标),测试 typecheck+真实浏览器冒烟零 JS 错误。向后兼容:anchorAssetId nullable 降级 preview,无阶段角色分镜生图行为完全不变,File 模式抛 DB_PERSISTENCE_REQUIRED。委托执行模式:父代理规划+检查+接管失败任务(后端 Task 6-7),子代理实现(后端 Task 1-5+前端阶段 1-2),token 176k(父 23k/13%+子 153k/87%),预估节省 45% 成本。待补充:端到端集成测试+候选资产清理策略+其他 provider seed 支持。
- 2026-08-06 模型管理卡片式重设计完成(完成记录 `2026-08-06_模型管理卡片式重设计.md`):设置页模型管理从「整行点击选中」改为卡片+显式操作——「设为当前」按钮、生效卡片渐变边条+「使用中」徽章、未配置密钥琥珀警告(按钮同色)、新增编辑入口(复用 AddModelModal 预填+既有 PATCH updateManagedModel 首次接 UI+清除密钥勾选)、删除内联两步确认(3 秒超时复位,不新增弹窗)。设置页模型管理 DOM 无任何测试锁定。工具经验:本次 In-app browser 不稳定(broker id mismatch/截图失败),仓库 playwright 在 `node_modules/.pnpm/playwright@1.61.1`,临时脚本截图可靠。
- 2026-08-06 设置页提示体系约定:服务端异常/成功通知一律走顶部居中 toast(4.5s 自动消失,`settings-store.clearFeedback()`),不再用常驻横幅;展示前剥离 `ERROR_CODE:` 前缀;弹窗内联错误用组件本地副本留存(toast 清 store 不影响),打开时重置。后续其他页面加提示应复用该模式。
- 2026-08-06 设置页「AI 密钥」「图片生成」tab 已按用户明示删除(只剩模型管理/外观,默认模型管理):用户接受数据库模式下无 UI 配/换/清密钥,服务端密钥渠道与 settings-store action 保留,运行时解析(active managed model 优先、同 providerId 共享槽位凭证、兜底 aiKey/图片槽位)不受影响;恢复入口从 git 历史找。关键架构事实:DB 模式模型行不能存密钥(secretRef 恒 null,text 行受 ck_credential_metadata_text_owner_shape 约束),凭证存内存 runtimeAIKey/runtimeImageSecrets/runtimeManagedModelSecrets;文件模式密钥在 macOS Keychain(`managed_${kind}_${id}`)。服务端三处 MANAGED_MODEL_* 错误文案已去掉对已删页面的引用。
- 2026-08-04 文稿库功能完成(任务 `2026-08-04_文稿库实施`,完成记录 `2026-08-04_文稿库功能完成.md`):全局文稿库=上传剧本/小说(.txt/.md)→确定性拆章(零 token,三层规则:章节号/空行/单章,卷与卷章同行支持,中文数字转换)→分组折叠阅读器(有卷按卷、无卷按100章分桶,默认展开第一组)→创建项目弹窗可选文稿自动建全部章节壳(直接引用原文,零AI,LocalChapter 带 documentWorkId/documentChapterId)。真实《凡人修仙传》验证:1.txt(GBK)2574章+2.txt(UTF-8)1396章全部识别0断裂;创建项目后2574章节壳+DB引用零缺失。关键技术决策:①Chapter 加列必须 `ALTER TABLE ADD COLUMN`(Prisma 重建表破坏 244 个 trigger,migrate dev 实测报错),新 migration 需注册 g1-ledger overlay 清单 + script-workflow ledger + 独立 contract 文件;②大文件上传用 multipart(FileInterceptor 内存,60MB),绕开 Nest json bodyParser 100kb 限制,无需加 express 依赖;③multer 中文文件名按 latin1 解码会 mojibake,需 latin1→utf8 重解;④文件读取必须按存储时检测的编码解码(GBK 文稿 utf8 读乱码);⑤migration_lock.toml 会被 migrate deploy 重写格式需 git checkout 恢复。延后项:文稿备份密封改造(sealed 校验链改动大)、删除 outbox、剧本页按需加载文稿正文。测试:shared 280/280、server 784/784、web 契约 55/55、e2e db 19/19+file 4/4。
- 2026-08-03 漫画成稿产品定位确认为「普通用户 + AI 排版为主 + 简单调整」(方向A),并已实现(完成记录 `2026-08-03_漫画成稿简单调整定位.md`,设计文档 `任务记录/2026-08-03_漫画成稿现状设计评审/simple_adjust_design.md`):编辑器从专业工具收缩为傻瓜微调,选中文字/气泡就近工具条直接给缩小/放大文字、粗体、7 色文字色板(复用正式命令 `text.apply_range_style` / `applyRichTextRangeStyle`+`balloon.replace_text_document`,粗体按字体目录匹配同 family 700/400 字重);专业能力默认隐藏(`advancedToolsVisible=false` 统一开关,代码保留可恢复):裁切、精确调整、画格属性、crop-controls、文字角色/SFX、富文本编辑器、气泡外观/数值/尾巴5字段、图层Tab、阅读顺序、换图镜头条、右键裁切、画格模板。e2e m4/m5 重写为简单路径并锁定「默认隐藏」态,db 18/18 + file 4/4 + server 777/777 + web 契约 55/55 全绿。关键教训:开关不能用 localStorage(违反 g5-m3「编辑器不使用浏览器存储」契约);e2e strict mode 下「删除对象」按钮在工具条与属性面板重复,需限定 `.inspector .property-panel`;m5 拖拽对比必须拖拽前重新读 API(beforeDragBalloon 曾读旧 saved 导致 undefined)。

- 2026-08-03 漫画成稿两项交互修正:①点击镜头素材只滚动定位不再自动选中画格(selectSourceShot 去掉 selectElement);②「属性」按钮与属性浮层(inspector)整体按 `advancedToolsVisible` 隐藏——普通用户不再有入口,锁定/隐藏/删除走右键菜单与工具条,文字溢出提示改为画布红色 outline(`.has-text-overflow`)而非属性面板摘要;m5 溢出验证改「放大40次→has-text-overflow 类→导出阻断」。db 18/18 + web 契约 55/55 全绿。
- 2026-08-03 漫画成稿取消选中行为补全:除 Konva stage 右键空白外,点击画布外留白(stage-scroll padding 区)也清除选中(`handleStageBackgroundPointerDown`:非只读/有选中/无就地编辑与右键菜单时,target 不在 `.document-canvas` 内即清空);m4 断言「选中画格→点画布外→is-selected 计数0」。db 18/18 全绿。
- 2026-08-03 漫画成稿画布空白右键取消选中已实现:Konva stage 加 `contextmenu` 监听(`handleStageContextMenu`,target===stage 且非只读时 preventDefault+emit clearSelection),LayoutExportWorkspace 清空 selectedElementIds;节点右键有 cancelBubble 不冲突。m4 新增「选中画格→右键画布空白→is-selected 计数0」断言,db 18/18 全绿。
- 2026-08-03 漫画成稿删除/重建二次确认改自定义弹窗:新增 `LayoutConfirmDialog.vue`(Teleport+样式对齐 DeleteProjectDialog),替换两处 `window.confirm`(deleteElementsWithConfirm、rebuildLegacyDraft),状态驱动 `requestConfirm`+`runConfirmAction`;m4 新增"右键删除→自定义 dialog→取消/确认"断言。db 18/18 全绿。项目约定:全站禁用原生 window.confirm/alert,二次确认一律自定义弹窗。
- 2026-08-03 漫画成稿镜头素材点击行为已修:点击镜头卡片现在会定位并选中该镜头对应的画格(`selectSourceShot` 用 `projectVisibleShotPlacementsV1` 查 placement,selectCanvas→selectElement→stageScroll.scrollTo 居中滚动),不再只是选中素材供「自由图」按钮用;未放置的镜头点击无动作。e2e m4 新增「点击镜头→panel_frame.is-selected 计数1」断言,db 18/18 全绿。
- 2026-08-03 漫画成稿左栏移除段落列表(完成记录追加三):段落=竖条漫的 canvas 容器(段),普通用户不理解也不需要,「新增/复制/移动段落」专业操作;左栏只留镜头素材栏+来源状态;删除 addCanvas/duplicateCanvas/moveCanvas 三函数,selectCanvas 保留;e2e m4 锁「无段落导航」态,m6 改添加文字制造修改。db 18/18 + web 契约 55/55 全绿。
- 2026-08-03 漫画成稿用户反馈已修:默认缩放 0.24→0.4;选中工具条显示不全**根治**=工具条从 `.document-canvas`(overflow:hidden,宽=画布宽×缩放)移到 `.stage-wrap` 可视层,坐标用「画布居中偏移+元素画布坐标−滚动偏移」换算,按可视区+实测尺寸钳制(工具条比画布宽也能浮出画布显示);模板结构调整教训:oldString 必须含完整开合标签,误删开标签 typecheck 兜不住;移除工具条「更多设置」按钮(与工具条重复);hover 提示缩短(色板用颜色名,复制/锁定/删除)。e2e m5 新增工具条完整在视口内的断言。e2e db 18/18 + server 777/777 全绿。注意:工具条 aria-label 用颜色名后 e2e 断言需同步(如"文字颜色 红色");`cutover-cli-guards.spec.ts` RCUT-SEC-08 单独跑必绿,全量并行时偶发失败属环境残留,与业务改动无关。
- 2026-08-03 漫画成稿产品定位确认为「普通用户 + AI 排版为主 + 简单调整」(方向A),并已实现(完成记录 `2026-08-03_漫画成稿简单调整定位.md`,设计文档 `任务记录/2026-08-03_漫画成稿现状设计评审/simple_adjust_design.md`):编辑器从专业工具收缩为傻瓜微调,选中文字/气泡就近工具条直接给缩小/放大文字、粗体、7 色文字色板(复用正式命令 `text.apply_range_style` / `applyRichTextRangeStyle`+`balloon.replace_text_document`,粗体按字体目录匹配同 family 700/400 字重);专业能力默认隐藏(`advancedToolsVisible=false` 统一开关,代码保留可恢复):裁切、精确调整、画格属性、crop-controls、文字角色/SFX、富文本编辑器、气泡外观/数值/尾巴5字段、图层Tab、阅读顺序、换图镜头条、右键裁切、画格模板。e2e m4/m5 重写为简单路径并锁定「默认隐藏」态,db 18/18 + file 4/4 + server 777/777 + web 契约 55/55 全绿。关键教训:开关不能用 localStorage(违反 g5-m3「编辑器不使用浏览器存储」契约);e2e strict mode 下「删除对象」按钮在工具条与属性面板重复,需限定 `.inspector .property-panel`;m5 拖拽对比必须拖拽前重新读 API(beforeDragBalloon 曾读旧 saved 导致 undefined)。
- 2026-08-03 漫画成稿现状设计评审结论:数据链路与导出门禁产品级不动;P0 架构债=LayoutExportWorkspace.vue 3755 行单文件;P1=Undo 全文档快照成本、就地编辑丢混排(已通过隐藏富文本编辑器接受)、多选变换不完整;P2=长条漫无缩略图、导出完成无返回编辑出口、导出对话框独立配色、文字预检全量重算。深层设计问题(§6):用户能力错配(产品没选边)、用户修改不回流(自动排版学不到东西,唯一提升路径)、章节级vs整本成品断裂(G6素材包后置)。已确认方向A,剩余候选方向 B(专业编辑)/C(成品交付)/D(用户分级)。

- 2026-07-30 漫画成稿编辑器极简重构已完成(ADR-0023,完成记录 `2026-07-30_漫画成稿编辑器极简重构.md`):流程保持「自动排版→手调→导出」,只改编辑器 UI/交互。三栏改画布优先(左栏页面导航+镜头素材默认收起为 is-overlay 浮层、右侧属性面板改按需浮层、页面设置/模板进 LayoutCanvasSettingsDrawer 抽屉);选中元素就近浮动工具条(画格=裁切/复制/锁定/删除,气泡=类型/尾巴/…,多选=对齐/分布);拖拽实时预览(Konva dragmove emit previewTransform,与提交同用 normalizeKonvaTransformBatchV1 归一,松手才提交);双击文字/气泡就地改字(textarea 按首段样式重建段落,复用正式命令,IME 安全,代价是丢失段内混排样式);有限本地撤销(session 内存快照栈 ≤50、无 redo、replaceFromServer 清空,不复用已退役 LayoutCommandHistoryV1);主题废弃私有蓝 `--le-accent:#4f8cff` 改紫 `#8b5cf6`。主编排器拆出 LayoutExportDialog.vue/LayoutCanvasSettingsDrawer.vue。关键教训:e2e 逼出「左栏浮层盖住画布工具列」点击拦截,修复为左栏展开时工具列右移 272px;浮层 top 必须从 45px(工具条高度)起;服务端 g5 spec 也锁存前端源码,拆组件后断言需同步到抽屉文件;e2e 等待锚点从 shot-tray 改为 `.document-canvas` + 显式展开浮层。验证:单测 777/777、web 契约 55/55、e2e db 18/18、真实浏览器冒烟零错误(冒烟改动已恢复)。

- 2026-07-26 成稿编辑器右键上下文菜单已完成(完成记录 `2026-07-26_成稿右键上下文菜单.md`):Konva 命中层 contextmenu emit + 图层行 @contextmenu,菜单项复用既有动作(复制/裁切/分离/放回/上下移/锁定/隐藏/删除),右键未选中元素先选中。关键教训:`runContextMenuAction` 必须先执行动作再关菜单——闭包引用的 `contextMenuElement` 在菜单关闭后为 null,先关会导致动作静默不执行且菜单不关闭;e2e m4 覆盖画布右键锁定与图层行右键解锁。后续可做:多选批量菜单、画布空白处右键入口。

- 2026-07-26 e2e 收缩遗留已全部清完:「按镜头排版」手动初始化确认废弃(自动排版取代);m4/m5/m6/smart-compose-m5/candidate-decision 全部按基础版重写(smart-compose 4 删 2,只测退役 pending 预览的用例直接删除),db 18/18 + file 4/4 全绿。重写逼出真实 V2 bug:`layout-editor-session.ts` 的 `toV2UserCommand` 用 `structuredClone(command)`,而「应用到选区」等命令 payload 携带 Vue 响应式 Proxy → 克隆抛错被 actionError 吞掉,V2 草稿下范围样式静默无效(V1 无此克隆故旧测试一直绿);已改 JSON 序列化克隆(命令 payload 本是严格 JSON 契约)。基础版语义注意:TEXT_OVERFLOW blockingScopes=["export"] 在导出对话框是 blocked 硬阻断(只有返回修改),不是 review;导出完成后对话框遮罩会拦截画布点击,后续操作前需点「完成」关闭。
- 2026-07-26 e2e m7/m8 已按基础版重写并通过(db 3/3):m7 用例 1 为「首次自动排版→layout-simple-export→导出对话框→真实 PNG 产物」闭环,用例 2 保留全部 V2 API 断言但 UI 改走来源横幅「同步最新镜头」与 review「按当前文字导出」,历史恢复改 API restore;m8 去掉 Undo/导出本章/立即保存,画布尺寸改"预览→应用→再应用改回",新增 V1 旧格式禁止导出门,历史 Pending(仅接受 schemaVersion 1 批次)与手机只读不变。经验:`layout-editor-shell` 是根容器常驻可见,等待编辑器就绪要用 `shot-tray` 或 `.editor-shell`;导出对话框"导出完成"文案出现两次,locator 必须 `.first()`,否则 strict mode 抛错被 catch 吞掉会假超时。
- 2026-07-26 漫画成稿体验评估与 P0 修复（任务记录 `2026-07-26_漫画成稿体验评估与P0修复`）：评估结论为数据链路产品级（命令化/CAS 自动保存/导出预检），短板在画布直接操纵（盲拖、无画布文字编辑、无 undo）与第三套蓝色私有主题（`--le-accent:#4f8cff`，选中态蓝/薄荷绿/橙三色并存）。已修 P0：Delete/Backspace 删除选中元素（统一 `deleteElementsWithConfirm`，文字/气泡/自由图也需确认，含已放置镜头画格提示）；缩放统一 0.1–0.8（滑杆原 max 0.6 与滚轮不一致）；清理 `activeTool === "text"` 死代码链（selectDomElementInTextTool、Konva is-pass-through 条件、wheel 守卫、DOM pointerdown 死绑定、工具按钮 is-active）；删除孤儿组件 `LayoutDocumentVisualPreview.vue`（此前仅被 LayoutReadOnlyPreviewView 契约测试读源码锁存）与 `LayoutDocumentMiniPreview.vue`（零引用）。顺带修复 07-24 收缩漏改的三处过时源码断言（g5-m5-text-font、w1-web-route、g5-m8-cutover，把已退役 UI 的 toContain 改为锁退役态）。已知缺口：e2e `layout-publication-m7`/`layout-mobile-ai-m8` 仍测已退役的“导出本章/保存不可变版本/版本历史/Undo”四步出版 UI，ADR-0022 收缩时未同步，当前 db e2e 套件预计红灯，需按基础版流程重写。P1 后置项(拖拽实时预览、画布双击编辑文字、编辑器主题并入项目视觉、有限本地 Undo)已于 2026-07-30 极简重构全部完成(见上方 ADR-0023 条目)。

- 2026-07-24 漫画成稿拖拽失效根因与修复：`addText()`/`addBalloon()` 把 `activeTool` 置为 `"text"`，文字模式下 Konva 交互层 pass-through、命中框不可拖、DOM 无拖拽逻辑，用户添加文字/气泡后一切拖不动；画布文字预览（`LayoutElementTextPreview`）是纯静态的，文字模式在画布上没有任何编辑能力，纯陷阱。修复：添加后保持 `select` 工具；`activeTool === "text"` 分支成为不可达死代码（`selectDomElementInTextTool`、Konva pass-through），待清理。e2e 同步：m5 删除"文字模式拖动不变"旧断言；m4 画格点击改 `mouse.click` 坐标点击（Konva 层在 DOM 之上是设计）、阅读顺序断言前先点画布空白取消选中——m4 既有失败由此修复，db 套件 20/20 全绿。诊断方法：dev 实例（5173/4310）+ 根目录临时 Playwright 脚本直接复现真实章节。
- 2026-07-24 漫画成稿基础版已按 ADR-0022 完成收缩：普通路径只保留“首次无稿自动排版一次 → 人工画布逐项调整 → 自动保存 → 一个导出入口”。撤销/重做、立即保存、二次智能调整、范围/intent、建议对比、应用/放弃、版本历史和四步出版 UI 全部退出；底层 Working Copy CAS、双 preflight、不可变 Revision、Publication、renderer 与 Artifact 保留。顶栏只显示自动保存、手机预览和“导出”。规则 fallback 初稿只承诺内容与保守几何，不宣称专业审美；具体属性区后续可继续按真实逐项任务简化，但不得恢复二次 AI 或复杂发布流程。

- 2026-07-21 DB-only 遗留收缩与数据库复核完成：G1 历史基线为 44 models/194 triggers，fresh 当前为 53 张业务表/242 live triggers/17 migrations；0017 的 9 张表和 20 个 trigger 由现行 A4/B1～B5 来源/批次/忠实度/pending 密封路径依赖，全部保留。G1 DSL/source rebuild/write CLI、旧启发式整本导入/reset/impact preview、重复 cutover coordinator、旧 candidate lock、同步参考图 facade、测试专用入口和孤儿前端组件已退役，累计完整删除 31 个代码文件；Server TypeScript 当前 76,376 行（生产 52,146、测试 24,230），Web TS/Vue 22,730 行，Shared 14,042 行。正式切换唯一实现为 `DbCutoverService + CutoverEvidenceStore + cutover-runner`；保留冻结 manifest、`schema.prisma`、0001～0017、release identity/runtime ledger/overlay contracts、backup/restore/importer、file bridge guard、Asset 文件写入和 trigger 语义测试。后续不得恢复第二套生成器或协调器；新 Schema 只走 forward-only migration + 小 contract + runtime catalog；trigger 迁出必须先证明 FK/事务/CAS 等价。Story/Storyboard/Preflight file fallback 需整体退役决策，不能零散删除。Server/Web/Shared 均启用 unused 编译门禁。
- 2026-07-21 E2E OpenCode 合同与请求隔离：loopback fake provider 必须在 `format.type=json_schema` 时返回 `info.structured`，未请求固定结构时继续返回 `parts[].text`；“Provider 未被调用”类断言只能检查本用例开始游标后的请求增量，不能读取整场累计历史。当前 Playwright 串行约定为 `workers: 1`、`fullyParallel: false`；若未来并行共享 provider，需改用测试级 correlation ID。最近签收为 env 36/36、prepare 3/3、file 4/4、DB 15/15。
- 2026-07-21 七阶段真实全流程已用 Codex 隔离浏览器闭环：新项目 `雨夜末班车·真实验收` 完成 Script→Story→Storyboard→Preflight→11 镜 CandidateLock→LayoutPublication→AssetPackage。长期实现约定：DB WorkbenchAsset 公共 meta 保留 `createdAt` 供最新重生成素材排序；相同正式来源/文档的 Preflight 确认必须重放；Runware 候选使用 FLUX.2 Dev `inputs.referenceImages`、最多 4 张，不恢复 FLUX.1 IP Adapter；DB 素材包必须直接投影 current Script/Story/Storyboard/Preflight、sealed Layout、Candidate/Lock、Character 并复制匹配当前来源的 ready layout publication artifacts，不能回读 legacy chapter 文件；第 7 步只有 current ExportRevision.kind=`asset_package` 才算完成，旧包只能标历史。真实流程可用但当前 Runware 视觉质量仍有空背景、伪文字、额外人脸和身份/画风漂移，不能当发布金样。
- 2026-07-21 素材包交付缺口：当前 `POST .../asset-package/export` 只在受控 workspace 创建目录和 Asset 记录，`AssetPackageWorkspace` 只显示不可点击的相对路径；没有 ZIP、读取/下载路由或用户下载按钮。不得再把“文件存在/manifest 完整”表述为用户已能取得导出物；正式签收还需后端受控 ZIP 下载、前端下载入口和真实浏览器下载回归。
- 2026-07-21 页面视觉长期偏好：用户明确否决扁平灰、过度素净的 V1，后续 UI 应保留 `#0b1020` 深夜蓝黑、克制紫色渐变、轻玻璃分层和薄荷绿色完成态，只做信息层级、密度和一致性精修；默认隐藏内部 ID、英文技能名、DB Working Copy、来源摘要、文件路径和开发诊断。全页面评审基线位于 `output/ui-redesign/`，含 12 张现状截图、12 张新版稿、总览与提示词；该评审包不是生产实现，需用户确认后再组件化落地。
- 漫画成稿长期正式决策：用户可见第 6 步为“漫画成稿”，内部 `layout_export` stepKey、`/layout` 路由和正式 publication 协议保持不变。普通首次进入必须零配置生成完整结构化成稿，覆盖画格布局、非破坏裁切、阅读顺序、全部原始对白/旁白、气泡与尾巴；生成结果直接进入现有编辑器。AI 只在无 Working Copy 时排版一次，之后由人逐项调整并自动保存；系统不得静默改写或丢失对白。ADR-0022 取代 ADR-0021 尚未实施的 Facade/修复 Proposal，也取代此前“后续 full/scoped 智能调整与一次撤销”的产品要求；Shared/Server 历史能力可兼容读取，但普通 Web 不再创建对应请求。
- 2026-07-22 智能成稿 M0～M3 离线基础：`tests/fixtures/smart-layout/` 固定 10 个语义组、12 个条漫/页漫变体、69 个 active Shot 和 59 条对白/旁白。M0 红灯为画格 5/69、气泡 0/59；M2 规则初稿实现内容 100%、0 改写/overflow；M3 strict 视觉证据、fallback、三候选与稳定评分实现后，生产 renderer 自动预筛为画格 69/69、气泡 59/59，裁切、主体避让、shape-safe、尾巴语义均为 100%。旧版合成测量图 HTML/CSV 因普通人无法识别判断标准，已降级为工程证据。V2 `AIR-QA-COMIC-FINAL-001-v1` 真实图入口覆盖 12 变体、69 画格、59 气泡、30 整页/段，每轮 158 项，manifest=`sha256:d602087ee3a33aaaa90be4976a5bef996b32f7b370873e2a1dff5b909dc84ddf`；结果和生成器继续 fail-closed。2026-07-23 用户实际复核确认竖向从上到下读序及主体避让当前可接受，但重复草图无法可靠判断专业节奏，主要已知问题是部分气泡外观较丑或形状不自然；用户明确授权先继续。长期产品闭环固定为“自动完整成稿 → 同一编辑器手调 → 视觉 AI 局部重排预览，可应用/放弃/撤销”。158 项 A/B 保持真实 `pending`，改为可选内部研究，不再阻塞 M4；M3 以 `complete_with_accepted_visual_risk` 退出。正式 renderer 与 Shared/Web 必须共用素材实尺寸 cover 矩阵和 `balloon_shape_v1`；系统/设备气泡语义、手/武器/关键动作/关键物品类型化保护和智能 SFX 仍是后续项。
- 2026-07-22 漫画成稿对白追溯与兼容规则：自动生成的每条对白/旁白必须用稳定 item ID 持久绑定到唯一气泡，不能只把映射留在任务报告。用户改文时保留 binding 并保护 text；用户确认删除/隐藏时留下 `user_suppressed` tombstone，后续智能操作不得补回；复制气泡的副本不继承正式对白 binding。V1 历史 Revision 永不改写；V1 Working Copy 升级 V2 时，既有可见对象因无法证明未被人工调整而默认 `explicit_preserve`，只在用户显式释放并预览后允许智能修改。
- 2026-07-23 智能成稿 M4～M6 历史底层：Shared/Server 实现章节级 `layout_compose`、权威 Storyboard/current lock/ready Asset/角色/字体/策略来源冻结、initial V2 Working Copy、full/scoped V2 Pending、幂等/冲突与重启恢复；M6 视觉分析只接受 strict 主体/脸部/focal/text-safe/视觉中心，缓存绑定 `assetId + assetDigest`，逐 Shot 非法、超时、失败或无配置均回退。`GenerationTask.observedEvidenceJson` 只允许 legacy import 且终态 task 不回写，ADR-0020 的不可变 `LayoutCompositionApplication` 与实际 WC/Pending 同事务创建，只承载 exactly-once apply 证据。2026-07-24 ADR-0022 后，full/scoped/Pending 仅作历史兼容，普通 Web 只创建 initial，不再展示对比、应用、保护释放或 Undo。
- 已有 DB V1 成稿兼容边界：Web 可恢复 V1 Working Copy 供人工编辑，历史 V1 Revision 永不改写；基础版不再为 V1 或 V2 提供后续 AI 排版。对一般项目如需 V2 语义必须显式升级并默认保护既有对象。测试项目 `雨夜末班车·真实验收/雨夜点名` 的可变 Working Copy 已在用户授权下于 2026-07-24 再次重建，当前为 V2 `layout_wc_4d49df6c-6eae-45b2-8c5c-782b46b91ed3`，9 段/11 画格/19 条正式绑定；上游与历史 Revision/Publication 未删除。
- 2026-07-23 漫画成稿重生成与 fallback 边界：只重建某章可变 Working Copy 不必重做分镜、候选图或 Asset，也不应删除正式 LayoutRevision/Export/Artifact。产品级“正式退回第 6 步”仍是另一件事：现行 trigger 禁止普通业务清空 `Chapter.currentLayoutRevisionId`，需受控 reopen 协议。本次真实重生成及其 4 次后续调整全部为 `rule_fallback`，11 个镜头均 `visual_analysis_not_configured`；Working Copy v66 的 19 个气泡中有 7 个与画格相交，真实预览明确遮挡人物、手、手机和雨伞，且后续 reflow 把 initial 的 3 个相交扩大为 7 个。预览按统一比例忠实呈现文档，并非缩放故障。长期硬门应为：无视觉区域时禁止对白/旁白进入图片画格，只能放外部/侧挂文字带；有视觉区域时任何 face/body/focal protected overlap 都拒绝候选。设置页 `configured=true` 仅代表 text provider fingerprint 存在，不代表当前排版任务取得 runtime API key；DB 中 owner=`opencode` 且无可恢复 secretRef 时，排版会静默关闭 visualAnalysisProvider。当前评分还会把气泡面积至少 82% 位于来源画格内当作关联正确，空 subjects 让 `subjectOcclusionOk` 真空通过；hard gate 未检查气泡几何、主体遮挡或 direct-usable rate。产品必须显式提示“本次仅规则排版”，不得把 fallback 冒充智能主体避让成功。
- 2026-07-23 成熟竖向条漫排版参考结论：专业作品不以“气泡与画格零相交”为统一规则，而是优先把气泡主体放在画格间留白或天空、墙面等真实负空间，只让尾巴或少量边缘跨过画格；多人对白会在画面构图阶段把人物压到下半区，为上方气泡预留空间。画格宽高与左右位置应随建立镜头、近景、物件插入和情绪变化，不应全部同宽同高；白/黑大留白承担停顿、转场和情绪节奏。普通对白、内心、喊叫、旁白、拟声词必须分型评分。长期安全边界不变：无视觉证据时气泡主体外置；有可信安全区时才允许进入画格，并对脸、人物主体、手、关键动作和关键物品设硬保护。候选图生成阶段应支持预留对白负空间，不能只靠后期排版补救。
- 2026-07-23 标准本地 DB-only 实例已应用唯一待部署 migration `0018_layout_document_v2_working_copy`；迁移前备份位于 `/Users/liyadong/.airoaming/data/db/airoaming-pre-0018-2026-07-23.sqlite`，SHA-256=`6215d7d5115ea3fb5c51895839ea84d57633869161379ca6ff10516615754a3c`。升级后根目录 `pnpm dev` 可正常启动 5173 Web 与 4310 API。
- 2026-07-13 D2-A6 已完成并提交 `075986f`：`ProjectDeleteOutboxService` 统一处理 `asset.promote`、`asset.delete`、`project.delete_files`、`secret.delete_old_ref`、`legacy_metadata.archive`，包含严格 payload/digest/scope/path/sentinel、claim/heartbeat/lease recovery、5s/30s retry、terminal fencing；项目删除先 intent/deleting，再 processed file event 后 purge，Nest restart 可恢复。capability registry 为 8/36/0，下一阶段为 D2-A7 final importer/verifier/ready coordinator。
- 2026-07-13 D2-A7 已提交 `7a41d5c`：final importer/verifier/ready coordinator、FIN-01～10、CLI 门禁、54 files/391 tests 和双 Review 通过。D2-A8 双 fresh/replay/restart/legacy isolation 见证 D2-WIT-01～05 通过，当前进入 M6 tooling/C0～C7 隔离演练；capability 真实值仍为 8/36/`blockedIds=[]`。
- final cutover 前读取点审计结论：业务 DB read-model/Task 持久读已闭合，物理 Asset storage 是允许的文件边界；D2-A1-2 已补齐 Settings DB metadata、macOS SecretStore adapter、原子明文移除和递归 sentinel scan。secret old-ref 删除仍由 D2-A6 Outbox 闭合，真实 Keychain 与真实凭据不得在自动测试中触碰。

- `文档/` 是项目事实源；实现前先读文档，改变产品、架构、数据、模块边界或功能完成后同步文档。
- 正式文档使用中文，文档要服务人和 AI；重要结论必须落到对应 Markdown 文件，不把聊天隐含上下文当事实源。
- `$deep-think` 是按需技能，只在用户明确调用、任务高风险或跨多模块时使用；不是每次对话默认流程。
- 2026-07-16 外部创作 Skill 调研已按 A+ 方案采纳：不整体导入网上 Skill，按创作侧四阶段吸收 P1 灵感质量门槛、P2 因果大纲与结局方向、P3 章节场景契约、P4 分层修订、P5 连续性检查，并以 P6 固定评测覆盖当前 5 个公开 Skill、7 个模型阶段。公开调研确认“黄金钩子”并非统一标准；可继续候选钩子类型矩阵、`钩子→承接→预告`、单章 `种→养→收`、黄金三章可选模式、高潮逆推和悬念承诺/回收账本，不能把“每章必爽”、固定钩子数量或未经实证的留存数字做成通用硬规则。第一版保持现有 JSON/Markdown、用户确认点和受控写入不变；生产 `SKILL.md`、动态 Prompt、Agent 说明与测试必须同步，连续性承诺不得超出真实注入上下文。正式字段为 `keyConflict`。
- 当前应用入口是项目库；创建项目弹窗已实现“项目名称”和默认空的必选“漫画版式（竖向条漫/分页漫画）”，版式创建后不可直接修改，创建成功后直接进入项目工作区第 1 步“剧本”。HTTP/DB/新 artifact 只接受 canonical 两值。
- 项目库操作区 banner 源文件为 `apps/web/src/assets/project-library/hero-banner.png`，由 `ProjectCommandPanel.vue` 静态导入；该受跟踪图片缺失会导致 Vite 启动/构建无法解析资源。
- 项目库删除入口保持为项目卡片封面右上角删除按钮；卡片底部 `MoreHorizontal` 不是删除入口。删除按钮组件事件使用明确的 `request-delete`，按钮常驻可点击，hover 只改变样式。删除确认弹窗 `DeleteProjectDialog` 必须在项目列表页显式导入。删除成功后前端应先从本地项目列表移除卡片，再刷新校准后端列表，避免完整刷新延迟造成“点击删除没用”的感知。
- 删除项目属于用户二次确认后的破坏性项目容器清理：必须删除项目运行时索引、`workspace/projects/{projectId}` 目录、该项目 `GenerationTask` 和项目级对话运行态缓存；正在运行的异步生成任务不能在删除后重新写回该项目目录；不能影响全局设置、其他项目目录或其他项目任务。
- 项目工作区隐藏全局左侧导航和顶部搜索；左侧是公共“对话框”，右侧是当前步骤主工作区；顶部保留返回项目列表和紧凑 7 步流程。
- 左侧公共对话框必须随当前步骤切换标题、开场白、输入占位和附件入口；剧情结构步骤显示剧情结构助手语义，不展示剧本阶段“帮我找灵感/上传剧本文本”的文案。
- 项目用户可见主链路是：剧本、剧情结构、分镜工作台、出图准备、候选图工作台、漫画成稿、素材包；代码中的顶部 workflow 保持 `project_story -> story_structure -> storyboard -> image_preflight -> image_candidates -> layout_export -> asset_package`。`project_characters` 不再是顶部主流程步骤，只保留为 `/characters` 项目级常驻资产入口。
- 项目角色库是项目级常驻资产入口，不再作为顶部主流程步骤；顶部“角色库”默认以项目内弹窗/抽屉打开，保留当前章节步骤上下文；弹窗默认显示当前章节相关角色、当前阻塞角色和待处理新角色，并提供 `查看全部角色` 切换到全量角色库；`/characters` 路由只作为深度维护或兼容入口。剧本大纲或导入剧本中的主角和常驻角色应先进入项目级 `Character` 草稿，剧情结构和分镜阶段发现的新角色进入待处理队列。
- 前端角色库入口实现口径：`TopBar` 角色库按钮和出图准备/分镜前检查中的“打开角色库”都应打开项目内 `ProjectCharactersModal`，不要默认 `router.push(projectRoute(projectId, "characters"))`；旧 `characters` 路由保留给深度维护和兼容。弹窗内 `ProjectCharactersWorkspace(initialView="context")` 优先按当前章 `shots` 与 `storyStructure` 匹配当前相关角色，用户可切换到全部角色。
- 项目角色库可沿用“左侧公共 AI 对话 + 右侧工作区”结构；左侧负责生成、解释、重生成、合并建议和批量调整，右侧是正式项目角色库事实源。角色名是项目内身份标识，普通角色图片生成弹窗不能改名；弹窗里的描述是给 AI 生图的形象描述 / prompt。
- `Character` 是角色库和角色定稿图的唯一正式事实源；`ProjectContextFact(type=character)` 如存在，只能作为由 `Character` 派生的 AI 上下文快照或历史事实，不能成为第二份并列角色库。
- 当前 Shared 文件态 DTO 名 `ProjectCharacter` 与正式实体 `Character` 表达同一角色记录；`StoryStructureCharacterCard.projectCharacterId` 是兼容字段名，引用 `Character.id`，不得据此新建第二套角色实体或表。
- 项目角色库右侧显示角色列表、角色描述、角色图和定稿图状态；普通卡片主按钮不展示 `确认预览`、`重新生成预览` 这类中间状态，只保留 `生成角色图`、`生成定稿`、`锁定定稿` 等用户能理解的动作；改生图描述 / prompt 和重新生成主要放在图片生成弹窗或 hover 编辑中。
- 本章角色图列表是剧情结构和出图准备中的轻量审核组件，不是完整角色库：默认只展示当前章节相关角色、当前阻塞角色和待处理新角色；卡片 hover 显示编辑/删除。编辑打开角色图片生成弹窗，角色名只读，只能改 AI 生图描述 / prompt；重新生成在 UI 上直接替换当前卡片图，底层可保留 `Asset` 或视觉版本追溯但不展示版本选择器；删除只删除当前图片版本，不删除 `Character`。
- 剧情结构确认是本章角色自动同步点：确认 `structure.json` 时系统按角色名匹配项目角色库；已有角色复用已有档案和角色图，不重复创建、不重复生成；新角色自动创建为项目级 `Character(source=story_structure)`，并自动排队生成第一张角色图。用户不需要先确认入库，只在不满意时编辑生图描述、重生成或删除当前图片。
- 所有进入项目角色库的角色默认自动异步生成 `preview_front` 第一张角色图；`preview_front` 是底层角色视觉身份锚点，不作为普通用户按钮文案出现。用户触发 `生成定稿` 时，系统内部自动使用当前第一张角色图作为参考，不要求用户先点击“确认预览”。主角、常驻角色和当前章重要出镜角色在出图准备阶段前应生成并确认 `final_reference` 角色定稿图；角色视觉定稿不阻塞剧情结构，主要阻塞候选图生成。`final_reference` 是一张组合图，包含正面半身、正面全身、侧面全身、背面全身；临时/背景角色默认只保留预览图和描述。角色图是漫画制作资产，prompt 必须明确漫画/条漫/漫剧角色设定图和非真人照片约束，`dark_realistic` 等写实类风格只表示漫画写实氛围。
- 2026-07-09 出图准备定稿口径已收口：`lead`/`recurring` 不论本章出镜几次都必须锁定 `final_reference` 四要素角色定稿组合图（正面半身、正面全身、侧面全身、背面全身）；`chapter`/`minor`/`extra` 按本章实际出镜次数判断，出镜 ≥2 次（`appearanceCount > 1`）才要求定稿，只出镜 1 次的路人不强制（由剧情内容驱动，不按 level 一刀切）。判断函数 `isRequiredPreflightReferenceCharacter` 三处实现必须同口径：后端 `character-domain.util.ts`、前端 `workbench-preflight.ts`、`ImagePreflightWorkspace.vue`。场景参考图检查新增：本章被引用场景无 `referenceAssetId` 只给 warning 提示（`missing_scene_reference` issue），不阻塞出图；`ImagePreflightSceneCheck` 新增 `referenceAssetId`/`referenceReady` 字段。
- 角色定稿图在未被漫画候选图使用前可重新生成、替换或编辑；一旦用于生成 `Candidate`，旧参考图不能覆盖，后续换形象应创建新视觉版本并保留旧候选图追溯。
- 章节推进采用“按章流水线”：完成某一章剧本后，优先进入该章剧情结构；角色库待处理项只提示，不阻塞结构化；分镜完成后进入出图准备，检查本章出镜角色、场景和画风参考后才能进入候选图。不要求整部作品所有章节剧本都先写完。
- 完整剧本导入与 AI 创作是两条不同链路。旧 file-mode `import_script_to_chapters` 整文件覆盖入口已在 DB-only 退役；A2～A5 与 B1～B5 均已接通，两路只在正式 `ChapterScriptVersion` 汇合，不新增 ChapterPlan 或页面内容字段。
- 2026-07-15 用户已确认采用 `A+` 双路径方案：上游区分 `AI 创作` 与 `已有剧本分析/整理`，两路方法、质量门和阶段 Prompt 不同，但在“已确认 ChapterScriptVersion”汇合，之后继续使用现有 StoryStructure、分镜和下游流程。V1 不新增正式 `ChapterPlan`；AI 创作在项目大纲 Markdown 中保存轻量章节卡，导入路线保存观察性大纲和拆章预览，不要求两路中间产物字段相同。澄清循环以“没有阻断下一阶段的问题”为退出条件，非阻断疑点记录为假设或风险。
- 2026-07-15 A+ 的页面边界已确认：项目大纲、章节卡、导入分析和拆章预览只作为对话、预览或内部项目产物，暂不增加剧本页和剧情结构页字段。现有 StoryStructure 继续承载 `synopsis`、`direction`、角色卡、场景卡、beats 和 notes，只分析已确认章节正文；只有未来出现独立查询、编辑或版本比较的真实需求时，才评估把章节卡升级为正式 ChapterPlan。
- 2026-07-15 AI 创作 A1 已确认：系统自动判断用户是在找灵感还是已有明确题材；找灵感进入 3 个候选，明确题材跳过灵感直接进入项目大纲；完整原稿转已有剧本分析路线。只有无法判断路线或硬约束矛盾时才提阻断问题；A1 不增加表单、页面字段、正式产物或单独确认门。
- 2026-07-15 AI 创作 A2 已确认：找灵感时固定生成 3 个候选，沿用现有 `title/genreTags/logline/keyConflict/visualHook/firstChapterDirection` 和当前页面展示；P1 反差、人物压力、冲突发动机、视觉承诺与结局潜力只做内部质量门。用户可选择一个、换一批或改用自己的明确题材；V1 不做单候选编辑、候选合并或页面评分，选择后直接进入项目大纲。
- 2026-07-18 项目名称已确认为项目库中的管理标签，不是故事题材、标题或创作事实：A2～A5、通用对话、剧情结构、分镜、持久任务、角色参考图和 AI 状态工具均不得默认把它送入模型；只有用户本轮明确写出时才能作为用户输入使用。新建项目不再静默把管理名复制为 `storyTitle/description`；历史 `storyTitle === name` 同值副本不迁移，在 Prompt 边界按未确认处理。
- 2026-07-15 AI 创作 A3 已确认：项目大纲保留现有基础信息、主要角色和情节概要，并在同一 Markdown 中增加轻量“章节安排”；每章只含标题、本章目标、核心冲突、关键转折、结尾钩子和下一章衔接，不新增正式 ChapterPlan、页面字段或章节正文。大纲在现有对话预览中展示，修改通过对话生成完整新候选，只确认最新一版；确认后才进入章节生成。
- 2026-07-15 AI 创作 A4 已确认并于 2026-07-16 实现：大纲确认本身不生成章节；只有用户在当前章明确输入“生成当前章节”或点击携带明确生成 intent 的组合按钮才进入 A4。裸“继续”、章节切换和批量生成表达都不创建 pending。A4 只生成一个目标章，读取确认大纲/目标卡/必要前章正式正文并以 `sourceSetDigest` 复核后密封来源；目标已有正式正文、非空 Working Copy 或 active pending 时阻断。
- 2026-07-15 AI 创作 A5 已确认：待确认章节草稿必须可完整只读查看，pending 本身不提供手动修改；“采用草稿”只进入可编辑 Working Copy，之后可手动保存，或按`continuity_logic / structure_character / scene_dialogue / prose_format`调用现有`script-chapter-editing`生成新的 revision pending；点击“完成本章”才发布正式`ChapterScriptVersion`并允许 StoryStructure。查看、采用、丢弃、保存、完成和下一章入口都是确定性动作，只有采用后的可选 AI 修订调用模型。
- 2026-07-15 完整剧本导入的确认规则已拍板：拆章目录整体确认一次后，一次创建全部待确认章节草稿，让用户能在章节列表中自由切换查看；不限制检查顺序或次数。拆章目录确认不等于批准正文，只有用户点击某章“确认章节”后，该章才落入正式章节剧本，其他章继续保持待确认草稿；不得整本批量自动正式化。V1 草稿阶段只提供查看和确认，不提供手动修改或 AI 重新整理；不满意的章节保持待确认，修改能力以后单独设计。
- 2026-07-15 成熟产品与开源流程验证上述双路径：Boords 将 `Import Script / Write with AI / Blank` 分入口后汇入同一分镜流；Sudowrite 将整稿导入与大纲生成分开但统一到 Story Bible/Outline/章节；Novelcrafter、LumenX 使用结构/实体预览后确认；Jellyfish 使用候选确认和 `ready` 门。长期产品原则应是“入口分流、同标准产物、来源可追溯、结构化预览/报告、阻断问题才追问、确认后发布、高成本生成前再设门”，而非无限问答或整稿一次性直接覆盖正式数据。长稿应分组处理并保留原文范围/覆盖证据；结构化输入应尽量绕过不必要的 AI 改写。
- “AI 分步生成、用户确认后推进”已经是现有七阶段实现：共享章节状态链、前后端 workflow、步骤门禁以及完成/确认/锁定/导出动作均已存在，不应作为 D2 新功能重复建设。D2 只后置自动跨步骤、批量调度和一键生产的详细边界；当前先完成 G0–G5 到 current `layout_publication`/`layout_done` 的阶段性闭环，未来再做 G6/G7 到 ZIP/`exported` 的完整七阶段验收。
- 七阶段补全保留 G0–G7 总结构，但 2026-07-11 用户决定当前开发波次只到 G5：G0 行为刻画 → G1 DB-only → G2 版本/freshness → G3 漫画版式 → G4 候选定稿返修 → G5 成稿出版；G6 素材包 V2 和 G7 ZIP 总验收后置。第七步导航和现有素材包骨架保留，不改回六阶段，也不把旧目录包视为完成版。
- G0 测试安全网已于 2026-07-11 实现：常用门禁为 `corepack pnpm test`、`typecheck:e2e`、`test:e2e:env`、`test:e2e:prepare`、`test:e2e`、`test:e2e:repeat` 和 `test:all`。E2E 使用临时 workspace、唯一 runId/marker、loopback fake provider、假 key、HTTP/WebSocket 网络守卫与受控 PID；Node 22.17.1 下显式 `node --import tsx`，Nest/Vite 直接解析 Shared source。G0～G4 已完成，G5 M0～M8 技术验收已通过并等待用户签收；G6/G7 后置。
- 出图准备中的待处理出镜角色可以加入角色库、合并到已有角色、标记临时或忽略；加入/临时会创建或复用项目级 `Character` 并写 `source = image_preflight`，所有操作都会改写正式 `storyboard.json` 的 `Shot.characterIds` 并清空旧 `preflight.json`。
- 出图准备的轻量场景/画风检查已收口：正式 `Shot.sceneId` 必须匹配当前章剧情结构场景卡；`preflight.json` 写入 `sceneChecks` 和 `styleCheck`，自定义画风先作为 warning，不阻塞候选图。
- 出图准备页面的用户语言应以“当前只需要做什么”和“本章角色锁定”为主，不暴露 `preview_front`、`final_reference`、primary reference 等内部资产字段；角色库弹窗可显示“角色效果图 / 角色定稿 / 使用这版 / 锁定角色”，底层数据契约仍保持不变。
- 「剧情结构」的产品语义已确认：它不是给读者看的剧本，也不是最终分镜，而是给制作使用的结构表，包含当前章节摘要、角色、场景和剧情节拍，并作为分镜生成上游输入。
- 剧情结构中的 `StoryBeat` 按关键剧情事件切分，不和最终分镜/镜头一一对应；分镜工作台再把一个 beat 拆成多个 shot。
- 剧情结构里的角色卡和场景卡默认是本章结构卡，归属于当前 `StoryVersion`；剧情结构生成必须优先读取项目角色库，主角和常驻角色关联已有 `Character`，不能重复创建。本章新出现的重要角色进入待处理队列，用户可加入角色库、合并到已有角色、标记临时或忽略；本章新场景只有用户确认复用后才提升为项目级资产。
- 剧情结构页面主形态是可编辑结构表，不是 Markdown 文档编辑器；底层保存当前章节 `structure.json` / `StoryVersion.structureJson`，供分镜任务读取。
- 剧情节拍可包含轻量 `visualFocus` 画面重点，但不得包含镜头编号、景别、机位、构图或完整分镜稿；这些属于分镜工作台的 `Shot`。
- AI 生成剧情结构后不能自动继续生成分镜；必须用户编辑/确认剧情结构后，才可触发 `shot_generate`。
- 剧情结构步骤页面约定：左侧仍使用公共对话框但注入剧情结构 prompt；右侧以元素化结构表展示确认后的 `structureJson`，不使用文本编辑器；可编辑字段右上角显示小笔图标，点击后变输入框，失焦保存并恢复文本态。
- 剧情结构待确认预览已确认：AI 生成后右侧可以先展示“待确认预览”，方便用户检查；确认后才转为正式结构表/`structure.json`，未确认预览不能触发分镜生成。
- 剧情结构按章节逐章生成和确认：一个 `Chapter` 对应自己的 `StoryVersion` / `structure.json`，不把整本剧本一次性生成成全局剧情结构。
- 剧情结构步骤也需要章节下拉/切换器，展示每章结构状态；`Chapter.status = structured` 且 `currentStoryVersionId` 存在表示该章剧情结构完成，待确认预览不推进章节状态。
- 剧情结构章节下拉展示所有章节；`draft` 章节不能生成结构，只提示先完成本章剧本；`script_done` 章节可生成待确认预览；`structured` 及后续章节可查看/编辑结构，重新生成前必须提示会影响后续分镜、候选图和排版。`需更新` 是 UI 派生状态，只在结构 `sourceScriptVersionId` 与章节 `currentScriptVersionId` 不一致时显示，不使用 `chapter.updatedAt` 判定。
- 剧情结构入口按“当前章节”解锁：任一路线形成正式 `ChapterScriptVersion` 后，当前章可直接进入剧情结构；系统不自动生成 StoryStructure，也不要求其他章节先完成。
- 分镜工作台同样按章节逐章生成和确认：只有已完成剧情结构的章节才能生成分镜；AI 生成后先展示待确认预览，用户确认后才写入当前章节 `storyboard.json` / `Shot[]` 并将章节推进到 `storyboard_done`，未确认分镜不能触发候选图生成。
- 分镜工作台产物是 `Shot`，是用于出图和排版的镜头/画格；一个剧情结构 `StoryBeat` 可拆成多个 `Shot`。`Shot.promptDraft` 只是后续提示词生成的草稿输入，不等同于最终图片 provider Prompt。
- 分镜工作台需在同一次生成中产出漫画画格字段和基础漫剧镜头字段；正式模型是同一个 `Shot` 核心下分 `comic` / `motion` 两组表达字段。漫画关注阅读顺序、画格构图、对白气泡/旁白和页面/条漫节奏；漫剧关注时间轴、画面持续、运镜、配音角色、台词、字幕/声音和画面类型。M1 采用一个 `Shot` 对应一组 `comic` 和一组 `motion`，但不能把漫画一格和漫剧一镜永久写死为一一对应；完整轻漫剧成片、TTS、字幕和视频导出仍后置。
- 2026-05-30 分镜工作台第一版已实现：共享契约新增 `StoryboardJson` / `ChapterStoryboard` / `StoryboardShot`；后端确认后写入 `workspace/projects/{projectId}/chapters/{chapterSlug}/storyboard.json` 并将章节推进到 `storyboard_done`；前端 `StoryboardWorkspace.vue` 提供章节下拉、待确认预览、镜头卡、`comic` / `motion` 字段编辑、新增和删除镜头。
- 分镜对话工具已接入 `DialogueService`：`storyboard` 步骤可触发 `generate_storyboard` 生成待确认预览，待确认预览写入 `workspace/projects/{projectId}/chapters/{chapterSlug}/storyboard.pending.json`；用户确认后触发 `confirm_storyboard` 或右侧确认 API 写入正式 `storyboard.json` 并清除 pending 文件。
- 分镜章节下拉状态以 `ChapterListItem.storyboardStatus` 为准：`pending_confirmation` 显示待确认，`storyboard_done` 显示已完成，避免切换到其他章节后只看当前 `WorkbenchSnapshot.pendingStoryboard` 导致待确认章节误显示“可生成”。
- 2026-05-30 修复分镜生成长请求超时：OpenCode 普通消息默认超时调整为 300 秒；超时和页面刷新/连接断开不再自动重试，避免 120 秒超时重试一次后以 `This operation was aborted` 误导用户。分镜 prompt 以 `structure.json` 为事实源，章节剧本只作为截断参考输入。
- 2026-05-31 分镜链路审计结论：第 3 章真实样例中 8 个 `StoryBeat` 拆成 16 个 `Shot`，`beatId`、`sceneId`、角色名和 `comic` / `motion` 字段完整且与 `structure.json` 对齐；已补齐待确认分镜 `storyboard.pending.json` 持久化，剩余缺口是镜头缩略预览、拖拽重排、单镜头重写、批量重编号和候选图入口尚未实现。
- 2026-05-30 剧情结构第一版已实现：共享契约新增 `StoryStructureJson` / `ChapterStoryStructure`；后端确认后写入 `workspace/projects/{projectId}/chapters/{chapterSlug}/structure.json` 并将章节推进到 `structured`；前端 `StoryStructureWorkspace.vue` 提供章节下拉、待确认预览、正式元素化结构表和字段级小笔编辑。
- 剧情结构对话工具已接入 `DialogueService`：`story_structure` 步骤可触发 `generate_story_structure` 生成待确认预览，用户确认后触发 `confirm_story_structure` 或右侧确认 API 写入正式结构；待确认预览只存在当前对话线程，正式事实以 `structure.json` 为准。
- 左侧对话确认剧情结构和右侧按钮确认剧情结构必须保持同等刷新语义：`confirm_story_structure` 成功后除了合并 `storyStructure/chapter`，还要刷新当前项目运行态，拿到后端同步的 `snapshot.characters/assets/tasks`，否则本章角色图列表会误显示空态。
- 对话框组件公共，但对话记录按步骤隔离；跨步骤只共享用户已确认的事实、保存文档和锁定产物。
- AI 不直接操作本地物理路径；剧本阶段允许 AI 通过 AI漫游受控工具/API 整理、生成和编辑章节草稿，其他阶段写入权限需按各自契约单独确认。
- 第一阶段采用 OpenCode 作为项目对话框 AI Runtime；AI漫游自己的业务事实源仍是项目、对话、已确认事实和产物。
- OpenCode 对话默认模型当前兜底为 `self/gpt-5.5`
- 2026-08-05 模型管理（M1）+ 对话运行时衔接（M2-对话）已完成：设置页「模型管理」tab（对话/图片两区块，添加/删除/点击选中，禁止删当前选中，预置 GPT/Kimi/DeepSeek 对话 + OpenAI/Grok/Runware 图片）；对话运行时默认模型跟随 activeTextModelId（getRuntimeAIKeySettings），对话面板 header 模型选择器切换即生效并持久化，消息气泡显示模型，「AI 密钥」tab 保存与模型管理选中双向同步（syncActiveTextModelWithAIKey）；v1 升级时 aiKey 有凭证则镜像同源模型为默认选中。契约 `AppSettings.models: ManagedModelItem[]`，存储 v1→v2 增量兼容。已知限制：DB 模式不支持模型凭证（G1 trigger owner 白名单+Outbox）、固定槽位模型不可在模型管理删除、DB active 不持久化、DB 模型 id=model_<providerId> 而文件模式为 uuid。图片生成运行时已接（S3）：activeImageModelId 跟随、协议按 providerId 推断（doubao/grok/runware 关键字默认 openai）、候选图工作区快捷切换、syncActiveImageModelWithProvider 双向同步。方案：`文档/04_方案与决策/2026-08-05_模型管理功能设计方案.md`。：设置页新增「模型管理」tab，对话/图片两区块，用户可添加/删除/点击选中模型，禁止删除当前选中；首次预置 GPT/Kimi/DeepSeek 对话 + OpenAI/Grok/Runware 图片 6 项；选中状态持久化（文件模式完整，DB 模式 active 不落库）。契约：`AppSettings.models: ManagedModelItem[]`，存储 v1→v2 增量兼容。运行时衔接（选中模型接入对话/图片生成）未做，待用户确认后 S2。已知限制：DB 模式不支持模型凭证（G1 trigger owner 白名单 + Outbox 约束）、固定槽位（AI 密钥/图片生成）模型不可在模型管理删除、DB 模式模型 id 为 `model_<providerId>` 而文件模式为 uuid。方案：`文档/04_方案与决策/2026-08-05_模型管理功能设计方案.md`。，可通过 `OPENCODE_PROVIDER_ID` 和 `OPENCODE_MODEL_ID` 覆盖；2026-05-29 诊断确认旧默认 `aurora/gpt-5.4` 背后的 timicc provider 返回 `503 No available accounts`，不再作为默认。
- 2026-07-18 Grok 文本运行时已闭环：页面与业务保留逻辑 `xai/grok-4.5`，AI漫游管理的 OpenCode 启动时注册专用 `airoaming_xai`，使用 `@ai-sdk/openai-compatible`、用户 Base URL 和显式模型目录，消息与 auth 均映射到该运行时 ID；密钥不进入配置正文。模型列表已移除未注册 fallback。文本与图片配置的 `baseUrl + keyFingerprint` 完全一致时可从 SecretStore 恢复同一密钥以支持冷启动，不一致时不复用。真实中转、受控 OpenCode、新项目对话和删除 auth 后冷启动均通过，未调用图片生成；显式外部 OpenCode 仍需由外部实例自行注册 Provider。
- 2026-07-20 文本设置页的“自定义 OpenAI 兼容”当前硬编码为逻辑 providerId `self`；任何自定义 Base URL/模型都会显示为 `self/<modelId>`。SQLite `ProviderConfig` 虽可保留多条文本 provider，但当前 Settings DTO/API/UI 只暴露一个默认 `aiKey`，不是完整的多 Provider 新增/回显/切换界面。OpenCode Go 不能仅因自定义 Base URL 就运行为 `airoaming_self`：本机有效 Go Key 在 OpenCode 内置 `opencode-go` 认证槽位，`airoaming_self` 无 Key 会返回 `401 Missing API key`；同一运行时的 `opencode-go/grok-4.5` 已真实返回 `OPENCODE_GO_OK`。页面逻辑名可保持 `self`，但官方 Go 端点应映射到内置 `opencode-go`；当前 `configured` 只根据 Key 指纹，不等于 runtime 认证已可用。
- 首页全局设置页已接入 `/settings`：当前只保留 `AI 密钥` 和 `外观设置` 两个分组。AI 密钥保存到后端 workspace 全局设置，前端读取时只展示掩码和指纹；外观设置支持 `system/dark/light` 并由前端应用到 `document.documentElement`。
- 设置页保存成功提示按分组作用域展示：AI 密钥保存提示只在 `AI 密钥` 分组显示，外观保存提示只在 `外观设置` 分组显示，避免全局 notice 跨页签残留。
- 全局设置保存路径为 `workspace/settings/app-settings.json`，`workspace/settings/*` 已被 `.gitignore` 排除，只保留 `.gitkeep`；后续正式形态应替换为系统 keychain、数据库加密字段或专用密钥服务。
- `OpenCodeRuntimeService` 默认 `providerId/modelId` 优先读取全局设置；当设置中存在 API Key 时，后端在调用 OpenCode 前通过 OpenCode auth 接口服务端同步，不向前端回传完整密钥。
- 图片生成 provider 配置必须独立于 OpenCode 对话 `aiKey`；当前支持 `openaiImageProvider`、`doubaoImageProvider`、`grokImageProvider` 三套配置，通过 `activeImageProvider = openai | doubao | grok` 切换。`baseUrl` 作为最终 API 前缀使用，后端只去掉末尾 `/` 后拼接 `/images/generations` 或 `/images/edits`，不自动追加 `/v1`。真实图片 API Key 不写入文档、Git、任务输出或错误日志。
- Grok 图片生成默认模型为 `grok-imagine-image-quality`，官方默认 Base URL 为 `https://api.x.ai/v1`；用户使用中转时在设置页填写中转后的最终 API 前缀。Grok 文生图走 JSON `/images/generations` 并使用 `aspect_ratio/resolution`；Grok 图生图走 JSON `/images/edits`，参考图以 data URI 传入，不使用 OpenAI multipart `images.edit()`。
- Shared 生成任务枚举固定为 10 类，target 固定为 8 类（含 `character/scene`）。图片生成在本地 MVP 使用进程内串行队列：`image_generate` 通过 `ImageCandidateService.imageQueue` 逐个生成候选；`character_reference_generate` 和 `scene_reference_generate` 通过 `CharacterReferenceService.characterReferenceQueue` 逐个生成参考图。场景任务 target 为 `scene` 且必须携带章节 ID；顶部任务队列应独立刷新，不能为刷新队列重载剧本/结构/分镜编辑内容。
- 2026-06-01 项目角色库第一版已接入：共享契约、后端 API、前端 `/characters` 路由和右侧角色库工作区已实现；后端保存 `shared/characters.json`、`shared/assets.json`，角色参考图保存到 `assets/characters/{characterId}/visual-vNNN/` 并通过受控素材文件接口预览。
- 项目角色库状态规则已收口：重新生成角色参考图只新增候选资产，不直接覆盖已有 `primaryReferenceAssetId`；用户确认 `final_reference` 后才替换定稿图。角色提升为主角/常驻时必须拥有 `final_reference`，不兼容的旧 primary 需要清空并重新生成定稿图。`in_use` 角色后端必须锁定，避免破坏已生成候选图追溯。
- 设置页“图片生成”分组当前支持 OpenAI、豆包、Grok、Runware 四套独立图片 provider 配置；前端只展示状态和指纹，不回显明文。DB-only metadata 使用 `ProviderConfig/CredentialMetadata`，完整图片 key 由后端 SecretStore 保存。
- 剧本页右侧编辑器已换为 CodeMirror Markdown，保存接口当前仍保存纯文本 `sourceText`。
- 2026-05-29 产品口径更新：剧本页不再需要最右侧“当前章节信息”面板；当前剧本区域就是写剧本正文，主线、出场角色和场景列表不再常驻展示，应后置到剧情结构步骤或局部结果中；AI 对话框不再提供“分析剧情”快捷入口。
- 2026-07-14 已完成真实 DB-only 切换，SQLite 是生产业务事实源；file mode 只作受限兼容路径，首笔 DB-only 业务写后禁止回退。当前根目录 `pnpm dev` 与 `.env.example` 没有显式设置 `AIROAMING_PERSISTENCE_MODE=db`，发布/一键启动必须补齐 DB URL、迁移与 worker 配置，不能把默认开发命令当成 DB-only 产品启动器。
- 本地 workspace 路径由后端统一管理，前端不能直接写本地物理路径。
- 2026-07-15 AI 创作章节推进口径：`Chapter` 是项目内一等工作单元；完成本章后停留当前章并可进入本章剧情结构，仅在已确认大纲存在下一章卡时增加下一章入口，不提供“继续下一章”。切换只改变当前章节和对话作用域，不触发 A4；用户明确输入“生成当前章节”后，A4 才读取当前章章节卡与上一章正式正文，第 N 章生成要求第 N-1 章正式。角色库待处理项只提示，不阻塞剧情结构。
- 2026-07-15 已有剧本路线 B1 口径：复用剧本页对话框 `.txt/.md` 附件或长文本粘贴；用户发送分析/拆章请求后，系统保存未经改写的内部原稿副本，只检查可读性、内容类型、多文件顺序、用户目标和既有章节冲突。无阻断问题时不设确认门并进入 B2；B1 不生成大纲、拆章目录、Chapter 或章节草稿。V1 已有非空/正式章节的项目不允许被导入覆盖，提示改用空项目。
- 2026-07-15 已有剧本路线 B2 口径：基于带稳定`sourceRef/blockRef`的不可变原稿生成`outlineRole=observed`的大纲和拆章候选；优先保留明确章/话/幕/集边界，建议边界必须有连续来源范围与证据，禁止固定字数、固定章数或黄金钩子切分。长稿不得首尾截断，应分层扫描、全局合并和边界复核；覆盖由服务端范围并集计算，不接收模型自报百分比。B2 不创建 Chapter/pending，结果交 B3。
- 2026-07-15 已有剧本路线 B3 口径：在剧本页对话结果卡只读展示观察性大纲、全文覆盖、warnings 和完整拆章候选；不新增主页面字段，不提供目录表格内手动编辑。用户可在对话中反馈并返回 B2 生成完整新候选、取消导入，或对最新整本目录确认一次。确认只批准章节数、顺序、原稿范围、排除项和建议标题，并授权 B4 创建全部 pending；不代表章节正文已正确或正式。
- 2026-07-15 已有剧本路线 B4 口径：B3 确认后按 confirmed chapter map 原子建立全部章节入口；每章独立执行`import.materialize`生成现有六区块 Markdown，再执行`import.verify`检查来源覆盖、无来源新增、顺序、对白、人物和跨章污染，只有无硬问题才写只读 import pending。单章失败隔离、无 20 章上限、全部章节至少尝试一次才进入 B5；B4 不写正式版本或 StoryStructure。
- 2026-07-15 A+ Prompt 体系设计已完成：保留五个公开 Skill，内部有七个模型 stage contract（A2、A3、A4、A5.3、B2、B4 materialize、B4 verify）；A1/B1/B3/B5和确认/正式化由后端编排。实施必须同步`SKILL.md`、运行时 Stage Prompt、Shared Schema/严格 parser、来源与状态门、P6 fixtures 和页面投影，不能只改 Skill 文档或一条长 Prompt。推荐顺序为共享输出契约→来源/状态/pending→A 路线→B 路线→真实用户路径验收。
- 2026-07-15 双流程实施包 1 已完成：`packages/shared/src/script-workflow-contract.ts` 提供七个模型阶段的严格契约，覆盖灵感恰好 3 个、A3 四区块/等量连续章节卡、统一六区块章节 Markdown、`import-analysis/1.0` 和 `import-fidelity/1.0`。2026-07-16 A2～A5 与 B1～B5 均已接入严格契约、一次格式修复、密封 pending 与真实页面。
- 2026-07-15 已有剧本路线 B5 口径：所有章节至少完成一次生成尝试后进入逐章检查；用户可任意顺序完整只读查看 import pending，不提供手动编辑、AI 重新整理、采用后编辑、丢弃或批量确认。不满意时保持待确认；点击一次“确认章节”原子发布正式 `ChapterScriptVersion`，绑定原稿、confirmed chapter map 和 source range，停留当前章并解锁该章 StoryStructure。其他章节 pending/failed 不阻塞当前正式章向下生产。
- 2026-07-15 双路线一致性复核通过：AI 创作走“方向/大纲与轻量章节卡 → 单章 pending → 采用 Working Copy → 完成本章”，已有剧本走“原稿副本 → 观察性大纲与拆章候选 → 一次确认目录 → 全章 import pending → 逐章确认”；两路只在正式 `ChapterScriptVersion` 汇合，StoryStructure 必须绑定精确 `sourceScriptVersionId`。AI 第 N 章要求第 N-1 章正式，切章不触发生成；导入复用空白默认第 1 章，所有章节至少完成一次生成尝试后才进入 B5。内容页面字段不变，但需要 B3 结果卡、完整 pending 查看和按来源区分动作。
- 章节内产物后续优先挂到 `chapterId`，包括章节剧本、结构化剧情、分镜、候选图、排版和导出；项目级角色、世界观、通用场景和共享素材继续挂到 `projectId`。
- 目标本地结构为 `workspace/projects/{projectId}/chapters/chapter-001/script.md` 等章节目录；`story/story_draft.source.txt` 旧兼容路径已移除，新项目不再创建、读取或写入旧 `story/` 目录。
- 章节共享契约已接入 `packages/shared`：包括 `ChapterStatus`、章节列表/详情/版本 DTO、保存草稿、完成本章请求响应；`WorkbenchSnapshot.chapters/currentChapter` 是剧本页当前章节读取主契约。
- 后端已接入默认章节创建、章节列表、Working Copy 和完成本章正式版本；新严格大纲下，完成本章只在确认大纲存在下一张章节卡时创建入口，标题取章节卡，最终章不创建多余章节。页面停留当前章并优先进入本章剧情结构，项目角色库不再作为剧情结构前置门槛。
- 前端剧本编辑器的“清空本章”只作用于当前打开章节；DB-only 已退役并删除项目级 `POST /api/projects/{projectId}/script/reset`、旧整本导入及目录物理清理助手，正文变更只走逐章 Working Copy/版本流程。
- 前端剧本页已接入章节列表、`/projects/:projectId/script/:chapterId`、章节级保存草稿和完成本章；手动新建章节、重命名、删除、排序和后续产物失效提示仍待实现。
- 前端同项目章节切换不应清空 `WorkbenchSnapshot`；`openProject(..., { preserveSnapshot: true })` 用于保留工作台壳、顶部流程和左右布局，只局部刷新当前章节快照与章节级对话线程，避免切章时出现整页“正在打开项目”。
- 生成任务中心已强制章节作用域任务带 `chapterId`：`story_parse`、`shot_generate`、`shot_prompt_generate`、`image_generate`、`layout_export` 创建时必须有 `target.chapterId`；`input.chapterId` 省略会由服务端写回，不一致会拒绝。`shot_prompt_generate` / `image_generate` 创建前还必须通过章节级出图准备 guard，成功后任务 input 会注入 `imagePreflightId`、`sourceStoryboardId` 等追溯字段。
- 项目 workflow 为 7 步：`ProjectWorkflow` 定义 `project_story -> story_structure -> storyboard -> image_preflight -> image_candidates -> layout_export -> asset_package`，其中项目角色库是常驻资产入口，不作为顶部主流程步骤；`storyboard_done` 后进入出图准备，用户确认后写入 `preflight.json`，确认记录与当前分镜匹配且派生检查仍通过时才推进到候选图。
- `ConversationThread.stepKey` 的正式 scope 包含上述七个 workflow key，并额外允许辅助域 `project_characters`；不能漏掉 `image_preflight`。
- 剧本对话当前已覆盖“聊天辅助 + OpenCode 流式输出 + 已保存/编辑器当前章节上下文 + 用户提供剧本整理 + 灵感种子生成 + AI 受控写章节草稿”；刷新或离开页面导致 SSE 中断时，后端会 abort 对应 OpenCode 请求并把不活跃的旧 `running` assistant 消息收敛为失败；正式停止生成、对话持久化和完整回退仍待实现。
- 剧本阶段有“AI 创作”和“已有剧本分析整理”双来源，不新增独立导入主按钮。AI 创作每次提供 3 个灵感种子或接受明确题材，先确认含轻量章节卡的项目大纲，再按章生成；已有剧本先保存原稿、确认整本拆章目录，再生成全部只读 pending 并逐章确认。两路只在正式 `ChapterScriptVersion` 汇合。
- 项目级「剧本大纲」采用固定四区块：`基础信息`（含明确正整数章数）、`主要角色`、带唯一结局方向的`情节概要`、与章数等量且连续的`章节安排`；每张章节卡只含目标、冲突、转折、钩子和下一章衔接。大纲在对话中展示并确认；确认本身不生成章节，后续必须有明确当前章生成指令。
- 剧本阶段 AI 工具应按业务能力暴露：读取当前章节/章节列表/项目事实、整理用户提供剧本并写入章节、生成灵感种子、从种子生成项目级剧本大纲、从确认大纲生成目标章节、更新当前章节草稿、记录 AI 写入来源；不暴露本地物理路径或 shell。
- 剧本阶段 AI 统一边界契约默认覆盖所有剧本对话：普通建议不写入，灵感先生成候选，导入/改写/生成章节必须走 AI漫游受控工具/API；覆盖非空章节、替换整本剧本、低可信拆章或可能导致后续产物失效时必须先让用户确认。
- 剧本阶段需要独立 agent/skill 约束：定义 AI 是漫画剧本协作者，区分固定剧本导入、灵感共创、章节起草、章节编辑和一致性检查；AI 可通过受控工具更新章节草稿，但必须保留来源追溯。
- AI漫游已采用 `apps/server/opencodeAI` 作为 OpenCode AI 资产源码目录，参考 AuroraPlatformWeb 的 `agents / skills / tools` 组织方式，但内容必须是 AI漫游自己的漫画创作资产；当前已落地剧本协作 agent 和 `script-import-normalize` skill，运行时模板复制和真实 tool bridge 尚未接入。
- 已有剧本生产路线固定为 B1～B5：`.txt/.md` 或长文本先保存不可变原稿，B3 只接受“确认拆章目录”，B5 只接受逐章“确认章节”；禁止旧“确认导入/确认覆盖”整本写入和覆盖非空项目。
- 剧本 AI 创作已接入严格 Skill 链路：`script-inspiration-seeding` 恰好生成 3 个方向；`script-outline-drafting` 生成四区块大纲与等量轻章节卡；`script-chapter-drafting` 只在明确当前章生成指令下使用密封来源生成六区块 AI pending。pending 在正文区完整只读展示，采用后进入 Working Copy，完成才发布正式版本；章节切换和裸“继续”不会调用生成。章节内改写仍通过 `script-chapter-editing`，不得覆盖确认版本。
- 项目大纲确认后会成为稳定来源版本，但确认动作本身不生成章节。用户在当前章显式要求生成后，AI 正文先进入章节 pending；采用后进入 Working Copy，完成本章才发布正式 `ChapterScriptVersion`。灵感种子本身仍是候选产物，不作为章节正文事实源。
- 2026-05-29 发现项目文件元数据缺口：`project.json.status` 当前仍可能写成 `draft`，而 `GET /api/projects` 会根据当前章节正文推导 `story_ready`；后续应统一文件状态与运行时视图。
- 灵感种子选择不再直接写入章节：前端灵感卡片按钮是“生成大纲”；大纲结果卡片提供“确认并生成章节”。后端仍支持“1/2/3”“第一个”“选第 2 个”等文本选择表达；看起来像选择但无法识别时，应提示用户明确序号或点击按钮，不能静默走普通聊天。
- 剧本灵感入口必须优先命中后端受控工具分流，避免普通 OpenCode 聊天生成不可保存的大纲候选；`给点灵感`、`给些灵感`、`给几个故事种子`、`来几个方向` 等口语表达应触发 `generate_inspiration_seeds`。
- 剧本阶段最终用户可见章节正文统一为固定格式「章节剧本」：章节标题、基础方向、本章方向、剧本亮点、视觉基调、剧本正文、本章结尾；最终章节剧本不输出剧本名称、主体列表、正式场景列表、剧情节拍、分镜剧本、镜头编号、图片 Prompt 或 JSON，这些后置到项目级标题、剧情结构、分镜工作台和候选图阶段。
- `剧本名称/剧集名称` 是项目级作品名，从已确认剧本大纲同步到 `Project.storyTitle`，显示在章节下拉框右侧，不写进章节正文。`第 X 章：章节标题` 是章节名，保存、AI 写入或服务从 workspace 恢复旧项目时应同步到 `Chapter.title`，保证顶部章节选择器和编辑器标题一致。
- 章节正文防损坏约定：保存草稿和完成本章必须在剥离项目级剧本名称后再次校验正文非空；加载非 `draft` 章节时，如正式 `script.md` 为空但 `currentScriptVersionId` 对应版本正文完整，应从当前版本自愈并补写正式正文。明确清空章节会退回 `draft` 并移除当前版本链，不触发自愈。
- 章节最近一次 AI 写入来源通过 `Chapter.lastScriptRevision` 暴露，并写入 `chapter.json` 与 `script.revisions/latest.json`；字段包含 `threadId`、`messageId`、`toolCallId`、`operation` 和摘要。完整写入历史和回退仍待实现。
- AI 写入章节、清空当前章节、保存草稿和完成本章后的前端更新应走章节级局部合并，不再为更新剧本编辑器重新拉完整 workbench；`DialogueToolResult` 写入类结果应带 `currentChapter` 和 `chapters`，前端据此更新 `snapshot.currentChapter/chapter list/story/workflow` 与项目预览。
- 对话过程展示已参考 AuroraPlatformWeb：`DialogueThread.toolResults` 保存 AI 受控工具/技能结果，前端按 `messageId` 挂回 assistant 消息；过程卡片只表达技能/工具状态和结构化详情，`DialogueMessageItem.content` 作为面向用户的最终回复用独立气泡自然展开，正文气泡不做内部滚动，避免工具结果混入普通正文或让用户误以为没有最终回复。前端刷新项目工作台时需先读取 workbench 得到当前章节，再加载 `projectId + stepKey + chapterId` 对话线程，避免默认读到空的项目级线程。
- 剧本文档编辑器的滚动契约：`ProjectWorkbenchView -> ScriptDocumentEditor -> MarkdownTextEditor -> CodeMirror` 这条 flex 高度链路必须逐层保留 `min-height: 0` 和合适的 `overflow`，由 CodeMirror `.cm-scroller` 接管正文内部滚动，避免长剧本撑开工作台。
- 参考 AuroraPlatformWeb 的灵感文档设计：左侧对话推动流程，右侧固定文档承载持久事实；附件和用户输入只是临时上下文，正式事实必须落到工作区稳定路径，并通过状态/文件事件同步 UI。
- 2026-07-08 项目现状盘点与候选图工作台第一版：当前主流程前半段较稳，已实现项目库、工作区、章节剧本、角色库、剧情结构、分镜、出图准备；候选图步骤已从通用占位升级为真实工作台。
- 候选图工作台“本章镜头”的叙事顺序事实源必须是当前正式 Storyboard 文档中的 `Shot.order`，不能使用稳定 Shot ID、Candidate 创建时间或数据库自然顺序；DB Candidate workbench 和页面投影都应保持该顺序，并用“ID 字典序与 order 相反”的样例做回归保护。
- 2026-07-09 主链路后半段闭环：`image_generate` 走真 worker（`ImageCandidateService` + `ImageProviderService` 串行队列），候选落 `candidates.json` 与章节候选目录；支持锁定（每镜一个）与「完成本章候选图」→ `images_done`。`LayoutExportService` 已有一镜一页、复制源图到 exports/pages 的同步服务骨架；`AssetPackageService` 已有本地目录 + manifest 的同步服务骨架，两者当前都不是同名任务类型的真实 worker，也不等于 G5/G6 完成。前端排版/素材包工作台已替换占位；`WorkbenchSnapshot` 新增 `chapterLayout`，`candidates` 为真实数据。正式拼版、文字气泡、PDF/条漫切片、ZIP/下载后置。验证：`corepack pnpm -r typecheck` 与 shared 15 + server 48 tests 通过。真图仍依赖设置页 imageProvider。
- 当前新写素材规范路径只有一套：候选图为 `chapters/{chapterSlug}/candidates/{shotId}/{candidateId}.{ext}`（现实现为 WebP），角色视觉为 `assets/characters/{characterId}/visual-vNNN/preview.webp|final-reference.webp`，场景参考图为 `chapters/{chapterSlug}/scenes/{sceneId}/background.webp`；Asset 使用 G1 的 `storageKey` 等 22 个顶层字段，`WorkbenchAsset.path/meta` 只作迁移前兼容投影。
- ADR-0007 分镜正式结构使用顶层 `shotType/cameraAngle`、英文受控 `comic.panelRhythm`、`motion.cameraMovement/frameType`、数字 `durationMs` 和 `voiceLines[]`；旧顶层/运动层 `voiceRole/line` 只允许兼容读取，不得作为新 `shot_generate` 输出。
- 2026-07-09 开源 AI 漫画平台调研结论：AI漫游“出图准备”应保留为章节级检查和版本放行步骤，核心价值是正式分镜版本绑定、角色定稿门槛、参考图资产注入和候选任务追溯；多数热门开源通用生图工具没有这类漫画业务 preflight。后续重点不应把出图准备做重，而应增强候选工作台：prompt 预览/编辑、单镜重画、批量生成、候选批次、废弃/保留状态和生成历史。正式调研文档见 `文档/04_方案与决策/2026-07-09_开源AI漫画平台出图准备调研.md`，可视化 HTML 报告见 `文档/04_方案与决策/2026-07-09_开源AI漫画平台出图准备调研.html`。
- 2026-07-10 全功能竞品对照复核：AI漫游的差异化仍是项目/章节流水线、正式分镜、preflight、候选锁定、素材包 manifest 和任务追溯，但不得写成“市场独家”；2026-07-09 报告中的 prompt 不可见、无候选批次、无批量生成、分镜无缩略图/重排已被后续 P0 实现淘汰。当前真实缺口是候选 `selected/rejected/superseded` 状态操作与持久化、prompt 编辑与版本追溯、多镜拼版、气泡/旁白、长图/ZIP/PDF、局部修图和发布/视频延展。原 `全功能竞品对照-终版.html` 还误判 AI Comic Factory 不支持单格重画和 prompt 编辑，并含 `独家/最强/80%` 等无证据表述，不能直接作为排期依据；复核见 `文档/04_方案与决策/2026-07-10_全功能竞品对照复核.md`，完整可讨论页面见 `文档/04_方案与决策/2026-07-10_全功能竞品对照复核版.html`。
- 2026-07-10 漫画版式决策（ADR-0009）：创建项目弹窗的必选字段名为“漫画版式”，默认空，只提供 `vertical_scroll` 竖向条漫和 `paged_comic` 分页漫画；创建后不可直接修改。四格是 `LayoutPreset`，不与漫画版式同层。
- 2026-07-12 G3-core 已实现并通过静态/临时运行复核；G3-M 本质是 G1 maintenance/full importer/backup/cutover runtime 加 comic-format decision plugin，不是独立 mapper。M0～M5 已完成；后续 Luna 按 D2 至 M6 连续总 Handoff 内部逐切片验收、提交后自动续跑。DB capability registry 非零或 final/activation 前置未完成时仍必须 fail-closed。
- 2026-07-10 候选返修决策（ADR-0010）：候选定稿使用不可变 `CandidateLockRevision`；换图/取消定稿先预览影响并新增修订，下游布局显示 stale，旧布局、旧导出和旧任务结果按来源保留，不能覆盖历史。
- 2026-07-11 G4 开发级方案已获用户确认：Candidate 收藏/废弃不驱动下游；定稿使用不分叉的线性 `CandidateLockRevision`，replace/clear 先 preview 并带 expected revision + JCS impact digest 提交。丢响应只在 previous/action/target 精确匹配时幂等返回；更换不改旧 Layout/Export/Asset/current pointers/milestone，只派生 stale。lock set 结构完整性与来源 applicability 分轴；已导出章节在上游 current 时仍可生新候选，只有正式更换才让下游过期。G4 只建 stale 契约和门禁，画布解决由 G5 实现。G4-A～F 已于 2026-07-15 由 `79dc806`/`9cd599a`/`179be50`/`894d1e8`/`3826611`/`81c922a` 完成，总体 Scrutiny 与 Runtime/User Review 均通过，状态=`G4_PASSED`。
- G4 Candidate runtime 状态闭集为 `generated/rejected/superseded`，favorite 与 current final 分离；当前定稿只由 CandidateLockRevision + Shot current pointer 表达。0012 是不重复 G1 列/check/immutable trigger 的 forward-only 线性历史/CAS overlay，应用启动要求精确成功的 12 段 ledger。legacy 只有 Shot 直接 `lockedCandidateId` 且 Candidate/Asset/scope 可验证时建 v1；`selected` 只转 favorite，`locked` status 不推断 current。
- 2026-07-10 高自由成稿编辑器首版边界（ADR-0011）已由 G5 M2～M8 实现：受控矩形画格、横竖排字符范围富文本、四类气泡 + 单尾巴、临时多选不保存通用 Group、桌面完整编辑/手机只读、默认收起的 AI 助手抽屉；AI 建议先形成可预览 pending，用户 apply 后才进入一次可撤销命令。不做无限白板、绘画、节点工作流或画布内 inpaint。
- 2026-07-11 G5 成稿编辑器方案已于 2026-07-15 完成 M0～M8 技术验收与最终用户签收：严格 LayoutDocument 是编辑/预览/渲染事实源；Working Copy 与显式线性 LayoutRevision 分离；FontAsset 复用 Asset，AI pending 复用 PendingDialogueArtifact。正式输出是一个 `layout_publication` 多 Artifact 版本：页漫必含 PNG 可附 PDF，条漫必含切片可附能力受限长图。总体状态=`G0_G5_COMPLETE`，不自动进入 G6。
- 2026-07-11 文档完备性复核是开发授权前的时间点快照；其后 G0 已完成，G1 已完成 Schema/migration 与 Project/Chapter/Script 最小 DB 垂直切片，G2-A0～F4 已完成 Shared versioning、0009 overlay、Working Copy/pending/version/freshness、Preflight、持久任务、四类 worker 与 completion transaction。真实 provider smoke、独立 Outbox consumer、legacy importer 和 capability switch 仍后置。
- 2026-07-10 全量数据库化边界（ADR-0012）已采纳并于 2026-07-14 完成真实切换：影子导入 + 短暂停写一次 DB-only，SQLite 为唯一业务事实源；关系核心 + 版本化 Json + 可重建投影，文本 key 归 OpenCode auth，图片 key 归后端 SecretStore；任务采用 SQLite `GenerationTask/TaskAttempt` + 单进程持久 worker/lease。首次 DB 写后 file-only 永久禁止，只能使用兼容 DB 应用或协调备份恢复。旧 metadata/archive/backup 保留且不能自动删除。
- G1 DB-only Schema 冻结基线为 44 个模型、Prisma 6.19.3；后续只追加 forward-only overlay：0009 G2、0010 G3、0011 purge、0012 G4 CandidateLock、0013 G5 Working Copy、0014 source binding、0015 publication、0016 legacy layout cutover、0017 剧本双流程来源状态 9 模型。`PrismaService` 启动时精确核验完整 0001～0017 runtime ledger；不得改写历史 migration 或执行 down migration。
- G1 切换契约：Importer 只读 maintenance/停机生成且 pre/post manifest 一致的脱敏 snapshot；G1 bridge release 必须在同一旧进程通过 MaintenanceCoordinator 排空写入并生成无秘密 runtime bundle，bridge 启动前不可观察的旧内存态必须诚实报告。Asset 使用 temp→staged+Outbox→rename→ready，任务使用 claimToken fencing + TaskConcurrencySlot；旧 metadata 移入独立只读档案而非 chmod 整个 Asset 项目树。`PersistenceState.firstBusinessWriteAt` 是退回 file-only 的终点，之后只能回滚兼容 DB 应用或协调应用 backup。
- 2026-07-12 G1 纠偏已完成（ADR-0014）：自建双 Reviewer attestation/sealed bundle/CAS 不再位于生产或写入关键路径，历史 reviews 只作留痕；删除 2702 行流程实现、2699 行对应测试和 317 行 CLI，保留直接 Schema/migration/隔离验证。显式 `AIROAMING_PERSISTENCE_MODE=db` 已通过公开 Service 支持 Project/Chapter/Script 创建、草稿、完章和重启读回；默认仍为 file，不双写、不自动 migrate、不触碰真实数据。G2 可从该 substrate 开始，但 rowVersion/CAS、完整 current/version 发布事务和并发证据尚未实现。
- 2026-07-11 G2 上游版本链开发级方案已获用户确认、ADR-0013 已采纳：保持 G1 44 模型，Script 使用 Chapter Working Copy，Story/Storyboard 复用 pending version 做 copy-on-write，Preflight 使用实时预览与不可变 revision；confirmed 版本不可原地修改，旧候选/布局/导出不因返修清空。2026-07-12 G2-E1/E2 已实现 ProductionState/Workflow/NewWorkGate、Preflight SourceSnapshot、preview/confirm/replay 和 Storyboard replacement stale 派生；F1 已实现无副作用 TaskApplicabilityGuard，持久 worker、history、capability switch 仍后置。
- 2026-07-12 G2-F2～F4 已实现四类 G2 DB runtime task substrate 与 completion：shared `TaskSourceProjectionV1` canonical comparator/digest，server `PersistentTaskRepository` 覆盖 create/seal/idempotency、全局并发槽、claim/lease、heartbeat、finish/retry、cancel、expired-lease recovery；`PersistentG2TaskCreateGuardService` 在 DB API 创建前重建 current storyboard/shot/preflight source projection 并执行 `NewWorkGate`；`PersistentTaskWorkerService` 接入 OpenCode/image provider、15 秒 heartbeat、严格 V2 输出、TaskApplicabilityGuard、Story/Storyboard pending apply transaction、Asset staged→ready 和 Candidate 追溯；Task detail API 返回 Attempt 历史。四类任务幂等模板与 G1 注册表一致，source rows 按 storyboard 依赖优先写入以满足 SQLite 触发器。fresh SQLite 集成已验证 current/historical applicability、候选资产、current lock 不变、pending CAS、projection rebuild、retry、迟到 fencing 与恢复。真实 provider smoke test、独立 Outbox consumer、legacy importer 和 capability switch 仍后置。
- G2 freshness 契约：`current/stale/historical/pending` 只从 current/pending 指针、来源 ID、`sourcePolicyVersion` 和 RFC 8785 摘要派生；dirty/pending 工作稿保留旧正式链但阻止新任务，新上游正式确认后旧下游才 stale。Story 摘要排除 SceneVisual，Storyboard 摘要排除候选锁定/生成状态；Preflight 聚合分镜、实际角色/场景视觉、Asset sha 和风格。prompt/image 创建门禁冻结 `promptSpec` 与 SHA-256 generation spec；迟到任务必须通过 claimToken、active target、rowVersion 和 sourceDigest，否则只登记 historical，图片 historical 结果可保留候选但不能改 current lock。
- 分镜返修确认的长期契约：重新生成只建立 pending，不切 current；确认请求必须原样使用 pending VersionSummary 冻结的 `sourceId/sourceDigest`，禁止从 Workbench 展示 DTO 重算正式来源摘要。服务端在确认事务内对比 current Story，真实 ID/digest 变化返回 `UPSTREAM_SOURCE_STALE`；切换后旧 Candidate/Layout/Export 按来源保留为历史、Preflight 派生需更新，章节里程碑保持 `max(current, storyboard_done)`。已有下游成果时，影响清单应放在“确认分镜”而非“重新生成”之前。
- 2026-07-12 G2 五份施工资料已补齐并通过静态复核，A0～F4 已按小切片交付。0009 固定 0 新表/0 新字段/0 rebuild/0 G2 CHECK、2 个 active pending partial unique index、14 个 trigger；为支持 B1 clear，G1 0008 Working Copy CHECK 允许 current Script + 空 dirty Working Copy。F2～F4 已补齐 persistent task runtime、四类 worker、统一创建门禁、Asset/Candidate completion 与 history；禁止再建设自签 Reviewer/CAS 流程。
- 2026-07-12 G3-M0 maintenance gate 已实现并通过 6 项 MNT、server 全量测试、typecheck 与 G1 三项检查：`MaintenanceCoordinator` 状态机/lease、Projects/Dialogue/Tasks/ToolCallback/Settings 五类 participant、loopback+0600 token 管理入口和 `maintenance` CLI。M0 runtime bundle 仅是诚实骨架，显式记录 bridge 前不可观察的对话/任务状态；snapshot、redactor、importer、verify、backup、activate 仍未实现，不能据此执行 production cutover。
- 2026-07-12 G3-M1 已实现并通过 SNP/runtime 14 项、server 38 files/209 tests、typecheck 与 G1 三项检查：`SnapshotService` 只读取显式 workspace/staging/runtime-bundle，生成 pre/post source manifest、snapshot manifest、transforms、settings.redacted、payload 和 SEALED；symlink/special path/source race/secret 命中均 fail-closed。M1 仍不包含 decision codec、importer、verify、backup、activate，sealed snapshot 只能交给后续切片。
- 2026-07-12 G3-M2 已实现并通过 MAP/DEC 14 项、server 40 files/223 tests、typecheck 与 G1 三项检查：独立 comic-format mapper、三个具体 issue code、MigrationIssue detail/resolution、shared canonical decisionsDigest、reportDigest 和 `migration:decisions:check` CLI。M2 不写 MigrationRun/目标 DB，不代表 importer/shadow/final 完成。
- 2026-07-12 G3-M3-A0 已实现并通过定向 7 项、server 42 files/230 tests、typecheck 与 G1 三项检查：纯内存 `MigrationLedger` 固化 run/issue/source 状态机、终态不可变、sourceDigest 冲突与 provenance 单调升级；`MigrationAuditService` 只读 sealed snapshot，验证 manifest/payload digest，复用 M2 mapper/issue/report 生成 comicFormat 审计。`migration:audit:check` 仅是离线审计中间 CLI，不等于最终 `db:audit`；Prisma ledger、完整 importer、db-import/db-verify 仍未实现。
- 2026-07-12 G3-M3-A1 已实现并通过数据库集成 4 项、定向 12 项、server 43 files/235 tests、typecheck 与 G1 三项检查：`PrismaMigrationLedgerRepository` 接入 fresh SQLite，`DatabaseMigrationAuditService` 与 `db:audit` 持久化 audit run/issue/source；真实 trigger 证明终态 run 不可修改/删除、旧 run 不被新 run 修改、sourceDigest 冲突阻断、provenance 只能逐级升级。A1 仍不消费 decisions artifact、不创建 Project/Chapter，full importer/db-import/db-verify/activate 未完成。
- 2026-07-12 G3-M3-A2 已实现并提交：`ProjectChapterShadowImporter` 只读取 sealed snapshot，在 Project INSERT 前消费并校验 decisions，稳定写入 Project/Chapter/ImportedEntitySource，单事务回滚与同库 replay 已用 fresh SQLite 证明；`db:import --kind shadow` 可用，`--kind final` fail-closed。A2 全量 server 44 files/239 tests、typecheck、G1 三项检查通过；Script/Outline 及后续实体、db-verify、backup、activate 仍未完成。
- 2026-07-12 G3-M3-A3 已实现：`ScriptOutlineShadowImporter` 在 A2 目标实体上导入 Outline version 1 与 `ChapterScriptVersion` history，恢复 current 指针并遵守 G2 working-copy/rowVersion 约束；`db:import --kind shadow --slice script-outline` 可用，final 继续 fail-closed。A3 全量 server 44 files/240 tests、typecheck、G1 三项检查通过；pending/revision、Story/Storyboard 及完整 importer 仍未完成。
- 2026-07-12 G3-M3-A4 已实现：`ScriptPendingRevisionShadowImporter` 导入 `script-pending.json` 与 `script.revisions/latest.json`，pending 不创建 ScriptVersion；未导入 Dialogue 时旧 thread/message/tool FK 置空，原引用保留在 source evidence，`--slice script-pending-revision` 可用。A4 全量 server 44 files/241 tests、typecheck、G1 三项检查通过；Story/Storyboard 及完整 importer 仍未完成。
- 2026-07-12 G3-M3-A5 已实现：`StoryShadowImporter` 将 `structure.json` 规范化为 StoryVersion，并重建 ChapterScene/StorySceneProjection/StoryBeatProjection；source 可证明时遵守 pending→projection→confirmed→current trigger 顺序，source 不足写 `STORY_SOURCE_UNRESOLVED` blocker 且不插入伪 confirmed。`db:import --slice story` 已接入，A5 集成 2 项通过；Storyboard/Shot 及后续 full importer 仍未完成。
- 2026-07-12 G3-M3-A6 已实现：`StoryboardShadowImporter` 将 `storyboard.json` 导入 StoryboardVersion、稳定 Shot 和 StoryboardShotProjection；source 绑定 current Story，按 pending→Shot/Projection→confirmed→current；未导入角色 token 直接拒绝。`db:import --slice storyboard` 已接入，A6 链路和全量 244 tests 通过；Character/Asset/Visual、Preflight、Candidate/Lock、Task、Layout/Export、Dialogue、verifier/activate 仍未完成。
- 2026-07-12 G3-M3-A7 已实现：`CharacterShadowImporter` 导入 `shared/characters.json`，并让 Story V2 旧角色 ID 与 Character sourceKey/target ID 一致；没有 Asset/Visual 证据时不写 preview/primary 指针。`db:import --slice characters` 已接入，A7 定向集成通过；Asset/CharacterVisual/SceneVisual、Preflight 与后续 full importer 仍未完成。
- 2026-07-12 G3-M3-A8 已实现：`AssetShadowImporter` 导入 `shared/assets.json` 的稳定 Asset 身份、章节归属、类型、MIME 推断和 `meta` JSON 摘要；没有物理文件 hash/bytes/尺寸证据时保持 `staged`，不创建 CharacterVisual/SceneVisual，不标记 ready。`db:import --slice assets` 已接入，A2-A8 集成 11 项通过；物理文件证据、视觉关系、Preflight、Candidate/Task、verifier/backup/activate 仍后置。
- 2026-07-12 G3-M3-A9 已实现：`AssetVisualShadowImporter` 在显式 workspaceRoot 下校验快照物理文件 sha256/bytes/MIME/图片尺寸，安全写入目标 storageKey 后执行 staged→ready；随后导入 CharacterVisual、SceneVisual 和 current 指针，缺物理文件不建立视觉关系。`db:import --slice asset-visuals --workspace-root` 已接入，A9 全量门禁通过并提交 `b203647`；Preflight、Candidate/Lock、Task、Layout/Export、Dialogue、verifier/backup/activate 仍后置。
- 2026-07-12 G3-M3-A10 已实现并提交：`PreflightShadowImporter` 与 `db:import --slice preflight` 只接受与当前 Storyboard `documentDigest` 匹配的 V2 source snapshot；旧 V1/ID-only 或来源不完整记录 `PREFLIGHT_SOURCE_UNRESOLVED` blocker，不创建伪造 ready/current。A10 定向 2 项、全量 server 44 files/249 tests、typecheck 与 G1 三项门禁通过。
- 2026-07-12 G3-M3-A11A 已实现：`TaskShadowImporter` 与 `db:import --slice tasks` 将完整旧 input/output 导入 `legacy_imported`，缺 output 导入不可执行 `legacy_stub`；固定 retryDisabled/maxAttempts/attempt，任务不会进入 runtime claim。A11A 定向 1 项、全量 server 44 files/250 tests、typecheck 与 G1 三项门禁通过，待提交。
- 2026-07-12 G3-M3-A11B 已实现：`CandidateShadowImporter` 与 `db:import --slice candidates` 验证 Candidate 的 Shot/Task/Asset 同 scope；旧 selected/locked 转为 generated 历史，不创建 current CandidateLockRevision。A11B 定向 1 项通过，CandidateLockRevision 仍后置。
- 2026-07-12 G3-M3-A11C 已实现：`CandidateLockShadowImporter` 与 `db:import --slice candidate-locks` 只从 sealed snapshot 原始 storyboard 的 `lockedCandidateId` 恢复 `CandidateLockRevision(action=lock, origin=legacy_import, decidedAt=null)`，验证 Candidate/Shot scope 后同事务更新 Shot current pointer；仅 Candidate.status=locked 不足以锁定，replay 不重复。verifier/backup/activate 仍后置。
- 2026-07-12 G3-M3-A12 已实现：`LayoutShadowImporter` 与 `db:import --slice layout` 将旧 `layout/layout.json` 包成 `legacy_chapter_layout_v1` `LayoutWorkingCopy`，按 Shot/Candidate/Lock/Asset 证据计算 complete/unresolved 与 sourceBindings；本切片不创建 LayoutRevision/current。旧导出、full importer 与 M4 双 fresh shadow 仍后置。
- 2026-07-12 G3-M3-A12/A13 已推进：A12 已提交 `9e79deb`；A13 `ExportShadowImporter` 与 `db:import --slice exports` 将旧 exports manifest/文件证据写为 `ExportRevision(kind=layout_publication,status=failed,origin=legacy_import,completionApplicability=legacy_unresolved)`，不创建 ExportArtifact/current，按 scope/group/revision 稳定 sourceKey 并支持 replay。A13 全量 server 45 文件/257 tests、typecheck、G1 三项检查通过；Dialogue/provider、read-model/full importer、M4/M5/M6 仍后置。
- 2026-07-12 G3-M3-A14 已实现：`ProviderShadowImporter` 与 `db:import --slice providers` 从脱敏 `settings.redacted.json` 恢复 ProviderConfig、CredentialMetadata、AppPreference 非秘密元数据；ProviderConfig 保持 disabled，CredentialMetadata 保持未配置且不写 secretRef/fingerprint，A14 全量 server 45 文件/258 tests、typecheck、G1 三项检查通过。Dialogue runtime bundle 仍是 M0 不可观察骨架，read-model/full importer、M4/M5/M6 仍后置。
- 2026-07-12 G3-M3-A15 已实现：maintenance 支持注册 dialogue runtime provider，`DialogueService` 可封存线程/消息/toolResult 及 ScriptDialogueService 三类 pending Map；`DialogueShadowImporter` 与 `db:import --slice dialogue` 导入 captured Conversation*、closed DialogueRuntimeSession 和 PendingDialogueArtifact，deferred 不捏造实体，A15 定向 27 项通过，pending source evidence/payloadDigest/replay 已验证。M4 正式签字、M5/M6 仍后置。
- 2026-07-12 G3-M3/M4 已推进：`migration-source-evidence.registry.ts` 按 entityType 注册单文件、Chapter（chapter.json + script.md）复合 digest 和 runtime bundle canonical digest；`MigrationVerifyService` 合并 source/snapshot manifest 并对未知来源规则 fail-closed。`FullShadowImporter`/`db:import --slice full` 固定编排 16 个 shadow slice、保留独立 MigrationRun，聚合 reportDigest 排除 runId；双 fresh DB 的 16 slice inventory、公共 `WorkbenchSnapshot` 语义 DTO、ready Asset sha256/bytes 和 DB-only 写隔离均已通过验证。Asset 通过 `legacyName/legacyPath` 维持旧公共投影，内部 physical evidence 不泄漏到 `meta`；M4 仍等待正式验收，M5/M6 未完成。
- 2026-07-12 G3-M4 verifier 继续加固：`IMP-M4-04` 验证已注册来源摘要篡改，`IMP-M4-05` 验证 runtime 必须锚定 `runtime-bundle.json`，`IMP-M4-06` 验证单文件实体必须使用对应 storage-key pattern；`IMP-M3-FULL-03` 证明 full 编排保留 failed slice 并停止。来源门禁均保持 fail-closed，M4 仍等待正式验收，M5/M6 未完成。
- 2026-07-10 候选图整页化根因与正式契约：正式 Candidate 必须是一个 shot 对应一张单幅、静态、无文字/气泡/分格的干净底图，再由排版成稿阶段组合页面。旧 Grok 批次整页化源于 Prompt 含章节名、对白/旁白、`comic panel`、`vertical_scroll`，并把全章第一张四分格角色定稿图误作每镜 edit 底图且未传目标比例。
- 2026-07-10 候选图干净底图实现已通过静态复核、待真实模型验收：后端 `CandidateGenerationSpec` 统一 Prompt/引用/尺寸，前端只提交 shotId、数量和受控描述 override；引用只取当前 shot 已确认单人 preview 与当前场景，priority 为第一主体 100、场景 90、其余主体从 80 递减；Grok/OpenAI/Seedream 按能力上限确定性裁剪并记录省略 assetId，比例异常写 warning；旧候选读取为 `legacy_unspecified`。构建、三包类型检查、shared 15 + server 61 tests 与本地页面复核通过，真实 Grok 中转单镜生成尚未执行。
- 2026-07-19 多角色与场景一致性深化结论：镜头中的必需角色和已有场景参考不能因 Provider 图片槽位不足被静默降级成文字或删除。推荐先冻结 `ShotVisualRequirementSet`，再按 Provider 编译可追溯 `ReferencePlan`，分别覆盖 `identity/environment/composition`；`omittedRequired` 非空时不得创建正式任务。0～2 人且角色图+场景能完整传入时优先独立参考；三人以上或容量/输入预算不足时才用全员身份板，或“焦点角色单图 + 其余角色板”。Grok 双人+场景正好占满 3 槽，不应为形式统一强制合板；三人同等重要可用“全员身份板+场景+可选构图板”，有明确焦点可用“焦点单图+其余角色板+场景构图板”。当前 Grok 单场景参考被舍弃及超限固定“2角色+场景”均是本地适配缺口。
- 2026-07-19 多角色身份板长期规则：身份板是任务级派生缓存，不替代 CharacterVisual/Asset；每个角色只出现一次，优先 `preview_front`，完整四视图 `final_reference` 禁止直接嵌入，除非先通过可验证规则提取单人视图。板内不放姓名、编号、格线、水印或剧情场景，必须记录来源、顺序、裁切/缩放、布局版本、尺寸和摘要。它确定节省引用槽位；是否省 token/成本取决于合成后总像素和 Provider 计费。单人单元低于经真实 A/B 验证的最小可读尺寸时必须转分层编辑、换 Provider 或拆镜头，不能继续压缩。
- 2026-07-19 代表性产品调研与产品档位：Boords/LTX 的核心是可复用 Elements/Assets 与工作流编译，未公开底层是否拼图；Runway/Vidu 使用命名多参考和构图控制，Midjourney 在单 Omni Reference 限制下建议多人同图，Adobe/Ideogram/Scenario 用 Canvas/Mask 定向修复，Scenario/Freepik 对长期角色使用 LoRA/Multi-LoRA。AI漫游采用四档混合策略：默认自动=`按人数/焦点/Provider 自适应直传或打包`，质量优先=`独立重点角色+场景/构图`，失败修复=`锁定 shot_master 后 mask 单角色`，长期核心 IP 才训练角色模型。静态多人板只解决“是谁”；当前 `comic.composition` 仍是文字，未来场景构图板必须建立在可校对的结构化站位上并通过污染 A/B，不能把未来能力冒充当前实现。
- 2026-07-09 项目提示词资产盘点：当前梳理到 27 个提示词/AI 指令资产，覆盖 Dialogue 动态提示词、OpenCode agent/skills、角色/场景/候选图生图提示词、工具描述和共享格式约束。外部借鉴优先级：P23-P26（角色参考图、场景参考图、候选图最终 provider prompt、候选图前端预览 prompt）最适合借鉴 Stable Diffusion / ComfyUI / Fooocus 等生图提示词结构；P05-P12/P19-P22 只能借鉴故事工程和漫画镜头语言方法；P01-P04、P13-P18、P27 必须保留项目自有事实源和工具边界。盘点 HTML 见 `文档/04_方案与决策/2026-07-09_项目提示词资产盘点.html`，外部借鉴 HTML 见 `文档/04_方案与决策/2026-07-09_提示词外部借鉴优先级.html`。
- 2026-07-09 候选图与分镜体验 P0 已落地(6项)：候选按 taskId 分批次展示(最新展开/旧批折叠)、5态视觉(locked绿/selected蓝/rejected灰/superseded淡)、prompt 预览可见(提取 `utils/candidate-prompt.ts` 纯函数，store+组件共用)、分镜镜头卡缩略预览(shot.lockedCandidateId→candidates→assetId→projectAssetFileUrl)、分镜原生 HTML5 拖拽重排+重新编号(放弃 vuedraggable 因 pnpm store 版本冲突；原生零依赖)、批量生成全章未锁镜头(store.generateAllUnlockedShots 循环 createTask)。零新后端 API、零新依赖。候选生成后端已是 push(追加不覆盖)，candidate.taskId 天然批次标识。分镜改动会让候选图失效(storyboard.service.ts L238-241)，拖拽重排已有候选时弹确认。候选"废弃/备选"持久化API后置。
- 2026-07-02 DialogueService 巨石拆分：全仓最大源文件 `apps/server/src/dialogue/dialogue.service.ts` 从 3014 → 515 行(−83%)，套用 ProjectsService 门面委托模式(ADR-0005)。按对话工作流拆 3 个子 service：`ScriptDialogueService`(剧本 import/inspiration/outline/chapter，1023行，持有 3 个 pending Map)、`StoryStructureDialogueService`(剧情结构，270行，持有 pendingStoryStructures)、`StoryboardDialogueService`(分镜，231行，无 pending Map 走 projectsService 持久层)；纯函数抽 6 个 util(dialogue-types/intent/prompt/json/text/key)。核心会话生命周期(threads/stream/turn)留在 DialogueService。子 service 的 OpenCode session 解析器用 `setEnsureSession(fn)` 回调注入(3 处 constructor 调用)，避免重复持有线程状态；项目删除清理用 `clearForProject(id)` + `tryDeleteThreadState(threadId)` 回调。调用面零变更(DialogueController 仍只依赖 DialogueService)。typecheck+46 tests 全绿。详见 `文档/05_执行与记录/功能完成记录/2026-07-02_DialogueService拆分.md`。运行时对话链路待人工验证。
- 大文件拆分方法论(已验证两次)：ProjectsService 8 轮(5236→930)、DialogueService 5 轮(3014→515)。模式：① 纯函数先抽 util(零风险)→ ② 带状态工作流按业务域拆 service(门面委托 + 回调注入解循环/共享状态)→ ③ 清理死代码 → 每轮 typecheck+test+提交。循环依赖用「依赖底层 repository/store 而非门面 service」+「回调注入」解开，类型循环用 `import type`。ESM 项目 import 路径必须带 `.js` 后缀。
- 2026-07-13 G3-M4 已正式完成：full shadow 固定 16 slice、来源注册表、decisions/report/ledger/effective identity、双 fresh/replay、公开 DTO、Asset 与 DB-only 隔离均有临时根证据；`db:import --kind final` 继续 fail-closed。
- 2026-07-13 M5-A4-1 已实现并独立提交：coordinated backup 在 Prisma 派生读取前取得 SQLite `BEGIN IMMEDIATE` 写入栅栏，DB/ready Asset/settings 读取与复制在同一 fence 内；DB/Asset 复制后有摘要与完整性复核；backup/restore CLI 拒绝额外 positional、孤立值、重复/未知/缺值参数。A4-2 完成 restore release identity、16-slice ledger、open issue、PersistenceState 和 raw/semantic tamper 门禁；A4-3 完成 DB/Asset/report/settings/restored roots sentinel 扫描、symlink/重叠/storageKey 路径门和 inventory 绑定补偿清理；A4-4 完成恢复后 closed maintenance/API/firstBusinessWriteAt rehearsal。A4 集成 spec 33/33、server 全量 49 files/340 tests 和全量门禁通过，M5 恢复 `completed`；D2/M6 tooling 已纳入连续开发授权，真实切换仍需独立授权。
- 2026-07-13 D2-A0 已完成：`db-capability-registry.ts` 新增操作级 registry，登记全部 36 个 `assertDatabaseOperationSupported()` 门禁调用点，字段包含 capability/owner/source/read/write/evidence；源码覆盖测试防止新增门禁漏登记，聚合 blocked 计算会检查操作级写状态。当前只有 `generation_task_create` 有 DB guard 公开证据，其余 35 个操作保持 unsupported；CLI report 输出 8 个聚合项+36 个操作，`--check` 仍 fail-closed。
- 2026-07-13 D2-A1-2 已完成 Settings/SecretStore 隔离收口：macOS Keychain adapter 可注入 fake executor，settings 使用 temp→write→fsync→rename，递归 redactor/sentinel 覆盖 DB/settings/report/log/task/artifact/export；自动测试未触碰真实 Keychain。后续 production entry 复核发现 adapter 的 put 仍把 secret 放在 `security -w <secret>` argv 中，因此 R0-A 必须改为非 argv 敏感输入通道，不能把该隔离通过解释为生产切换入口已安全。
- 2026-07-13 D2-A2 路线已按数据库硬约束拆分：A2-1 先交付 Project metadata、ensure chapter、AI ChapterScriptPending、Script Outline、Web G2 Script 双模式和 identity-map/restart/isolation；A2-2 再闭合 clear/import/reset 的安全 replacement 与 legacy operation 退役语义。A2-1 五份施工资料已通过 Handoff 静态复核但代码尚未实现；完成 A2-1 后 blockedIds 仍为 6，连续总 Handoff 要求先做内部复核与提交，再自动进入 A2-2，不得越级跳到 A3/M6。
- 2026-07-13 连续 D2 执行最新事实：A2-1 已提交 `1f22861`；A2-2 已提交 `077762d`，7 个危险 legacy clear/import/reset/pending 入口退休，blockedIds=5；A3-1 已提交 `9087115`，7 个旧 Story/Storyboard/Preflight facade 入口退休并统一 G2 CAS API，blockedIds=4；P4 A3-2A Character identity slice 已实现 `extract_characters`/`update_character` 的 DB Character 直写与 identity-map refresh，legacy workspace 隔离通过，待独立提交。当前全量 server 54 files/364 tests 通过，真实数据、provider、Keychain、Outbox、final importer、M6 与 cutover 仍禁止。
- 2026-07-13 P4 A3-2A 继续推进：`queue_character_reference` 已在 DB 模式写入持久 `GenerationTask`，冻结 Character source projection/digest 并支持同输入幂等 replay；定向 24/24、server 54 files/365 tests 及全量静态门禁通过。该切片不调用 provider、不写物理图片、不伪造 Asset/Visual ready；worker/staging/Visual 仍后置。
- 2026-07-13 P4 worker 继续推进：`character_reference_generate` 已接入持久 worker，fake handler 证据覆盖 claim/source fencing、staged→ready Asset、CharacterVisual、preview 指针与迟到 historical；定向 26/26、server 54 files/367 tests 及全量静态门禁通过。final primary confirm、delete、SceneVisual 仍后置，blockedIds 仍为 4。
- 2026-07-13 P4 confirmation 继续推进：DB `confirm_character_preview` 与 `confirm_character_reference` 已实现，preview 按角色层级决定 final queue，final 才切 primaryVisual/status；定向 28/28、server 54 files/369 tests 及全量静态门禁通过。delete、SceneVisual 和 aggregate blocked capability 仍后置。
- 2026-07-13 P4 SceneVisual worker 继续推进：`scene_reference_generate` 内部 DB completion 已完成 staged→ready Asset、SceneVisual/currentVisual source fencing，定向 29/29；公开 scene queue/source projection、delete、CandidateLock 仍后置，blockedIds 仍为 4。
- 2026-07-13 P4 场景 queue 已完成：公开 `queue_scene_reference` 绑定 `ChapterScene` source projection（policy `scene-reference-source-v1`），持久 task 支持 replay；定向 30/30、server 54 files/371 tests、全量静态门禁通过。Character delete/CandidateLock 仍后置，聚合 blockedIds 仍为 4。
- 2026-07-13 P4 CandidateLock 已完成：DB `lock_candidate` 以 `CandidateLockRevision(origin=runtime)` 线性事务更新 Shot current pointer，同候选 replay 不新增 revision；Candidate.status 不写非法 locked 值。Character delete 与 complete images 仍后置，聚合 blockedIds 仍为 4。
- 2026-07-13 P4 `complete_chapter_images` 已完成：DB 模式要求 current storyboard、ready confirmed preflight、全镜 current CandidateLockRevision，再以 Chapter rowVersion CAS 推进 `images_done`；Character delete 仍因 Outbox consumer 未实现而后置。
- 2026-07-13 P4 三个参考图旧 operation 已 retired：`ensure_character_previews`、`generate_character_reference`、`generate_scene_reference` 在 DB 模式稳定 409，replacement 为逐目标 queue + worker；Character delete 仍唯一未收口项。
- 2026-07-13 D2-A4 Layout/Export DB-only 已实现：LayoutWorkingCopy digest/replay、LayoutRevision + LayoutSourceBinding seal、layout ExportRevision/Artifact、受控 workspace temp→fsync→rename、DB-derived asset package 均有 fresh SQLite 证据；capability `layout_export` implemented/restartCovered=true，blockedIds 从 4 降至 3。Character delete 物理清理仍必须等 P8 Outbox，Dialogue runtime 与 Project delete/Outbox 尚未实现。
- 2026-07-13 M6-A1 已推进到隔离 A1-5：A1-3 生产业务写统一经过 `PrismaService.runBusinessTransaction`，owner registry/source gate 覆盖 12 个业务 mutation owner，独立提交 `c969bb9`；A1-4 综合 rehearsal 使用真实临时 SQLite、真实 `FinalImportOrchestrator` 16 slices、Ready/Backup/Restore/Nest API/Activate 和首笔写，并持久化 C0～C7 evidence，代码提交 `b4b4a18`。ready/recovery 只读启动时 SettingsService 不再 seed 默认 provider。Scrutiny/Runtime Review 与功能完成记录已生成；server 全量 61 files/412 tests、workspace typecheck、server/web build、Prisma/G1 checks、capability blockedIds=[] 通过。矩阵中的故障注入、crash-resume、路径和安全负例若仍 `not_run` 不得伪造通过；真实 workspace、真实凭据、真实 Keychain、真实切换仍禁止。
- 2026-07-13 M6-A1 隔离证据已收口：BK/RDY/ACT/EVD/TX/RB/SEC/PATH、server 61 files/425 tests 与静态门禁通过；production entry 复核确认 final/ready 仍 fake-only、activate maintenance/evidence 可省略且 parser/CLI/runner 不完整，Keychain put 另有 secret-in-argv 缺口。因此状态固定为 `isolated_complete / production_entry_changes_required / real_cutover_no_go`，唯一入口是 `2026-07-13_R0-R2真实切换施工包` 的 R0-A。R0-A 只做临时根代码收口；真实 plan 在 C0 前冻结，C0 无授权只读落证，之后 AUTH-C1 绑定 C0、AUTH-C5 绑定 C4、AUTH-C7 绑定 C6，最后还需 OBS-01～10；G4/G5 不得抢跑。
- 2026-07-13 R0-A 第一轮实现已新增 non-argv Keychain stdin boundary、CutoverCredentialVerifier、CutoverSettingsService（legacy prestage/CAS atomic redact）、strict runtime profile、CutoverPlan/Evidence store、activate required flags 与 db:cutover C0～C7 action wiring；30 个定向隔离测试和 server typecheck 通过，真实 SecretStore/workspace/database/provider/maintenance 调用次数为 0。当前仍需 fresh-chain、故障回滚、C7 reconcile、全量回归和 Scrutiny/Runtime Review，未达到 real-cutover-ready。
- 2026-07-13 R0-A 隔离门禁继续收口：evidence store 已校验 C6_READY/COMPLETED 内容、resume conflict、semantic/raw tamper、rename failure 和 db_only completion reconcile；ready/backup/activate 可显式使用 strict cutover runtime profile，CLI 在 Prisma/plan 副作用前拒绝 fake root、缺失 required flags 和未知 step。完整 R0-A 定向为 12 个 spec/88 个测试，服务端全量为 67 个 spec/453 个测试；workspace typecheck、server/web build、Prisma/G1/capability/diff 全绿。协议层两个 fresh C0～C7 chain 已通过，但真实 domain action chain、C1/C3 注入、C4/C5/C7 故障回滚仍未完成，Scrutiny/Runtime Review 为 changes_requested，真实系统操作次数保持 0。
- 2026-07-13 R0-A runner 继续收口：将 `db:cutover` C0～C7 动作抽取到 `cutover-runner.service.ts`，依赖可注入 maintenance fetch、Prisma、SecretStore、snapshot、backup/restore、archive、activate 与无 shell migration executor；两个 fresh 临时根已跑通真实 domain C0～C7，另有 fresh 链模拟 C7 execute 后崩溃并由新实例恢复，首笔业务写入后 file bridge 被拒绝；C1～C4 统一失败矩阵、C5/C7 失败路径均验证不写后续证据并关闭 Prisma，runner 会恢复 DB 环境变量。最终定向 13 个 spec/97 个测试、服务端全量 68 个 spec/462 个测试，typecheck/build/diff 门禁通过。唯一剩余门槛是独立 Review，真实 workspace/dataRoot/Keychain/provider/maintenance 调用次数保持 0，不得生成 AUTH 或进入 R0-B。
- 2026-07-14 已为 R0-A 独立收口新增 `luna_independent_review_handoff.md`：Luna 只做独立 Scrutiny/隔离 Runtime Review，必要时修 R0-A 并独立提交；通过后只能到 `ready_for_real_cutover_authorization_review`，不得自动进入 R0-B/R1/R2。随后审计发现 production runner 可被 fake SecretStore 环境变量间接劫持，且 C3/C4 输出、evidence 子路径、C7 backup pointer 存在 symlink/非原子写/摘要未绑定风险；已改为直接绑定 `MacOSKeychainSecretStore`，补齐祖先链与临时文件清理，并补 RCUT-SEC-08、RCUT-PATH-01/02/03、RCUT-EVD-09。当前定向 13/103、全量 68/468，真实操作仍为 0。
- 2026-07-14 Luna 独立复核已落档：`luna_independent_runtime_review.md` 为 `passed_isolated`，`luna_independent_scrutiny_review.md` 为 `changes_requested`。剩余长期风险是 macOS `security -w -U` 无 TTY stdin 行为尚未在禁止真实 Keychain条件下证明，以及 C0 尚未执行 release-specific SH-01～10/人工 SH-10；因此 R0-A 总态保持 `production_entry_changes_requested / real_cutover_no_go`，不得生成 AUTH 或进入 R0-B。
- 2026-07-14 项目级完成度复核：当前静态漫画 G0～G5 按工作量与成熟度估算完成 55%～65%，长期漫画 MVP 含 G6 约完成 50%～55%，再含基础视频约完成 40%～45%。R0-A 在 60 秒 single-fork 下为 68 spec/462 tests 全绿，但默认 `pnpm test` 有 4 个慢测因 5 秒 timeout 失败；Playwright 4/4 只覆盖 file mode。DB-only Web 目前只有 Script 接 G2 新 API，Story/Storyboard/Preflight 仍调用 DB 模式已退役旧路由，preflight confirm 还有重复 route；该 adapter、route 清理与 DB-mode E2E 必须加入真实 C7 前门禁。G4/G5 正式实现与验收仍未完成，不能用 D2 的 CandidateLock/Layout 数据骨架代替。
- 2026-07-14 R0-A 独立复核最终事实：最新工作树定向 14 spec/106 tests、服务端全量 69 spec/471 tests，typecheck、server/web build、Prisma、G1、capability `blockedIds=[]`、diff check 全部通过；Luna Runtime=`passed_isolated`，Scrutiny=`changes_requested`。C0 已强制 identity-bound shadow gate（SH-01～SH-10、每项 evidence digest、MigrationReport、人工 reviewer、gateDigest），C3 失败清理新建 DB/`-wal/-shm`/dataRoot 已由 RCUT-C3-ROLLBACK 证明。真实 Keychain/provider/workspace/dataRoot/维护 API/数据库均未触碰；唯一 R0-A 阻塞为 macOS `security -w -U` child-stdin 无 TTY 平台证据，真实 SH gate 属 R0-B，禁止生成 AUTH 或进入 R0-B/R1/R2。
- 2026-07-14 R0-A Keychain 参数修正：macOS `security add-generic-password -h` 明确要求 `-w` 为最后选项才能进入提示/stdin 路径；生产 adapter 已由 `-w -U` 改为 `-U -w`，secret 仍不进入 argv，新增 RCUT-SEC-11。修复后全量 69 spec/472 tests、SecretStore/runner 定向 18 tests 与静态门禁通过；未触碰真实 Keychain，等待 Luna 重审 P1-A，真实 SH gate 仍属 R0-B。
- 2026-07-14 Luna 重审结论：`-w` 参数顺序代码问题已关闭；Scrutiny 仍 `changes_requested`，Runtime `passed_isolated`。仅帮助输出与 fake executor 不足以证明真实 macOS 子进程消费 stdin，仍需获授权 disposable-keychain put/get/delete/probe smoke；当前禁止真实 Keychain，保持 `real_cutover_no_go`，真实 SH gate 仍属 R0-B。
- 2026-07-14 R0-A 最终隔离改进：`MacOSKeychainSecretStore` 增加可选绝对 disposable `keychainPath`，生产默认不指定；路径含 NUL/相对路径拒绝，新增 RCUT-SEC-12。全量 69 spec/473 tests、Keychain/runner 定向 19 tests 与静态门禁通过；该入口只为未来获授权的隔离 smoke 服务，当前仍禁止真实 Keychain。
- 2026-07-14 Luna 最终复核：全量独立重跑 69 spec/473 tests（151.45s，exit 0），Scrutiny=`changes_requested` 仅保留真实 macOS TTY/pipe smoke，Runtime=`passed_isolated`；代码、disposable path、静态门禁均通过，未触碰默认 Keychain、未生成 AUTH。
- 2026-07-14 disposable probe 修正：显式 `keychainPath` 的 `probe` 使用 reserved-account `find-generic-password` 只读探测，missing 视为 keychain 可达；默认生产路径仍使用 `default-keychain`。定向 2 spec/19 tests 和 Luna 独立检查通过，真实 Keychain/AUTH 仍为 0。
- 2026-07-14 disposable Keychain 平台 smoke：用户授权后，生产 `MacOSKeychainSecretStore` 在临时 HOME（临时 `Library/Keychains`/`Preferences`）和临时默认 keychain 中完成 put/get/delete/probe；无 TTY child stdin 写入两行相同 secret，fingerprint 匹配，secret 不进入 argv/stdout/stderr；真实默认 keychain 与搜索列表未改变，临时根已删除，未生成 AUTH。最新服务端全量 69 spec/472 tests 通过，静态门禁全绿。生产 adapter 不再接收 keychainPath，隔离统一靠临时 HOME。
- 2026-07-14 安全留痕：早期错误 smoke 参数曾把 keychain 路径当作 `-w` 密码并短暂创建合成 probe 条目；已用精确 account/service 删除并复核默认 keychain/search list 未改变，未使用真实凭据。后续已移除该路径参数方案。
- 2026-07-14 G0～G5 连续施工顺序固定为 `S0 -> W1 -> R0B -> R1 -> R2 -> G4 -> G5`；当前唯一入口为 `文档/05_执行与记录/任务记录/2026-07-14_G0至G5剩余连续施工/luna_current_handoff.md`。用户稳定偏好是无排期、依赖满足即连续执行；日期只作留痕。G6/视频不自动开始。
- S0 默认测试门禁提交 `f07f516`；W1 提交 `3898182`/`4fe1dfa`，已接入 Story/Storyboard/Preflight DB-only Web/API、唯一 Preflight confirm 路由、409 刷新提示和 fresh SQLite E2E。
- v5 是最终有效 cutover identity：plan=`2ba999ff...fc096`，C0～C7、C6_READY/COMPLETED 和首写/file guard 已通过，C7 evidence=`sha256:987d9a9466c220544ea010b6d74ead34971b3b2eb1188388bb3a4ba66c6a1452`。旧 v1～v4 plan/AUTH/evidence 只作历史，不得复用。长期门禁仍要求真实 plan 绑定明确窗口、source/release/runtimeInstanceId；“例如/示例”的时间不得直接写入计划。
- 2026-07-14 R2 OBS-01～10 与双 Review 已通过，状态为 `DB_ONLY_OBSERVATION_PASSED`。修复提交：`62da892`（协调 purge/0011）、`0be5621`（db-only-coordinated backup/restore）、`7ddeb21`（迁移 Asset 按 storageKey 读取）、`a90f546`（Workbench 章节纯读取）。目标/备份/恢复 DB digest=`sha256:cab0b96d88dc24a7e87925aea6bc04441d0f8db0e76fac5537ce4ab64c49d739`；1 项目、2 章节、67/67 Asset 可读；secret scan 427 文件/4 SQLite/0 hit。
- R2 后仍禁止自动删除 backup/archive、执行 down migration、回退 file-only 或进入 G6/视频链路。G4 已完成：定稿权威收口为 Shot current CandidateLockRevision；preview/commit 共用 impact resolver，stale/unresolved/digest mismatch 在 Server 事务内阻断新 Layout/Export/Package，迟到下游任务只能 historical。G5-M0～M8 技术门禁与用户签收已完成，总体=`G0_G5_COMPLETE`。
- DB-only 图片密钥保存约定：相同密钥按 fingerprint 幂等复用既有 secretRef；已有 secretRef 时若缺少受控 `secret.delete_old_ref` 事件，不允许直接轮换为不同密钥，必须在覆盖 SecretStore 和运行内存前 fail-closed。
- 2026-07-15 G5-M0 已完成：`tests/fixtures/layout/` 固定 8 份 LayoutDocument、3 张本地 PNG、Inter WOFF2、20 canvas/200 element 与 document/source/profile/asset manifest/RenderPlan known-answer；旧 file-mode PNG 出口已证明只是复制源图，DB 旧出口只发布 legacy layout.json。提交=`53b65e4`；M1 后 corpus red gate 已更新为 M5/M7 的真实 production owner，历史 M0 digest 保留在完成记录。
- 2026-07-15 G5-M1/ADR-0016 已由 `68b00cb` 定版：生产主路线为 Konva 交互 adapter + DOM IME 输入 + 独立 HTML RenderScene + pinned Playwright/Chromium PNG/PDF；Konva/DOM 私有状态不得落盘。A 的 PNG/PDF/40 页/8192 切片/字体 cmap/IME/100 命令/性能硬门全部通过；B 的 resvg 2.6.2 在 1080×8192 切片 native abort，不进入首版 renderer。Inter/Noto 字节固定且 OFL，缺 glyph/字体 fail-closed；M2 必须用版本化 Shared 实现固定 Unicode grapheme policy，不能依赖客户端宿主 Intl。
- 2026-07-15 G5-M2 已由 `e93d70f` 完成：`packages/shared/src/layout/` 是无 DOM/DB/文件系统/Konva 依赖的 Layout V1 生产内核，固定 `unicode_17_0_uax29_rev47` grapheme policy，包含严格文档/出版 codec、摘要与来源投影、cover/crop、气泡/preset 和 39 类原子命令及 inverse/batch/history。验证为 M2 29/29、fixture 3/3、Shared 83/83、Server 536/536，Scrutiny=`passed`、Runtime=`passed_isolated`；M3 保存必须注入真实 Asset sha/尺寸，M5 关闭字体，M7 关闭正式 renderer。
- 2026-07-15 G5-M3 已由 `ec71594` 完成：0013 forward-only overlay 将发布 ledger 扩为 13 段；Working Copy 只保存目标 DB V1，800ms autosave、5 MiB batch、expected rowVersion CAS、no-op/replay/recovery，且不自动创建正式 Revision或写 legacy layout。桌面编辑器与窄屏只读已真实复核，rowVersion 1→2→3、窄屏 0 写入；默认 E2E 按 file/DB 模式分离并穷尽登记 spec。Shared 86/86、Server 544/544、E2E env 33/33、file 4/4、DB 3/3，当前进入 M4。
- 2026-07-15 G5-M4 已由 `93a58b2` 完成：Server 只读 source catalog 只消费 Shot current CandidateLockRevision 与 ready Asset；Web 已接 PanelFrame/nested contentImage、FreeImage、Shot tray 可见放置、7 preset、cover crop/zoom/offset/rotate/flip、reading order 和 canvas reorder；Shared 批量规则固定页漫每 4 镜头分页、条漫每 1 镜头分段。所有修改只进 DB Working Copy，源 Asset sha 不变，不创建正式 Revision/Publication。Shared 91/91、Server 546/546、env 33/33、file 4/4、DB 4/4、M4 browser 1/1，当前进入 M5。
- 2026-07-15 G5-M5 已由 `cd35053` 完成：固定 Noto Sans SC 400/700 WOFF2 的 sha/cmap/weight/style/OFL/embedding，FontAsset 按 staged→Outbox→ready provision；Web 只加载 verified Asset-ID FontFace，不回退系统字体。横竖排富文本、IME 单命令、纯文本 paste、Unicode grapheme 范围样式、overflow、四类气泡与单尾巴均进入同一 DB-only LayoutDocument/command/autosave。Shared 96/96、Server 549/549、file 4/4、DB 5/5、M5 repeat 3/3，双 Review=`passed`；PDF 字体嵌入仍归 M7，当前进入 M6。
- 2026-07-15 G5-M6 已由 `429ec69` 完成：来源替换只消费 DB current CandidateLockRevision/ready Asset，preview/commit 同 digest/crop 规则且只写 Working Copy，并作为一个原子命令提交；LayoutRevision/SourceBinding 按 unsealed→bindings→seal→Chapter current→WC basedOn 单事务线性创建，历史恢复不移动正式 current。preflight issue/digest 稳定，warning 必须确认，重复/未知 key 拒绝。0014 forward-only 修复复合 sourceDigest 被误当 Asset.sha256 的旧 trigger，同时保留 scope/ready/sha/seal 门禁。Shared 104/104、Server 551/551、file 4/4、DB 6/6，双 Review=`passed`；当前基础版不向用户暴露 Undo/Redo。
- 2026-07-15 G5-M7 已由 `d8ed6cc` 完成：固定 Chromium RenderScene 只读 sealed Revision；持久 `layout_export`、claim/heartbeat、staged Asset/Outbox、fenced finalize、PNG/PDF/条漫切片/条件长图/canonical manifest 与迟到 historical 均通过。20 段切片可像素精确拼回，CJK PDF 字体嵌入且无本地路径。
- 2026-07-15 G5-M8 历史底座由 `fc9ea47` 完成：手机 lazy GET-only 预览、strict PendingEditorCommandSet、profile resize、图片字节/EXIF/色彩空间/动画标准化门禁、0016 legacy convert/rebuild 与键盘/a11y/reduced-motion。2026-07-24 基础版继续复用手机预览、profile 与媒体门禁，但普通 Web 已删除 Pending apply/discard 与 Undo。
- 2026-07-15 G0～G5 文档与顺序回归复核完成：正式产品/UI/模块/路线图/验收文档已对齐 `G0_G5_COMPLETE`；新鲜结果为 Shared 115/115、Server 单 worker 568/568、file Chromium 4/4、G2 DB-only 2/2、G4 1/1、G5 DB-only 8/8，render/migration/typecheck/build/Prisma 全绿。默认并发 Server 有两条 backup/restore 固定 5 秒 timeout，隔离与单 worker 通过，属于测试稳定性债。G6 素材包、G7 与轻量视频继续延期；不删除 backup/archive、不执行 down migration、不回退 file-only。
- 2026-07-15 页面运行复核补齐 DB 剧情结构确认链：Web 对未绑定结构角色使用请求态 `unresolved-story-character:<normalized-name>`，DB Working Copy 在事务内按名复用/创建 Character 并只持久化真实 ID，beat 角色名转换为结构卡 ID，确认后自动补排 `preview_front` 持久任务；DB 角色列表必须刷新数据库而非 ProjectStore 旧缓存。新增 W1-E2E-05 后 DB Chromium 9/9、file 4/4、environment 34/34、Project DB integration 38/38 通过。请求态临时引用不得进入持久 StoryVersion。
- 用户要求阶段完成必须形成可追溯提交；完成标记、功能记录、测试证据和对应代码应在同一收口点提交。若完成后又发现并修复问题，必须另建后续修复提交，不能长期留在工作树，也不能把不同任务一次混合提交。
- 2026-07-16 A+ 剧本双流程及导入增强已接通：不建 ChapterPlan、不改页面内容字段，两路只在正式 `ChapterScriptVersion` 汇合。AI 创作 A2～A5 使用显式单章生成、前章正式门禁、来源密封、AI pending 采用/丢弃、Working Copy 和完成本章；切章与裸“继续”不生成。已有剧本 B1～B5 使用不可变原稿、隔离 session 长稿分层、整本目录一次确认、后台逐章生成/忠实度验证、启动中断恢复、页面进度、失败章单独重试、import pending 全文只读与逐章直接确认；不提供手动/AI 重新整理、采用、丢弃或批量确认。`ScriptImportBatch/Item` 是唯一进度事实源；当前 Worker 限定本地单服务进程，空闲不轮询 SQLite，多实例需另加 lease。P6 首组固定回归已锁住跨上传文件的连续章节、最终 JSON 连续截断 fail-closed、同一真实 SQLite 的新应用实例中断恢复；该证据不得表述为真实 OS kill 或多实例安全。
- 2026-07-16 AI 创作 P1/P2 已生产接线，2026-07-18 已将稳定生产与一次修复 Prompt 归位 `script-inspiration-seeding` / `script-outline-drafting` references：保持现有灵感 6 字段 JSON、大纲四区块 Markdown、页面和确认流程；Shared 继续负责严格格式，Server 只对高置信伪差异、缺少转折/结果连接、空泛结局、章节卡核心字段完全重复和终章未显式收束做固定触发。格式错误与质量错误共用一次修订总上限；质量失败可定向重写，第二次仍失败不交付/不保存。更细的新鲜度、情绪力度和商业判断继续由 Prompt 内部自检与用户选择承担，不扩张成数据库评分协议。
- 2026-07-16 AI 创作 A4 的 P3/P5 已生产接线，2026-07-18 已将主生成、质量重写和格式修复 Prompt 归位 `script-chapter-drafting/references/`：保持现有章节 Markdown、页面、数据库、显式单章触发和 A5 不变；Server 只注入确认大纲/目标与相邻卡/前章正式全文并拦截无效剧情或动作、通用结束点、多场复制、章节卡四项承诺完全不可观察和完全不承接前章结尾锚点的明显重置稿。格式与质量共用一次修订总上限，第二次仍失败不创建 AI pending。P5 只对运行时提供并以版本摘要密封的上一章当前正式全文负责，第 1 章或证据不足时跳过；历史 `generate_script_from_seed` 协议值保留，但不可达的旧“灵感直出第 1 章”私有 Prompt/方法已移除。
- 2026-07-18 A5.3 章节修订 Prompt 已归位 `script-chapter-editing/references/`：主修订读取当前编辑器保护基线、用户要求和必要的前章正式全文；Server 仍确定 `continuity / development / scene_dialogue / prose` 唯一最高层并执行 strict parser、P4 越层/有效修改和 P5 不退化固定门。P4、P5、格式分别使用独立 Skill 修复合同，但共用一次总上限；第二次失败不创建 AI pending，模型期间前章版本或 pending 状态变化仍拒绝写入。页面、六区块字段、数据库、A5 采用/丢弃/完成流程和付费调用规则不变。
- 2026-07-18 已有剧本 Prompt 已归位 `script-import-normalize/references/`：B2 observed 分析、B4 忠实章节整理、B4 忠实度验证保持三份独立模型合同，格式失败分别最多重试一次且不得改剧情。Shared 继续负责 `import-analysis/1.0`、固定章节 Markdown、`import-fidelity/1.0` 和引用校验；Server 继续负责不可变来源、长稿分层、动态 Schema 示例、lineRef、失败隔离和逐章确认。目录一次确认后创建全部章节入口、待确认全文只读、用户自由切换并逐章点击“确认章节”的 B1～B5 流程不变；Server 全量 739/739，真实模型与付费调用为 0。
- 2026-07-16 AI 创作 P4 分层修订与编辑 P5 已生产接线：采用章节草稿后的明确改写按“连续性 > 发展性 > 场景与对白 > 文字”识别最高层，不增加页面层级选择、字段、数据库或确认节点；建议/评价类请求不触发写入。第 N 章 DB-only 编辑读取第 N-1 章当前正式全文，P5 只保护当前稿已有承接不退化；缺失前章在模型前阻断，创建 pending 的事务复核上一章 ID、版本和摘要。第 1 章跳过，file-mode 仍为 P4-only。revision pending 仍是 `kind=legacy` 且无持久 base draft/previous script provenance，不得表述为完整历史审计。
- 2026-07-16 双流程 P6 总验收完成：固定 5 个公开 Skill、7 个模型阶段；`import.materialize/verify` 是目录确认后的系统阶段，不新增用户对话 Skill。A2/A3/A4/A5/B1/B2 正反触发、B2/B4 Prompt 禁止项和 Shared 七阶段 fixture 已统一；Server 643/643、AI 显式路径重复 5/5、双路线 2/2、类型/构建/5 Skill 校验全绿。页面、Schema、输出字段和确认动作均不变。
- 2026-07-16 对话运行时约定：流式与非流式生成中的 assistant message 都必须登记为 active，线程 GET/settle 不得把当前非流式消息提前结算为 interrupted；`P7-DIALOGUE-DB-02` 锁住该竞态。OpenCode 新会话号在本次调用中必须用局部不可变值持久化，不能从可被页面旧 DB hydration 覆盖的共享线程字段回读；hydration 对内存会话按 active DB 状态二次核验，`P7-DIALOGUE-DB-01/03` 同时保护关闭旧会话与并发创建。
- 2026-07-16 双路线真实模型浏览器验收已由 `PARTIAL / NOT_ACCEPTED` 修复并提升为 `ACCEPTED`：DB-only 章节生成/修订工具结果必须统一刷新运行态，使 pending 正文与 `scriptPendingSuggestion` 同步，页面生成后无需刷新即可采用；B2 Prompt/Skill 必须明确 `excludedRanges[].sourceRange` 单数条目结构。B4 忠实度规则允许有原文证据的摘要、情绪、氛围和视觉标签归纳，但仍阻断具体剧情新增、语义反转，以及把标签/录音/档案等非口头文本重分类为人物对白。真实 `self/gpt-5.5` 新项目已完成 AI pending 立即采用和两章完整剧本“目录一次确认→后台逐章整理/验证→失败章单独重试→逐章确认正式→允许进入剧情结构”；隔离导入批次最终 `completed`，2 个 item/版本均 confirmed/import，pending=0。DB Playwright 11/11、类型/构建通过；全量单元首次 641/643，两个无关备份用例固定 5 秒超时，隔离复跑 2/2 通过。
- 2026-07-16 双流程汇合后的剧情结构质量门已完成：不改页面、Schema、字段、确认点或公开 Skill；DB 模式只在 Script Working Copy clean 且 current ID 一致时，按 `currentScriptVersionId` 读取正式正文。Prompt 禁止用项目大纲补写本章未发生事件；固定门检查正文逐场一一对应、每场至少一个 beat、人物覆盖、合法枚举、连续顺序、有效引用和非空壳/非重复事件。格式与质量共用一次定向修复，第二次失败不创建 pending。历史非固定 Markdown 只执行结构自身检查，不宣称逐场覆盖。Server 107 files / 652 tests、类型/构建、DB 页面生成确认链 1/1 通过。
- 2026-07-16 剧情结构真实模型验收通过：真实 `self/gpt-5.5` 在两个隔离新项目中分别从 AI 创作正式章节和 import 正式章节生成、预览并确认结构。AI 样例为 3 角色/7 场景/15 beats，导入样例为 3 角色/1 场景/6 beats；两者 StoryVersion 均精确绑定当前正式 ScriptVersion、章节 `structured`、分镜解锁、console 0 error/warn。导入目录一次确认后 4 章全部可查看，但只确认第 1 章，其余 3 章保持 `pending_ready`，证明逐章确认不会被后台批量越权。该证据不等同于多题材商业质量穷尽评测。
- 2026-07-16 分镜与生图提示词当前阶段完成：分镜页面字段和“用户明确生成→待确认草稿→用户确认→正式 StoryboardVersion”不变。S1 负责生成/调整、结构本地引用映射和正式版本接线；S2 已完成新 AI 输出严格契约、beat 覆盖/顺序/一致性等固定质量门和一次定向修复，二次失败不写 pending，历史 normalize 不变。P23 角色参考图、P24 场景参考图、P25/P26 候选图实际 Prompt/页面预览已升级，并从下游反推 P06 漫画单帧与精简 `promptDraft`。候选图普通任务、DB 持久任务、页面预览和 worker 共用唯一领域 builder；Provider Profile 按 OpenAI/Doubao/Grok 当前单 Prompt 网关编译并冻结到新任务，不再统一追加 `Avoid:`。S3 已用真实 `self/gpt-5.5` 在两个隔离新项目中通过：AI 12 beats→24 shots 首次通过，import 7 beats→12 shots 命中空对白画格后一次修复通过；两路精确版本链、pending/确认边界和 console 0 error/warn 均通过。S4 真实图片质量仍需单独授权。
- 2026-07-17 图片 Prompt S4 离线基线已固定：角色预览、角色四视图、场景参考，加无角色远景、单人近景、双人对话、多人群像、场景特效 5 类候选镜头；五类镜头分别编译 OpenAI/Doubao/Grok 共 15 个 profile，真实授权后每项 2 张，总计 30 张。所有案例直接调用生产 builder，离线检查全部通过，生产 Prompt 不变；真实人物/场景一致性、构图、文本污染和 provider 参考省略仍是 `not_run`，必须单独授权后验收。
- 2026-07-17 图片 Prompt S4 真实视觉 A/B 已完成：用户授权最多 30 次，attempt ledger 保证失败占槽、无自动重试/补位；实际 21 次请求为 OpenAI 首次编辑 503 后停止、Doubao/Grok 各 10/10 成功。Doubao 人工契约分 14/30，参考复现强但请求固定 `watermark:true` 导致 10/10“AI生成”水印，尺寸映射 10/10 偏离冻结 3:2/2:3，四人镜头 2/2 缺人；Grok 24/30、当前适配下最佳，但单参考旧港 2/2 被比例策略舍弃、四人最多 3 张参考且一次双人图出现斜切分格。OpenAI 只判端点不可用，不评价画质。下一步先修 provider 适配（水印/尺寸/参考上限/503），再用同一语料小规模复测，不先堆 Prompt 或改页面字段。
- 2026-07-17 图片 Prompt 专业 V2 已离线收口：基于官方资料与热门开源方法形成角色预览、角色四视图、场景参考和五类漫画候选镜头合同；候选图使用 `AIROAMING_SHOT_CONTRACT_V2` 结构语义，OpenAI/Grok 编译英文、Doubao 编译中文，参考图职责按实际入图追加，Doubao 关闭水印并保持 3:2/2:3。V1/V2 测试入口、ledger 和 Prompt manifest 已隔离，全量 120 files/722 tests 通过。用户因预算明确禁止继续生成图片，真实 V1/V2 视觉质量为 `not_run`，不得宣称通过；V1 编译器保留，付费 A/B 只有未来重新确认 Provider、模型、价格、请求上限和预算后才能恢复。
- 2026-07-18 九个生产 Skill 的稳定 Prompt 单一事实源已闭合在 `apps/server/opencodeAI/skills/`，加载器 fail-closed；分镜持久/对话路径共享完整 Skill，provider 职责、JSON 示例、画风/版式词汇以及 P1/P2/A4/A5.3/已有剧本 B2-B4/剧情结构 Prompt 均由对应 references 提供，并有源码防回流测试。后端只保留动态事实、严格 Schema/引用/质量校验、状态机和受控写入。页面字段、确认流程、数据库 Schema 和任务协议不变，真实图片调用为 0；P6 离线 evaluator 和固定 Validator 不应为追求形式统一而改成公开 Skill。
- 2026-07-18 剧情结构 Prompt 已归位 `structure-story-parse`：对话与持久 `story_parse` 共用同一主模板、语义 JSON 示例、固定质量门和一次定向修复。AI 只输出当前页面需要的 `synopsis/direction/characters/scenes/beats/notes`，后端补本地 ID、版本来源和待事务解析的角色引用，再由 `StoryVersionRepository` 按名复用或创建正式 Character；持久任务只读取 `sourceProjection` 冻结的精确 ScriptVersion，不读取未冻结的当前大纲。页面字段、确认门、数据库 Schema 和任务协议不变，付费媒体调用为 0。
- 2026-07-18 项目完成度审计：G0～G5 当前开发波次继续为 `G0_G5_COMPLETE`；完整七阶段漫画 MVP 约 80%～85%，主要缺 G6 正式 ZIP/下载素材包与发布收口；完整愿景含轻漫剧约 60%～65%，TTS/字幕/BGM/视频导出未实现。新鲜回归为 Shared 153、Server 722、file Chromium 4/4、DB Chromium 13/13，构建/类型/Prisma/render/migration 全绿。交付风险包括本地分支领先远端、图片 Prompt V2 未提交、无全局零预算图片锁，以及 `全流程与字段清单`/`核心数据模型` 仍保留旧状态。
- 2026-07-16 漫画/漫剧双轨分镜 Prompt V2 已进入生产代码：保留 StoryStructure 事实源、本地引用、`comic / motion`、pending/confirm、严格契约和一次修复，Prompt 内部按共享剧情事实→漫画分镜→漫剧分镜→双轨边界编排。漫画独立设计静态瞬间、构图、阅读顺序、气泡/旁白和格间节奏；漫剧独立设计开始/变化/结束状态、人物表演、运镜、内容时长、配音和尾首帧衔接，漫剧不是漫画动态投影。当前 M1 仍是“内容字段双轨、镜头骨架共用”，镜头数量、order、shotType 和 cameraAngle 尚未独立；页面/Schema 不变，`promptDraft` 只属于静态候选图。黄金三秒、固定秒数、9:16、CTA、九宫格、8K/焦段/负面词不进入通用分镜默认规则，既有钩子只允许视觉强化。V1/V2 真实 A/B 结论为 `MIXED / V2_DIRECTIONALLY_BETTER`。后续 V2.1 增加单镜主要变化、动态负载第二镜触发、新增漫画格静态价值保护和可见正文对白逐字引用；相同项目 `self/gpt-5.5` 复测结论为 `MIXED / V21_DIALOGUE_LOAD_BETTER_STATE_LOAD_UNRESOLVED`：AI `voiceLines>3` 镜头由 7 降为 0、对白逐字命中 31/31、两路首次通过，但 AI 总时长由 127.0s 增至 161.5s，仍有多个 10～12.5s 镜头承载多次状态变化。当前保留 V2.1 对白减负/忠实引用/漫画保护，下一轮聚焦状态边界和超长镜头范围；未形成多样例重复证据前不扩独立序列 Schema。
- 2026-07-17 V2.2 状态边界实验结论为 `MIXED / V22_STATE_BOUNDARY_BETTER_DIALOGUE_LOAD_REGRESSED`，未进入生产：相同项目/模型复测中，AI 超 10 秒镜头由 5 个降为 1 个，后半段状态边界更清楚；但最大单镜 voiceLines 从 3 升到 9，11/23 镜超过 3 条，完整正式正文离线逐字命中 59/61，总时长增至 180.0s。导入路线保持 11 镜、最大 2 条对白、6/6 命中。该实验 Prompt 当时回滚到 V2.1，并直接形成下一版“正式对白候选与分段→状态边界→共享 Shot→comic 静态价值”的输入；整章逐字硬门必须同时改正文上下文组装，不能只堆 Prompt 规则。
- 2026-07-17 双轨分镜 Prompt V2.3 已采用为生产基线：从精确正式 `ChapterScriptVersion` 编译全章对白、旁白和有声音证据的引号候选，内部按“选择最少必要台词→最多两段对白→状态边界→共享 Shot→comic 静态价值”执行；固定 Markdown 可解析时 `motion.voiceLines[].line` 必须逐字命中候选，历史纯文本降级兼容，来源/格式/既有质量共用一次修复。相同项目和 `self/gpt-5.5` 真实复测中，AI 18 镜、146s、42/42 逐字命中、最大 4 条/镜、2 镜超过 3 条、0 镜超过 10s；导入 11 镜、60.5s、6/6 命中、最大 2 条/镜。两路首次通过、保持待确认、付费媒体调用为 0。已知非阻断残留是 AI 开场未明确表现旧机器刹车声；后续应做 Beat `summary/outcome` 语义软评测，不扩 Storyboard 字段或脆弱字符串硬门。
- 2026-07-17 Beat `summary/outcome` 语义软评测已落为独立 P6 QA CLI：只读取精确 StoryStructure/Storyboard，模型逐 Beat 输出 `covered/partial/missing/contradicted` 和同 Beat 镜头证据，本地严格校验并派生 `pass/warning/fail`；不接生产生成、数据库、页面、自动修复或确认。`self/gpt-5.5` 对 V2.3 两路线各复测两次：AI 样本 4 个非完整 Beat 完全稳定并复现旧机器刹车声缺失；导入样本 2 个核心问题稳定、另有 2 个边缘 partial 单次出现。长期规则是把重复评测稳定交集用于 Prompt A/B 诊断，单次 warning 不得作为生产硬门；V2.3 仍是生产基线。
- 2026-07-17 双轨分镜 Prompt V2.4“Beat 事实账本→双轨逐项承载→逐 Beat 反查”实验已拒绝并回滚：V2.4a 将 AI 语义从 20/6 改善到 24/2、导入改善到 14/2，但镜头/时长扩大为 AI 21 镜/181.5s、导入 15 镜/79s；V2.4b 增加合并承载和时长压缩后，AI 仍扩大到 25 镜/194s，导入语义回到 12/4。生产 Prompt/测试已完整恢复 V2.3，页面/Schema/DTO/数据库/确认流程无变化。长期规则：不要在生成 Prompt 中堆叠全量逐事实映射；下一轮先扩固定样例，再测试低权重定向提示，并同时以语义、镜头数和总时长判定。OpenCode 文本并发退出和 evaluator 偶发附加文本均应 fail-closed，后者不得接生产硬门。
- 2026-07-17 分镜 Beat 语义固定样例集已建立：5 类题材、14 Beat、28 个 `summary/outcome` 维度，覆盖声音触发、屏幕身份、对白选择、动作结果、关系状态及 `covered/partial/missing/contradicted` 四态；QA CLI 支持 dry-run、串行 repeat、单 fixture 重试，并分开统计人工预期一致度与模型重复稳定度。`self/gpt-5.5` 全集首轮有效结果为 51/52（98.1%）预期一致、23/24（95.8%）重复稳定；动作契约失败定向两轮重试 100%，唯一语义波动是抽象关系状态。长期边界：corpus 只校准 evaluator，不替代生成 Prompt 的真实 A/B，不接生产硬门、数据库、页面或自动修复；V2.3 保持生产基线，V2.4 继续拒绝。
- 2026-07-17 双轨分镜 Prompt V2.5“低权重定向风险扫描”实验已拒绝且未接生产：3 个新真实文本样本的 V2.3 在声音触发、屏幕身份和行动结果 6 个目标维度上已全部 2/2 `covered`，V2.5 改善样本为 0；屏幕身份从 4 镜增到 6 镜并超过上限，声音样本抽象关系 outcome 从 `covered` 退为 `partial`。6/6 生成与 12/12 评测首次通过，固定 corpus 回归 27/28。生产继续默认 V2.3；V2.5 只保留显式 QA 变体、3 个 fixture 和 A/B CLI。长期规则：不要把每类 evaluator 发现继续回灌成生成 Prompt；独立 QA 报告优于自动补镜、自动修复或生产硬门。
- 2026-07-16 OpenCode 文本生成安全边界：普通文本模型只负责生成，不需要直接调用文件、命令或其他系统工具；普通新会话必须以 deny-all permission 创建，每次发送消息再用 `tools={"*":false}` 重置权限，以覆盖升级前仍会复用的旧会话。唯一例外是 2026-07-21 起的独立 JSON Schema 会话：OpenCode 1.18.3 必须调用内置 `StructuredOutput` 才能返回 `info.structured`，因此不加会话级 deny-all，改由消息级 `tools={"*":false,"StructuredOutput":true}` 只开放该工具。AI漫游业务工具继续在应用层受控执行。AI 章节 pending 只能保存与目标 project/chapter 同域的对话 thread/message 审计 ID；项目级对话来源需安全剥离这些 ID，同时保留密封的大纲、章节卡和前章正式版本来源。
- 2026-07-18 本地标准启动已正式收口为 DB-only：根目录 `pnpm dev` / `pnpm dev:server` 默认连接 `~/.airoaming/data/db/airoaming.sqlite` 与 `~/.airoaming/workspace`，启动前核验数据库存在、17 个 migration ledger 完整、`PersistenceState=db_only` 且已激活；任一条件不满足即失败关闭，禁止创建空库或静默回退 file mode。稳定运行实例已通过既有 shadow/final importer 和 C0～C7 迁入 `测试`、`Grok文本回归-0718` 两个项目，旧 workspace 与迁移前备份保留；迁移后已发生 DB 首写，后续不得回切 file 写入。真实 `xai/grok-4.5` A4 文本生成已验证为来源密封的 AI pending，未自动采用/发布，图片任务、候选图和 Asset 均为 0。
- 2026-07-18 DB-only 图片凭据接线缺口已修复：`ProviderShadowImporter` 继续只导入脱敏元数据；`FinalImportOrchestrator` 只在 Keychain/Fake SecretStore fingerprint 独立验证和 final slice 验证均成功后，通过事务绑定 opaque secretRef、fingerprint、configured/enabled。既有成功 run 仅允许显式 repair replay，重复执行不增加 MigrationRun、不轮换 secretRef，冲突或缺失目标 fail-closed。当前 OpenAI/豆包/Grok 均 configured，active 仍为 Grok，运行时密钥可加载；原 10 个失败任务/30 次 attempt 保留，修复全过程图片调用为 0。历史失败任务不得自动重试，后续生成必须由用户明确触发。
- 2026-07-18 本地开发运行约定：只保留一套根目录 `pnpm dev`。重复启动会形成 `5173`/`5174` 两个 Vite 页面并共同代理同一 `4310` 后端，页面内存状态可能不一致，服务重启窗口还可能留下动态模块/HMR 错误；判断生成失败必须以新建 `GenerationTask`、任务时间和 Provider 错误为准，不能把旧页面或历史失败任务当成新失败。
- 2026-07-18 DB-only 图片运行时已完成一次真实页面验证：用户明确授权后，从标准 `5173 + 4310` 实例提交“阿肃”角色图；任务队列经历 `0 → 1 → 0`，角色卡从“生成中”变为可预览的“定稿”，真实图片可正常显示。该结果证明当前 active Grok 图片 Provider 的凭据加载、GenerationTask worker、Asset/CharacterVisual 持久化与页面回显链路已贯通；本次仅调用 1 张角色图，未生成三视图或其他图片。
- 2026-07-18 “杀令入棺”图片素材已真实完成：9 张角色参考图、阿肃/小棠/铁锚/沈婆 4 张锁定三视图、5 张场景参考图。Grok 图片任务需要逐张串行提交与等待；同批集中提交会触发 Provider 503。未成年人角色图应避免把出血、伤口或医疗细节直接写入生图描述，可用病容/虚弱等非图形化表达保留剧情语义。
- 2026-07-19 七阶段流程阻断已修复：`needs_confirmation/needs_update` 与 `done/active` 一样可进入处理，`waiting/blocked` 仍不可越级；DB Workbench read model 从 `ChapterScene.currentVisual -> SceneVisual.assetId` 只读投影场景 `referenceAssetId`，不回写不可变 StoryVersion；历史剧情结构/分镜预览在权威状态推进后标记“已处理”，正式结构重复确认保持幂等。用户当前项目已正常点击进入并确认 12 镜，随后进入出图准备；5 个场景图均被识别，剩余阻塞是 4 个出镜角色尚未定稿，未调用图片 Provider。
- 2026-07-19 视觉素材门禁已按 ADR-0018 完成根因修复：素材要求在剧情结构确认时由共享 `level + entityType` 策略固定——human lead/recurring/chapter=`final_reference`，human minor/extra 与 creature/group=`preview_front`，voice=`none`；`appearanceCount` 只展示。分镜生成与 DB 持久化都只能引用当前确认结构主体，出图准备是缺项阻断的纯检查门，不生成、不修复、不临时升级。DB 新预检写 `preflight-source-v2`，v1 可读但 stale；group 保守别名共用素材身份，旧结构页面投影即时去重、再次确认时逐步收敛。全过程无付费图片调用；Shared 165 + Server 767 共 932 条、类型/构建、DB Web gate 3/3 和真实浏览器复核通过。市场对照继续支持“先登记类型化视觉元素→分镜引用→生产前纯门禁”；通用 prop/object 模型后置。
- 2026-07-19 场景图重生成安全规则已收口：DB `scene_reference_generate` 每次使用 `scenes/{sceneKey}/visuals/{assetId}/background.webp` 独立路径，失败清理只能删除本次新文件，不能覆盖/删除当前 SceneVisual 的旧图。物理文件 ENOENT 时接口返回 `404 PROJECT_ASSET_FILE_NOT_FOUND` 并把 Asset 从 `ready` 校正为 `missing`；剧情结构页显示明确缺失提示。Preflight `sceneChecks.referenceAssetId` 必须是 `Asset.id`，`SceneVisual.id` 只保留在 source snapshot 的 `visualId`。历史固定路径只读兼容，不作为 DB 新资产合同。
- 2026-07-19 场景图显式生成必须携带操作级 `requestId`：同编号幂等重放，旧任务终态后新点击使用新编号并创建新任务；同一项目/章节/`ChapterScene.id` 已有活动任务时仍直接返回活动任务，避免重复付费。DB 任务 `target.id` 是 `ChapterScene.id`，页面必须用 `task.input.sceneKey` 对齐剧情结构 `scene.id` 显示生成状态，不能混用两类 ID。
- 2026-07-19 Preflight 视觉来源 freshness 已完成根修：正式 production-state 与 NewWorkGate 会用当前 Storyboard 实际引用集合重建 live source snapshot，并区分 storyboard、出镜角色、引用场景和画风变化；旧 Asset 变 `missing` 或当前 SceneVisual/CharacterVisual 更换后，旧 Preflight 立即 stale，第 4 步 `needs_update`、第 5 步禁用，`image_generate` 在任务落库前受控 409。重新确认只创建新的不可变 PreflightRevision，Story/Storyboard/milestone 不受纯视觉变化影响；DB trigger 继续作为最后安全网。Shared 167、Server 770、DB 集成与真实无付费页面复核通过。
- 2026-07-19 候选图必需引用 P0 已完成：file/DB-only 共用 `candidate_reference_plan_v1`，容量内独立直传，超限时只把全部角色编成确定性 `cast_identity_board_v1` 并保留独立场景；成功必须 `omittedRequired=[]`，缺失、损坏、final-only、来源无效或超技术容量均在联网前失败。DB final 仅作出图批准证据，物理身份输入使用单人 preview；新 final 保存并优先沿 `sourceVisualId` 冻结精确 preview，旧空来源资产只在同角色存在唯一可验证的更早 preview 时兼容，禁止猜裁布局不统一的四视图或使用可变当前指针。真实三人资产离线编译为身份板+场景两槽并覆盖 4/4 来源，类型检查、聚焦 26 项和 Server 784 项回归通过；真实 Provider 视觉 A/B 未运行，不能把输入覆盖宣称为成图质量保证。
- 2026-07-19 候选图镜头视觉编译层已完成并演进为自动详细说明：市场公开流程只支持“拆镜时产出可视描述 + Elements/参考绑定 + 单镜 Prompt 可见可改 + 按需重写/局部重做”，不能据此声称竞品内部也使用本项目形态的 Skill。本项目不新增顶层关卡、数据库表或平行描述字段；`shot_generate` 在同一用户动作内先生成分镜骨架，再由 `storyboard-shot-generate` 章节级第二段自动整理 `coreAction/comic.panelDescription/comic.composition/promptDraft`，两段全部通过后才写 pending。第二段按 order 一一对应，只覆盖四个静态字段；绑定角色必须点名，多个人类同镜必须写总人数，中文数量兼容“两”，非人主体不混入人类计数。多人总数与 group 范围必须由后端按镜头编译为显式约束，不让模型从整章角色表反推；裸群体名仍 blocking，“一群+实际 group 名”可提取为中性 `groupCountHint`。返修 Prompt 需把错误码翻译为数组下标、目标字段和具体改写动作。Dialogue 与持久 worker 共用同一整理器，各阶段首次失败最多返修一次。构图阅读箭头与内容地点跳转必须分域校验；正式台词候选应去掉行首表演提示和成对外层引号；结构中的字迹/屏幕数字等事实必须翻译为非文字可见信号和人物反应，不能进入静态生图文字要求。候选图工作台默认读取详细三段草稿，`shot_prompt_generate` 仅作按需“重新优化本镜头”，`image-candidate-generate` 继续确定性编译 Provider Prompt/参考。DB 有 pending 时，候选图工作台只能读取 current Storyboard Shot，禁止按章节混扫 pending Shot。真实“杀令入棺”最新形成 12 镜 pending，群体镜头均带中性范围，浏览器可见且未自动确认；这是单章文本链路证据，不等同于多题材 A/B 或真实成图质量保证。
- 2026-07-20 Grok 图片成本档统一为默认 1K：`ImageProviderService` 的文生图、单图编辑和多图编辑都显式发送 `resolution`；`GROK_IMAGE_RESOLUTION` 仅允许 `1k|2k`，未配置默认 `1k`，非法值联网前失败。该运行策略不改变候选任务的 `requestedSize/sizePolicyVersion`，也不套用于 OpenAI/豆包；真实付费图片调用为 0。
- 2026-07-20 Runware 已作为第四个图片 Provider 接入并设为当前 provider：无参考草稿默认 FLUX.1 Schnell `runware:100@1`；挑中草稿单图精修固定使用 FLUX.2 Dev `runware:400@1` + `referenceImages`；带角色/场景参考的候选图使用 FLUX.1 Dev `runware:101@1` + IP-Adapter `runware:56@1`，并保持 `candidate_reference_plan_v1` 全来源覆盖。专用 key 只保存到 macOS Keychain；`security -w` 的 TTY 提示由 Expect 私有 TTY 回答、密钥经 fd 3 传入，不进入 argv/stdout/stderr。inpainting/FLUX Fill、LoRA/ControlNet 尚未接入；FLUX.2 Dev 商用前需复核当时有效许可。完整回归 Shared 167/167、Server 822/822，真实付费图片调用为 0。
- 标准 DB-only 正式数据根与代码仓库分离：SQLite 位于 `~/.airoaming/data/db/airoaming.sqlite`，Asset/workspace 位于 `~/.airoaming/workspace`，图片凭据明文位于 macOS Keychain。只删除仓库不会清理这些数据，但会删除仓库内旧 file-mode `workspace/`、本地恢复包和项目文档；删除 `~/.airoaming` 才会直接丢失当前数据库与素材。
- 应用内项目删除是最终物理清理，不是软删除或归档：`active -> deleting -> project.delete_files processed -> DB purge`，会删除该项目素材、任务、对话和业务事实。2026-07-20 标准启动未发现通用 Outbox 后台消费者，现场 3 条项目删除事件停在 `pending/attempt=0`；这些残留不得当作备份，后续处理仍会删除。2026-07-21 用户明确决定当前不修该调度缺口，作为已接受风险保留，后续不得擅自扩张任务范围处理。
- 2026-07-21 剧情结构人物契约已根治：Shared 统一按括号层级解析「出场人物」，只在括号外分隔、提取尾部括号前纯名并忽略全括号说明；历史正式正文兼容读取，质量门不再把 `陈姨（电话/门外）` 等拆成伪角色。新 AI/导入输出、序列化和正式发布边界要求人物名单只含纯名，状态、电话、门外、遗照、新闻说明必须移到描述/动作/对白/旁白。普通对话修改建议仍不等于正式 ScriptVersion 已更新。
- 2026-07-21 剧情结构对话与持久 `story_parse` 已改用独立 OpenCode JSON Schema 会话并直接消费 `info.structured`，不再从普通对话文本正则提 JSON；隔离目录避免仓库规则污染。OpenCode 1.18.3 的 `json_schema` 通过内置 `StructuredOutput` 工具实现，因此结构会话不能使用会话级 deny-all；消息必须设置 `tools={"*":false,"StructuredOutput":true}`，只开放该内置回传工具。主生成和一次修复都失败时必须同时保留两次错误。真实 `opencode-go/grok-4.5` 页面已生成 6 场景/9 beats 的待确认结构，未自动确认。
- 2026-07-23 专业条漫排版安全合同已落地：`rule_fallback` 表示视觉保护未验证，所有自动气泡主体必须在全部画格外且尾巴关闭，不能再因空主体数据获得保护满分；`vision` 只允许气泡完整落入同来源 verified `textSafeRegion`，并硬避让脸、身体、焦点、其他画格和气泡。`layout_compose` 冻结 `layout_typography_preset_v1`，Noto Sans SC 400/500/700/900 真实字体面与普通/思考/强调/旁白语义样式在编辑器、只读预览和 renderer 共用，旧任务/digest 兼容读取。目标章节自动排版 v71 经两处气泡宽度人工精修后保存为最终 Working Copy v73，桌面与 390px 手机均 0 overflow、0 气泡碰撞、0 气泡/画格相交；运行时视觉凭据仍未接通，系统必须继续明确报告 `visual_analysis_not_configured` / `visual_protection_unverified`，不得静默宣称视觉保护通过。
- 2026-07-24 漫画成稿专业编辑 P0/P1/P2 已完成：`LayoutDocument`、Shared Editor Command、Working Copy、人工保护、不可变 Revision 与 Server RenderScene 继续是唯一事实源；Konva 10.3.0 只作为 Web interaction adapter，私有选择/viewport/history 不落盘。V1/V2 Revision、Preflight、Publication 与恢复/replay 已形成联合正式链路；V2 同时冻结完整 `revisionDocumentDigest` 和 V1 可见投影 `visibleDocumentDigest`，migration `0019_layout_revision_v2_publication` 不改写历史 V1，并允许在途 V1 publication 跨 migration 完成。来源替换以同 Shot 全 appearance 单批提交，publication task 精确绑定 V1/V2 source policy 和 sealed DB source rows，worker 独立重建来源与双摘要。Pending/手机权威预览、气泡/富文本/SFX/图层能力已接入且未越过 V3；Shared 247、Web 31、Server 777 测试和 V2 DB-only 页面闭环通过，双重复核无阻断。滤镜、网点、文字特效、新气泡轮廓和图层命名仍需新 ADR/LayoutDocumentV3；Comical-JS 只在新轮廓需求成立时 PoC，Manga Editor Desu/TUI/React Komik/Komiko 不进入主画布生产依赖。
- 2026-07-24 漫画成稿富文本与预览状态长期修复：DOM→Shared command 边界按根 childNodes 顺序读取标准段落、未知块和根文本并按正式模型水合，编辑器按 element id 隔离，避免根级替换保存为空和跨对象 composition 泄漏。Shared `VISIBLE_TEXT_EMPTY` 对可见 text/balloon、Unicode whitespace/Cf fail-closed，ZWJ emoji 不误判。dirty 手机预览同步开空窗、等待既有 autosave 并只在最新保存成功后导航；保存响应以 project/chapter/load generation 隔离，不能跨章节污染。原 focus-scoped snapshot history 与 Undo/Redo 已随 ADR-0022 基础版删除，不再作为当前能力或验收要求。
- 2026-07-24 “雨夜点名”内容复核纠正了此前只验证编辑功能后的过高结论：只读页虽能加载 11/11 锁定图片，但正式 Storyboard 的 8 个对白镜头、16 条 voice line 和 3 个旁白镜头没有进入 Layout，`dialogueBindings=[]`；Working Copy 只有一个无 source/speaker 绑定、且不存在于正式分镜的手工 thought 气泡和一个 SFX。多格还存在嵌字乱码及动作/场景偏差，因此该样本只能证明编辑/保存/预览技术链可用，不能证明漫画内容成稿正确或桌面创作链达到可交付质量。长期验收必须增加 Storyboard→Layout 对白/旁白覆盖账本、来源绑定和真实逐格语义复核。
- 2026-07-24 漫画成稿对白与导出安全边界：V2 preflight 是唯一对白准确性判定，系统遗漏、重复、错绑、悬空、隐藏/透明、完全出画布或无用户所有权文字直接阻断且无强行导出；用户主动改字、隐藏正式文字或新增自定义文字时，弹窗必须展示镜头、说话者、原文和当前文字，确认只对当前文档/preflight 身份有效。正式气泡删除映射为可恢复隐藏，改字后显隐保持文本。单一导出内部依次 autosave、Working Copy preflight、不可变 Revision、Revision preflight、Publication；Revision/Publication 网络歧义必须精确重放原请求，Publication history/get/POST 的旧响应不得让 ready/failed/cancelled 回退。真实测试章已重建为 9 段、11 镜头、19/19 binding，并生成两次 ready Publication；修改前备份为 `/private/tmp/airoaming-before-layout-reset-20260724.sqlite`。
- 2026-08-11 角色阶段（CharacterStage）机制已落地第一层：`character_stages` 表（migration 0021）+ `Character.anchorAssetId` 定妆锚点，`CharacterStageService`（DB 模式专用，file 模式抛 `DB_PERSISTENCE_REQUIRED_FOR_CHARACTER_STAGE_SERVICE`）负责创建/查询/更新/删除阶段与"保脸换装"垫图：参考图优先级为 上一阶段 final/preview > anchorAssetId > primaryReference > preview，垫图走 `ImageProviderService.editImage`，预览资产 role=`character_stage_preview`、路径 `projects/{pid}/assets/characters/{cid}/stages/{sid}/preview.webp`，`previewAssetId/finalAssetId` 无外键需服务内显式回收。`prisma generate` 与 `packages/shared build` 改 schema/DTO 后必须重跑（本次已重建）。本机 Node 20 无 `node:sqlite`，backup/migration 相关 spec 是已知环境失败。
- 2026-08-11 角色定妆接口已落地：`CharacterReferenceService.generateAnchorCandidates`（prompt = customPrompt ?? appearance ?? 角色名+角色定位；并发出图默认 3 张，seed = 基准秒+index 互不相同；候选 meta `kind=anchor_candidate`+`characterId`+`seed`，不进 referenceAssetIds；DB 资产 role=`character_anchor_candidate`）与 `confirmAnchor`（校验归属后写 anchorAssetId 并刷缓存）；错误码 CHARACTER_NOT_FOUND / ASSET_NOT_FOUND；`databaseCharacterToLocal` 已映射 anchorAssetId；`generateImage` 支持可选 seed（仅 Runware 透传，其余 provider 靠随机采样保证差异）；端点 `POST .../anchor-candidates` 与 `POST .../confirm-anchor`。旧项目 anchorAssetId 为 null 时读取方降级 previewReferenceAssetId。未选中候选资产暂不清理，Web 定妆 UI 未接。
- 2026-08-11 角色参考图提示词已改为参考图优先：`buildCharacterReferencePrompt`（`apps/server/src/projects/reference-prompt.util.ts`）新增第 6 参数 `hasReferenceImage`（默认 false），角色已有参考图（`anchorAssetId || primaryReferenceAssetId || previewReferenceAssetId`）时 APPEARANCE 固定为「参考所提供的角色图片，保持面部特征、发型和整体气质一致」，文字 appearance 降级为 ADDITIONAL_NOTES「补充细节：{appearance}」（无 appearance 时为空串）；无参考图时行为不变。6 个角色模板（preview/final v1/v2 + creature/group）新增 `{{ADDITIONAL_NOTES}}` 变量；调用侧在 `character-reference.service.ts` 两处（file 队列与 DB 持久任务）传入，`image-prompt-baseline` 基线不传（默认 false）。模板渲染器要求模板变量必须全部由调用方提供，缺变量抛 `OPENCODE_PROMPT_TEMPLATE_VARIABLE_MISSING`。
