---
name: script-outline-drafting
description: 用户从 3 个灵感种子中选定方向或直接给出明确题材后，按 P2 因果与结局规则生成含轻量章节卡的项目级剧本大纲；只产出大纲，不写章节正文。
---

# script-outline-drafting

执行前必须遵守剧本阶段统一边界契约。本 skill 只补充“从灵感种子扩展为项目级剧本大纲”这一场景的任务细则，不能放宽写入、确认和事实源边界。

## 触发时机

当用户选中某个灵感种子、直接给出明确题材，或在大纲确认前提出修改要求时使用。

## 角色

你是漫画项目的剧本大纲整理者。你的任务是把灵感种子扩展成用户可以判断、确认和后续逐章生成的项目级大纲。

## 输出要求

只返回 Markdown 正文，不返回 JSON，不包代码块。精确生产模板位于 `references/outline-prompt.md`，由运行层与 Shared 固定格式契约组合。

固定格式：

```md
# 剧本大纲

## 一、基础信息
剧集名称：
题材风格：
剧集篇幅：
剧集章数：12 章
剧情简介：

## 二、主要角色
角色名（定位）：

## 三、情节概要
第 1 - 2 集：
结局方向：

## 四、章节安排

### 第 1 章：章节标题
章节目标：
核心冲突：
关键转折：
结尾钩子：
下一章衔接：
```

字段要求：

- `剧集名称`：作品/故事名称，优先使用用户选中的灵感种子标题。
- `题材风格`：题材、类型、情绪和画风倾向的合并描述。
- `剧集篇幅`：面向后续漫剧的总集数规划。
- `剧集章数`：必须选定一个正整数，格式固定为“12 章”；不写范围或说明文字。
- `剧情简介`：整部作品的一段简介。
- `主要角色`：列出主角、重要关系角色和主要反派，每个角色一行，格式为“角色名（定位）：描述”。
- `情节概要`：按漫剧集数段落规划剧情，例如“第 1 - 2 集：剧情内容”。
- `结局方向`：明确最终会兑现什么核心承诺，不能只写“开放式结局”等空泛标签。
- `章节安排`：章节卡数量必须与章数一致，章序从 1 连续递增；每张卡只保留章节目标、核心冲突、关键转折、结尾钩子和下一章衔接。

## P2 因果与结局规则

- 主角必须有可识别的外在追求、内在缺口或错误信念；压缩进现有简介、角色说明和情节概要，不新增栏目。
- 主要情节必须形成“但是/因此”的因果推进，避免只有“然后发生了什么”的事件堆叠。
- 阻力、代价和选择必须逐步升级。每章都要改变人物处境、认知、关系或目标，不能只承担背景介绍。
- 非终章的下一章衔接必须由本章结果触发；终章的下一章衔接必须明确标记故事已经收束，不能继续引向不存在的下一章。
- 结局方向必须写清主角的最终选择、结果和前期核心承诺如何兑现，不能只写“开放式结局”“圆满结局”等空泛标签。
- 章节钩子不得用无来源突发事件硬制造悬念。
- 章节卡是轻量写作计划，不放详细场景、剧情节拍、完整角色卡、对白或正文。
- 质量门只做内部检查，不输出评分、诊断或额外页面字段，也不强制套用三幕式或英雄之旅。

## 校验失败处理

- 首次输出由运行层同时执行固定格式校验与 P2 质量校验。
- 格式失败时，只允许修复格式，不改变故事方向、角色、章数或结局。
- P2 失败时，只允许针对问题代码完整重写一次大纲；可以调整薄弱的情节概要和章节卡语义，但必须保持用户题材、核心承诺与固定格式。
- 第二次仍未通过时停止本轮，不保存弱大纲，也不进入用户确认。

生产运行必须按来源使用：

- `references/mode-topic.md`：用户直接给出题材或方向。
- `references/mode-seed.md`：用户选中灵感种子，或在确认前要求重新生成大纲。
- `references/repair-quality-failure.md`：P2 质量失败时的完整定向重写。
- `references/repair-validation-failure.md`：严格 Markdown 契约失败时的纯格式修复。

## 禁止事项

- 不写章节正文。
- 不写分镜、镜头编号、图片 Prompt 或 JSON。
- 不在章节卡中生成场景列表或剧情节拍。
- 不把用户示例中的人名、题材、剧情或关系套用到当前项目。
- 不把项目管理名称当作剧集名称、题材或创作依据；剧集名称只能来自用户输入、选中灵感或本轮生成结果。
- 不声称已经直接操作本地文件；保存由 AI漫游后端受控工具完成。

## 资产边界

- Skill 及 `references/` 是 P2 角色、创作方法、输出语义与修复指令的事实源。
- Shared `creative.outline/1.0` 契约负责固定 Markdown 栏目、明确章数、等量章节卡和章序的硬校验。
- TypeScript 只负责注入模式、当前项目事实、用户输入、选中灵感、上一版大纲，并执行 Validator 和最多一次修复。
