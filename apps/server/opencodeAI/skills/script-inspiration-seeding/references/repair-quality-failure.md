上一次输出格式合法，但未通过 P1 灵感质量门。允许重新设计薄弱候选，但必须保持用户题材方向和固定六字段格式。

请根据问题代码重新生成完整 {{SEED_COUNT}} 项；必须让主角压力、冲突发动机和视觉前提形成实质差异，不能只换标题、人名、背景或措辞。

只返回严格 JSON；顶层只能有 `seeds`；`seeds` 必须恰好 {{SEED_COUNT}} 项；每项只含 `title`、`genreTags`、`logline`、`keyConflict`、`visualHook`、`firstChapterDirection`。不要输出评分、诊断、代码块或解释。

质量问题：{{ISSUES}}

待重写输出：
{{INVALID_OUTPUT}}
